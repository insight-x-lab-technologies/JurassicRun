# 6.1 — Schema Supabase Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Entregar os artefatos versionados do schema online do JurassicRun — migração SQL (tabelas + RLS + trigger de integridade) num schema Postgres dedicado, constantes TS compartilhadas, env scaffold, docs e uma guarda de regressão que casa o SQL com as constantes.

**Architecture:** Schema Postgres dedicado `jurassicrun` num banco Supabase compartilhado. Identidade via Supabase Auth anônimo (`players.id = auth.uid()`), habilitando RLS por linha. Flag `verified` travada ao `service_role` por trigger (seam do anti-cheat 6.4). Como não há Postgres no CI, a guarda de regressão valida o **texto** da migração `.sql` contra o módulo de constantes TS.

**Tech Stack:** PostgreSQL (Supabase), TypeScript estrito, Vitest.

## Global Constraints

- **Não tocar `src/core/`** — item 100% infra/meta; determinismo 67 permanece inalterado (sem re-pin de goldens).
- **`src/services/` não importa DOM/IO/phaser**; o módulo de constantes é TS puro, sem `@supabase`.
- **TypeScript estrito**, sem `any` sem justificativa. Sigo o padrão dos outros `src/services/*`.
- **Nenhum segredo commitado.** `.env` é git-ignored; só `.env.example` (chave publishable) entra no repo.
- **`npm run check`** (typecheck + lint) e **`npm test`** verdes ao fim de cada task.
- Nomes canônicos: schema `jurassicrun`; tabelas `players`, `scores`, `challenge_entries`, `trophies`; modos `endless|daily|weekly`; trigger `lock_verified`.

---

## File Structure

- `src/services/online/schema.ts` — **novo**. Constantes puras: nome do schema, nomes de tabela, modos, e as colunas que a guarda verifica. Fonte única da verdade compartilhada por testes e (futuro) serviços 6.2+.
- `supabase/migrations/20260708000000_jr_schema.sql` — **novo**. DDL idempotente completa.
- `supabase/README.md` — **novo**. Como aplicar + passos manuais no dashboard (Exposed schemas, Anonymous sign-ins).
- `.env.example` — **novo**. Variáveis de ambiente (URL + anon key).
- `.gitignore` — **modificar**. Exceção `!.env.example`.
- `tests/online/schema-contract.test.ts` — **novo**. Guarda de regressão texto-SQL × constantes.

---

## Task 1: Módulo de constantes compartilhadas

**Files:**
- Create: `src/services/online/schema.ts`
- Test: `tests/online/schema.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `SUPABASE_SCHEMA: 'jurassicrun'`
  - `TABLES: { readonly players: 'players'; readonly scores: 'scores'; readonly challengeEntries: 'challenge_entries'; readonly trophies: 'trophies' }`
  - `TABLE_NAMES: readonly string[]` (valores de `TABLES`)
  - `ONLINE_MODES: readonly ['endless','daily','weekly']`
  - `VERIFIED_TABLES: readonly ['scores','challenge_entries']` (tabelas com flag `verified` + trigger)
  - `TABLE_COLUMNS: Record<string, readonly string[]>` (colunas esperadas por tabela, usadas pela guarda)

- [ ] **Step 1: Write the failing test**

`tests/online/schema.test.ts`:
```ts
import { describe, expect, it } from 'vitest';
import {
  SUPABASE_SCHEMA,
  TABLES,
  TABLE_NAMES,
  ONLINE_MODES,
  VERIFIED_TABLES,
  TABLE_COLUMNS,
} from '@services/online/schema';

describe('online schema constants', () => {
  it('nomeia o schema dedicado', () => {
    expect(SUPABASE_SCHEMA).toBe('jurassicrun');
  });

  it('expõe as 4 tabelas canônicas', () => {
    expect(TABLES).toEqual({
      players: 'players',
      scores: 'scores',
      challengeEntries: 'challenge_entries',
      trophies: 'trophies',
    });
    expect([...TABLE_NAMES].sort()).toEqual(
      ['challenge_entries', 'players', 'scores', 'trophies'],
    );
  });

  it('lista os modos online e as tabelas com verified', () => {
    expect(ONLINE_MODES).toEqual(['endless', 'daily', 'weekly']);
    expect(VERIFIED_TABLES).toEqual(['scores', 'challenge_entries']);
  });

  it('declara colunas por tabela cobrindo cada tabela', () => {
    for (const t of TABLE_NAMES) {
      expect(TABLE_COLUMNS[t]?.length ?? 0).toBeGreaterThan(0);
    }
    expect(TABLE_COLUMNS['players']).toContain('name');
    expect(TABLE_COLUMNS['scores']).toContain('verified');
    expect(TABLE_COLUMNS['challenge_entries']).toContain('final_hash');
    expect(TABLE_COLUMNS['trophies']).toContain('trophy_id');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/online/schema.test.ts`
Expected: FAIL — módulo `@services/online/schema` não existe.

- [ ] **Step 3: Write minimal implementation**

`src/services/online/schema.ts`:
```ts
/**
 * Constantes do schema online (Supabase) do JurassicRun — fonte única da verdade
 * compartilhada entre a guarda de contrato do SQL e os serviços online (6.2+).
 * TS puro: sem IO, sem `@supabase`. Isolamento por schema Postgres dedicado.
 */
export const SUPABASE_SCHEMA = 'jurassicrun' as const;

export const TABLES = {
  players: 'players',
  scores: 'scores',
  challengeEntries: 'challenge_entries',
  trophies: 'trophies',
} as const;

export const TABLE_NAMES = [
  TABLES.players,
  TABLES.scores,
  TABLES.challengeEntries,
  TABLES.trophies,
] as const;

export const ONLINE_MODES = ['endless', 'daily', 'weekly'] as const;

/** Tabelas com flag `verified` travada ao service_role via trigger `lock_verified`. */
export const VERIFIED_TABLES = ['scores', 'challenge_entries'] as const;

/** Colunas esperadas por tabela — a guarda de contrato casa isto com a migração `.sql`. */
export const TABLE_COLUMNS: Record<string, readonly string[]> = {
  players: ['id', 'name', 'avatar', 'created_at'],
  scores: [
    'id', 'player_id', 'mode', 'seed', 'score', 'distance',
    'food', 'near_misses', 'level', 'verified', 'created_at',
  ],
  challenge_entries: [
    'id', 'player_id', 'mode', 'seed', 'score', 'distance',
    'food', 'near_misses', 'timeline', 'final_hash', 'verified', 'created_at',
  ],
  trophies: ['player_id', 'trophy_id', 'unlocked_at'],
};
```

Nota: se `@services` não estiver no `tsconfig`/vitest aliases, confirmar o alias existente (os outros serviços usam `@services/...`). Reusar o mesmo.

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/online/schema.test.ts`
Expected: PASS (4 testes).

- [ ] **Step 5: Commit**

```bash
git add src/services/online/schema.ts tests/online/schema.test.ts
git commit -m "feat(6.1): constantes compartilhadas do schema online (schema/tabelas/modos/colunas)"
```

---

## Task 2: Migração SQL (schema + tabelas + RLS + trigger)

**Files:**
- Create: `supabase/migrations/20260708000000_jr_schema.sql`

**Interfaces:**
- Consumes: os nomes canônicos da Task 1 (schema/tabelas/colunas/trigger) — devem casar exatamente.
- Produces: o arquivo `.sql` que a guarda da Task 3 valida.

Esta task não tem teste próprio automatizável (não há Postgres no CI); a validação vem da guarda de texto na Task 3. O deliverable é o `.sql` idempotente abaixo, escrito na íntegra.

- [ ] **Step 1: Escrever a migração completa**

`supabase/migrations/20260708000000_jr_schema.sql`:
```sql
-- JurassicRun — schema online (item 6.1). Isolamento por schema dedicado num banco
-- Supabase compartilhado. Idempotente: seguro reaplicar. Identidade via auth anônimo
-- (players.id = auth.uid()); flag `verified` travada ao service_role (anti-cheat 6.4).

create schema if not exists jurassicrun;

-- ── players ────────────────────────────────────────────────────────────────
create table if not exists jurassicrun.players (
  id         uuid primary key references auth.users(id) on delete cascade,
  name       text not null check (char_length(name) between 1 and 20),
  avatar     text,
  created_at timestamptz not null default now()
);

-- ── scores (leaderboard, todos os modos) ─────────────────────────────────────
create table if not exists jurassicrun.scores (
  id          bigint generated always as identity primary key,
  player_id   uuid not null references jurassicrun.players(id) on delete cascade,
  mode        text not null check (mode in ('endless','daily','weekly')),
  seed        text not null,
  score       double precision not null,
  distance    double precision not null,
  food        integer not null check (food >= 0),
  near_misses integer not null check (near_misses >= 0),
  level       integer not null check (level >= 0),
  verified    boolean not null default false,
  created_at  timestamptz not null default now()
);
create index if not exists scores_mode_score_idx
  on jurassicrun.scores (mode, score desc);

-- ── challenge_entries (replays verificáveis, Diário/Semanal) ──────────────────
create table if not exists jurassicrun.challenge_entries (
  id          bigint generated always as identity primary key,
  player_id   uuid not null references jurassicrun.players(id) on delete cascade,
  mode        text not null check (mode in ('daily','weekly')),
  seed        text not null,
  score       double precision not null,
  distance    double precision not null,
  food        integer not null check (food >= 0),
  near_misses integer not null check (near_misses >= 0),
  timeline    jsonb not null,
  final_hash  text not null check (final_hash <> ''),
  verified    boolean not null default false,
  created_at  timestamptz not null default now(),
  unique (player_id, seed)
);

-- ── trophies (conquistas por jogador) ────────────────────────────────────────
create table if not exists jurassicrun.trophies (
  player_id   uuid not null references jurassicrun.players(id) on delete cascade,
  trophy_id   text not null,
  unlocked_at timestamptz not null default now(),
  primary key (player_id, trophy_id)
);

-- ── trigger de integridade: só o service_role pode marcar verified=true ──────
create or replace function jurassicrun.lock_verified()
returns trigger language plpgsql as $$
begin
  if auth.role() is distinct from 'service_role' then
    new.verified := false;
  end if;
  return new;
end $$;

drop trigger if exists lock_verified on jurassicrun.scores;
create trigger lock_verified before insert or update on jurassicrun.scores
  for each row execute function jurassicrun.lock_verified();

drop trigger if exists lock_verified on jurassicrun.challenge_entries;
create trigger lock_verified before insert or update on jurassicrun.challenge_entries
  for each row execute function jurassicrun.lock_verified();

-- ── RLS ──────────────────────────────────────────────────────────────────────
alter table jurassicrun.players            enable row level security;
alter table jurassicrun.scores             enable row level security;
alter table jurassicrun.challenge_entries  enable row level security;
alter table jurassicrun.trophies           enable row level security;

-- players: leitura pública; escrita só da própria linha.
drop policy if exists players_select_public on jurassicrun.players;
create policy players_select_public on jurassicrun.players
  for select using (true);
drop policy if exists players_insert_own on jurassicrun.players;
create policy players_insert_own on jurassicrun.players
  for insert with check (id = auth.uid());
drop policy if exists players_update_own on jurassicrun.players;
create policy players_update_own on jurassicrun.players
  for update using (id = auth.uid()) with check (id = auth.uid());
drop policy if exists players_delete_own on jurassicrun.players;
create policy players_delete_own on jurassicrun.players
  for delete using (id = auth.uid());

-- scores: leitura pública; insert da própria linha; imutável (sem update/delete no cliente).
drop policy if exists scores_select_public on jurassicrun.scores;
create policy scores_select_public on jurassicrun.scores
  for select using (true);
drop policy if exists scores_insert_own on jurassicrun.scores;
create policy scores_insert_own on jurassicrun.scores
  for insert with check (player_id = auth.uid());

-- challenge_entries: leitura pública; insert/update da própria linha (melhor tentativa).
drop policy if exists challenge_entries_select_public on jurassicrun.challenge_entries;
create policy challenge_entries_select_public on jurassicrun.challenge_entries
  for select using (true);
drop policy if exists challenge_entries_insert_own on jurassicrun.challenge_entries;
create policy challenge_entries_insert_own on jurassicrun.challenge_entries
  for insert with check (player_id = auth.uid());
drop policy if exists challenge_entries_update_own on jurassicrun.challenge_entries;
create policy challenge_entries_update_own on jurassicrun.challenge_entries
  for update using (player_id = auth.uid()) with check (player_id = auth.uid());

-- trophies: leitura pública; insert da própria linha (centrais via service_role).
drop policy if exists trophies_select_public on jurassicrun.trophies;
create policy trophies_select_public on jurassicrun.trophies
  for select using (true);
drop policy if exists trophies_insert_own on jurassicrun.trophies;
create policy trophies_insert_own on jurassicrun.trophies
  for insert with check (player_id = auth.uid());
```

- [ ] **Step 2: Sanidade local do arquivo**

Run: `test -f supabase/migrations/20260708000000_jr_schema.sql && grep -c "create policy" supabase/migrations/20260708000000_jr_schema.sql`
Expected: imprime `11` (11 policies: players 4 + scores 2 + challenge_entries 3 + trophies 2).

- [ ] **Step 3: Commit**

```bash
git add supabase/migrations/20260708000000_jr_schema.sql
git commit -m "feat(6.1): migração SQL do schema online (tabelas + RLS + trigger lock_verified)"
```

---

## Task 3: Guarda de contrato + env scaffold + docs

**Files:**
- Create: `tests/online/schema-contract.test.ts`
- Create: `.env.example`
- Create: `supabase/README.md`
- Modify: `.gitignore` (adicionar `!.env.example`)

**Interfaces:**
- Consumes: `SUPABASE_SCHEMA`, `TABLE_NAMES`, `TABLE_COLUMNS`, `VERIFIED_TABLES` (Task 1); o arquivo `.sql` (Task 2).
- Produces: nada consumido adiante.

- [ ] **Step 1: Write the failing test**

`tests/online/schema-contract.test.ts`:
```ts
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { describe, expect, it } from 'vitest';
import {
  SUPABASE_SCHEMA,
  TABLE_NAMES,
  TABLE_COLUMNS,
  VERIFIED_TABLES,
} from '@services/online/schema';

const SQL = readFileSync(
  fileURLToPath(
    new URL(
      '../../supabase/migrations/20260708000000_jr_schema.sql',
      import.meta.url,
    ),
  ),
  'utf8',
);

describe('migração casa com as constantes do schema', () => {
  it('cria o schema dedicado', () => {
    expect(SQL).toContain(`create schema if not exists ${SUPABASE_SCHEMA}`);
  });

  it('cria cada tabela no schema dedicado', () => {
    for (const t of TABLE_NAMES) {
      expect(SQL).toContain(`create table if not exists ${SUPABASE_SCHEMA}.${t} (`);
    }
  });

  it('declara cada coluna esperada de cada tabela', () => {
    for (const t of TABLE_NAMES) {
      const start = SQL.indexOf(`${SUPABASE_SCHEMA}.${t} (`);
      const body = SQL.slice(start, SQL.indexOf(');', start));
      for (const col of TABLE_COLUMNS[t] ?? []) {
        expect(body, `${t}.${col}`).toMatch(new RegExp(`\\b${col}\\b`));
      }
    }
  });

  it('habilita RLS em todas as tabelas', () => {
    for (const t of TABLE_NAMES) {
      expect(SQL).toContain(
        `alter table ${SUPABASE_SCHEMA}.${t}`,
      );
      expect(SQL).toMatch(
        new RegExp(`alter table ${SUPABASE_SCHEMA}\\.${t}\\s+enable row level security`),
      );
    }
  });

  it('tem policy de select e de insert por tabela', () => {
    for (const t of TABLE_NAMES) {
      expect(SQL, `${t} select`).toContain(`create policy ${t}_select_public on ${SUPABASE_SCHEMA}.${t}`);
      expect(SQL, `${t} insert`).toContain(`create policy ${t}_insert_own on ${SUPABASE_SCHEMA}.${t}`);
    }
  });

  it('trava verified ao service_role e anexa o trigger nas tabelas com verified', () => {
    expect(SQL).toContain('function jurassicrun.lock_verified()');
    expect(SQL).toContain(`auth.role() is distinct from 'service_role'`);
    for (const t of VERIFIED_TABLES) {
      expect(SQL).toMatch(
        new RegExp(`create trigger lock_verified before insert or update on ${SUPABASE_SCHEMA}\\.${t}`),
      );
    }
  });
});
```

- [ ] **Step 2: Run test to verify it passes**

Run: `npx vitest run tests/online/schema-contract.test.ts`
Expected: PASS (o `.sql` da Task 2 já satisfaz o contrato).

Se algum caso falhar, corrija a divergência **no `.sql`** (não relaxe a guarda) — a guarda é a fonte de verdade do contrato.

- [ ] **Step 3: Provar que a guarda pega divergência (testar o teste)**

Temporariamente renomeie `near_misses` → `nearMisses` numa coluna de `scores` no `.sql`, rode a guarda: deve FALHAR no caso "declara cada coluna". Reverta. (Passo manual de disciplina TDD; não commitar a quebra.)

- [ ] **Step 4: Criar `.env.example`**

`.env.example`:
```
# JurassicRun — configuração do cliente Supabase (Fase 6). Copie para `.env` (git-ignored)
# e preencha. NÃO commite `.env`. A anon/publishable key é segura no cliente (protegida por RLS).
VITE_SUPABASE_URL=https://wpbryudhqerpnzbpmcio.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_YvN2XGdZp8IidXB9zOtC-g_wy9RkeXN
```

- [ ] **Step 5: Adicionar exceção no `.gitignore`**

Adicionar após a linha `.env.*`:
```
!.env.example
```

- [ ] **Step 6: Criar `supabase/README.md`**

`supabase/README.md`:
```markdown
# Supabase — JurassicRun (Fase 6)

Banco compartilhado `InsightXLabGamesHub`. Todos os objetos do JurassicRun vivem no
**schema dedicado `jurassicrun`** (isolamento de multi-projeto — sem colisão de nomes).

## Aplicar a migração

**Opção A — SQL Editor (dashboard):** cole o conteúdo de
`migrations/20260708000000_jr_schema.sql` e rode. É idempotente (seguro reaplicar).

**Opção B — Supabase CLI:** `supabase db push` (requer `supabase link` ao projeto
`wpbryudhqerpnzbpmcio` com a senha do Postgres).

## Passos manuais no dashboard (uma vez)

1. **Settings → API → Exposed schemas:** adicionar `jurassicrun` (senão o PostgREST/anon
   key não enxerga as tabelas). O cliente JS usará
   `createClient(url, key, { db: { schema: 'jurassicrun' } })` (wiring em 6.2).
2. **Authentication → Providers → Anonymous sign-ins:** habilitar (o 6.2 registra o
   jogador via anonymous sign-in; `players.id = auth.uid()`).

## Modelo de segurança

- RLS ligada em todas as tabelas. Leitura pública (leaderboards mostram nomes/troféus);
  escrita só da própria linha (`= auth.uid()`).
- `scores`/`challenge_entries` têm `verified`, travada em `false` para o cliente por
  trigger `lock_verified`. Só a Edge Function de verificação (6.4, service_role) marca
  `verified = true` após re-simular `(seed, timeline)` e conferir `final_hash`.
```

- [ ] **Step 7: Rodar a suíte inteira + typecheck**

Run: `npm run check && npm test`
Expected: verde. Determinismo inalterado (core intocado).

- [ ] **Step 8: Commit**

```bash
git add tests/online/schema-contract.test.ts .env.example .gitignore supabase/README.md
git commit -m "test(6.1): guarda de contrato SQL×constantes + env scaffold + docs de aplicação"
```

---

## Self-Review

**1. Spec coverage:**
- D1 schema dedicado → Task 1 (`SUPABASE_SCHEMA`), Task 2 (`create schema`), guarda Task 3. ✅
- D2 auth anônimo / `players.id=auth.uid()` → Task 2 (FK + policies), README passo 2. ✅
- D3 `verified` travado → Task 2 (trigger), guarda Task 3. ✅
- D4 tipos numéricos → Task 2 (`double precision`/`integer check>=0`). ✅
- D5 `timeline jsonb` → Task 2 + `TABLE_COLUMNS`. ✅
- 4 tabelas + RLS → Tasks 1–3. ✅
- Artefatos: migração/README/`.env.example`/constantes/guarda → Tasks 1–3. ✅
- Aplicar no banco vivo = fora de escopo (passo do usuário) → documentado no README. ✅

**2. Placeholder scan:** sem TBD/TODO; todo SQL/TS/teste está na íntegra. ✅

**3. Type consistency:** nomes de tabela/coluna/schema/trigger idênticos entre `schema.ts` (Task 1), `.sql` (Task 2) e a guarda (Task 3). A guarda existe justamente para travar isso em CI. ✅
