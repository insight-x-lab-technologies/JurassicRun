# Fase 2.5 — Fluxo de partida: Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dar ao vertical slice um ciclo de partida real (ready → playing → dead → restart) com seed aleatória exibida, sobre o core determinístico intocado.

**Architecture:** Uma máquina de estados PURA (`MatchController`) por cima do core, alimentada por uma factory de partida injetável (seed aleatória fora do core via `crypto`). A casca Phaser (`GameScene`) lê o controller a cada frame; a borda de pressão de flap (`onFlap`) dirige as transições. Padrão de 2.1–2.4: módulos puros testáveis × casca sem teste de unidade.

**Tech Stack:** TypeScript estrito, Vitest, Phaser (só na casca), i18next.

## Global Constraints

- `src/core/` **NÃO é tocado** (REGRA 1 determinismo). A aleatoriedade da seed vem de FORA do core (via `crypto`).
- Módulos em `src/render/` que forem testados por unidade **não importam `phaser`** (env node).
- Hot path por frame **alocação-zero** (REGRA 3): a máquina de estados só trabalha nas transições.
- Nenhuma string visível hardcoded (REGRA 4): texto novo via chave i18next nos **10 locales** (`en, es, pt-BR, fr, it, de, ja, zh, ko, hi`), paridade garantida por `tests/i18n/locales.test.ts`.
- TypeScript estrito, sem `any` sem justificativa.
- Seed canônica Endless = `endless:<TOKEN>`, TOKEN = 7 chars Crockford (via `@core/seed`).

---

### Task 1: Seed Endless aleatória (`seedSource.ts`)

**Files:**
- Create: `src/render/seedSource.ts`
- Test: `tests/render/seedSource.test.ts`
- Modify: `src/render/index.ts` (reexportar a parte pura)

**Interfaces:**
- Consumes: `endlessSeed`, `randomEndlessToken` de `@core/seed`.
- Produces:
  - `endlessSeedFromUint32(value: number): string` — puro; `endlessSeed(randomEndlessToken(value >>> 0))`.
  - `randomEndlessSeed(): string` — casca; usa `crypto.getRandomValues`.

- [ ] **Step 1: Write the failing test**

```ts
// tests/render/seedSource.test.ts
import { describe, it, expect } from 'vitest';
import { endlessSeedFromUint32 } from '@render/seedSource';
import { endlessSeed, randomEndlessToken } from '@core/seed';

describe('endlessSeedFromUint32', () => {
  it('produz endless:<token de 7 chars Crockford>', () => {
    const s = endlessSeedFromUint32(0);
    expect(s).toMatch(/^endless:[0-9A-HJKMNP-TV-Z]{7}$/);
  });

  it('é determinística e casa com endlessSeed(randomEndlessToken(v))', () => {
    const v = 123456789;
    expect(endlessSeedFromUint32(v)).toBe(endlessSeed(randomEndlessToken(v)));
    expect(endlessSeedFromUint32(v)).toBe(endlessSeedFromUint32(v));
  });

  it('trata o valor como uint32 (>>>0) para negativos/grandes', () => {
    expect(endlessSeedFromUint32(-1)).toBe(endlessSeedFromUint32(0xffffffff));
    expect(endlessSeedFromUint32(0x1_0000_0000)).toBe(endlessSeedFromUint32(0));
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/render/seedSource.test.ts`
Expected: FAIL — `@render/seedSource` não existe.

- [ ] **Step 3: Write minimal implementation**

```ts
// src/render/seedSource.ts
import { endlessSeed, randomEndlessToken } from '@core/seed';

/** Parte PURA: uint32 → seed Endless canônica exibível. Determinística. */
export function endlessSeedFromUint32(value: number): string {
  return endlessSeed(randomEndlessToken(value >>> 0));
}

/**
 * Casca (não testada por unidade): sorteia um uint32 real via `crypto` — a
 * aleatoriedade do Endless vem de FORA do core (permitido; ver core/seed/seed.ts).
 */
export function randomEndlessSeed(): string {
  const buf = new Uint32Array(1);
  crypto.getRandomValues(buf);
  return endlessSeedFromUint32(buf[0]!);
}
```

- [ ] **Step 4: Reexportar a parte pura em `src/render/index.ts`**

Adicionar a linha (mantendo a ordem existente, após `./manifest` ou no fim):

```ts
export * from './seedSource';
```

Nota: `seedSource.ts` importa só `@core/seed` (sem phaser) ⇒ seguro reexportar do index puro.

- [ ] **Step 5: Run test to verify it passes**

Run: `npx vitest run tests/render/seedSource.test.ts`
Expected: PASS (3 testes).

- [ ] **Step 6: Commit**

```bash
git add src/render/seedSource.ts src/render/index.ts tests/render/seedSource.test.ts
git commit -m "feat(2.5): seed Endless aleatória fora do core (endlessSeedFromUint32 + randomEndlessSeed)"
```

---

### Task 2: Borda de pressão de flap (`FlapInputSource.press` → boolean + `onFlap`)

**Files:**
- Modify: `src/render/input.ts` (`press` retorna boolean)
- Modify: `src/render/controls.ts` (`bindGameControls` aceita `onFlap?`)
- Test: `tests/render/input.test.ts` (estender)

**Interfaces:**
- Consumes: nada novo.
- Produces:
  - `FlapInputSource.press(id: string): boolean` — `true` na pressão fresca, `false` no autorepeat.
  - `bindGameControls(target: Window, controls: { flap: FlapInputSource; pause: PauseController; onFlap?: () => void }): () => void`.

- [ ] **Step 1: Write the failing test (estende `tests/render/input.test.ts`)**

Adicionar ao final do `describe` de `FlapInputSource` (ou novo `describe`):

```ts
import { describe, it, expect } from 'vitest';
import { FlapInputSource } from '@render/input';

describe('FlapInputSource.press (borda)', () => {
  it('retorna true na 1ª pressão de um id e false no autorepeat', () => {
    const src = new FlapInputSource();
    expect(src.press('key:Space')).toBe(true);
    expect(src.press('key:Space')).toBe(false); // autorepeat do mesmo id
  });

  it('volta a retornar true só após release completo do id', () => {
    const src = new FlapInputSource();
    expect(src.press('pointer:1')).toBe(true);
    src.release('pointer:1');
    expect(src.press('pointer:1')).toBe(true); // novo toque = borda de novo
  });

  it('ids distintos são bordas independentes', () => {
    const src = new FlapInputSource();
    expect(src.press('pointer:1')).toBe(true);
    expect(src.press('pointer:2')).toBe(true);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/render/input.test.ts`
Expected: FAIL — `press(...)` retorna `void`/`undefined`, não `true`.

- [ ] **Step 3: Implement — `press` retorna boolean em `src/render/input.ts`**

Trocar o método `press`:

```ts
  /** Pressiona uma fonte (id estável). Retorna true numa pressão fresca (borda), false no autorepeat. */
  press(id: string): boolean {
    if (this.active.has(id)) {
      // autorepeat: mesmo id pressionado enquanto já ativo — marca "segurado"
      this.wasHeld = true;
      return false;
    }
    this.active.add(id);
    this.latched = true;
    return true;
  }
```

- [ ] **Step 4: Implement — `onFlap` em `src/render/controls.ts`**

Estender a assinatura e disparar `onFlap` só na borda:

```ts
export function bindGameControls(
  target: Window,
  controls: { flap: FlapInputSource; pause: PauseController; onFlap?: () => void },
): () => void {
  const { flap, pause, onFlap } = controls;
  const onPointerDown = (e: PointerEvent): void => {
    if (flap.press(`pointer:${e.pointerId}`)) onFlap?.();
  };
  const onPointerUp = (e: PointerEvent): void => flap.release(`pointer:${e.pointerId}`);
  const onKeyDown = (e: KeyboardEvent): void => {
    if (PAUSE_KEYS.includes(e.code)) {
      pause.toggle();
      return;
    }
    if (FLAP_KEYS.includes(e.code)) {
      e.preventDefault(); // Space/ArrowUp não rolam a página
      if (flap.press(`key:${e.code}`)) onFlap?.();
    }
  };
  // ... (onKeyUp, onBlur, addEventListener, cleanup: inalterados)
```

(Manter o resto de `bindGameControls` — `onKeyUp`, `onBlur`, os `addEventListener` e o cleanup retornado — exatamente como está.)

- [ ] **Step 5: Run tests to verify they pass**

Run: `npx vitest run tests/render/input.test.ts`
Expected: PASS (existentes + 3 novos).

- [ ] **Step 6: Commit**

```bash
git add src/render/input.ts src/render/controls.ts tests/render/input.test.ts
git commit -m "feat(2.5): press retorna borda (bool) + onFlap em bindGameControls"
```

---

### Task 3: `MatchController` (máquina de estados pura)

**Files:**
- Create: `src/render/match.ts`
- Test: `tests/render/match.test.ts`
- Modify: `src/render/index.ts` (reexportar `match`)

**Interfaces:**
- Consumes: `createWorld`, `WorldState` de `@core/sim`; `FixedStepLoop` de `./loop`; `InputSource` de `./input`.
- Produces:
  - `type MatchPhase = 'ready' | 'playing' | 'dead'`.
  - `interface MatchInit { world: WorldState; seedLabel: string }`.
  - `interface MatchHooks { onNewMatch?: () => void }`.
  - `class MatchController` com:
    - `new (input: InputSource, factory: () => MatchInit, hooks?: MatchHooks)`
    - getters `phase: MatchPhase`, `world: WorldState`, `loop: FixedStepLoop`, `seedLabel: string`
    - `advance(dtSeconds: number): void`
    - `notifyFlap(): void`

- [ ] **Step 1: Write the failing test**

```ts
// tests/render/match.test.ts
import { describe, it, expect } from 'vitest';
import { MatchController } from '@render/match';
import type { MatchInit } from '@render/match';
import { createWorld } from '@core/sim';
import { NullInputSource } from '@render/input';

// Factory determinística: seeds numeradas + mundo com seed real (dino cai e morre sem flap).
function makeFactory(): () => MatchInit {
  let n = 0;
  return () => {
    const seedLabel = `endless:TEST${n++}`;
    return { world: createWorld({ seed: seedLabel }), seedLabel };
  };
}

// Avança tempo suficiente (s) para o dino cair do startY e morrer no chão.
function advanceUntilDead(m: MatchController, maxSeconds = 5): void {
  let t = 0;
  while (m.phase !== 'dead' && t < maxSeconds) {
    m.advance(1 / 60);
    t += 1 / 60;
  }
}

describe('MatchController', () => {
  it('nasce em ready com a primeira partida montada', () => {
    const m = new MatchController(new NullInputSource(), makeFactory());
    expect(m.phase).toBe('ready');
    expect(m.seedLabel).toBe('endless:TEST0');
    expect(m.world.tick).toBe(0);
  });

  it('advance em ready é no-op (sim congelada)', () => {
    const m = new MatchController(new NullInputSource(), makeFactory());
    m.advance(1);
    expect(m.world.tick).toBe(0);
    expect(m.world.distance).toBe(0);
    expect(m.phase).toBe('ready');
  });

  it('notifyFlap em ready inicia playing e advance passa a rodar steps', () => {
    const m = new MatchController(new NullInputSource(), makeFactory());
    m.notifyFlap();
    expect(m.phase).toBe('playing');
    m.advance(1 / 60);
    expect(m.world.tick).toBe(1);
  });

  it('em playing, a morte do mundo leva a dead', () => {
    const m = new MatchController(new NullInputSource(), makeFactory());
    m.notifyFlap();
    advanceUntilDead(m);
    expect(m.phase).toBe('dead');
    expect(m.world.alive).toBe(false);
  });

  it('notifyFlap em playing é no-op (mesma partida/seed)', () => {
    const m = new MatchController(new NullInputSource(), makeFactory());
    m.notifyFlap();
    m.advance(1 / 60);
    const seedBefore = m.seedLabel;
    m.notifyFlap();
    expect(m.phase).toBe('playing');
    expect(m.seedLabel).toBe(seedBefore);
  });

  it('notifyFlap em dead reinicia: nova seed, world novo, ready, onNewMatch chamado', () => {
    let resets = 0;
    const m = new MatchController(new NullInputSource(), makeFactory(), {
      onNewMatch: () => { resets++; },
    });
    m.notifyFlap();
    advanceUntilDead(m);
    expect(m.phase).toBe('dead');

    m.notifyFlap(); // restart
    expect(m.phase).toBe('ready');
    expect(m.seedLabel).toBe('endless:TEST1');
    expect(m.world.tick).toBe(0);
    expect(m.world.alive).toBe(true);
    expect(resets).toBe(1);
  });

  it('o loop aponta para o world corrente após restart', () => {
    const m = new MatchController(new NullInputSource(), makeFactory());
    m.notifyFlap();
    advanceUntilDead(m);
    m.notifyFlap(); // restart → ready, world novo
    expect(m.loop.world).toBe(m.world);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/render/match.test.ts`
Expected: FAIL — `@render/match` não existe.

- [ ] **Step 3: Write minimal implementation**

```ts
// src/render/match.ts
import type { WorldState } from '@core/sim';
import { FixedStepLoop } from './loop';
import type { InputSource } from './input';

export type MatchPhase = 'ready' | 'playing' | 'dead';

/** Uma partida recém-criada: mundo inicial + rótulo da seed (exibido no HUD). */
export interface MatchInit {
  world: WorldState;
  seedLabel: string;
}

export interface MatchHooks {
  /** Disparado ao montar uma nova partida (a app liga em FlapInputSource.reset). */
  onNewMatch?: () => void;
}

/**
 * Máquina de estados do ciclo de partida (ready → playing → dead → restart).
 * PURO (sem phaser): possui o WorldState + FixedStepLoop da partida corrente.
 * A sim só avança em `playing`; `ready`/`dead` ficam congelados.
 */
export class MatchController {
  private readonly input: InputSource;
  private readonly factory: () => MatchInit;
  private readonly hooks: MatchHooks;
  private _phase: MatchPhase = 'ready';
  private _world!: WorldState;
  private _loop!: FixedStepLoop;
  private _seedLabel = '';

  constructor(input: InputSource, factory: () => MatchInit, hooks: MatchHooks = {}) {
    this.input = input;
    this.factory = factory;
    this.hooks = hooks;
    this.startMatch(); // primeira partida em ready (sem disparar onNewMatch no load)
  }

  get phase(): MatchPhase {
    return this._phase;
  }
  get world(): WorldState {
    return this._world;
  }
  get loop(): FixedStepLoop {
    return this._loop;
  }
  get seedLabel(): string {
    return this._seedLabel;
  }

  /** Avança a simulação só em `playing`; transiciona para `dead` quando o mundo morre. */
  advance(dtSeconds: number): void {
    if (this._phase !== 'playing') return;
    this._loop.advance(dtSeconds);
    if (!this._world.alive) this._phase = 'dead';
  }

  /** Borda de pressão de flap vinda da casca. */
  notifyFlap(): void {
    if (this._phase === 'ready') {
      this._phase = 'playing';
    } else if (this._phase === 'dead') {
      this.startMatch();
      this.hooks.onNewMatch?.();
    }
    // em `playing`: no-op (o flap é tratado pelo core via InputSource).
  }

  private startMatch(): void {
    const init = this.factory();
    this._world = init.world;
    this._seedLabel = init.seedLabel;
    this._loop = new FixedStepLoop(this._world, this.input);
    this._phase = 'ready';
  }
}
```

- [ ] **Step 4: Reexportar em `src/render/index.ts`**

Adicionar:

```ts
export * from './match';
```

- [ ] **Step 5: Run test to verify it passes**

Run: `npx vitest run tests/render/match.test.ts`
Expected: PASS (7 testes).

- [ ] **Step 6: Commit**

```bash
git add src/render/match.ts src/render/index.ts tests/render/match.test.ts
git commit -m "feat(2.5): MatchController (ready→playing→dead→restart) puro"
```

---

### Task 4: Chave i18n `match.tapToStart` (10 locales)

**Files:**
- Modify: `src/i18n/locales/{en,es,pt-BR,fr,it,de,ja,zh,ko,hi}.json`
- Test: `tests/i18n/locales.test.ts` (já cobre paridade — deve permanecer verde)

**Interfaces:**
- Produces: chave i18next `match.tapToStart`.

- [ ] **Step 1: Adicionar o namespace `match` em cada locale**

Em cada arquivo `src/i18n/locales/<lng>.json`, adicionar um bloco `"match"` de topo (irmão de `"app"`/`"hud"`), com a tradução de "Tap to start":

- `en`: `"match": { "tapToStart": "Tap to start" }`
- `es`: `"match": { "tapToStart": "Toca para empezar" }`
- `pt-BR`: `"match": { "tapToStart": "Toque para começar" }`
- `fr`: `"match": { "tapToStart": "Touchez pour commencer" }`
- `it`: `"match": { "tapToStart": "Tocca per iniziare" }`
- `de`: `"match": { "tapToStart": "Zum Starten tippen" }`
- `ja`: `"match": { "tapToStart": "タップして開始" }`
- `zh`: `"match": { "tapToStart": "点击开始" }`
- `ko`: `"match": { "tapToStart": "탭하여 시작" }`
- `hi`: `"match": { "tapToStart": "शुरू करने के लिए टैप करें" }`

Preservar a formatação JSON existente (indentação, vírgulas). Se em dúvida sobre traduções, usar a skill `add-locale`.

- [ ] **Step 2: Run i18n parity test**

Run: `npx vitest run tests/i18n/locales.test.ts`
Expected: PASS (paridade dos 10 locales com `en`, incluindo `match.tapToStart`).

- [ ] **Step 3: Commit**

```bash
git add src/i18n/locales
git commit -m "feat(2.5): chave i18n match.tapToStart nos 10 locales"
```

---

### Task 5: Integração na casca Phaser (`GameScene`, `game.ts`, `main.ts`)

**Files:**
- Modify: `src/render/GameScene.ts`
- Modify: `src/render/game.ts`
- Modify: `src/app/main.ts`
- Modify: `src/render/constants.ts` (constantes do prompt de início)

**Interfaces:**
- Consumes: `MatchController` (Task 3), `randomEndlessSeed` (Task 1), `bindGameControls` c/ `onFlap` (Task 2), `match.tapToStart` (Task 4).
- Produces: `createGame(parent, match: MatchController, deps?: { pause?: PauseController }): Phaser.Game`.

Sem teste de unidade (casca Phaser/DOM — padrão de 2.1–2.4). Verificação: `npm run check` + Playwright no fim.

- [ ] **Step 1: Constantes do prompt de início em `src/render/constants.ts`**

Adicionar ao fim do arquivo:

```ts
/** Prompt do estado `ready` (2.5): centralizado, entre HUD (900) e overlay de pausa (1000). */
export const READY_PROMPT_DEPTH = 950;
export const READY_PROMPT_FONT_SIZE = '12px';
export const READY_PROMPT_COLOR = '#ffffff';
```

- [ ] **Step 2: `game.ts` passa a receber o `MatchController`**

Reescrever `src/render/game.ts`:

```ts
import * as Phaser from 'phaser';
import { GameScene } from './GameScene';
import { PauseController } from './input';
import type { MatchController } from './match';
import { VIEW_WIDTH, VIEW_HEIGHT, SKY_COLOR } from './constants';

export interface GameDeps {
  pause?: PauseController;
}

/** Cria o Phaser.Game montando a GameScene que renderiza a partida corrente do `match`. */
export function createGame(
  parent: string | HTMLElement,
  match: MatchController,
  deps: GameDeps = {},
): Phaser.Game {
  const pause = deps.pause ?? new PauseController();
  return new Phaser.Game({
    type: Phaser.AUTO,
    parent,
    width: VIEW_WIDTH,
    height: VIEW_HEIGHT,
    backgroundColor: SKY_COLOR,
    scale: { mode: Phaser.Scale.FIT, autoCenter: Phaser.Scale.CENTER_BOTH },
    scene: [new GameScene(match, pause)],
  });
}
```

- [ ] **Step 3: `GameScene` lê o `MatchController`**

Editar `src/render/GameScene.ts`:

1. Imports: remover `FixedStepLoop` e `InputSource` diretos; adicionar `MatchController`; adicionar as constantes do prompt.

```ts
import type { MatchController } from './match';
import { PauseController } from './input';
// ... manter os demais imports; ADICIONAR às constantes importadas:
//   READY_PROMPT_DEPTH, READY_PROMPT_FONT_SIZE, READY_PROMPT_COLOR
```

2. Campos e construtor — substituir `world`/`inputSource`/`loop`/`seedLabel` por `match`:

```ts
export class GameScene extends Phaser.Scene {
  private readonly match: MatchController;
  private readonly pause: PauseController;
  private parallaxTiles: Phaser.GameObjects.TileSprite[] = [];
  private gfx!: Phaser.GameObjects.Graphics;
  private pauseOverlay!: Phaser.GameObjects.Graphics;
  private hudText!: Phaser.GameObjects.Text;
  private hudTicker!: HudTicker;
  private readyPrompt!: Phaser.GameObjects.Text;

  constructor(match: MatchController, pause: PauseController) {
    super('GameScene');
    this.match = match;
    this.pause = pause;
  }
```

3. Em `create()`: remover `this.loop = new FixedStepLoop(...)`. Após criar o HUD, criar o prompt de início centralizado:

```ts
    // Prompt de início (2.5): visível só no estado `ready`.
    this.readyPrompt = this.add
      .text(VIEW_WIDTH / 2, VIEW_HEIGHT / 2, i18n.t('match.tapToStart'), {
        fontSize: READY_PROMPT_FONT_SIZE,
        color: READY_PROMPT_COLOR,
      })
      .setOrigin(0.5)
      .setScrollFactor(0)
      .setDepth(READY_PROMPT_DEPTH);
```

4. Reescrever `update()` para ler `this.match` (substitui `this.world`/`this.loop`):

```ts
  override update(_time: number, deltaMs: number): void {
    const paused = this.pause.paused;
    this.pauseOverlay.setVisible(paused);
    if (paused) return; // congela a sim; o último frame desenhado permanece sob o overlay

    const match = this.match;
    match.advance(deltaMs / 1000); // no-op fora de `playing`
    this.readyPrompt.setVisible(match.phase === 'ready');

    const loop = match.loop;
    const world = match.world;

    // Câmera segue o dino interpolado; vertical não scrolla (o mundo cabe na altura).
    this.cameras.main.scrollX = loop.renderX - DINO_SCREEN_X;

    const scrollX = this.cameras.main.scrollX;
    for (let i = 0; i < this.parallaxTiles.length; i++) {
      this.parallaxTiles[i]!.tilePositionX = parallaxTileOffset(scrollX, PARALLAX_LAYERS[i]!.scrollFactor);
    }

    const g = this.gfx;
    g.clear();
    for (const o of world.obstacles) this.drawEntity(g, o);
    for (const c of world.collectibles) this.drawEntity(g, c);
    this.drawPrimitive(g, DINO_TYPE_ID, world.pterodactyl.hitbox, loop.renderX, loop.renderY);

    const fps = this.hudTicker.tick(deltaMs / 1000);
    if (fps !== null) this.refreshHud(fps);
  }
```

5. `refreshHud(fps)` — trocar `this.world` por `this.match.world` e `this.seedLabel` por `this.match.seedLabel`:

```ts
  private refreshHud(fps: number): void {
    const world = this.match.world;
    const v = formatHudValues({
      distance: world.distance,
      food: world.food,
      fps,
      level: world.level,
      speed: world.scrollSpeed,
      seed: this.match.seedLabel,
    });
    this.hudText.setText([
      i18n.t('hud.distance', { value: v.distance }),
      i18n.t('hud.food', { value: v.food }),
      i18n.t('hud.fps', { value: v.fps }),
      i18n.t('hud.level', { value: v.level }),
      i18n.t('hud.speed', { value: v.speed }),
      i18n.t('hud.seed', { value: v.seed }),
    ]);
  }
```

(`drawEntity`, `ensureLayerTexture`, `drawPrimitive` ficam inalterados.)

- [ ] **Step 4: `main.ts` monta o ciclo real**

Reescrever `src/app/main.ts`:

```ts
import { i18n } from '@services/i18n';
import { createWorld } from '@core/sim';
import { createGame } from '@render/game';
import { FlapInputSource, PauseController } from '@render/input';
import { MatchController } from '@render/match';
import { randomEndlessSeed } from '@render/seedSource';
import { bindGameControls } from '@render/controls';

async function bootstrap(): Promise<void> {
  await i18n.init();
  document.documentElement.lang = i18n.getLanguage();
  document.title = i18n.t('app.title');

  const flap = new FlapInputSource();
  const pause = new PauseController();
  pause.onPause = () => flap.reset(); // anti-flap-fantasma ao pausar

  // Cada partida nasce com uma seed Endless aleatória (fora do core), exibida no HUD.
  const match = new MatchController(
    flap,
    () => {
      const seed = randomEndlessSeed();
      return { world: createWorld({ seed }), seedLabel: seed };
    },
    { onNewMatch: () => flap.reset() }, // tap de restart não vira o 1º flap
  );

  createGame('app', match, { pause });
  bindGameControls(window, { flap, pause, onFlap: () => match.notifyFlap() });
}

void bootstrap();
```

- [ ] **Step 5: Typecheck + suíte completa**

Run: `npm run check && npm test`
Expected: `check` limpo (tsc + eslint + guarda anti-não-determinismo); todos os testes verdes.

- [ ] **Step 6: Commit**

```bash
git add src/render/GameScene.ts src/render/game.ts src/app/main.ts src/render/constants.ts
git commit -m "feat(2.5): GameScene/main dirigidos por MatchController (ready→playing→dead, seed aleatória)"
```

---

### Task 6: Verificação de determinismo + fechamento do item

**Files:**
- Modify: `docs/roadmap/PHASE-02-endless-vertical-slice.md` (marcar 2.5 `[x]`)
- Modify: `CLAUDE.md` ("Estado atual")

- [ ] **Step 1: Bateria de determinismo (core intocado ⇒ deve ficar idêntica)**

Run: `npm run test:determinism`
Expected: 54 testes de determinismo verdes, sem alteração (nada em `src/core/` mudou).

- [ ] **Step 2: Verificação completa**

Run: `npm run check && npm test`
Expected: `check` limpo; suíte inteira verde (incluindo os novos `seedSource`/`match`/`input` e a paridade i18n).

- [ ] **Step 3: Marcar 2.5 concluído em `docs/roadmap/PHASE-02-endless-vertical-slice.md`**

Trocar:

```md
### 2.5 Fluxo de partida
- [ ] Iniciar Endless com seed aleatória (exibida).
- [ ] Morte ao colidir; dificuldade cresce; reinicia do zero a cada partida.
```

por `- [x]` nas duas linhas.

- [ ] **Step 4: Atualizar "Estado atual" no `CLAUDE.md`**

Atualizar o parágrafo da Fase 2 para incluir 2.5 concluído (itens 2.1–2.5 concluídos; faltam 2.6–2.7) e adicionar um parágrafo-resumo de 2.5 no estilo dos anteriores (peças: `seedSource.ts`, `MatchController`, borda de `press`/`onFlap`, prompt `match.tapToStart`, integração `GameScene`/`main`; core intocado; suíte verde; observação de que o overlay de Game Over é 2.6). Ajustar a linha "Próximo:" para **2.6 (Game Over overlay)**.

- [ ] **Step 5: Commit**

```bash
git add docs/roadmap/PHASE-02-endless-vertical-slice.md CLAUDE.md
git commit -m "docs(2.5): marca 2.5 concluído (fase/estado) — próximo 2.6"
```

---

## Self-Review

**1. Spec coverage:**
- Seed aleatória exibida → Task 1 (`randomEndlessSeed`/`endlessSeedFromUint32`) + HUD já mostra seed (Task 5 liga `seedLabel`). ✔
- Máquina de estados ready→playing→dead + restart do zero → Task 3. ✔
- Borda de flap (`press`→bool, `onFlap`) → Task 2. ✔
- Prompt de início i18n → Task 4 + Task 5. ✔
- Integração casca + remoção da demo → Task 5. ✔
- Core intocado / determinismo → Task 6 (verificação). ✔
- Overlay de Game Over adiado p/ 2.6 → registrado na spec e no fechamento (Task 6 Step 4). ✔

**2. Placeholder scan:** todo passo de código traz o código real; sem TBD/TODO. ✔

**3. Type consistency:** `MatchController`/`MatchInit`/`MatchHooks`/`MatchPhase`, `endlessSeedFromUint32`, `press(): boolean`, `bindGameControls(..., onFlap?)`, `createGame(parent, match, deps)` usados de forma consistente entre Tasks 1–5. `loop.world` (getter público existente em `FixedStepLoop`) usado no teste da Task 3. ✔
