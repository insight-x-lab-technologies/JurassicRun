# UI W2: chrome — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Sub-telas ganham barra de navegação inferior emoldurada, títulos dourados ornamentados e cards emoldurados com portraits grandes — casando o chrome do conceito.

**Architecture:** `gen-ui.mjs` extrai `nav.bar` de `ui.remaining` e regenera portraits de dino maiores; um componente `NavBar` (nav.bar 9-slice + ícones) é montado nas sub-telas via `App.tsx`; CSS dá título dourado + cards emoldurados.

**Tech Stack:** Node ESM, Vitest+happy-dom, TypeScript estrito, CSS (border-image, background-clip), Preact/signals.

## Global Constraints

- `src/core/` **NÃO é tocado** ⇒ determinismo **67**.
- Sem dep nova; sem string i18n nova (reusa `nav.*`).
- URLs de imagem via `theme.ts` com `import.meta.env.BASE_URL`; assets de runtime em `public/ui/`.
- Preservar 7.2 (sem scroll horizontal; alvos ≥44px; retrato+paisagem).
- Um commit por task; rodapé `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

### Task 1: `gen-ui.mjs` — `nav.bar` + portraits de dino maiores

**Files:**
- Modify: `scripts/gen-ui.mjs`
- Modify: `tests/render/gen-ui.test.ts`
- Regenera+commita: `public/ui/nav.bar.png` (novo) + `public/ui/dino.<10 ids>.png` (maiores)

- [ ] **Step 1: Região `nav.bar`** — em `scripts/gen-ui.mjs`, na fonte `{ out:'remaining', ... regions:[...] }`, adicionar ao array de regiões (após `medal.bronze`):

```js
    { name: 'nav.bar', x: 0.03, y: 0.55, w: 0.94, h: 0.14 },
```

- [ ] **Step 2: Portraits de dino maiores** — nas 10 fontes de dino de `UI_SOURCES` (as `{ out:'dino.<id>', ..., maxDim: 160, regions:[...] }`), trocar `maxDim: 160` por `maxDim: 256`. (São geradas por um `.map(...)` sobre a lista de ids — trocar o `160` literal p/ `256` no objeto do map.)

- [ ] **Step 3: Teste** — em `tests/render/gen-ui.test.ts`, novo `it`:

```ts
  it('gera a barra de navegação (nav.bar)', () => {
    expect(renderUi().map((o) => o.out)).toContain('nav.bar');
  });
```

- [ ] **Step 4: FALHAR → gerar → PASSAR + suíte**

Run: `npx vitest run tests/render/gen-ui.test.ts` (FAIL: nav.bar ausente) → `npm run gen:ui` → `npx vitest run tests/render/gen-ui.test.ts && npm test && npm run check`
Expected: `gen:ui` imprime `nav.bar` + regenera os 10 dinos; testes verdes.

- [ ] **Step 5: Commit**

```bash
git add scripts/gen-ui.mjs tests/render/gen-ui.test.ts public/ui/nav.bar.png public/ui/dino.starter.png public/ui/dino.lodestone.png public/ui/dino.goldbeak.png public/ui/dino.midas.png public/ui/dino.nine-lives.png public/ui/dino.aegis.png public/ui/dino.prospector.png public/ui/dino.harvester.png public/ui/dino.phoenix.png public/ui/dino.guardian.png
git commit -m "feat(ui-w2): gen-ui extrai nav.bar + portraits de dino maiores (256)"
```

---

### Task 2: `--ui-navbar` no theme + componente NavBar + fiação

**Files:**
- Modify: `src/app/theme.ts`, `src/app/styles/tokens.css`, `src/app/theme.test.ts`
- Create: `src/app/components/NavBar.tsx`
- Create: `tests/app/navbar.test.tsx`
- Modify: `src/app/App.tsx`, `src/app/styles/global.css`

**Interfaces:**
- Consumes: `--ui-navbar` (novo), `icon.<rota>`/`nav.bar` (Task 1), `navigate`/`Screen` (router).
- Produces: `<NavBar current={Screen} />`.

- [ ] **Step 1: Teste do theme (falha)** — em `src/app/theme.test.ts`, novo `it`:

```ts
it('applyPackTheme seta --ui-navbar', () => {
  applyPackTheme(packForId('classic'));
  expect(document.documentElement.style.getPropertyValue('--ui-navbar')).toContain('ui/nav.bar.png');
});
```

- [ ] **Step 2: theme + tokens** — em `src/app/theme.ts` (junto das outras): `root.style.setProperty('--ui-navbar', \`url(${base}ui/nav.bar.png)\`);`. Em `tokens.css`: `--ui-navbar: none;`.

- [ ] **Step 3: Componente `NavBar`** — criar `src/app/components/NavBar.tsx`:

```tsx
import type { VNode } from 'preact';
import { navigate, type Screen } from '../router';
import { i18n } from '@services/i18n';

const NAV: readonly Screen[] = ['daily', 'weekly', 'nest', 'shop', 'expansions', 'leaderboard', 'settings'];

export function NavBar({ current }: { current: Screen }): VNode {
  const base = import.meta.env.BASE_URL;
  return (
    <nav class="navbar" aria-label={i18n.t('app.title')}>
      {NAV.map((screen) => (
        <button
          key={screen}
          type="button"
          class={`navbar__item${screen === current ? ' navbar__item--active' : ''}`}
          aria-current={screen === current ? 'page' : undefined}
          data-testid={`navbar-${screen}`}
          onClick={() => navigate(screen)}
        >
          <img class="navbar__icon" src={`${base}ui/icon.${screen}.png`} alt="" aria-hidden="true" />
          <span class="navbar__label">{i18n.t(`nav.${screen}`)}</span>
        </button>
      ))}
    </nav>
  );
}
```

- [ ] **Step 4: Teste do NavBar** — criar `tests/app/navbar.test.tsx`:

```tsx
// @vitest-environment happy-dom
import { describe, it, expect } from 'vitest';
import { render } from 'preact';
import { NavBar } from '../../src/app/components/NavBar';
import { router } from '../../src/app/router';

describe('NavBar', () => {
  it('renderiza os 7 destinos e navega no clique', async () => {
    const host = document.createElement('div');
    render(<NavBar current="nest" />, host);
    const items = host.querySelectorAll('.navbar__item');
    expect(items.length).toBe(7);
    expect(host.querySelector('.navbar__item--active')?.getAttribute('data-testid')).toBe('navbar-nest');
    (host.querySelector('[data-testid="navbar-shop"]') as HTMLButtonElement).click();
    await Promise.resolve();
    expect(router.route.value).toBe('shop');
  });
});
```
(ajuste o import de `router`/`route` ao que `../src/app/router` exporta — provavelmente `router.route` ou um sinal `route`; verifique `src/app/router/index.ts`.)

- [ ] **Step 5: Fiação no `App.tsx`** — nas sub-telas de navegação (`daily, weekly, nest, shop, expansions, leaderboard, settings, trophies, profile`), renderizar `<NavBar current={route}/>` após o componente da tela; **não** em `home/play/onboarding`. Ex.: um helper `NAV_SCREENS: ReadonlySet<Screen>` e, no retorno do `App`, `{NAV_SCREENS.has(route) && <NavBar current={route} />}` ao lado do switch de tela. (Preserve o `default: never` do switch.)

- [ ] **Step 6: CSS da NavBar** — em `global.css`, adicionar:

```css
.navbar {
  position: fixed;
  left: 0; right: 0; bottom: 0;
  z-index: 50;
  display: flex;
  justify-content: center;
  gap: var(--space-1);
  padding: var(--space-2) var(--space-3);
  padding-bottom: max(var(--space-2), env(safe-area-inset-bottom));
  border-style: solid; border-color: transparent;
  border-width: 10px 24px;
  border-image: var(--ui-navbar) 40% 20% fill / 10px 24px / 0 stretch;
  overflow-x: auto;
}
.navbar__item {
  display: inline-flex; flex-direction: column; align-items: center; gap: 2px;
  min-width: 3.5rem; min-height: 44px;
  background: none; border: none; cursor: pointer;
  color: var(--color-text-muted); font: inherit; font-size: var(--font-size-sm);
  padding: var(--space-1) var(--space-2);
}
.navbar__item--active { color: var(--color-accent); }
.navbar__icon { width: 1.6rem; height: 1.6rem; }
.navbar__label { white-space: nowrap; }
/* conteúdo das sub-telas não fica sob a barra */
.screen { padding-bottom: calc(var(--space-6) + 4rem); }
@media (max-width: 640px) { .navbar__label { display: none; } } /* estreito: só ícones */
```

- [ ] **Step 7: Rodar — testes + tipos**

Run: `npx vitest run src/app/theme.test.ts tests/app/navbar.test.tsx && npm run check && npm test`
Expected: verde.

- [ ] **Step 8: Commit**

```bash
git add src/app/theme.ts src/app/styles/tokens.css src/app/theme.test.ts src/app/components/NavBar.tsx tests/app/navbar.test.tsx src/app/App.tsx src/app/styles/global.css
git commit -m "feat(ui-w2): barra de navegação inferior (nav.bar 9-slice + ícones)"
```

---

### Task 3: Títulos dourados + cards emoldurados + portraits

**Files:**
- Modify: `src/app/styles/global.css`

**Interfaces:** nenhuma API (casca CSS; validado no Playwright da Task 4).

- [ ] **Step 1: Títulos dourados** — em `global.css`, SUBSTITUIR a regra `.screen__title { … }` por:

```css
.screen__title {
  font-size: var(--font-size-xl);
  font-weight: 800;
  letter-spacing: 0.02em;
  margin: 0;
  color: var(--color-gold); /* fallback */
  background: linear-gradient(180deg, #f2d878 0%, #c9a227 55%, #a07d1e 100%);
  -webkit-background-clip: text;
  background-clip: text;
  -webkit-text-fill-color: transparent;
  text-shadow: 0 2px 6px rgba(0, 0, 0, 0.85);
}
```

- [ ] **Step 2: Cards emoldurados + portrait grande** — em `global.css`, adicionar (sobrepõe as regras existentes de `.dino-card`/`.expansion-card`/`.trophy-card`):

```css
/* UI W2: cards como painel 9-slice + portrait grande no topo. */
.dino-card,
.expansion-card,
.trophy-card {
  background: transparent;
  border-style: solid; border-color: transparent;
  border-width: 14px;
  border-image: var(--ui-panel) 30% fill / 14px / 0 stretch;
  border-radius: 0;
  overflow: hidden;
}
img.dino-card__avatar,
img.expansion-card__avatar {
  width: 100%;
  height: 8rem;
  border-radius: var(--radius-sm);
  object-fit: cover;
  object-position: center top;
}
.nest__grid,
.expansions__grid {
  grid-template-columns: repeat(auto-fit, minmax(11rem, 1fr));
}
```

- [ ] **Step 3: Tipos + suíte**

Run: `npm run check && npm test`
Expected: verde (sem teste novo — casca CSS).

- [ ] **Step 4: Commit**

```bash
git add src/app/styles/global.css
git commit -m "feat(ui-w2): títulos dourados + cards emoldurados com portrait grande"
```

---

### Task 4 (controlador, inline): validação widescreen + tuning + docs

- [ ] **Step 1:** `npm run build` + `npx vite preview`; Playwright **1366×768**. Sub-telas (Ninho/Expansões/Loja): **barra de nav inferior** emoldurada + 7 ícones, destino atual destacado; **título dourado**; **cards emoldurados com portrait grande** (dino/expansão). Screenshots.
- [ ] **Step 2:** **Tuning** — insets/border-image da nav e dos cards, altura do portrait, cor do título: ajustar em `global.css` (inline, commit) até casar o conceito.
- [ ] **Step 3:** **390×844** — nav compacta (só ícones) sem scroll horizontal; cards empilham legíveis. Screenshot.
- [ ] **Step 4:** Atualizar `CLAUDE.md` (W2 feita; W3/W4 restantes; anotar W2b adiado = painel dino ativo + header chips). Commit.

**Gotcha (7.2/8.3):** SW serve dist antigo — `unregister()` + limpar caches + `?nocache=<ts>`.

## Self-Review

- **Cobertura:** nav.bar+portraits (T1) ✓; NavBar componente+theme+fiação (T2) ✓; títulos+cards+portraits (T3) ✓; validação+docs (T4) ✓; core intocado ✓.
- **Placeholders:** valores de border-image/insets/altura são placeholders ajustados na T4.
- **Consistência:** `nav.bar` (T1) → `--ui-navbar` (T2) → `.navbar` border-image (T2); `icon.<rota>`/`nav.<rota>` reusados; `--ui-panel` (Tier-1) agora usado nos cards (T3); `dino.<id>`/`cover.<id>` (Rodada C, maiores) nos portraits.
