# 4.8 Configurações — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Uma tela de Configurações offline com volume, música-menu on/off, música-gameplay on/off e idioma (troca ao vivo), tudo persistido.

**Architecture:** Novo serviço reativo `src/services/settings/` no molde puro×casca (store puro + storage localStorage injetável + `SettingsService` singleton com `@preact/signals`), consumido por uma `SettingsScreen`. Idioma tem efeito real imediato via re-render da raiz `App`; volume e músicas são seams persistidos que o 4.10 (Áudio) consumirá.

**Tech Stack:** TypeScript estrito, Preact, `@preact/signals`, i18next (`@services/i18n`), Vitest + happy-dom.

## Global Constraints

- **`src/core/` NÃO é tocado** ⇒ determinismo intacto (67 golden hashes). Nada deste plano importa/altera `src/core/`.
- **REGRA 4 (i18n):** nenhuma string visível hardcoded; toda chave existe nos **10 locales** (`en, es, pt-BR, fr, it, de, ja, zh, ko, hi`). Paridade exata é validada por `tests/i18n/locales.test.ts`.
- TypeScript estrito, sem `any` sem justificativa. `src/services/` pode usar IO/DOM na casca (não é core).
- Padrão de referência: `src/services/wallet/` (`store.ts`/`storage.ts`/`index.ts`).
- Design tokens CSS disponíveis: `--color-{bg,surface,surface-2,text,text-muted,primary,on-primary,accent,danger}`, `--font-size-{sm,md,lg,xl}`, `--space-*`, `--radius-{sm,md,lg}`. Sem cor hardcoded; mobile-first; alvos de toque ≥44px.
- Gotcha recorrente (signals + happy-dom): após evento DOM que dispara `useState`/sinal, um `render()` síncrono pode não flushar — usar `await Promise.resolve()`; `useEffect` precisa de macrotask (`setTimeout`).

---

### Task 1: `settings/store.ts` — estado puro + operações

**Files:**
- Create: `src/services/settings/store.ts`
- Test: `tests/services/settings/store.test.ts`

**Interfaces:**
- Consumes: `SupportedLanguage`, `DEFAULT_LANGUAGE`, `SUPPORTED_LANGUAGES` de `@i18n/locales/index`.
- Produces:
  - `interface SettingsState { readonly volume: number; readonly menuMusic: boolean; readonly gameplayMusic: boolean; readonly language: SupportedLanguage }`
  - `initialSettingsState(): SettingsState`
  - `sanitizeVolume(v: number): number`
  - `setVolume(s, v): SettingsState`, `setMenuMusic(s, on): SettingsState`, `setGameplayMusic(s, on): SettingsState`, `setLanguage(s, lng: string): SettingsState`
  - `isSupportedLanguage(lng: string): lng is SupportedLanguage`

- [ ] **Step 1: Write the failing test**

```ts
// tests/services/settings/store.test.ts
import { describe, it, expect } from 'vitest';
import {
  initialSettingsState,
  sanitizeVolume,
  setVolume,
  setMenuMusic,
  setGameplayMusic,
  setLanguage,
  isSupportedLanguage,
} from '@services/settings/store';

describe('settings/store', () => {
  it('estado inicial: volume 80, músicas on, idioma en', () => {
    expect(initialSettingsState()).toEqual({
      volume: 80,
      menuMusic: true,
      gameplayMusic: true,
      language: 'en',
    });
  });

  it('sanitizeVolume clampa e arredonda; inválido ⇒ 0', () => {
    expect(sanitizeVolume(50.4)).toBe(50);
    expect(sanitizeVolume(50.6)).toBe(51);
    expect(sanitizeVolume(-5)).toBe(0);
    expect(sanitizeVolume(140)).toBe(100);
    expect(sanitizeVolume(Number.NaN)).toBe(0);
    expect(sanitizeVolume(Number.POSITIVE_INFINITY)).toBe(100);
  });

  it('setVolume aplica sanitização e é imutável', () => {
    const s = initialSettingsState();
    const s2 = setVolume(s, 30);
    expect(s2.volume).toBe(30);
    expect(s.volume).toBe(80); // original intacto
    expect(setVolume(s, 200).volume).toBe(100);
  });

  it('toggles de música', () => {
    const s = initialSettingsState();
    expect(setMenuMusic(s, false).menuMusic).toBe(false);
    expect(setGameplayMusic(s, false).gameplayMusic).toBe(false);
    expect(s.menuMusic).toBe(true); // imutável
  });

  it('setLanguage: válido troca; inválido retorna a MESMA referência', () => {
    const s = initialSettingsState();
    expect(setLanguage(s, 'pt-BR').language).toBe('pt-BR');
    expect(setLanguage(s, 'xx')).toBe(s);
  });

  it('isSupportedLanguage', () => {
    expect(isSupportedLanguage('de')).toBe(true);
    expect(isSupportedLanguage('xx')).toBe(false);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/services/settings/store.test.ts`
Expected: FAIL (módulo não existe).

- [ ] **Step 3: Write minimal implementation**

```ts
// src/services/settings/store.ts
import {
  SUPPORTED_LANGUAGES,
  DEFAULT_LANGUAGE,
  type SupportedLanguage,
} from '@i18n/locales/index';

export interface SettingsState {
  readonly volume: number; // inteiro 0..100
  readonly menuMusic: boolean;
  readonly gameplayMusic: boolean;
  readonly language: SupportedLanguage;
}

export function initialSettingsState(): SettingsState {
  return { volume: 80, menuMusic: true, gameplayMusic: true, language: DEFAULT_LANGUAGE };
}

/** Clampa a [0,100] e arredonda; NaN ⇒ 0, +∞ ⇒ 100. */
export function sanitizeVolume(v: number): number {
  if (Number.isNaN(v)) return 0;
  const rounded = Math.round(v);
  if (rounded < 0) return 0;
  if (rounded > 100) return 100;
  return rounded;
}

export function isSupportedLanguage(lng: string): lng is SupportedLanguage {
  return (SUPPORTED_LANGUAGES as readonly string[]).includes(lng);
}

export function setVolume(s: SettingsState, v: number): SettingsState {
  return { ...s, volume: sanitizeVolume(v) };
}

export function setMenuMusic(s: SettingsState, on: boolean): SettingsState {
  return { ...s, menuMusic: on };
}

export function setGameplayMusic(s: SettingsState, on: boolean): SettingsState {
  return { ...s, gameplayMusic: on };
}

/** Idioma inválido ⇒ retorna a MESMA referência (no-op). */
export function setLanguage(s: SettingsState, lng: string): SettingsState {
  if (!isSupportedLanguage(lng)) return s;
  return { ...s, language: lng };
}
```

Nota: `Math.round(Infinity)` é `Infinity`; `Infinity > 100` ⇒ retorna 100. ✔

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/services/settings/store.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/services/settings/store.ts tests/services/settings/store.test.ts
git commit -m "feat(4.8): settings/store puro (estado + sanitização de volume/idioma)"
```

---

### Task 2: `settings/storage.ts` — persistência localStorage robusta

**Files:**
- Create: `src/services/settings/storage.ts`
- Test: `tests/services/settings/storage.test.ts`

**Interfaces:**
- Consumes: `SettingsState`, `initialSettingsState`, `sanitizeVolume`, `isSupportedLanguage` de `./store`.
- Produces:
  - `interface SettingsStorage { load(): SettingsState; save(state: SettingsState): void }`
  - `const STORAGE_KEY = 'jurassicrun.settings.v1'`
  - `memorySettingsStorage(initial?: SettingsState): SettingsStorage`
  - `localStorageSettingsStorage(): SettingsStorage`
  - `parseState(raw: string): SettingsState` (exportada para teste)

- [ ] **Step 1: Write the failing test**

```ts
// tests/services/settings/storage.test.ts
import { describe, it, expect, beforeEach } from 'vitest';
import {
  memorySettingsStorage,
  localStorageSettingsStorage,
  parseState,
  STORAGE_KEY,
} from '@services/settings/storage';
import { initialSettingsState } from '@services/settings/store';

describe('settings/storage', () => {
  it('parseState: JSON inválido ⇒ defaults', () => {
    expect(parseState('não-é-json')).toEqual(initialSettingsState());
    expect(parseState('[]')).toEqual(initialSettingsState());
    expect(parseState('null')).toEqual(initialSettingsState());
  });

  it('parseState saneia por campo sem descartar os válidos', () => {
    const raw = JSON.stringify({
      volume: 250,          // fora de faixa ⇒ 100
      menuMusic: 'sim',     // não-boolean ⇒ default true
      gameplayMusic: false, // válido ⇒ preservado
      language: 'xx',       // desconhecido ⇒ en
    });
    expect(parseState(raw)).toEqual({
      volume: 100,
      menuMusic: true,
      gameplayMusic: false,
      language: 'en',
    });
  });

  it('parseState preserva valores válidos', () => {
    const raw = JSON.stringify({ volume: 40, menuMusic: false, gameplayMusic: true, language: 'ja' });
    expect(parseState(raw)).toEqual({ volume: 40, menuMusic: false, gameplayMusic: true, language: 'ja' });
  });

  it('memory storage: round-trip', () => {
    const st = memorySettingsStorage();
    const next = { volume: 10, menuMusic: false, gameplayMusic: false, language: 'de' as const };
    st.save(next);
    expect(st.load()).toEqual(next);
  });

  it('localStorage storage: save→load round-trip', () => {
    localStorage.clear();
    const st = localStorageSettingsStorage();
    const next = { volume: 25, menuMusic: false, gameplayMusic: true, language: 'fr' as const };
    st.save(next);
    expect(JSON.parse(localStorage.getItem(STORAGE_KEY)!)).toMatchObject({ version: 1, volume: 25 });
    expect(st.load()).toEqual(next);
  });

  it('localStorage vazio ⇒ defaults', () => {
    localStorage.clear();
    expect(localStorageSettingsStorage().load()).toEqual(initialSettingsState());
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/services/settings/storage.test.ts`
Expected: FAIL (módulo não existe).

- [ ] **Step 3: Write minimal implementation**

```ts
// src/services/settings/storage.ts
import {
  initialSettingsState,
  sanitizeVolume,
  isSupportedLanguage,
  type SettingsState,
} from './store';

export interface SettingsStorage {
  load(): SettingsState;
  save(state: SettingsState): void;
}

export const STORAGE_KEY = 'jurassicrun.settings.v1';

export function memorySettingsStorage(initial: SettingsState = initialSettingsState()): SettingsStorage {
  let state = initial;
  return {
    load: () => state,
    save: (s) => {
      state = s;
    },
  };
}

export function parseState(raw: string): SettingsState {
  const base = initialSettingsState();
  try {
    const data: unknown = JSON.parse(raw);
    if (typeof data !== 'object' || data === null || Array.isArray(data)) return base;
    const d = data as Record<string, unknown>;
    const volume = typeof d.volume === 'number' ? sanitizeVolume(d.volume) : base.volume;
    const menuMusic = typeof d.menuMusic === 'boolean' ? d.menuMusic : base.menuMusic;
    const gameplayMusic = typeof d.gameplayMusic === 'boolean' ? d.gameplayMusic : base.gameplayMusic;
    const language =
      typeof d.language === 'string' && isSupportedLanguage(d.language) ? d.language : base.language;
    return { volume, menuMusic, gameplayMusic, language };
  } catch {
    return base;
  }
}

export function localStorageSettingsStorage(): SettingsStorage {
  return {
    load(): SettingsState {
      try {
        const raw = localStorage.getItem(STORAGE_KEY);
        return raw === null ? initialSettingsState() : parseState(raw);
      } catch {
        return initialSettingsState();
      }
    },
    save(state: SettingsState): void {
      try {
        localStorage.setItem(
          STORAGE_KEY,
          JSON.stringify({ version: 1, ...state }),
        );
      } catch {
        // storage indisponível (modo privado); persistência é best-effort.
      }
    },
  };
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/services/settings/storage.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/services/settings/storage.ts tests/services/settings/storage.test.ts
git commit -m "feat(4.8): settings/storage localStorage com parseState saneado por campo"
```

---

### Task 3: `settings/index.ts` — `SettingsService` reativo + i18n

**Files:**
- Create: `src/services/settings/index.ts`
- Test: `tests/services/settings/service.test.ts`

**Interfaces:**
- Consumes: store (`initialSettingsState`, `setVolume`, `setMenuMusic`, `setGameplayMusic`, `setLanguage`, `SettingsState`), storage (`localStorageSettingsStorage`, `memorySettingsStorage`, `SettingsStorage`), `i18n` de `@services/i18n`.
- Produces: `settingsService` singleton com sinais `ReadonlySignal` `volume`/`menuMusic`/`gameplayMusic`/`language`; métodos `init(storage?)`, `setVolume(v)`, `setMenuMusic(on)`, `setGameplayMusic(on)`, `setLanguage(lng)` (async). Reexporta `memorySettingsStorage` type `SettingsStorage`.

- [ ] **Step 1: Write the failing test**

```ts
// tests/services/settings/service.test.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { settingsService } from '@services/settings';
import { memorySettingsStorage } from '@services/settings/storage';
import { i18n } from '@services/i18n';

describe('SettingsService', () => {
  beforeEach(async () => {
    await i18n.init();
  });

  it('init carrega estado persistido e aplica o idioma no i18n', async () => {
    const storage = memorySettingsStorage({
      volume: 30,
      menuMusic: false,
      gameplayMusic: true,
      language: 'pt-BR',
    });
    await settingsService.init(storage);
    expect(settingsService.volume.value).toBe(30);
    expect(settingsService.menuMusic.value).toBe(false);
    expect(settingsService.language.value).toBe('pt-BR');
    expect(i18n.getLanguage()).toBe('pt-BR');
  });

  it('setVolume/setMenuMusic comitam sinal e persistem', async () => {
    const storage = memorySettingsStorage();
    await settingsService.init(storage);
    settingsService.setVolume(15);
    settingsService.setMenuMusic(false);
    expect(settingsService.volume.value).toBe(15);
    expect(settingsService.menuMusic.value).toBe(false);
    expect(storage.load().volume).toBe(15);
    expect(storage.load().menuMusic).toBe(false);
  });

  it('setLanguage troca sinal, i18n e persiste', async () => {
    const storage = memorySettingsStorage();
    await settingsService.init(storage);
    await settingsService.setLanguage('de');
    expect(settingsService.language.value).toBe('de');
    expect(i18n.getLanguage()).toBe('de');
    expect(storage.load().language).toBe('de');
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/services/settings/service.test.ts`
Expected: FAIL (módulo não existe).

- [ ] **Step 3: Write minimal implementation**

```ts
// src/services/settings/index.ts
import { signal, computed, type ReadonlySignal } from '@preact/signals';
import {
  initialSettingsState,
  setVolume,
  setMenuMusic,
  setGameplayMusic,
  setLanguage,
  type SettingsState,
} from './store';
import {
  localStorageSettingsStorage,
  memorySettingsStorage,
  type SettingsStorage,
} from './storage';
import { i18n } from '@services/i18n';

class SettingsService {
  private storage: SettingsStorage = memorySettingsStorage();
  private readonly _state = signal<SettingsState>(initialSettingsState());

  readonly volume: ReadonlySignal<number> = computed(() => this._state.value.volume);
  readonly menuMusic: ReadonlySignal<boolean> = computed(() => this._state.value.menuMusic);
  readonly gameplayMusic: ReadonlySignal<boolean> = computed(() => this._state.value.gameplayMusic);
  readonly language: ReadonlySignal<string> = computed(() => this._state.value.language);

  async init(storage: SettingsStorage = localStorageSettingsStorage()): Promise<void> {
    this.storage = storage;
    const state = storage.load();
    this._state.value = state;
    await this.applyLanguage(state.language);
  }

  setVolume(v: number): void {
    this.commit(setVolume(this._state.value, v));
  }

  setMenuMusic(on: boolean): void {
    this.commit(setMenuMusic(this._state.value, on));
  }

  setGameplayMusic(on: boolean): void {
    this.commit(setGameplayMusic(this._state.value, on));
  }

  /** Troca o idioma: aplica no i18n ANTES de comitar o sinal (para o re-render já ler as strings novas). */
  async setLanguage(lng: string): Promise<void> {
    const next = setLanguage(this._state.value, lng);
    if (next === this._state.value) return; // idioma inválido: no-op
    await this.applyLanguage(next.language);
    this.commit(next);
  }

  private async applyLanguage(lng: string): Promise<void> {
    await i18n.changeLanguage(lng);
    document.documentElement.lang = i18n.getLanguage();
    document.title = i18n.t('app.title');
  }

  private commit(state: SettingsState): void {
    this._state.value = state;
    this.storage.save(state);
  }
}

export const settingsService = new SettingsService();
export type { SettingsStorage } from './storage';
```

Nota: `applyLanguage` toca `document` — permitido na casca de serviço (não é core). Em ambiente de teste happy-dom `document` existe.

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/services/settings/service.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/services/settings/index.ts tests/services/settings/service.test.ts
git commit -m "feat(4.8): SettingsService reativo (sinais + i18n.changeLanguage na troca de idioma)"
```

---

### Task 4: i18n — chaves `settings.*` (10 locales) + nomes nativos

**Files:**
- Modify: `src/i18n/locales/{en,es,pt-BR,fr,it,de,ja,zh,ko,hi}.json` (adicionar bloco `settings`)
- Modify: `src/i18n/locales/index.ts` (adicionar `LANGUAGE_NATIVE_NAMES`)
- Test: `tests/i18n/locales.test.ts` (já existe — passa a exigir paridade das novas chaves; sem edição)

**Interfaces:**
- Produces: chaves `settings.{title,volume,menuMusic,gameplayMusic,language,on,off,back}` em 10 locales; `LANGUAGE_NATIVE_NAMES: Record<SupportedLanguage, string>` em `@i18n/locales/index`.

- [ ] **Step 1: Adicionar o bloco `settings` a cada locale**

Adicione ao objeto raiz de cada arquivo JSON o bloco abaixo (traduções nativas por idioma):

`en.json`:
```json
"settings": {
  "title": "Settings",
  "volume": "Volume",
  "menuMusic": "Menu music",
  "gameplayMusic": "Gameplay music",
  "language": "Language",
  "on": "On",
  "off": "Off",
  "back": "Back"
}
```
`es.json`:
```json
"settings": { "title": "Ajustes", "volume": "Volumen", "menuMusic": "Música del menú", "gameplayMusic": "Música del juego", "language": "Idioma", "on": "Sí", "off": "No", "back": "Volver" }
```
`pt-BR.json`:
```json
"settings": { "title": "Configurações", "volume": "Volume", "menuMusic": "Música do menu", "gameplayMusic": "Música do jogo", "language": "Idioma", "on": "Ligada", "off": "Desligada", "back": "Voltar" }
```
`fr.json`:
```json
"settings": { "title": "Paramètres", "volume": "Volume", "menuMusic": "Musique du menu", "gameplayMusic": "Musique de jeu", "language": "Langue", "on": "Activé", "off": "Désactivé", "back": "Retour" }
```
`it.json`:
```json
"settings": { "title": "Impostazioni", "volume": "Volume", "menuMusic": "Musica del menu", "gameplayMusic": "Musica di gioco", "language": "Lingua", "on": "Sì", "off": "No", "back": "Indietro" }
```
`de.json`:
```json
"settings": { "title": "Einstellungen", "volume": "Lautstärke", "menuMusic": "Menümusik", "gameplayMusic": "Spielmusik", "language": "Sprache", "on": "An", "off": "Aus", "back": "Zurück" }
```
`ja.json`:
```json
"settings": { "title": "設定", "volume": "音量", "menuMusic": "メニュー音楽", "gameplayMusic": "ゲーム音楽", "language": "言語", "on": "オン", "off": "オフ", "back": "戻る" }
```
`zh.json`:
```json
"settings": { "title": "设置", "volume": "音量", "menuMusic": "菜单音乐", "gameplayMusic": "游戏音乐", "language": "语言", "on": "开", "off": "关", "back": "返回" }
```
`ko.json`:
```json
"settings": { "title": "설정", "volume": "볼륨", "menuMusic": "메뉴 음악", "gameplayMusic": "게임 음악", "language": "언어", "on": "켜짐", "off": "꺼짐", "back": "뒤로" }
```
`hi.json`:
```json
"settings": { "title": "सेटिंग्स", "volume": "वॉल्यूम", "menuMusic": "मेन्यू संगीत", "gameplayMusic": "गेमप्ले संगीत", "language": "भाषा", "on": "चालू", "off": "बंद", "back": "वापस" }
```

Insira o bloco em posição consistente (ex.: após o bloco existente `trait` ou antes de `nest`); a posição não afeta a paridade (o teste compara key-paths).

- [ ] **Step 2: Adicionar `LANGUAGE_NATIVE_NAMES` ao index**

Ao final de `src/i18n/locales/index.ts`, adicione:
```ts
export const LANGUAGE_NATIVE_NAMES: Record<SupportedLanguage, string> = {
  en: 'English',
  es: 'Español',
  'pt-BR': 'Português (BR)',
  fr: 'Français',
  it: 'Italiano',
  de: 'Deutsch',
  ja: '日本語',
  zh: '中文',
  ko: '한국어',
  hi: 'हिन्दी',
};
```

- [ ] **Step 3: Rodar o teste de paridade + typecheck**

Run: `npx vitest run tests/i18n/locales.test.ts && npm run check`
Expected: PASS (paridade das novas chaves em 10 locales; TS limpo). Se algum locale acusar divergência, corrija o bloco desse arquivo.

- [ ] **Step 4: Commit**

```bash
git add src/i18n/locales
git commit -m "feat(4.8): chaves i18n settings.* (10 locales) + LANGUAGE_NATIVE_NAMES"
```

---

### Task 5: `SettingsScreen` + fiação (App, bootstrap, CSS)

**Files:**
- Create: `src/app/screens/SettingsScreen.tsx`
- Modify: `src/app/App.tsx` (rota `settings` → `<SettingsScreen />`; `App` lê `settingsService.language.value` no topo)
- Modify: `src/app/main.tsx` (`await settingsService.init()` no bootstrap; remove fixação direta de `document.lang`/`title`)
- Modify: `src/app/styles/global.css` (bloco `.settings`)
- Test: `tests/app/SettingsScreen.test.tsx`

**Interfaces:**
- Consumes: `settingsService` (`@services/settings`), `LANGUAGE_NATIVE_NAMES` + `SUPPORTED_LANGUAGES` (`@i18n/locales/index`), `i18n` (`@services/i18n`), `back` (`../router`).

- [ ] **Step 1: Write the failing component test**

```tsx
// tests/app/SettingsScreen.test.tsx
import { describe, it, expect, beforeEach } from 'vitest';
import { render, cleanup } from '@testing-library/preact';
import { SettingsScreen } from '@app/screens/SettingsScreen';
import { settingsService } from '@services/settings';
import { memorySettingsStorage } from '@services/settings/storage';
import { i18n } from '@services/i18n';

describe('SettingsScreen', () => {
  beforeEach(async () => {
    await i18n.init();
    await settingsService.init(memorySettingsStorage());
    cleanup();
  });

  it('renderiza os 4 controles', () => {
    const { container } = render(<SettingsScreen />);
    expect(container.querySelector('[data-testid="settings-volume"]')).not.toBeNull();
    expect(container.querySelector('[data-testid="settings-menu-music"]')).not.toBeNull();
    expect(container.querySelector('[data-testid="settings-gameplay-music"]')).not.toBeNull();
    expect(container.querySelector('[data-testid="settings-language"]')).not.toBeNull();
  });

  it('trocar o volume atualiza o serviço', () => {
    const { container } = render(<SettingsScreen />);
    const slider = container.querySelector('[data-testid="settings-volume"]') as HTMLInputElement;
    slider.value = '20';
    slider.dispatchEvent(new Event('input', { bubbles: true }));
    expect(settingsService.volume.value).toBe(20);
  });

  it('trocar o idioma no select troca o idioma ativo do i18n', async () => {
    const { container } = render(<SettingsScreen />);
    const select = container.querySelector('[data-testid="settings-language"]') as HTMLSelectElement;
    select.value = 'de';
    select.dispatchEvent(new Event('change', { bubbles: true }));
    await Promise.resolve();
    await Promise.resolve();
    expect(i18n.getLanguage()).toBe('de');
    expect(settingsService.language.value).toBe('de');
  });
});
```

Nota: se `@testing-library/preact` não estiver instalado, use o padrão dos testes de componente existentes (ver `tests/app/*.test.tsx`). Verifique com `ls tests/app` e `grep -r "render" tests/app` antes do Step 1 e espelhe o import usado lá; se o projeto usa `preact/render`-manual, adapte (o gotcha signals+happy-dom aplica-se).

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/app/SettingsScreen.test.tsx`
Expected: FAIL (componente não existe).

- [ ] **Step 3: Criar o componente**

```tsx
// src/app/screens/SettingsScreen.tsx
import type { VNode } from 'preact';
import { back } from '../router';
import { i18n } from '@services/i18n';
import { settingsService } from '@services/settings';
import { SUPPORTED_LANGUAGES, LANGUAGE_NATIVE_NAMES } from '@i18n/locales/index';

export function SettingsScreen(): VNode {
  const volume = settingsService.volume.value;
  const menuMusic = settingsService.menuMusic.value;
  const gameplayMusic = settingsService.gameplayMusic.value;
  const language = settingsService.language.value;

  return (
    <div class="screen settings">
      <h1 class="screen__title">{i18n.t('settings.title')}</h1>

      <label class="settings__row" for="settings-volume">
        <span class="settings__label">{i18n.t('settings.volume')}</span>
        <input
          id="settings-volume"
          data-testid="settings-volume"
          class="settings__slider"
          type="range"
          min={0}
          max={100}
          value={volume}
          onInput={(e) => settingsService.setVolume((e.currentTarget as HTMLInputElement).valueAsNumber)}
        />
        <span class="settings__value" aria-hidden="true">{volume}</span>
      </label>

      <label class="settings__row" for="settings-menu-music">
        <span class="settings__label">{i18n.t('settings.menuMusic')}</span>
        <input
          id="settings-menu-music"
          data-testid="settings-menu-music"
          type="checkbox"
          class="settings__toggle"
          checked={menuMusic}
          onChange={(e) => settingsService.setMenuMusic((e.currentTarget as HTMLInputElement).checked)}
        />
        <span class="settings__state" aria-hidden="true">
          {i18n.t(menuMusic ? 'settings.on' : 'settings.off')}
        </span>
      </label>

      <label class="settings__row" for="settings-gameplay-music">
        <span class="settings__label">{i18n.t('settings.gameplayMusic')}</span>
        <input
          id="settings-gameplay-music"
          data-testid="settings-gameplay-music"
          type="checkbox"
          class="settings__toggle"
          checked={gameplayMusic}
          onChange={(e) => settingsService.setGameplayMusic((e.currentTarget as HTMLInputElement).checked)}
        />
        <span class="settings__state" aria-hidden="true">
          {i18n.t(gameplayMusic ? 'settings.on' : 'settings.off')}
        </span>
      </label>

      <label class="settings__row" for="settings-language">
        <span class="settings__label">{i18n.t('settings.language')}</span>
        <select
          id="settings-language"
          data-testid="settings-language"
          class="settings__select"
          value={language}
          onChange={(e) => void settingsService.setLanguage((e.currentTarget as HTMLSelectElement).value)}
        >
          {SUPPORTED_LANGUAGES.map((lng) => (
            <option key={lng} value={lng}>
              {LANGUAGE_NATIVE_NAMES[lng]}
            </option>
          ))}
        </select>
      </label>

      <button type="button" class="btn btn--ghost" onClick={() => back()}>
        {i18n.t('settings.back')}
      </button>
    </div>
  );
}
```

- [ ] **Step 4: Fiar rota + re-render de idioma no `App.tsx`**

Em `src/app/App.tsx`: importe a tela e o serviço, troque o caso `settings`, e faça `App` depender do sinal de idioma.

Import (topo):
```tsx
import { SettingsScreen } from './screens/SettingsScreen';
import { settingsService } from '@services/settings';
```
No `switch` de `screenFor`, substitua:
```tsx
    case 'settings':
      return <PlaceholderScreen titleKey="screen.settings" />;
```
por:
```tsx
    case 'settings':
      return <SettingsScreen />;
```
No corpo de `App`, adicione a leitura do sinal de idioma ANTES do return (força re-render de toda a árvore ao trocar idioma):
```tsx
export function App(): VNode {
  // Assinar o idioma: qualquer troca re-renderiza a árvore inteira com as strings novas.
  settingsService.language.value;
  if (profileService.activeProfile.value === null) {
    return <OnboardingScreen />;
  }
  return screenFor(route.value);
}
```

- [ ] **Step 5: Bootstrap — `settingsService.init` dono do idioma**

Em `src/app/main.tsx`: importe `settingsService`, chame `await settingsService.init()` após `i18n.init()`, e remova a fixação manual de `document.lang`/`title` (agora feita por `settingsService.init`).

Substitua o corpo de `bootstrap` por:
```tsx
  await i18n.init();
  await settingsService.init();
  profileService.init();
  nestService.init();
  walletService.init();
  entitlementsService.init();
  trophyService.init();

  const root = document.getElementById('app');
  if (root === null) throw new Error('#app não encontrado');
  render(<App />, root);
```
E adicione o import: `import { settingsService } from '@services/settings';`

- [ ] **Step 6: CSS — bloco `.settings`**

Anexe a `src/app/styles/global.css`:
```css
.settings__row {
  display: flex;
  align-items: center;
  gap: var(--space-md);
  min-height: 44px;
  padding: var(--space-sm) 0;
}
.settings__label {
  flex: 1;
  color: var(--color-text);
  font-size: var(--font-size-md);
}
.settings__slider {
  flex: 2;
  min-width: 120px;
}
.settings__value {
  width: 3ch;
  text-align: right;
  color: var(--color-text-muted);
}
.settings__toggle {
  width: 24px;
  height: 24px;
}
.settings__state {
  width: 6ch;
  color: var(--color-text-muted);
  text-align: right;
}
.settings__select {
  flex: 2;
  min-height: 44px;
  padding: var(--space-sm);
  border-radius: var(--radius-md);
  background: var(--color-surface);
  color: var(--color-text);
  font-size: var(--font-size-md);
}
```

- [ ] **Step 7: Rodar os testes + typecheck**

Run: `npx vitest run tests/app/SettingsScreen.test.tsx && npm run check`
Expected: PASS + TS limpo.

- [ ] **Step 8: Commit**

```bash
git add src/app/screens/SettingsScreen.tsx src/app/App.tsx src/app/main.tsx src/app/styles/global.css tests/app/SettingsScreen.test.tsx
git commit -m "feat(4.8): tela de Configurações + troca de idioma ao vivo (App re-render) + bootstrap"
```

---

## Self-Review

**Spec coverage:**
- Volume → Task 1 (store) + 2 (storage) + 5 (slider). ✔
- Música menu/gameplay on/off → Tasks 1/2/5. ✔
- Idioma + troca ao vivo → Task 3 (`setLanguage`+i18n) + Task 5 (select + App re-render). ✔
- Persistência → Task 2 + `commit` na Task 3. ✔
- i18n 10 locales → Task 4. ✔
- Seams volume/música inertes (consumo em 4.10) → documentado; nenhum áudio acionado. ✔
- `src/core/` intocado → nenhum task importa core. ✔

**Placeholder scan:** sem TBD/TODO; todo código presente. ✔

**Type consistency:** `SettingsState`, `sanitizeVolume`, `setLanguage`, `settingsService.{volume,menuMusic,gameplayMusic,language}` consistentes entre tasks. `language` signal é `ReadonlySignal<string>` (valor sempre um `SupportedLanguage`); o `<select>` e `App` só o leem. ✔

**Risco conhecido (Task 5):** o framework de teste de componente pode diferir (`@testing-library/preact` vs render manual). O Step 1 instrui inspecionar `tests/app/` e espelhar o padrão existente antes de escrever o teste.
