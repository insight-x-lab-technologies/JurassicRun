# PWA (7.1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tornar o JurassicRun instalável (Android/desktop) e jogável offline via manifesto Web App + service worker com precache dos assets do build.

**Architecture:** Padrão puro×casca. Manifesto num módulo TS puro testável (`src/pwa/manifest.ts`) consumido pela casca `vite.config.ts`. Ícones placeholder gerados por um script node-native puro (`scripts/gen-icons.mjs`, encoder PNG via `node:zlib`, zero dep nova) e comitados em `public/icons/`. Registro do SW injetado pelo `vite-plugin-pwa` (`autoUpdate` + `injectRegister:'auto'`) — nenhum código de app.

**Tech Stack:** Vite 8 (rolldown), `vite-plugin-pwa` 1.3 (workbox), TypeScript estrito, Vitest, Node `zlib`.

## Global Constraints

- **REGRA 1 (determinismo):** `src/core/` **não é tocado**. Determinismo **67 inalterado**; nenhum golden re-pinado.
- **REGRA 2/5 (arte desacoplada + asset-spec):** ícones são trocáveis (arte real = Fase 8) ⇒ exigem asset-spec + registro.
- **REGRA 3 (performance):** manifesto/SW são artefatos de build; zero trabalho por frame.
- **REGRA 4 (i18n):** sem strings de UI novas (nome do manifesto é estático por plataforma; `"JurassicRun"` é marca).
- Cores casadas com os design tokens: `--color-bg #0e1116` (fundo/splash/tema), `--color-primary #4ea1ff` (destaque do ícone).
- Ícones: `icon-192.png` (192×192, `purpose any`), `icon-512.png` (512×512, `any`), `icon-maskable-512.png` (512×512, `maskable`).
- `base` de subdiretório fica para **7.3**: **não** fixar `scope`/`start_url` absolutos; deixar o plugin derivar do `vite base`.
- Verificação final: `npm test` + `npm run check` verdes; `npm run build` gera `dist/manifest.webmanifest` + `dist/sw.js` + `dist/icons/*.png`.

---

### Task 1: Gerador de ícones PNG puro + artefatos comitados

**Files:**
- Create: `scripts/gen-icons.mjs`
- Create (gerados, comitados): `public/icons/icon-192.png`, `public/icons/icon-512.png`, `public/icons/icon-maskable-512.png`
- Modify: `package.json` (script `gen:icons`)
- Test: `tests/pwa/icons.test.ts`

**Interfaces:**
- Consumes: nada (folha).
- Produces: `scripts/gen-icons.mjs` exporta `encodePng(width:number, height:number, rgba:Buffer) → Buffer` e `renderIcon(size:number, opts?:{maskable?:boolean}) → Buffer` (rgba com `size*size*4` bytes). Task 3 depende dos **arquivos** em `public/icons/`, não das funções.

- [ ] **Step 1: Escrever o teste que falha**

Create `tests/pwa/icons.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { readFileSync, existsSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { encodePng, renderIcon } from '../../scripts/gen-icons.mjs';

const PNG_SIG = Buffer.from([0x89, 0x50, 0x4e, 0x47, 0x0d, 0x0a, 0x1a, 0x0a]);
const root = fileURLToPath(new URL('../../', import.meta.url));

describe('encodePng', () => {
  it('emite a assinatura PNG e IHDR com as dimensões pedidas', () => {
    const rgba = Buffer.alloc(2 * 3 * 4, 0xff);
    const png = encodePng(2, 3, rgba);
    expect(png.subarray(0, 8).equals(PNG_SIG)).toBe(true);
    // IHDR começa em 8: len(4)+type(4)=16, largura em offset 16, altura em 20 (big-endian)
    expect(png.readUInt32BE(16)).toBe(2);
    expect(png.readUInt32BE(20)).toBe(3);
  });

  it('é determinístico (mesma entrada ⇒ mesmos bytes)', () => {
    const rgba = Buffer.alloc(4 * 4 * 4, 0x33);
    expect(encodePng(4, 4, rgba).equals(encodePng(4, 4, rgba))).toBe(true);
  });
});

describe('renderIcon', () => {
  it('retorna rgba do tamanho esperado', () => {
    expect(renderIcon(192).length).toBe(192 * 192 * 4);
    expect(renderIcon(512, { maskable: true }).length).toBe(512 * 512 * 4);
  });

  it('produz um PNG válido ao ser encodado', () => {
    const png = encodePng(64, 64, renderIcon(64));
    expect(png.subarray(0, 8).equals(PNG_SIG)).toBe(true);
    expect(png.readUInt32BE(16)).toBe(64);
  });
});

describe('ícones comitados em public/icons', () => {
  for (const name of ['icon-192.png', 'icon-512.png', 'icon-maskable-512.png']) {
    it(`${name} existe e começa com a assinatura PNG`, () => {
      const p = `${root}public/icons/${name}`;
      expect(existsSync(p)).toBe(true);
      expect(readFileSync(p).subarray(0, 8).equals(PNG_SIG)).toBe(true);
    });
  }
});
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run: `npx vitest run tests/pwa/icons.test.ts`
Expected: FAIL — `Cannot find module '../../scripts/gen-icons.mjs'`.

- [ ] **Step 3: Escrever `scripts/gen-icons.mjs`**

```js
// scripts/gen-icons.mjs
// Gera ícones PNG placeholder da PWA (fundo sólido + triângulo, ecoando o dino
// cosmético). Encoder PNG puro via node:zlib — zero dep. Rode `npm run gen:icons`.
// Arte real é Fase 8 (ver docs/assets/specs/pwa-icon.md).
import { deflateSync } from 'node:zlib';
import { mkdirSync, writeFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import path from 'node:path';

const BG = [0x0e, 0x11, 0x16]; // --color-bg
const FG = [0x4e, 0xa1, 0xff]; // --color-primary

const CRC_TABLE = (() => {
  const t = new Uint32Array(256);
  for (let n = 0; n < 256; n++) {
    let c = n;
    for (let k = 0; k < 8; k++) c = (c & 1) !== 0 ? 0xedb88320 ^ (c >>> 1) : c >>> 1;
    t[n] = c >>> 0;
  }
  return t;
})();

function crc32(buf) {
  let c = 0xffffffff;
  for (let i = 0; i < buf.length; i++) c = CRC_TABLE[(c ^ buf[i]) & 0xff] ^ (c >>> 8);
  return (c ^ 0xffffffff) >>> 0;
}

function chunk(type, data) {
  const len = Buffer.alloc(4);
  len.writeUInt32BE(data.length, 0);
  const body = Buffer.concat([Buffer.from(type, 'ascii'), data]);
  const crc = Buffer.alloc(4);
  crc.writeUInt32BE(crc32(body), 0);
  return Buffer.concat([len, body, crc]);
}

/** @param {number} width @param {number} height @param {Buffer} rgba @returns {Buffer} */
export function encodePng(width, height, rgba) {
  const sig = Buffer.from([0x89, 0x50, 0x4e, 0x47, 0x0d, 0x0a, 0x1a, 0x0a]);
  const ihdr = Buffer.alloc(13);
  ihdr.writeUInt32BE(width, 0);
  ihdr.writeUInt32BE(height, 4);
  ihdr[8] = 8; // bit depth
  ihdr[9] = 6; // color type: RGBA
  const stride = width * 4;
  const raw = Buffer.alloc((stride + 1) * height);
  for (let y = 0; y < height; y++) {
    const dst = y * (stride + 1);
    raw[dst] = 0; // filtro: none
    rgba.copy(raw, dst + 1, y * stride, y * stride + stride);
  }
  const idat = deflateSync(raw);
  return Buffer.concat([sig, chunk('IHDR', ihdr), chunk('IDAT', idat), chunk('IEND', Buffer.alloc(0))]);
}

/** @param {number} size @param {{maskable?:boolean}} [opts] @returns {Buffer} */
export function renderIcon(size, opts = {}) {
  const inset = opts.maskable === true ? 0.24 : 0.14; // safe-zone maior p/ máscara
  const rgba = Buffer.alloc(size * size * 4);
  for (let i = 0; i < size * size; i++) {
    rgba[i * 4] = BG[0];
    rgba[i * 4 + 1] = BG[1];
    rgba[i * 4 + 2] = BG[2];
    rgba[i * 4 + 3] = 0xff;
  }
  // triângulo apontando p/ direita (dino): topo-esq, meio-dir, base-esq
  const m = size * inset;
  const ax = m, ay = m;
  const bx = size - m, by = size / 2;
  const cx = m, cy = size - m;
  const area = (bx - ax) * (cy - ay) - (cx - ax) * (by - ay);
  for (let py = 0; py < size; py++) {
    for (let px = 0; px < size; px++) {
      const x = px + 0.5, y = py + 0.5;
      const w0 = ((bx - ax) * (y - ay) - (by - ay) * (x - ax)) / area;
      const w1 = ((cx - bx) * (y - by) - (cy - by) * (x - bx)) / area;
      const w2 = 1 - w0 - w1;
      if (w0 >= 0 && w1 >= 0 && w2 >= 0) {
        const i = (py * size + px) * 4;
        rgba[i] = FG[0];
        rgba[i + 1] = FG[1];
        rgba[i + 2] = FG[2];
        rgba[i + 3] = 0xff;
      }
    }
  }
  return rgba;
}

function main() {
  const root = fileURLToPath(new URL('..', import.meta.url));
  const dir = path.join(root, 'public/icons');
  mkdirSync(dir, { recursive: true });
  const outputs = [
    ['icon-192.png', encodePng(192, 192, renderIcon(192))],
    ['icon-512.png', encodePng(512, 512, renderIcon(512))],
    ['icon-maskable-512.png', encodePng(512, 512, renderIcon(512, { maskable: true }))],
  ];
  for (const [name, buf] of outputs) {
    writeFileSync(path.join(dir, name), buf);
    console.log(`escrito public/icons/${name} (${buf.length} bytes)`);
  }
}

// roda main() só quando executado como script (não no import do teste)
if (process.argv[1] === fileURLToPath(import.meta.url)) main();
```

- [ ] **Step 4: Adicionar o script npm e gerar os PNGs**

Modify `package.json` scripts, adicionar após `build:edge`:

```json
    "gen:icons": "node scripts/gen-icons.mjs"
```

Run: `npm run gen:icons`
Expected: imprime as 3 linhas "escrito public/icons/...".

- [ ] **Step 5: Rodar o teste e ver passar**

Run: `npx vitest run tests/pwa/icons.test.ts`
Expected: PASS (todos os describes, incluindo os 3 arquivos comitados).

- [ ] **Step 6: Commit**

```bash
git add scripts/gen-icons.mjs package.json public/icons tests/pwa/icons.test.ts
git commit -m "feat(7.1): gerador de ícones PNG puro + placeholders comitados"
```

---

### Task 2: Módulo puro do manifesto + opções do plugin

**Files:**
- Create: `src/pwa/manifest.ts`
- Test: `src/pwa/manifest.test.ts`

**Interfaces:**
- Consumes: os caminhos de ícone de Task 1 (`icons/icon-192.png` etc.).
- Produces: `export const pwaOptions: Partial<VitePWAOptions>` (com `.manifest`, `.registerType`, `.injectRegister`, `.workbox`, `.devOptions`, `.includeAssets`) — consumido por `vite.config.ts` na Task 3.

- [ ] **Step 1: Escrever o teste que falha**

Create `src/pwa/manifest.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { pwaOptions } from './manifest';

const HEX = /^#[0-9a-fA-F]{6}$/;

describe('pwaOptions', () => {
  it('usa autoUpdate + injeção automática do registro', () => {
    expect(pwaOptions.registerType).toBe('autoUpdate');
    expect(pwaOptions.injectRegister).toBe('auto');
  });

  it('não fixa scope/start_url absolutos (derivados do base em 7.3)', () => {
    expect(pwaOptions.manifest?.scope).toBeUndefined();
    expect(pwaOptions.manifest?.start_url).toBeUndefined();
  });

  it('desliga o SW em dev', () => {
    expect(pwaOptions.devOptions?.enabled).toBe(false);
  });
});

describe('pwaOptions.manifest', () => {
  const m = pwaOptions.manifest;

  it('tem nome, display standalone e cores hex válidas', () => {
    expect(m?.name).toBeTruthy();
    expect(m?.short_name).toBeTruthy();
    expect(m?.display).toBe('standalone');
    expect(m?.theme_color).toMatch(HEX);
    expect(m?.background_color).toMatch(HEX);
  });

  it('tem ícones 192 e 512 "any" + um 512 "maskable"', () => {
    const icons = m?.icons ?? [];
    expect(icons.some((i) => i.sizes === '192x192')).toBe(true);
    const any512 = icons.find((i) => i.sizes === '512x512' && (i.purpose ?? 'any').includes('any'));
    const mask512 = icons.find((i) => i.sizes === '512x512' && (i.purpose ?? '').includes('maskable'));
    expect(any512).toBeDefined();
    expect(mask512).toBeDefined();
  });

  it('usa caminhos de ícone relativos (corretos sob qualquer base)', () => {
    for (const i of m?.icons ?? []) expect(i.src.startsWith('/')).toBe(false);
  });
});
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run: `npx vitest run src/pwa/manifest.test.ts`
Expected: FAIL — `Cannot find module './manifest'`.

- [ ] **Step 3: Escrever `src/pwa/manifest.ts`**

```ts
import type { VitePWAOptions } from 'vite-plugin-pwa';

// Cores casadas com os design tokens (src/app/styles/tokens.css).
const THEME = '#0e1116'; // --color-bg
const BACKGROUND = '#0e1116';

/**
 * Contrato do manifesto/SW da PWA, isolado como módulo puro p/ ser testável sem
 * rodar o build (molde de src/services/online/schema.ts). Consumido pela casca
 * vite.config.ts. Caminhos de ícone são relativos ⇒ corretos sob qualquer `base`
 * (o subdiretório do GitHub Pages entra em 7.3, e o plugin deriva scope/start_url).
 */
export const pwaOptions: Partial<VitePWAOptions> = {
  registerType: 'autoUpdate',
  injectRegister: 'auto',
  includeAssets: ['icons/*.png'],
  manifest: {
    name: 'JurassicRun',
    short_name: 'JurassicRun',
    description: 'Side-scroller de pterodáctilo, determinístico e offline.',
    theme_color: THEME,
    background_color: BACKGROUND,
    display: 'standalone',
    orientation: 'any',
    categories: ['games'],
    icons: [
      { src: 'icons/icon-192.png', sizes: '192x192', type: 'image/png', purpose: 'any' },
      { src: 'icons/icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'any' },
      { src: 'icons/icon-maskable-512.png', sizes: '512x512', type: 'image/png', purpose: 'maskable' },
    ],
  },
  workbox: {
    globPatterns: ['**/*.{js,css,html,png,svg,ico,woff2}'],
    maximumFileSizeToCacheInBytes: 4 * 1024 * 1024,
    navigateFallback: 'index.html',
    cleanupOutdatedCaches: true,
  },
  devOptions: { enabled: false },
};
```

- [ ] **Step 4: Rodar o teste e ver passar**

Run: `npx vitest run src/pwa/manifest.test.ts`
Expected: PASS.

- [ ] **Step 5: Typecheck**

Run: `npm run check`
Expected: sem erros (tsc + eslint limpos).

- [ ] **Step 6: Commit**

```bash
git add src/pwa/manifest.ts src/pwa/manifest.test.ts
git commit -m "feat(7.1): manifesto PWA como módulo puro testável"
```

---

### Task 3: Fiação (vite.config + index.html) + asset-spec + verificação de build

**Files:**
- Modify: `vite.config.ts`
- Modify: `index.html`
- Create: `docs/assets/specs/pwa-icon.md`
- Modify: `docs/assets/asset-registry.md`

**Interfaces:**
- Consumes: `pwaOptions` de Task 2; arquivos `public/icons/*` de Task 1.
- Produces: build com `dist/manifest.webmanifest` + `dist/sw.js` + `dist/icons/*`.

- [ ] **Step 1: Ligar o plugin no `vite.config.ts`**

Substituir a linha do `VitePWA({ disable: true })` e o comentário acima dela. Novo `vite.config.ts`:

```ts
import { defineConfig } from 'vite';
import tsconfigPaths from 'vite-tsconfig-paths';
import { VitePWA } from 'vite-plugin-pwa';
import { pwaOptions } from './src/pwa/manifest';

export default defineConfig({
  plugins: [
    tsconfigPaths(),
    VitePWA(pwaOptions),
  ],
  // Vite 8 usa rolldown/oxc (não esbuild) para o transform de JSX; `esbuild.jsx` não
  // existe mais no tipo (o pacote `esbuild` nem é dependência). Equivalente em oxc:
  oxc: { jsx: { runtime: 'automatic', importSource: 'preact' } },
});
```

- [ ] **Step 2: Adicionar meta/links de PWA no `index.html`**

Substituir o `<head>` do `index.html` por (o `<link rel="manifest">` é injetado pelo plugin — não hardcodar):

```html
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <meta name="theme-color" content="#0e1116" />
    <meta name="description" content="Side-scroller de pterodáctilo, determinístico e offline." />
    <link rel="icon" type="image/png" href="icons/icon-192.png" />
    <link rel="apple-touch-icon" href="icons/icon-192.png" />
    <title>JurassicRun</title>
  </head>
```

- [ ] **Step 3: Rodar o build e verificar os artefatos**

Run: `npm run build && ls dist/manifest.webmanifest dist/sw.js dist/icons/`
Expected: os três caminhos existem; `dist/icons/` lista os 3 PNGs. (O `tsc --noEmit` do build também valida o `import` do `pwaOptions`.)

- [ ] **Step 4: Verificar o registro do SW no HTML do build**

Run: `grep -c "serviceWorker\|registerSW\|workbox" dist/index.html`
Expected: ≥ 1 (o plugin injetou o script de registro).

- [ ] **Step 5: Criar o asset-spec do ícone (REGRA 5)**

Create `docs/assets/specs/pwa-icon.md` (seguir `docs/assets/asset-spec-template.md`): id lógico `pwa-icon`; descreve os 3 tamanhos (192/512/maskable-512), fundo `#0e1116`, silhueta de pterodáctilo, safe-zone ~76% no maskable; nota de que o placeholder atual é o triângulo gerado por `scripts/gen-icons.mjs` e que a arte real (PNG AAA) entra na Fase 8 substituindo os arquivos em `public/icons/` sem tocar código.

Adicionar 1 linha ao registro em `docs/assets/asset-registry.md` apontando `pwa-icon` → `specs/pwa-icon.md` (status: placeholder).

- [ ] **Step 6: Suíte completa + check**

Run: `npm test && npm run check`
Expected: tudo verde (incluindo a paridade i18n e o guard de determinismo, intactos).

- [ ] **Step 7: Commit**

```bash
git add vite.config.ts index.html docs/assets/specs/pwa-icon.md docs/assets/asset-registry.md
git commit -m "feat(7.1): liga vite-plugin-pwa (manifest+SW) + meta PWA + asset-spec do ícone"
```

---

## Self-Review (feito ao escrever)

- **Cobertura da spec:** manifesto puro (Task 2) ✓; ícones gerados+comitados (Task 1) ✓; SW `autoUpdate`+`injectRegister:auto`+workbox (Task 2 config, Task 3 wiring) ✓; index.html meta/links (Task 3) ✓; asset-spec REGRA 5 (Task 3) ✓; testes unit+fs (Task 1/2) + verificação de build (Task 3) ✓; `base` não fixado ✓; i18n sem strings novas ✓; `src/core/` intocado ✓.
- **Placeholders:** nenhum "TBD"/"TODO" de implementação; todo código está presente.
- **Consistência de tipos:** `encodePng`/`renderIcon` assinaturas idênticas em Task 1 (script + teste); `pwaOptions` mesmo nome/forma em Task 2 (def+teste) e Task 3 (import).
- **Determinismo:** nenhum arquivo em `src/core/`; nenhum golden tocado.
