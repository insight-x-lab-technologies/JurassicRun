# 10.6 — Avatares de perfil (plano de implementação)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recomendado) ou superpowers:executing-plans para executar este plano task por task. Os passos
> usam checkbox (`- [ ]`).

**Goal:** substituir o avatar gerado (inicial + matiz) por uma grade de 12 avatares escolhíveis,
persistidos no perfil, com arte placeholder e o seam da arte AAA documentado.

**Architecture:** catálogo puro em `src/services/profile/avatars.ts`; `Profile.avatarId`
obrigatório no tipo e backfillado na leitura do storage; componente `Avatar` compartilhado
renderiza `public/ui/avatar.<id>.png` com fallback para a inicial; seletor na tela de Perfil.

**Tech Stack:** TypeScript estrito, Preact + `@preact/signals`, Vitest (`happy-dom` para telas,
`node` para arquivos), i18next, scripts `.mjs` zero-dep para arte.

**Spec:** `docs/superpowers/specs/2026-07-31-10.6-profile-avatars-design.md`.

## Global Constraints

- **`src/core/` INTOCADO.** Nenhum arquivo sob `src/core/` pode aparecer no diff. Determinismo
  permanece em 73 testes, sem re-pin de golden.
- **Nenhuma string visível hardcoded** (REGRA 4): tudo via `i18n.t`, chave presente nos **10**
  locales (`src/i18n/locales/{de,en,es,fr,hi,it,ja,ko,pt-BR,zh}.json`).
- **TypeScript estrito, sem `any`.** `npm run check` (tsc + eslint) tem de ficar limpo.
- **`STORAGE_KEY` de perfis NÃO muda** — continua `jurassicrun.profiles.v1`. Só o campo `version`
  do payload vai a `2`.
- **Caminho de imagem sempre com `import.meta.env.BASE_URL`** (o app é servido sob `BASE_PATH` no
  GitHub Pages).
- Rodar a suíte **inteira** (`npm test`) antes de fechar, não só os arquivos-alvo (precedente 9.8).
- Um commit por task, na branch `feat/10.6-profile-avatars`. Commitar **só** os arquivos da task
  (nunca `git commit -am`).

## Estrutura de arquivos

| Arquivo | Responsabilidade |
|---------|------------------|
| `src/services/profile/avatars.ts` (novo) | catálogo puro: ids, matizes, hash, resolução |
| `src/services/profile/store.ts` | `Profile.avatarId`, `createProfile`, `setAvatar`, `avatarFor` |
| `src/services/profile/storage.ts` | parse com backfill, payload `version: 2` |
| `src/services/profile/index.ts` | `profileService.setAvatar`, re-exports |
| `src/app/components/Avatar.tsx` (novo) | render do avatar (img + fallback de inicial) |
| `src/app/screens/ProfileScreen.tsx` | seletor + uso do componente |
| `src/app/screens/HomeScreen.tsx` | uso do componente |
| `src/app/styles/global.css` | `.avatar__img`, `.avatar-picker*` |
| `src/services/online/index.ts` | sincroniza `avatarId` |
| `scripts/gen-avatar-placeholder.mjs` (novo) | 12 medalhões placeholder |
| `docs/assets/specs/ui.avatars.md` (novo) | asset-spec do conjunto real |

---

### Task 1: Catálogo puro de avatares

**Files:**
- Create: `src/services/profile/avatars.ts`
- Test: `tests/services/profile/avatars.test.ts`

**Interfaces:**
- Consumes: nada.
- Produces: `AVATAR_IDS`, `type AvatarId`, `AVATARS`, `AvatarDef`, `isAvatarId(v: unknown)`,
  `hashId(s: string): number`, `defaultAvatarId(profileId: string): AvatarId`,
  `resolveAvatarId(raw: unknown, profileId: string): AvatarId`,
  `avatarDef(id: AvatarId): AvatarDef`.

- [ ] **Step 1: Escrever o teste que falha**

```ts
// tests/services/profile/avatars.test.ts
import { describe, it, expect } from 'vitest';
import {
  AVATAR_IDS,
  AVATARS,
  isAvatarId,
  defaultAvatarId,
  resolveAvatarId,
  avatarDef,
} from '@services/profile/avatars';

describe('catálogo de avatares', () => {
  it('tem 12 ids únicos', () => {
    expect(AVATAR_IDS).toHaveLength(12);
    expect(new Set(AVATAR_IDS).size).toBe(12);
    expect(AVATARS.map((a) => a.id)).toEqual([...AVATAR_IDS]);
  });

  it('cada avatar tem matiz própria dentro de [0, 360)', () => {
    const hues = AVATARS.map((a) => a.hue);
    expect(new Set(hues).size).toBe(hues.length);
    for (const h of hues) {
      expect(h).toBeGreaterThanOrEqual(0);
      expect(h).toBeLessThan(360);
    }
  });

  it('isAvatarId aceita só ids do catálogo', () => {
    expect(isAvatarId('a01')).toBe(true);
    expect(isAvatarId('a99')).toBe(false);
    expect(isAvatarId(3)).toBe(false);
    expect(isAvatarId(undefined)).toBe(false);
  });

  it('defaultAvatarId é determinístico e sempre cai no catálogo', () => {
    const ids = ['abc', 'zzz-1', '', '9f8e7d6c-1111-2222-3333-444455556666'];
    for (const id of ids) {
      const a = defaultAvatarId(id);
      expect(AVATAR_IDS).toContain(a);
      expect(defaultAvatarId(id)).toBe(a); // estável entre chamadas
    }
  });

  it('defaultAvatarId distribui ids diferentes por avatares diferentes', () => {
    const seen = new Set(
      Array.from({ length: 60 }, (_, i) => defaultAvatarId(`player-${i}`)),
    );
    expect(seen.size).toBeGreaterThan(1);
  });

  it('resolveAvatarId devolve o id válido e cai no default para lixo', () => {
    expect(resolveAvatarId('a05', 'p1')).toBe('a05');
    expect(resolveAvatarId('a99', 'p1')).toBe(defaultAvatarId('p1'));
    expect(resolveAvatarId(undefined, 'p1')).toBe(defaultAvatarId('p1'));
    expect(resolveAvatarId({ id: 'a01' }, 'p1')).toBe(defaultAvatarId('p1'));
  });

  it('avatarDef devolve a definição do id', () => {
    expect(avatarDef('a01').id).toBe('a01');
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run tests/services/profile/avatars.test.ts`
Expected: FAIL — `Failed to resolve import "@services/profile/avatars"`.

- [ ] **Step 3: Implementar**

```ts
// src/services/profile/avatars.ts
/**
 * Catálogo de avatares de perfil (10.6).
 *
 * Namespace PRÓPRIO, deliberadamente opaco: reusar os ids do `DINO_ROSTER` acoplaria identidade de
 * jogador a traços/entitlements. O `hue` é o fallback visual (disco colorido) quando o PNG do
 * avatar não carrega; a arte real vive em `public/ui/avatar.<id>.png`.
 */
export const AVATAR_IDS = [
  'a01', 'a02', 'a03', 'a04', 'a05', 'a06',
  'a07', 'a08', 'a09', 'a10', 'a11', 'a12',
] as const;

export type AvatarId = (typeof AVATAR_IDS)[number];

export interface AvatarDef {
  readonly id: AvatarId;
  readonly hue: number;
}

export const AVATARS: readonly AvatarDef[] = AVATAR_IDS.map((id, i) => ({
  id,
  hue: i * 30,
}));

export function isAvatarId(value: unknown): value is AvatarId {
  return typeof value === 'string' && (AVATAR_IDS as readonly string[]).includes(value);
}

/** Hash estável de string (mesma função que `avatarFor` usava): id igual ⇒ avatar igual em
 * qualquer aparelho, sem depender de RNG nem de relógio. */
export function hashId(s: string): number {
  let h = 0;
  for (let i = 0; i < s.length; i++) {
    h = (h * 31 + s.charCodeAt(i)) >>> 0;
  }
  return h;
}

export function defaultAvatarId(profileId: string): AvatarId {
  return AVATAR_IDS[hashId(profileId) % AVATAR_IDS.length]!;
}

/** Normaliza o que veio do storage: id do catálogo passa; qualquer outra coisa cai no default. */
export function resolveAvatarId(raw: unknown, profileId: string): AvatarId {
  return isAvatarId(raw) ? raw : defaultAvatarId(profileId);
}

export function avatarDef(id: AvatarId): AvatarDef {
  return AVATARS[AVATAR_IDS.indexOf(id)]!;
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run tests/services/profile/avatars.test.ts`
Expected: PASS (7 testes).

- [ ] **Step 5: Commit**

```bash
git add src/services/profile/avatars.ts tests/services/profile/avatars.test.ts
git commit -m "feat(10.6): catálogo puro de 12 avatares de perfil"
```

---

### Task 2: `Profile.avatarId` e o redutor `setAvatar`

**Files:**
- Modify: `src/services/profile/store.ts`
- Modify: `src/services/profile/index.ts`
- Test: `tests/services/profile/store.test.ts` (existente — acrescentar casos; se o arquivo não
  existir com esse nome, use o arquivo de teste de store que já cobre `createProfile`)

**Interfaces:**
- Consumes: `AvatarId`, `defaultAvatarId`, `isAvatarId`, `hashId` de Task 1.
- Produces: `Profile.avatarId: AvatarId`; `setAvatar(state, profileId, avatarId): ProfileState`;
  `profileService.setAvatar(avatarId: AvatarId): boolean`.

- [ ] **Step 1: Escrever os testes que falham**

```ts
// acrescentar ao teste de store de perfis
import { setAvatar, createProfile, emptyState } from '@services/profile/store';
import { defaultAvatarId, AVATAR_IDS } from '@services/profile/avatars';

it('createProfile deriva o avatar inicial do id', () => {
  const { profile } = createProfile(emptyState(), 'p1', 'Rex', 0);
  expect(profile.avatarId).toBe(defaultAvatarId('p1'));
  expect(AVATAR_IDS).toContain(profile.avatarId);
});

it('setAvatar troca só o perfil alvo', () => {
  let s = createProfile(emptyState(), 'p1', 'Rex', 0).state;
  s = createProfile(s, 'p2', 'Ptero', 0).state;
  const out = setAvatar(s, 'p1', 'a07');
  expect(out.profiles.find((p) => p.id === 'p1')!.avatarId).toBe('a07');
  expect(out.profiles.find((p) => p.id === 'p2')!.avatarId).toBe(defaultAvatarId('p2'));
});

it('setAvatar com id inexistente devolve o mesmo estado', () => {
  const s = createProfile(emptyState(), 'p1', 'Rex', 0).state;
  expect(setAvatar(s, 'nope', 'a07')).toBe(s);
});
```

E, no teste do serviço (`tests/services/profile/*service*`, ou novo `it` no arquivo existente que
usa `profileService`):

```ts
it('profileService.setAvatar altera o ativo e persiste', () => {
  const storage = memoryProfileStorage(emptyState());
  profileService.init(storage);
  profileService.create('Rex');
  expect(profileService.setAvatar('a09')).toBe(true);
  expect(profileService.activeProfile.value!.avatarId).toBe('a09');
  expect(storage.load().profiles[0]!.avatarId).toBe('a09');
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run tests/services/profile`
Expected: FAIL — `setAvatar` não exportado / `avatarId` não existe em `Profile`.

- [ ] **Step 3: Implementar**

Em `src/services/profile/store.ts`:

```ts
import { defaultAvatarId, hashId, type AvatarId } from './avatars';

export interface Profile {
  readonly id: string;
  readonly name: string;
  readonly createdAt: number;
  /** Avatar escolhido. Obrigatório de propósito: um call site que crie perfil sem avatar
   * quebra a compilação em vez de renderizar um perfil sem cara. */
  readonly avatarId: AvatarId;
}
```

`createProfile` passa a montar `const profile: Profile = { id, name, createdAt, avatarId: defaultAvatarId(id) };`

Redutor novo, no molde de `renameProfile`:

```ts
export function setAvatar(state: ProfileState, id: string, avatarId: AvatarId): ProfileState {
  if (!state.profiles.some((p) => p.id === id)) return state;
  return {
    ...state,
    profiles: state.profiles.map((p) => (p.id === id ? { ...p, avatarId } : p)),
  };
}
```

`avatarFor` passa a reusar o hash compartilhado (nada de segunda cópia do laço):

```ts
export function avatarFor(profile: Profile): { initial: string; hue: number } {
  const initial = profile.name.trim().charAt(0).toUpperCase() || '?';
  return { initial, hue: hashId(profile.id) % 360 };
}
```

Em `src/services/profile/index.ts`, importar `setAvatar as setAvatarIn` do store e acrescentar ao
serviço:

```ts
  setAvatar(avatarId: AvatarId): boolean {
    const id = this._state.value.activeId;
    if (id === null) return false;
    this.commit(setAvatarIn(this._state.value, id, avatarId));
    return true;
  }
```

e re-exportar o que as telas vão usar:

```ts
export { avatarFor };
export { AVATARS, AVATAR_IDS, avatarDef, type AvatarId } from './avatars';
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run tests/services/profile && npx tsc --noEmit`
Expected: PASS. Se algum outro arquivo montar `Profile` literal, o `tsc` aponta — corrija incluindo
`avatarId: defaultAvatarId(<id>)` (ou o id do teste).

- [ ] **Step 5: Commit**

```bash
git add src/services/profile/store.ts src/services/profile/index.ts tests/services/profile
git commit -m "feat(10.6): Profile.avatarId obrigatório e redutor setAvatar"
```

---

### Task 3: Persistência com backfill (payload v2)

**Files:**
- Modify: `src/services/profile/storage.ts`
- Test: `tests/services/profile/storage.test.ts` (existente; criar se não houver)

**Interfaces:**
- Consumes: `resolveAvatarId` (Task 1), `Profile` (Task 2).
- Produces: nada novo exportado; `parseState` passa a normalizar `avatarId`.

- [ ] **Step 1: Escrever os testes que falham**

```ts
import { localStorageProfileStorage, STORAGE_KEY } from '@services/profile/storage';
import { defaultAvatarId } from '@services/profile/avatars';

it('payload legado (sem avatarId) carrega com avatar derivado', () => {
  localStorage.setItem(
    STORAGE_KEY,
    JSON.stringify({ version: 1, profiles: [{ id: 'p1', name: 'Rex', createdAt: 1 }], activeId: 'p1' }),
  );
  const state = localStorageProfileStorage().load();
  expect(state.profiles[0]!.avatarId).toBe(defaultAvatarId('p1'));
});

it('avatarId inválido cai no default', () => {
  localStorage.setItem(
    STORAGE_KEY,
    JSON.stringify({
      version: 2,
      profiles: [{ id: 'p1', name: 'Rex', createdAt: 1, avatarId: 'a99' }],
      activeId: 'p1',
    }),
  );
  expect(localStorageProfileStorage().load().profiles[0]!.avatarId).toBe(defaultAvatarId('p1'));
});

it('round-trip preserva a escolha e grava version 2', () => {
  const storage = localStorageProfileStorage();
  storage.save({
    profiles: [{ id: 'p1', name: 'Rex', createdAt: 1, avatarId: 'a04' }],
    activeId: 'p1',
  });
  expect(JSON.parse(localStorage.getItem(STORAGE_KEY)!).version).toBe(2);
  expect(storage.load().profiles[0]!.avatarId).toBe('a04');
});

it('a chave de storage NÃO muda (bumpar apagaria os perfis)', () => {
  expect(STORAGE_KEY).toBe('jurassicrun.profiles.v1');
});
```

> O arquivo de teste precisa de `// @vitest-environment happy-dom` no topo para ter
> `localStorage`, e de `localStorage.clear()` num `beforeEach`.

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run tests/services/profile/storage.test.ts`
Expected: FAIL — `avatarId` `undefined` no perfil legado.

- [ ] **Step 3: Implementar**

```ts
import { resolveAvatarId } from './avatars';

/** Forma MÍNIMA persistida: `avatarId` NÃO é exigido aqui — payloads v1 não têm o campo e
 * rejeitá-los descartaria perfis reais. A normalização abaixo é que materializa o avatar. */
function isStoredProfile(value: unknown): value is { id: string; name: string; createdAt: number } {
  if (typeof value !== 'object' || value === null) return false;
  const v = value as Record<string, unknown>;
  return typeof v.id === 'string' && typeof v.name === 'string' && typeof v.createdAt === 'number';
}

function toProfile(raw: { id: string; name: string; createdAt: number }): Profile {
  const avatarId = resolveAvatarId((raw as Record<string, unknown>).avatarId, raw.id);
  return { id: raw.id, name: raw.name, createdAt: raw.createdAt, avatarId };
}
```

`parseState` usa `d.profiles.every(isStoredProfile)` e depois
`const profiles = d.profiles.map(toProfile);`. O `save` grava `{ version: 2, ...state }`.

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run tests/services/profile`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/services/profile/storage.ts tests/services/profile/storage.test.ts
git commit -m "feat(10.6): storage de perfis faz backfill de avatarId (payload v2)"
```

---

### Task 4: Arte placeholder — 12 medalhões

**Files:**
- Create: `scripts/gen-avatar-placeholder.mjs`
- Create: `scripts/gen-avatar-placeholder.d.mts`
- Create: `public/ui/avatar.a01.png` … `public/ui/avatar.a12.png` (saída do script)
- Modify: `package.json` (script `gen:avatars`)
- Test: `tests/assets/avatars.test.ts`

**Interfaces:**
- Consumes: `AVATAR_IDS` (Task 1); `encodePng` de `scripts/gen-icons.mjs`; `loadArt`,
  `contentBounds`, `cropResize` de `scripts/gen-atlas.mjs`.
- Produces: `renderAvatar(size, hue)` exportado (para teste unitário, se desejado) e os 12 PNGs.

- [ ] **Step 1: Escrever o teste que falha**

```ts
// tests/assets/avatars.test.ts
import { describe, it, expect } from 'vitest';
import { existsSync, statSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import { join } from 'node:path';
import { AVATAR_IDS } from '@services/profile/avatars';

const ROOT = fileURLToPath(new URL('../../', import.meta.url));

describe('arte dos avatares', () => {
  it('todo id do catálogo tem PNG em public/ui', () => {
    const missing = AVATAR_IDS.filter(
      (id) => !existsSync(join(ROOT, `public/ui/avatar.${id}.png`)),
    );
    expect(missing).toEqual([]);
  });

  it('nenhum PNG de avatar está vazio', () => {
    for (const id of AVATAR_IDS) {
      expect(statSync(join(ROOT, `public/ui/avatar.${id}.png`)).size).toBeGreaterThan(200);
    }
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run tests/assets/avatars.test.ts`
Expected: FAIL — 12 arquivos faltando.

- [ ] **Step 3: Implementar o gerador**

```js
// scripts/gen-avatar-placeholder.mjs
// PLACEHOLDER dos 12 avatares de perfil (10.6). Medalhão: fundo radial dos tokens + aro dourado +
// o pterodáctilo REAL de public/ui/dino.starter.png com rotação de matiz por índice. A arte AAA
// definitiva chega como folha 4×3 (ver docs/assets/specs/ui.avatars.md) e entra pelo gen-ui.mjs.
// Zero dep. Rode `npm run gen:avatars`.
import { encodePng } from './gen-icons.mjs';
import { loadArt, contentBounds, cropResize } from './gen-atlas.mjs';
import { existsSync, mkdirSync, writeFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';
import path from 'node:path';

const ROOT = fileURLToPath(new URL('..', import.meta.url));
const SIZE = 128;
const SUBJECT = { file: 'dino.starter.png', root: 'public/ui' };
const RIM = [0xc9, 0xa2, 0x27]; // --color-gold
const AVATAR_IDS = ['a01','a02','a03','a04','a05','a06','a07','a08','a09','a10','a11','a12'];

/** HSL→RGB (h em graus, s/l em 0..1). */
function hsl(h, s, l) {
  const c = (1 - Math.abs(2 * l - 1)) * s;
  const hp = ((h % 360) + 360) % 360 / 60;
  const x = c * (1 - Math.abs((hp % 2) - 1));
  const [r, g, b] = hp < 1 ? [c, x, 0] : hp < 2 ? [x, c, 0] : hp < 3 ? [0, c, x]
    : hp < 4 ? [0, x, c] : hp < 5 ? [x, 0, c] : [c, 0, x];
  const m = l - c / 2;
  return [Math.round((r + m) * 255), Math.round((g + m) * 255), Math.round((b + m) * 255)];
}

/** Assunto decodificado e recortado na bbox de conteúdo (memoizado por loadArt). */
function subject() {
  const img = loadArt(SUBJECT.file, SUBJECT.root);
  const b = contentBounds(img, 0, 0, img.w, img.h);
  return { img, sx: b.minX, sy: b.minY, sw: b.maxX - b.minX, sh: b.maxY - b.minY };
}

/** Medalhão RGBA `size`×`size`: disco colorido, aro dourado, silhueta do dino tingida. */
export function renderAvatar(size, hue) {
  const rgba = Buffer.alloc(size * size * 4);
  const c = (size - 1) / 2;
  const radius = size * 0.48;
  const rimHalf = size * 0.022;
  const inner = hsl(hue, 0.5, 0.42);
  const outer = hsl(hue, 0.55, 0.22);
  for (let y = 0; y < size; y++) {
    for (let x = 0; x < size; x++) {
      const d = Math.hypot(x - c, y - c);
      const i = (y * size + x) * 4;
      if (d > radius + rimHalf) { rgba[i + 3] = 0; continue; }
      const t = Math.min(1, d / radius);
      for (let k = 0; k < 3; k++) rgba[i + k] = Math.round(inner[k] + (outer[k] - inner[k]) * t);
      rgba[i + 3] = 255;
      const e = Math.abs(d - radius);
      if (e <= rimHalf + 1) {
        const a = Math.max(0, Math.min(1, rimHalf + 0.5 - e));
        for (let k = 0; k < 3; k++) rgba[i + k] = Math.round(rgba[i + k] + (RIM[k] - rgba[i + k]) * a);
      }
      // borda externa do disco: anti-aliasing do alpha
      if (d > radius + rimHalf - 1) rgba[i + 3] = Math.round(255 * Math.max(0, radius + rimHalf - d));
    }
  }
  // Assunto centrado, tingido pelo matiz (mistura 45% com o cinza do próprio pixel).
  const { img, sx, sy, sw, sh } = subject();
  const target = Math.round(size * 0.62);
  const dw = sw >= sh ? target : Math.max(1, Math.round((sw / sh) * target));
  const dh = sh >= sw ? target : Math.max(1, Math.round((sh / sw) * target));
  const px = cropResize(img, sx, sy, sw, sh, dw, dh);
  const tint = hsl(hue, 0.65, 0.72);
  const ox = Math.round((size - dw) / 2);
  const oy = Math.round((size - dh) / 2);
  for (let y = 0; y < dh; y++) {
    for (let x = 0; x < dw; x++) {
      const s = (y * dw + x) * 4;
      const a = px[s + 3] / 255;
      if (a === 0) continue;
      const d = ((oy + y) * size + ox + x) * 4;
      for (let k = 0; k < 3; k++) {
        const src = Math.round(px[s + k] * 0.55 + tint[k] * 0.45);
        rgba[d + k] = Math.round(src * a + rgba[d + k] * (1 - a));
      }
      rgba[d + 3] = Math.max(rgba[d + 3], Math.round(a * 255));
    }
  }
  return rgba;
}

function main() {
  const force = process.argv.includes('--force');
  const dir = path.join(ROOT, 'public/ui');
  mkdirSync(dir, { recursive: true });
  AVATAR_IDS.forEach((id, i) => {
    const file = path.join(dir, `avatar.${id}.png`);
    // NÃO sobrescreve arte existente sem --force: o gotcha da Fase 9 foi um gerador de
    // placeholder apagando a arte real ao rodar de novo.
    if (existsSync(file) && !force) { console.log(`pulado ${id} (já existe)`); return; }
    const png = encodePng(SIZE, SIZE, renderAvatar(SIZE, i * 30));
    writeFileSync(file, png);
    console.log(`escrito public/ui/avatar.${id}.png (${png.length} bytes)`);
  });
}
if (process.argv[1] === fileURLToPath(import.meta.url)) main();
```

```ts
// scripts/gen-avatar-placeholder.d.mts
export function renderAvatar(size: number, hue: number): Buffer;
```

Em `package.json`, dentro de `scripts`: `"gen:avatars": "node scripts/gen-avatar-placeholder.mjs",`

- [ ] **Step 4: Gerar a arte e rodar o teste**

Run: `npm run gen:avatars && npx vitest run tests/assets/avatars.test.ts`
Expected: 12 linhas "escrito public/ui/avatar.aNN.png" e PASS. Rodar `npm run gen:avatars` uma
segunda vez tem de imprimir "pulado" para os 12 (prova do guarda anti-sobrescrita).

- [ ] **Step 5: Commit**

```bash
git add scripts/gen-avatar-placeholder.mjs scripts/gen-avatar-placeholder.d.mts \
  package.json public/ui/avatar.a*.png tests/assets/avatars.test.ts
git commit -m "feat(10.6): gerador e 12 avatares placeholder (medalhões)"
```

---

### Task 5: Componente `Avatar` compartilhado

**Files:**
- Create: `src/app/components/Avatar.tsx`
- Modify: `src/app/screens/HomeScreen.tsx` (remove a cópia local, importa o componente)
- Modify: `src/app/screens/ProfileScreen.tsx` (idem)
- Modify: `src/app/styles/global.css`
- Test: `tests/app/avatar.test.tsx`

**Interfaces:**
- Consumes: `avatarFor`, `Profile` (Task 2); `avatarDef` (Task 1).
- Produces: `<Avatar profile={p} />` e `avatarSrc(id: AvatarId): string`.

- [ ] **Step 1: Escrever o teste que falha**

```tsx
// @vitest-environment happy-dom
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { render } from 'preact';
import { Avatar } from '@app/components/Avatar';
import type { Profile } from '@services/profile';

const profile: Profile = { id: 'p1', name: 'Rex', createdAt: 0, avatarId: 'a03' };

describe('Avatar', () => {
  let container: HTMLDivElement;
  beforeEach(() => {
    container = document.createElement('div');
    document.body.appendChild(container);
  });
  afterEach(() => {
    render(null, container);
    container.remove();
  });

  it('renderiza o PNG do avatar escolhido', () => {
    render(<Avatar profile={profile} />, container);
    const img = container.querySelector('img')!;
    expect(img.getAttribute('src')).toContain('ui/avatar.a03.png');
    expect(img.getAttribute('alt')).toBe('');
  });

  it('cai para a inicial quando a imagem falha', async () => {
    render(<Avatar profile={profile} />, container);
    container.querySelector('img')!.dispatchEvent(new Event('error', { bubbles: false }));
    await new Promise((r) => setTimeout(r, 0));
    expect(container.querySelector('img')).toBeNull();
    expect(container.textContent).toContain('R');
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run tests/app/avatar.test.tsx`
Expected: FAIL — módulo `@app/components/Avatar` não existe.

- [ ] **Step 3: Implementar**

```tsx
// src/app/components/Avatar.tsx
import { useState } from 'preact/hooks';
import type { VNode } from 'preact';
import { avatarFor, avatarDef, type AvatarId, type Profile } from '@services/profile';

/** Caminho do PNG do avatar. `BASE_URL` porque o app é servido sob subcaminho no GitHub Pages. */
export function avatarSrc(id: AvatarId): string {
  return `${import.meta.env.BASE_URL}ui/avatar.${id}.png`;
}

/**
 * Avatar do jogador: o PNG escolhido dentro de um disco da matiz do avatar. Se a imagem não
 * carregar (arte ausente, cache/rede), cai para a inicial do nome — a identidade nunca some.
 */
export function Avatar({ profile }: { profile: Profile }): VNode {
  const [failed, setFailed] = useState(false);
  const def = avatarDef(profile.avatarId);
  const { initial } = avatarFor(profile);
  return (
    <span class="avatar" style={{ backgroundColor: `hsl(${def.hue}, 55%, 45%)` }}>
      {failed ? (
        initial
      ) : (
        <img
          class="avatar__img"
          src={avatarSrc(profile.avatarId)}
          alt=""
          onError={() => setFailed(true)}
        />
      )}
    </span>
  );
}
```

Nas duas telas: apagar a função `Avatar` local e o import de `avatarFor`, e importar
`import { Avatar } from '../components/Avatar';`.

CSS (junto do bloco `.avatar`, `global.css:217`):

```css
.avatar__img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: 50%;
  display: block;
}
```

O `.avatar` já é `border-radius: 50%`; acrescentar `overflow: hidden;` a ele.

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run tests/app/avatar.test.tsx tests/app/home.test.tsx tests/app/profile-screen.test.tsx`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/app/components/Avatar.tsx src/app/screens/HomeScreen.tsx \
  src/app/screens/ProfileScreen.tsx src/app/styles/global.css tests/app/avatar.test.tsx
git commit -m "feat(10.6): componente Avatar compartilhado com fallback de inicial"
```

---

### Task 6: Seletor de avatar na tela de Perfil (+ i18n)

**Files:**
- Modify: `src/app/screens/ProfileScreen.tsx`
- Modify: `src/i18n/locales/{de,en,es,fr,hi,it,ja,ko,pt-BR,zh}.json`
- Modify: `src/app/styles/global.css`
- Test: `tests/app/profile-screen.test.tsx`

**Interfaces:**
- Consumes: `profileService.setAvatar` (Task 2), `AVATARS`/`avatarSrc` (Tasks 1/5).
- Produces: bloco `[data-testid="avatar-picker"]` com 12 `button[role="radio"]`
  (`data-avatar="<id>"`).

- [ ] **Step 1: Escrever os testes que falham**

```tsx
it('mostra os 12 avatares e marca o do perfil ativo', () => {
  render(<ProfileScreen />, container);
  const picker = container.querySelector('[data-testid="avatar-picker"]')!;
  const tiles = picker.querySelectorAll('button[role="radio"]');
  expect(tiles).toHaveLength(12);
  const checked = picker.querySelectorAll('[aria-checked="true"]');
  expect(checked).toHaveLength(1);
  expect(checked[0]!.getAttribute('data-avatar')).toBe(
    profileService.activeProfile.value!.avatarId,
  );
});

it('escolher um avatar troca o do jogador ativo', async () => {
  render(<ProfileScreen />, container);
  const tile = container.querySelector<HTMLButtonElement>('[data-avatar="a08"]')!;
  tile.dispatchEvent(new Event('click', { bubbles: true }));
  await Promise.resolve();
  render(<ProfileScreen />, container);
  expect(profileService.activeProfile.value!.avatarId).toBe('a08');
  expect(
    container.querySelector('[data-avatar="a08"]')!.getAttribute('aria-checked'),
  ).toBe('true');
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run tests/app/profile-screen.test.tsx`
Expected: FAIL — `[data-testid="avatar-picker"]` é `null`.

- [ ] **Step 3: Implementar**

Chaves i18n novas (bloco `profile` dos 10 locales) — pt-BR como referência:

```json
"avatar": "Avatar",
"avatarOption": "Avatar {{n}}"
```

Traduções: en `"Avatar"` / `"Avatar {{n}}"`; es `"Avatar"` / `"Avatar {{n}}"`; fr `"Avatar"` /
`"Avatar {{n}}"`; de `"Avatar"` / `"Avatar {{n}}"`; it `"Avatar"` / `"Avatar {{n}}"`; ja
`"アバター"` / `"アバター {{n}}"`; ko `"아바타"` / `"아바타 {{n}}"`; zh `"头像"` / `"头像 {{n}}"`;
hi `"अवतार"` / `"अवतार {{n}}"`.

No `ProfileScreen`, dentro de `profile__col--actions`, ANTES do `h2` de renomear:

```tsx
<h2 class="profile__heading">{i18n.t('profile.avatar')}</h2>
<div
  class="avatar-picker"
  data-testid="avatar-picker"
  role="radiogroup"
  aria-label={i18n.t('profile.avatar')}
>
  {AVATARS.map((a, i) => (
    <button
      key={a.id}
      type="button"
      role="radio"
      data-avatar={a.id}
      aria-checked={active?.avatarId === a.id}
      aria-label={i18n.t('profile.avatarOption', { n: i + 1 })}
      class={
        'avatar-picker__tile' +
        (active?.avatarId === a.id ? ' avatar-picker__tile--on' : '')
      }
      onClick={() => profileService.setAvatar(a.id)}
    >
      <img class="avatar-picker__img" src={avatarSrc(a.id)} alt="" />
    </button>
  ))}
</div>
```

CSS:

```css
.avatar-picker {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(3rem, 1fr));
  gap: var(--space-2);
}

.avatar-picker__tile {
  background: none;
  border: 2px solid transparent;
  border-radius: 50%;
  padding: 2px;
  cursor: pointer;
  line-height: 0;
}

.avatar-picker__tile--on {
  border-color: var(--color-gold);
}

.avatar-picker__img {
  width: 100%;
  aspect-ratio: 1;
  object-fit: cover;
  border-radius: 50%;
  display: block;
}
```

> Se `--color-gold` não existir em `tokens.css`, use o token dourado que existir lá (não invente
> valor hex novo).

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run tests/app tests/i18n`
Expected: PASS, inclusive a guarda de paridade dos 10 locales e o scanner AST de strings.

- [ ] **Step 5: Commit**

```bash
git add src/app/screens/ProfileScreen.tsx src/app/styles/global.css \
  src/i18n/locales tests/app/profile-screen.test.tsx
git commit -m "feat(10.6): seletor de avatar na tela de Perfil (10 idiomas)"
```

---

### Task 7: Sincronizar `avatarId` no online

**Files:**
- Modify: `src/services/online/index.ts:155-165`
- Test: `tests/online/*` (arquivo que já cobre o upsert de jogador; se não houver caso de avatar,
  acrescente um)

**Interfaces:**
- Consumes: `Profile.avatarId` (Task 2).
- Produces: `OnlinePlayer.avatar === profile.avatarId`.

- [ ] **Step 1: Escrever o teste que falha**

```ts
it('o jogador sincronizado leva o avatarId escolhido', async () => {
  profileService.init(memoryProfileStorage(emptyState()));
  profileService.create('Rex');
  profileService.setAvatar('a06');
  // …init do serviço online com o client falso já usado no arquivo…
  expect(fakeClient.lastUpsertedPlayer!.avatar).toBe('a06');
});
```

> Use o dublê de client já existente no arquivo de teste online; não crie um novo se houver um.

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run tests/online`
Expected: FAIL — recebe o matiz em string (ex.: `"137"`).

- [ ] **Step 3: Implementar**

Em `src/services/online/index.ts`, trocar

```ts
const { hue } = avatarFor(active);
const player: OnlinePlayer = { id, name: active.name, avatar: String(hue) };
```

por

```ts
// `players.avatar` é `text` sem check (6.1): o id do avatar cabe e é mais útil que a matiz
// derivada — a chave de identidade `${id}|${name}|${avatar}` re-upserta ao trocar de avatar.
const player: OnlinePlayer = { id, name: active.name, avatar: active.avatarId };
```

e remover o import de `avatarFor` se ficar sem uso.

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run tests/online && npx tsc --noEmit`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/services/online/index.ts tests/online
git commit -m "feat(10.6): online sincroniza avatarId em vez da matiz derivada"
```

---

### Task 8: Asset-spec, registro e fechamento dos docs

**Files:**
- Create: `docs/assets/specs/ui.avatars.md`
- Modify: `docs/assets/asset-registry.md`
- Modify: `docs/roadmap/PHASE-10-polish-and-portrait.md`
- Modify: `CLAUDE.md`

**Interfaces:** nenhuma (documentação).

- [ ] **Step 1: Escrever o asset-spec**

`docs/assets/specs/ui.avatars.md` no molde de `docs/assets/specs/ui.icons.md`, com:
identidade (`ui.avatars`, Tier 1, substitui o medalhão procedural), tabela dos **12 ids**
(`avatar.a01`…`avatar.a12`) com o retrato descrito de cada um, especificação técnica
(folha 4×3, célula 256×256, PNG com alpha, saída de runtime 128×128 em `public/ui/avatar.aNN.png`),
direção de arte (Style Bible), **bloco de prompt para geração por IA** (o teste
`tests/assets/registry-specs.test.ts` exige o bloco de prompt) e o checklist de aceite.

Incluir explicitamente a entrada de pipeline a acrescentar em `UI_SOURCES` (`scripts/gen-ui.mjs`)
quando a folha real existir:

```js
{ out: 'avatars', file: 'ui/ui.avatars.png', maxDim: 128,
  grid: { cols: 4, rows: 3, names: [
    'avatar.a01','avatar.a02','avatar.a03','avatar.a04',
    'avatar.a05','avatar.a06','avatar.a07','avatar.a08',
    'avatar.a09','avatar.a10','avatar.a11','avatar.a12'] } },
```

e a nota: ao adotar a arte real, **apagar `scripts/gen-avatar-placeholder.mjs`** (precedente dos
`gen-{parallax,obstacle}-placeholder`, que sobrescreviam a arte real).

- [ ] **Step 2: Registrar**

Acrescentar a linha de `specs/ui.avatars.md` em `docs/assets/asset-registry.md`, na tabela de UI,
com status `spec` (mesma coluna/valor usado pelos outros assets de UI).

- [ ] **Step 3: Rodar as guardas de docs**

Run: `npx vitest run tests/assets`
Expected: PASS (paridade registro↔spec e presença do bloco de prompt).

- [ ] **Step 4: Fechar o item na fase e no CLAUDE.md**

Em `docs/roadmap/PHASE-10-polish-and-portrait.md`: marcar 10.6 como `✅` no título e trocar os
`- [ ]` por `- [x]` (menos o 10.6b, que fica registrado como **não feito por decisão**), com um
bloco `>` de execução resumindo decisões e gotchas.

Em `CLAUDE.md`, atualizar "Estado atual": item 10.6 concluído, contagem de testes nova (rodar
`npm test` e usar o número real), Fase 10 `🚧 7/9`, próximo item **10.8**.

- [ ] **Step 5: Commit**

```bash
git add docs/assets/specs/ui.avatars.md docs/assets/asset-registry.md \
  docs/roadmap/PHASE-10-polish-and-portrait.md CLAUDE.md
git commit -m "docs(10.6): asset-spec dos avatares e fechamento do item"
```

---

## Verificação final (antes do PR)

- [ ] `npm test` — suíte **inteira** verde (não só os arquivos-alvo; precedente 9.8).
- [ ] `npm run check` — tsc + eslint limpos.
- [ ] `git diff main --stat` não mostra nenhum arquivo sob `src/core/` ⇒ determinismo segue 73;
      confirmar com `npx vitest run tests/determinism`.
- [ ] `npm run build` conclui (os 12 PNGs entram no precache do SW sem estourar o limite de 4 MB
      por arquivo).
