# i18n Completo (4.9) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Congelar a completude i18n (já auditada como completa) em guardas de regressão de teste, para que "todas as strings visíveis nos 10 idiomas" continue verdade quando novas telas chegarem.

**Architecture:** Só camada de teste. Guardas de **dados de locale** (paridade de placeholders, não-vazio, allowlist de não-traduzido) estendem `tests/i18n/locales.test.ts`. Um guard de **fonte** (scan de strings hardcoded) vive em `tests/i18n/no-hardcoded-strings.test.ts`. Nenhuma dependência nova; nenhum arquivo de produção tocado; `src/core/` intocado ⇒ determinismo inalterado (67).

**Tech Stack:** Vitest, TypeScript estrito, `node:fs`, i18next resources de `@i18n/locales/index`.

## Global Constraints

- **REGRA 4 (i18n):** nenhuma string visível ao usuário hardcoded — tudo via chaves i18next. Estes guardas são a prova automatizada dessa regra.
- **Determinismo:** nada em `src/core/` pode ser tocado. Este item não toca.
- Sem dependências novas. Usar só `node:fs`/regex e os recursos já importados.
- Os guardas devem **passar** com os locales atuais E **falhar** sob uma violação injetada (disciplina "testar o teste").
- Não adicionar/alterar chaves ou valores de tradução (auditoria já provou cobertura completa).
- `npm run check` limpo e `npm test` verde ao final de cada task.

---

### Task 1: Guardas de dados de locale — paridade de placeholders + não-vazio

**Files:**
- Modify: `tests/i18n/locales.test.ts`

**Interfaces:**
- Consumes: `resources`, `SUPPORTED_LANGUAGES`, `DEFAULT_LANGUAGE` de `@i18n/locales/index` (já importados no arquivo).
- Produces: helpers `flatten(obj) → Record<path,string>` e `placeholders(s) → string[]`, reusados pela Task 2.

- [ ] **Step 1: Adicionar helpers e os dois testes (falham só sob violação; passam hoje)**

Adicionar ao topo do arquivo (após o `keyPaths` existente) os helpers:

```ts
// Achata para { "a.b.c": "valor" } — só folhas string.
function flatten(obj: Record<string, unknown>, prefix = ''): Record<string, string> {
  const out: Record<string, string> = {};
  for (const [k, v] of Object.entries(obj)) {
    const path = prefix ? `${prefix}.${k}` : k;
    if (v !== null && typeof v === 'object') {
      Object.assign(out, flatten(v as Record<string, unknown>, path));
    } else {
      out[path] = v as string;
    }
  }
  return out;
}

// Conjunto ordenado de nomes de placeholder {{nome}} num valor.
function placeholders(s: string): string[] {
  return [...s.matchAll(/\{\{\s*([A-Za-z0-9_]+)\s*\}\}/g)].map((m) => m[1]).sort();
}
```

Adicionar dentro do `describe('locales', ...)`:

```ts
it('cada locale preserva os mesmos placeholders de interpolação que en', () => {
  const en = flatten(resources.en.translation);
  for (const lng of SUPPORTED_LANGUAGES) {
    const t = flatten(resources[lng].translation);
    for (const key of Object.keys(en)) {
      expect(
        placeholders(t[key] ?? ''),
        `${lng} > ${key}: placeholders divergem de en`,
      ).toEqual(placeholders(en[key]));
    }
  }
});

it('nenhum valor de tradução é vazio ou só-espaço', () => {
  for (const lng of SUPPORTED_LANGUAGES) {
    const t = flatten(resources[lng].translation);
    for (const [key, val] of Object.entries(t)) {
      expect(typeof val, `${lng} > ${key}: valor não é string`).toBe('string');
      expect(val.trim().length, `${lng} > ${key}: valor vazio`).toBeGreaterThan(0);
    }
  }
});
```

- [ ] **Step 2: Rodar e verificar que passam com os locales atuais**

Run: `npx vitest run tests/i18n/locales.test.ts`
Expected: PASS (todos os testes, incluindo os 2 novos).

- [ ] **Step 3: Testar o teste — placeholder parity pega violação**

Editar temporariamente `src/i18n/locales/de.json`, chave `hud.distance`: remover o `{{value}}` (ex.: `"Dist: m"`).
Run: `npx vitest run tests/i18n/locales.test.ts`
Expected: FAIL em "de > hud.distance: placeholders divergem de en".
Reverter a edição (`git checkout src/i18n/locales/de.json`).

- [ ] **Step 4: Testar o teste — não-vazio pega violação**

Editar temporariamente `src/i18n/locales/es.json`, chave `nav.shop`: `""`.
Run: `npx vitest run tests/i18n/locales.test.ts`
Expected: FAIL em "es > nav.shop: valor vazio".
Reverter (`git checkout src/i18n/locales/es.json`).

- [ ] **Step 5: check + commit**

Run: `npm run check`
Expected: sem erros de type/lint.

```bash
git add tests/i18n/locales.test.ts
git commit -m "test(4.9): guardas i18n de paridade de placeholders e valores não-vazios

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Guarda de não-traduzido com allowlist documentada

**Files:**
- Modify: `tests/i18n/locales.test.ts`

**Interfaces:**
- Consumes: `flatten` (Task 1), `resources`, `SUPPORTED_LANGUAGES`, `DEFAULT_LANGUAGE`, e o tipo `SupportedLanguage` de `@i18n/locales/index`.
- Produces: nada para tasks seguintes.

- [ ] **Step 1: Importar o tipo SupportedLanguage**

Alterar o import no topo para incluir o tipo:

```ts
import {
  SUPPORTED_LANGUAGES,
  DEFAULT_LANGUAGE,
  resources,
  type SupportedLanguage,
} from '@i18n/locales/index';
```

- [ ] **Step 2: Adicionar a allowlist + os dois testes**

Adicionar antes do `describe` (ou no topo do describe) a constante e o helper:

```ts
// Um valor de locale byte-idêntico ao en só é legítimo quando é marca, nome
// próprio, acrônimo/abreviação universal, empréstimo de gaming ou cognato real
// na língua. Cada par locale::chave abaixo veio da auditoria do item 4.9.
// Só adicionar aqui COM justificativa; senão, traduza a string.
const LOCALES_NON_EN = SUPPORTED_LANGUAGES.filter((l) => l !== DEFAULT_LANGUAGE);

const IDENTICAL_TO_EN_ALLOWLIST: ReadonlySet<string> = new Set<string>([
  // Marca "JurassicRun" — não se traduz.
  ...LOCALES_NON_EN.flatMap((l) => [`${l}::app.title`, `${l}::share.title`]),
  // Acrônimo universal "FPS".
  ...LOCALES_NON_EN.map((l) => `${l}::hud.fps`),
  // Nome próprio "Midas" (it/zh/ja/ko o localizam; estes o mantêm).
  'es::dino.midas.name', 'pt-BR::dino.midas.name', 'fr::dino.midas.name', 'de::dino.midas.name',
  // Abreviação "Dist" de Distância/Distance/Distanza/Distanz.
  'es::hud.distance', 'pt-BR::hud.distance', 'fr::hud.distance', 'it::hud.distance', 'de::hud.distance',
  // "Lv" — abreviação universal de nível em jogos (ja/ko).
  'ja::hud.level', 'ko::hud.level',
  // "Seed" — empréstimo padrão em alemão de gaming.
  'de::hud.seed',
  // "Game Over" — mantido em italiano.
  'it::gameover.title',
  // Cognatos alemães reais: "Nest"/"Wind"; empréstimo padrão "Shop".
  'de::nav.nest', 'de::screen.nest', 'de::nest.title', 'de::weather.wind',
  'de::nav.shop', 'de::screen.shop', 'de::shop.title',
  // Cognatos franceses reais: "Active"/"Glacier"/"Centurion".
  'fr::expansions.active', 'fr::expansion.glacier.name', 'fr::trophy.centurion.name',
  // "Volume" — idêntico em pt-BR/fr/it.
  'pt-BR::settings.volume', 'fr::settings.volume', 'it::settings.volume',
]);

// Valor que é só interpolação (ex.: "{{value}}") não tem o que traduzir.
const isPureInterpolation = (s: string): boolean => /^\s*\{\{[^}]+\}\}\s*$/.test(s);
```

Adicionar dentro do `describe('locales', ...)`:

```ts
it('todo valor idêntico ao en é traduzido ou está na allowlist justificada', () => {
  const en = flatten(resources.en.translation);
  const offenders: string[] = [];
  for (const lng of LOCALES_NON_EN) {
    const t = flatten(resources[lng].translation);
    for (const key of Object.keys(en)) {
      if (t[key] === en[key] && !isPureInterpolation(en[key])) {
        const pair = `${lng}::${key}`;
        if (!IDENTICAL_TO_EN_ALLOWLIST.has(pair)) offenders.push(pair);
      }
    }
  }
  expect(
    offenders,
    `strings não traduzidas (idênticas ao en) — traduza, ou justifique na allowlist: ${offenders.join(', ')}`,
  ).toEqual([]);
});

it('a allowlist de idênticos não tem entradas obsoletas', () => {
  const en = flatten(resources.en.translation);
  const stale: string[] = [];
  for (const pair of IDENTICAL_TO_EN_ALLOWLIST) {
    const [lng, key] = pair.split('::') as [SupportedLanguage, string];
    const t = flatten(resources[lng].translation);
    if (t[key] !== en[key]) stale.push(pair);
  }
  expect(
    stale,
    `entradas obsoletas na allowlist (não são mais idênticas ao en): ${stale.join(', ')}`,
  ).toEqual([]);
});
```

- [ ] **Step 3: Rodar e verificar que passam hoje**

Run: `npx vitest run tests/i18n/locales.test.ts`
Expected: PASS. (Os 53 pares idênticos estão todos na allowlist; nenhum offender.)

- [ ] **Step 4: Testar o teste — offender detectado**

Editar temporariamente `src/i18n/locales/es.json`, chave `nav.nest`: trocar `"Nido"` por `"Nest"` (copiando o inglês).
Run: `npx vitest run tests/i18n/locales.test.ts`
Expected: FAIL listando `es::nav.nest` como não-traduzido.
Reverter (`git checkout src/i18n/locales/es.json`).

- [ ] **Step 5: Testar o teste — entrada obsoleta detectada**

Editar temporariamente a allowlist no teste: adicionar `'es::nav.nest'` (que NÃO é idêntico — es tem "Nido").
Run: `npx vitest run tests/i18n/locales.test.ts`
Expected: FAIL em "entradas obsoletas na allowlist" listando `es::nav.nest`.
Reverter a edição do teste.

- [ ] **Step 6: check + commit**

Run: `npm run check`
Expected: sem erros.

```bash
git add tests/i18n/locales.test.ts
git commit -m "test(4.9): guarda de não-traduzido com allowlist auditada (53 pares legítimos)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: Guarda de fonte — nenhuma string visível hardcoded

**Files:**
- Create: `tests/i18n/no-hardcoded-strings.test.ts`

**Interfaces:**
- Consumes: `node:fs` (`readdirSync`/`readFileSync`/`statSync`), `node:path` (`join`). Escaneia `src/app/**` e `src/render/**`.
- Produces: nada.

- [ ] **Step 1: Criar o arquivo de teste**

Criar `tests/i18n/no-hardcoded-strings.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { readdirSync, readFileSync, statSync } from 'node:fs';
import { join } from 'node:path';

const ROOTS = ['src/app', 'src/render'];

function walk(dir: string): string[] {
  return readdirSync(dir).flatMap((name) => {
    const p = join(dir, name);
    return statSync(p).isDirectory() ? walk(p) : [p];
  });
}

function sourceFiles(): string[] {
  return ROOTS.flatMap(walk).filter((f) => /\.tsx?$/.test(f));
}

// Nó de texto JSX cru: ">Palavra ...<" começando com letra, só letras/pontuação
// (exclui {expressões}, glifos/emoji decorativos e código como ">(0)" ou "=> <").
const JSX_TEXT = />([A-Za-zÀ-ÿ][A-Za-zÀ-ÿ ,.!?'’-]*)</;
// Literal de string passado direto ao Phaser em vez de t(...).
const SETTEXT_LITERAL = /\.setText\(\s*['"`]/;
const ADDTEXT_LITERAL = /add\.text\(\s*-?[\d.]+\s*,\s*-?[\d.]+\s*,\s*['"`]/;

// Remove comentário de linha para não gerar falso-positivo em "//>texto".
const stripLineComment = (line: string): string => line.replace(/\/\/.*$/, '');

describe('no-hardcoded-strings', () => {
  it('o scanner realmente encontra arquivos-fonte (não passa por vacuidade)', () => {
    expect(sourceFiles().length).toBeGreaterThan(10);
  });

  it('nenhum nó de texto JSX cru — todo texto de UI via {t(\'chave\')}', () => {
    const offenders: string[] = [];
    for (const file of sourceFiles().filter((f) => f.endsWith('.tsx'))) {
      readFileSync(file, 'utf8')
        .split('\n')
        .forEach((line, i) => {
          const m = JSX_TEXT.exec(stripLineComment(line));
          if (m) offenders.push(`${file}:${i + 1}  >${m[1]}<`);
        });
    }
    expect(offenders, `texto JSX hardcoded — use {t('chave')}:\n${offenders.join('\n')}`).toEqual([]);
  });

  it('nenhum texto Phaser hardcoded — add.text/setText recebe t(...)', () => {
    const offenders: string[] = [];
    for (const file of sourceFiles()) {
      readFileSync(file, 'utf8')
        .split('\n')
        .forEach((line, i) => {
          const code = stripLineComment(line);
          if (SETTEXT_LITERAL.test(code) || ADDTEXT_LITERAL.test(code)) {
            offenders.push(`${file}:${i + 1}  ${line.trim()}`);
          }
        });
    }
    expect(offenders, `texto Phaser hardcoded — passe t('chave'):\n${offenders.join('\n')}`).toEqual([]);
  });
});
```

- [ ] **Step 2: Rodar e verificar que passa (0 offenders hoje)**

Run: `npx vitest run tests/i18n/no-hardcoded-strings.test.ts`
Expected: PASS (3 testes; a auditoria mostrou 0 strings hardcoded).

- [ ] **Step 3: Testar o teste — JSX cru detectado**

Editar temporariamente `src/app/screens/HomeScreen.tsx`: adicionar dentro do JSX um `<span>Hardcoded Text</span>`.
Run: `npx vitest run tests/i18n/no-hardcoded-strings.test.ts`
Expected: FAIL listando `HomeScreen.tsx:<linha>  >Hardcoded Text<`.
Reverter (`git checkout src/app/screens/HomeScreen.tsx`).

- [ ] **Step 4: Testar o teste — literal Phaser detectado**

Editar temporariamente `src/render/GameScene.ts`: numa linha, `something.setText('literal')`.
Run: `npx vitest run tests/i18n/no-hardcoded-strings.test.ts`
Expected: FAIL listando a linha do `setText('literal')`.
Reverter (`git checkout src/render/GameScene.ts`).

- [ ] **Step 5: check + rodar suíte inteira + commit**

Run: `npm run check`
Expected: sem erros.

Run: `npm test`
Expected: suíte inteira verde (determinismo inalterado, novos guardas passam).

```bash
git add tests/i18n/no-hardcoded-strings.test.ts
git commit -m "test(4.9): guarda de fonte contra strings de UI hardcoded (JSX + Phaser)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Fechamento (após as 3 tasks)

- Marcar 4.9 `[x]` em `docs/roadmap/PHASE-04-meta-offline.md`.
- Atualizar "Estado atual" do `CLAUDE.md` (4.9 concluído; próximo = 4.10 Áudio).
- Review final da branch (subagent `reviewer`).
- `verification-before-completion`: `npm test` + `npm run check` de verdade.
- Merge local `--no-ff` no `main` (sem remote GitHub configurado) e aposentar a branch.

## Self-Review (feito)

- **Cobertura da spec:** Guarda 1 (placeholders)→Task 1; Guarda 2 (não-vazio)→Task 1; Guarda 3 (allowlist não-traduzido)→Task 2; Guarda 4 (scan hardcoded)→Task 3. Todas cobertas.
- **Placeholders:** nenhum "TBD/TODO"; todo passo tem código/comando real e output esperado.
- **Consistência de tipos:** `flatten`/`placeholders` definidos na Task 1 e reusados na Task 2; `SupportedLanguage` importado na Task 2; nomes de regex/const consistentes na Task 3.
- **Escopo:** um plano coeso, test-only, sem decomposição adicional.
