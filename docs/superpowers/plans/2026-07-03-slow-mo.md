# Slow-mo (câmera lenta) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adicionar o power-up câmera lenta (slow-mo): coletá-lo faz o jogo ser percebido em câmera lenta por uma duração determinística, dando mais tempo real de reação — sem violar o passo fixo nem o determinismo.

**Architecture:** `slowMo` é um efeito temporário determinístico em `WorldState.effects` (ativado no pickup dentro de `step()`, ticado 1×/step, incluído no golden hash). A **lentidão** é aplicada só na camada de render: `FixedStepLoop.advance` escala o `dt` real por `SLOW_MO_TIME_SCALE` (<1) antes de somá-lo ao acumulador de passo fixo ⇒ menos steps/segundo real, sim byte-idêntica. Ver spec `docs/superpowers/specs/2026-07-03-slow-mo-design.md`.

**Tech Stack:** TypeScript estrito, Vitest. `src/core/powerup/` (puro), `src/render/loop.ts` (puro), `src/render/manifest.ts`, asset-specs em `docs/assets/specs/`.

## Global Constraints

- **Determinismo (REGRA 1):** `src/core/` sem `Math.random`/`Date.now`/`performance.now`; passo fixo `FIXED_DT=1/60`; mesma seed+inputs ⇒ mesmo estado. Após tocar core, rodar `npm run test:determinism`.
- **Arte desacoplada (REGRA 2):** colisão por hitbox lógica; todo id de catálogo tem entrada no `ASSET_MANIFEST` (há guarda de completude).
- **Performance (REGRA 3):** sem alocação por frame no hot path; a escala do loop usa só escalares.
- **i18n (REGRA 4):** nenhuma string nova neste item.
- **Asset-spec (REGRA 5):** todo objeto visual trocável tem asset-spec em `docs/assets/specs/` + registro.
- `src/core/` NÃO importa phaser/preact/DOM. `src/render/loop.ts` é puro (pode importar de `@core/*`).
- Um commit por task, suíte verde a cada commit. Placeholders de tuning são aceitáveis (herança do 3.1).

---

### Task 1: Registrar `slowMo` como power-up coletável (core + manifesto + goldens)

Registra o efeito determinístico e o torna spawnável/renderável, mantendo a suíte verde. NÃO inclui a lentidão (Task 2). Como a 5ª entrada no catálogo muda a distribuição do `pick` do spawner, os 3 goldens **com seed** são re-pinados aqui (mesmo commit) para o tree ficar verde.

**Files:**
- Modify: `src/core/powerup/types.ts` (união `PowerupKind`)
- Modify: `src/core/powerup/catalog.ts` (`POWERUP_CATALOG` + `KIND_BY_TAG`)
- Modify: `src/core/powerup/constants.ts` (`SLOW_MO_DURATION_STEPS`)
- Modify: `src/core/powerup/apply.ts` (`durationFor`)
- Modify: `src/render/manifest.ts` (`ASSET_MANIFEST`)
- Modify: `tests/core/powerup/catalog.test.ts`
- Modify: `tests/determinism/replay.determinism.test.ts` (re-pin dos 3 goldens seeded)
- Test: `tests/core/powerup/slowmo-pickup.test.ts` (novo)

**Interfaces:**
- Consumes: `activateEffect`, `isEffectActive`, `tickEffects`, `pickupPowerup`, `powerupKindForTag` (de `@core/powerup`); `SpawnType`/`SpawnConfig` (`@core/spawn`); `circle` (`@core/sim/hitbox`).
- Produces: `PowerupKind` inclui `'slowMo'`; `POWERUP_CATALOG` tem `powerup.slowMo`; `powerupKindForTag('powerup.slowMo') === 'slowMo'`; `SLOW_MO_DURATION_STEPS: number` exportado de `@core/powerup`; `ASSET_MANIFEST['powerup.slowMo']` existe.

- [ ] **Step 1: Atualizar o teste de catálogo (falha)**

Em `tests/core/powerup/catalog.test.ts`, incluir a 5ª entrada e o mapeamento:

```typescript
    expect(ids).toEqual([
      'powerup.shield',
      'powerup.extraLife',
      'powerup.magnet',
      'powerup.doubleCoin',
      'powerup.slowMo',
    ]);
```

```typescript
    expect(powerupKindForTag('powerup.doubleCoin')).toBe('doubleCoin');
    expect(powerupKindForTag('powerup.slowMo')).toBe('slowMo');
```

- [ ] **Step 2: Escrever o teste de pickup do slow-mo (falha)**

Criar `tests/core/powerup/slowmo-pickup.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { createWorld } from '@core/sim';
import type { Entity } from '@core/sim';
import {
  pickupPowerup,
  tickEffects,
  isEffectActive,
  SLOW_MO_DURATION_STEPS,
} from '@core/powerup';
import { circle } from '@core/sim/hitbox';

function slowMoPickup(): Entity {
  return {
    tags: ['powerup.slowMo'],
    transform: { position: { x: 0, y: 0 }, rotation: 0 },
    kinematics: { velocity: { x: 0, y: 0 } },
    hitbox: circle(8),
  };
}

describe('slow-mo pickup', () => {
  it('ativa o efeito slowMo por SLOW_MO_DURATION_STEPS e expira depois', () => {
    const w = createWorld({ worldHeight: 600, startY: 300 });
    const p = slowMoPickup();
    w.powerups.push(p);

    expect(pickupPowerup(w, p)).toBe(true);
    expect(w.powerups).toHaveLength(0);
    expect(isEffectActive(w.effects, 'slowMo')).toBe(true);

    // Ticka até 1 antes de expirar: ainda ativo.
    for (let i = 0; i < SLOW_MO_DURATION_STEPS - 1; i++) tickEffects(w.effects);
    expect(isEffectActive(w.effects, 'slowMo')).toBe(true);
    // Último tick: expira.
    tickEffects(w.effects);
    expect(isEffectActive(w.effects, 'slowMo')).toBe(false);
  });
});
```

> Nota ao implementador: confira o shape de `Entity` em `src/core/sim/types.ts` e ajuste o literal `slowMoPickup()` se algum campo diferir (ex.: nome de `kinematics`). O essencial é `tags[0] === 'powerup.slowMo'`.

- [ ] **Step 3: Rodar os testes e ver falhar**

Run: `npm test -- tests/core/powerup/catalog.test.ts tests/core/powerup/slowmo-pickup.test.ts`
Expected: FAIL (`'slowMo'` não é um `PowerupKind`; `SLOW_MO_DURATION_STEPS` não exportado; catálogo com 4 entradas).

- [ ] **Step 4: Estender a união `PowerupKind`**

Em `src/core/powerup/types.ts`:

```typescript
/** Tipos de power-up. */
export type PowerupKind = 'shield' | 'extraLife' | 'magnet' | 'doubleCoin' | 'slowMo';
```

- [ ] **Step 5: Adicionar a entrada no catálogo e no mapa de tags**

Em `src/core/powerup/catalog.ts`, acrescentar a 5ª entrada em `POWERUP_CATALOG`:

```typescript
  { id: 'powerup.doubleCoin', anchor: 'floating', makeHitbox: (rng) => circle(rng.range(7, 9)) },
  { id: 'powerup.slowMo', anchor: 'floating', makeHitbox: (rng) => circle(rng.range(7, 9)) },
```

E em `KIND_BY_TAG`:

```typescript
  'powerup.doubleCoin': 'doubleCoin',
  'powerup.slowMo': 'slowMo',
```

- [ ] **Step 6: Adicionar a constante de duração**

Em `src/core/powerup/constants.ts`, após `EXTRA_LIFE_GRACE_STEPS`:

```typescript
/** Duração do slow-mo (steps de mundo). ~3 s de conteúdo; tempo REAL sentido = steps·FIXED_DT ÷ SLOW_MO_TIME_SCALE. Placeholder de tuning. */
export const SLOW_MO_DURATION_STEPS = 180;
```

Verifique que `SLOW_MO_DURATION_STEPS` é reexportado por `src/core/powerup/index.ts` (o barrel exporta `./constants`; se for export nomeado, acrescente).

- [ ] **Step 7: Tratar `slowMo` em `durationFor`**

Em `src/core/powerup/apply.ts`, importar a constante e ampliar `durationFor`:

```typescript
import {
  SHIELD_DURATION_STEPS,
  MAGNET_DURATION_STEPS,
  DOUBLE_COIN_DURATION_STEPS,
  MAGNET_RADIUS,
  MAGNET_PULL_SPEED,
  EXTRA_LIFE_GRACE_STEPS,
  SLOW_MO_DURATION_STEPS,
} from './constants';
```

```typescript
/** Duração (steps) de cada power-up temporário. `extraLife` não é temporário. */
function durationFor(kind: 'shield' | 'magnet' | 'doubleCoin' | 'slowMo'): number {
  switch (kind) {
    case 'shield':
      return SHIELD_DURATION_STEPS;
    case 'magnet':
      return MAGNET_DURATION_STEPS;
    case 'doubleCoin':
      return DOUBLE_COIN_DURATION_STEPS;
    case 'slowMo':
      return SLOW_MO_DURATION_STEPS;
  }
}
```

> `pickupPowerup` já roteia qualquer kind ≠ `extraLife` pelo ramo `else` (`activateEffect(world.effects, kind, durationFor(kind))`), então nada mais muda ali.

- [ ] **Step 8: Adicionar a cor no manifesto de assets**

Em `src/render/manifest.ts`, dentro de `ASSET_MANIFEST`, após `powerup.doubleCoin`:

```typescript
  'powerup.doubleCoin': { kind: 'primitive', color: 0xffe14a },
  'powerup.slowMo': { kind: 'primitive', color: 0x66ffcc },
```

- [ ] **Step 9: Rodar os testes novos + manifesto (devem passar)**

Run: `npm test -- tests/core/powerup/catalog.test.ts tests/core/powerup/slowmo-pickup.test.ts tests/render/manifest.test.ts`
Expected: PASS (catálogo, pickup e guarda de completude do manifesto verdes).

- [ ] **Step 10: Re-pinar os 3 goldens com seed**

Run: `npm test -- tests/determinism/replay.determinism.test.ts`
Expected: FAIL nos 3 cenários **com seed** (`endless:GOLD1`, `GOLD1 difficulty:false`, `endless:GOLD2`) — a 5ª entrada mudou a distribuição do `pick`. O cenário **sem seed** continua verde.

Copie o `Received` (hash de 32 hex) de cada cenário falho para o campo `golden` correspondente em `tests/determinism/replay.determinism.test.ts` (linhas ~37, ~44, ~51). NÃO altere o golden do cenário sem seed (linha ~30). Rode de novo:

Run: `npm test -- tests/determinism/replay.determinism.test.ts`
Expected: PASS (4/4).

- [ ] **Step 11: Verificar core (determinismo + typecheck + suíte cheia)**

Run: `npm run check && npm run test:determinism && npm test`
Expected: typecheck limpo; bateria de determinismo verde; suíte cheia verde.

- [ ] **Step 12: Commit**

```bash
git add src/core/powerup src/render/manifest.ts tests/core/powerup tests/determinism/replay.determinism.test.ts
git commit -m "feat(3.2): registra slow-mo como power-up coletável (core + manifesto + goldens)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Escala de tempo do slow-mo no FixedStepLoop (a percepção)

Aplica a lentidão: durante o slow-mo, `advance` escala o `dt` real ⇒ menos steps/segundo real. É o comportamento central do item 3.2. Módulo `loop.ts` é puro ⇒ testável em node.

**Files:**
- Modify: `src/render/constants.ts` (`SLOW_MO_TIME_SCALE`)
- Modify: `src/render/loop.ts` (`FixedStepLoop.advance`)
- Test: `tests/render/loop.test.ts`

**Interfaces:**
- Consumes: `isEffectActive` (`@core/powerup`), `activateEffect` (`@core/powerup`, no teste), `SLOW_MO_TIME_SCALE` (`@render/constants`), `WorldState.effects`.
- Produces: `SLOW_MO_TIME_SCALE: number`; `advance(dt)` roda menos steps quando `slowMo` está ativo em `world.effects`.

- [ ] **Step 1: Adicionar a constante de escala no render**

Em `src/render/constants.ts`, após `MAX_FRAME_TIME`:

```typescript
/** Fator de escala de tempo do slow-mo (<1): durante o efeito, o dt real é encolhido antes
 * de entrar no acumulador de passo fixo ⇒ menos steps/segundo real (câmera lenta). O sim
 * fica byte-idêntico, só clocado devagar (mesma fps-independência já provada). Placeholder. */
export const SLOW_MO_TIME_SCALE = 0.4;
```

- [ ] **Step 2: Escrever os testes do loop (falham)**

Em `tests/render/loop.test.ts`, acrescentar imports e um bloco de testes:

```typescript
import { SLOW_MO_TIME_SCALE } from '@render/constants';
import { activateEffect } from '@core/powerup';
```

```typescript
describe('FixedStepLoop — slow-mo (escala de tempo)', () => {
  it('com slowMo ativo, um frame de FIXED_DT roda 0 steps (dt encolhido)', () => {
    const w = tallWorld();
    activateEffect(w.effects, 'slowMo', 600);
    const loop = new FixedStepLoop(w, new NullInputSource());
    // scale 0.4: FIXED_DT*0.4 < FIXED_DT ⇒ nenhum step completa neste frame.
    expect(loop.advance(FIXED_DT)).toBe(0);
    expect(loop.world.tick).toBe(0);
  });

  it('com slowMo ativo rodam menos steps que sem, para o mesmo dt total', () => {
    const slow = tallWorld();
    activateEffect(slow.effects, 'slowMo', 600);
    const slowLoop = new FixedStepLoop(slow, new NullInputSource());
    const fastLoop = new FixedStepLoop(tallWorld(), new NullInputSource());

    let slowSteps = 0;
    let fastSteps = 0;
    for (let i = 0; i < 10; i++) {
      slowSteps += slowLoop.advance(FIXED_DT);
      fastSteps += fastLoop.advance(FIXED_DT);
    }
    expect(fastSteps).toBe(10);
    // 10 frames · scale 0.4 = 4·FIXED_DT acumulados ⇒ 4 steps.
    expect(slowSteps).toBe(Math.floor(10 * SLOW_MO_TIME_SCALE));
    expect(slowSteps).toBeLessThan(fastSteps);
  });

  it('ao expirar o slowMo, o ritmo volta ao normal (1 step por FIXED_DT)', () => {
    const w = tallWorld();
    activateEffect(w.effects, 'slowMo', 1); // dura 1 step
    const loop = new FixedStepLoop(w, new NullInputSource());
    // Junta dt suficiente (2.5 frames · 0.4 = 1 step) p/ rodar 1 step lento e o tickEffects zerar.
    loop.advance(FIXED_DT * 2.5);
    expect(loop.world.tick).toBe(1);
    // slowMo já expirou no step anterior ⇒ agora um frame normal roda 1 step.
    expect(loop.advance(FIXED_DT)).toBe(1);
  });
});
```

> Nota: `activateEffect(effects, kind, durationSteps)` empurra `{ kind, remaining }` em `world.effects`. `tallWorld()` já existe no arquivo (mundo alto ⇒ dino não morre).

- [ ] **Step 3: Rodar e ver falhar**

Run: `npm test -- tests/render/loop.test.ts`
Expected: FAIL (advance ainda não escala; `advance(FIXED_DT)` retorna 1 com slowMo ativo).

- [ ] **Step 4: Implementar a escala em `advance`**

Em `src/render/loop.ts`, importar `isEffectActive` e `SLOW_MO_TIME_SCALE`:

```typescript
import { isEffectActive } from '@core/powerup';
import { MAX_FRAME_TIME, SLOW_MO_TIME_SCALE } from './constants';
```

Ajustar `advance` (o clamp continua sobre o dt real; a escala vem depois, 1×/advance):

```typescript
  advance(dtSeconds: number): number {
    const dt = dtSeconds > MAX_FRAME_TIME ? MAX_FRAME_TIME : dtSeconds;
    const scale = isEffectActive(this.world.effects, 'slowMo') ? SLOW_MO_TIME_SCALE : 1;
    this.accumulator += dt * scale;
    const pos = this.world.pterodactyl.transform.position;
    let steps = 0;
    while (this.accumulator >= FIXED_DT) {
      this.prevX = pos.x;
      this.prevY = pos.y;
      step(this.world, this.input.sample());
      this.accumulator -= FIXED_DT;
      steps += 1;
    }
    return steps;
  }
```

> Alocação-zero (só escalares, REGRA 3). Lido 1×/advance: a imprecisão de fronteira ao expirar (≤1 frame de ritmo) não afeta o estado do sim.

- [ ] **Step 5: Rodar e ver passar**

Run: `npm test -- tests/render/loop.test.ts`
Expected: PASS (todos os testes do loop, incluindo os 3 novos de slow-mo).

- [ ] **Step 6: Verificar typecheck + suíte cheia**

Run: `npm run check && npm test`
Expected: typecheck limpo; suíte cheia verde.

- [ ] **Step 7: Commit**

```bash
git add src/render/constants.ts src/render/loop.ts tests/render/loop.test.ts
git commit -m "feat(3.2): slow-mo escala o dt do FixedStepLoop (câmera lenta na camada de render)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: Asset-spec do slow-mo + registro (REGRA 5)

Documenta o objeto visual trocável do power-up (via padrão dos asset-specs existentes) e o registra. Sem código/testes; deliverable é a spec + registro.

**Files:**
- Create: `docs/assets/specs/powerup.slowMo.md`
- Modify: o registro de asset-specs (mesmo arquivo/índice onde `powerup.shield`/`doubleCoin` estão listados — localize com `grep -rl "powerup.doubleCoin" docs/assets/`)

- [ ] **Step 1: Localizar o padrão dos asset-specs de power-up existentes**

Run: `ls docs/assets/specs/ && grep -rl "powerup.doubleCoin" docs/assets/`
Leia `docs/assets/specs/powerup.doubleCoin.md` (ou `powerup.shield.md`) e o arquivo de registro para copiar o formato exato (cabeçalho, campos de cor/hitbox/descrição, linha de registro). Use a skill `create-asset-spec` se disponível.

- [ ] **Step 2: Criar `docs/assets/specs/powerup.slowMo.md`**

Espelhe o formato do asset-spec do `powerup.doubleCoin`, com:
- id lógico: `powerup.slowMo`
- cor placeholder do manifesto: `0x66ffcc` (mint)
- hitbox: círculo (raio ~7–9, `floating`), igual aos demais power-ups
- descrição: ícone de câmera lenta (ex.: ampulheta / relógio / caracol), pickup flutuante; efeito = o mundo é percebido em câmera lenta por ~3 s de conteúdo (mais no relógio)
- distinção cromática dos outros 4 power-ups (mint vs ciano/rosa/roxo/amarelo)

- [ ] **Step 3: Registrar o asset-spec**

Acrescente a linha de registro de `powerup.slowMo` no mesmo índice/registro onde os outros power-ups aparecem (formato idêntico ao de `powerup.doubleCoin`).

- [ ] **Step 4: Sanidade da suíte (docs não quebram nada)**

Run: `npm run check && npm test`
Expected: verde (mudança é só de docs).

- [ ] **Step 5: Commit**

```bash
git add docs/assets/
git commit -m "docs(3.2): asset-spec do power-up slow-mo + registro (REGRA 5)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Fechamento (após as 3 tasks + reviews)

- Rodar `superpowers:verification-before-completion`: `npm test`, `npm run check`, `npm run test:determinism` — evidência real.
- `determinism-guardian` sobre o diff de `src/core/` (contrato intacto).
- Marcar item 3.2 `[x]` em `docs/roadmap/PHASE-03-powerups-and-weather.md`.
- Atualizar "Estado atual" do `CLAUDE.md` (entrada 3.2).
- Integrar no `main` (merge local `--no-ff`, sem remote GitHub) e aposentar a branch.

## Self-review (cobertura da spec)

- Modelo (efeito determinístico + escala no render): Task 1 (efeito) + Task 2 (escala). ✔
- Core: união/catálogo/constante/duração/pickup: Task 1. ✔
- `hashState`: sem mudança (string), só re-pin de goldens: Task 1 Step 10. ✔
- Render: `SLOW_MO_TIME_SCALE` + `loop.advance`: Task 2. ✔
- Manifesto (cor + guarda de completude): Task 1 Step 8/9. ✔
- Asset-spec + registro (REGRA 5): Task 3. ✔
- i18n: nenhuma string nova — nenhuma task, correto. ✔
- Testes: pickup/catálogo (Task 1), crux do loop (Task 2), determinismo/goldens (Task 1 Steps 10–11). ✔
