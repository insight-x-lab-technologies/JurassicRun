# 10.7 — Mais 15 troféus (8 → 23) — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recomendado) ou superpowers:executing-plans para implementar task a task. Os passos usam
> checkbox (`- [ ]`).

**Goal:** ampliar o catálogo de troféus do JurassicRun de 8 para 23, com 10 campos novos no
agregado vitalício, migração transparente do storage e os 15 pares nome/descrição nos 10 idiomas.

**Architecture:** o agregado (`TrophyStats`) e o predicado de cada troféu continuam **puros**
(`src/services/trophy/{store,catalog}.ts`), testáveis com objetos simples. Os fatos que faltavam
entram por `MatchSummary` (nível, moedas creditadas, power-ups, modo, timestamp) e por
`TrophyEvalContext` (ranks de pódio). O único fato que não existe hoje em lugar nenhum — power-ups
apanhados — é derivado **fora do core** por um contador de diff de bitmask no `MatchController`,
espelhando o `AudioEventDetector` da 9.6.

**Tech Stack:** TypeScript estrito, Vitest (+ `happy-dom` nos testes de DOM), Preact + `@preact/signals`,
i18next (10 locales em `src/i18n/locales/*.json`).

## Global Constraints

- **`src/core/` é INTOCÁVEL neste item.** Nenhum arquivo sob `src/core/` pode ser criado, editado ou
  removido. Determinismo permanece em **73** testes, sem re-pin de golden e sem `npm run build:edge`.
- `src/core/` não pode ser importado por nada novo além do que já é importado (`WorldState`,
  `PowerupKind` no render — leitura apenas).
- **Zero alocação por frame** no caminho quente (`MatchController.advance`): só campos primitivos,
  nada de `Set`/array/objeto novo por chamada.
- **REGRA 4 (i18n):** nenhuma string visível hardcoded. Toda chave nova existe nos **10** arquivos
  `src/i18n/locales/{en,pt-BR,es,fr,de,it,ja,ko,zh,hi}.json`.
- **`STORAGE_KEY` do troféu NÃO muda**: continua `'jurassicrun.trophies.v1'`. Bumpar a chave apagaria
  os desbloqueios conquistados. Só o campo `version` do payload vai a `2`.
  `'jurassicrun.trophies.v1'` **não** pode ser adicionado a `LEGACY_STORAGE_KEYS`
  (`src/services/storage/legacy.ts`).
- Todo campo de `TrophyStats` é **inteiro ≥ 0** (garantido por `sanitizeStat`).
- TypeScript estrito, sem `any`. Comentários e nomes de teste em português, como o resto do projeto.
- Verificação final roda a **suíte inteira** (`npm test`), não só os arquivos-alvo (precedente 9.8).

## Estrutura de arquivos

| Arquivo | Responsabilidade | Task |
|---|---|---|
| `src/render/pickups.ts` (**novo**) | `KIND_BITS`, `effectMask`, `PowerupPickupCounter` — fonte única do diff de efeitos | 1 |
| `src/render/audioEvents.ts` | passa a importar o mask de `pickups.ts` (sem duplicar) | 1 |
| `src/render/match.ts` | possui o contador; expõe `powerupsCollected` | 1 |
| `src/services/trophy/store.ts` | `TrophyStats` (16 campos), `MatchSummary` (9 campos), `foldMatch`, `foldPodium`, `epochDay` | 2 |
| `src/services/trophy/storage.ts` | saneia os 16 campos; payload `version: 2`; migra v1 sem perder `unlocked` | 3 |
| `src/services/trophy/catalog.ts` | 8 troféus existentes + 15 novos | 4 |
| `src/services/leaderboard/index.ts` | `weeklyRankForSeed` | 5 |
| `src/services/trophy/index.ts` | `recordMatch` repassa ranks; `recordDailyPodium` dobra o pódio | 5 |
| `src/app/game/startGame.ts` | preenche os campos novos do `MatchSummary` e o rank semanal | 5 |
| `src/i18n/locales/*.json` (10) | 15 × (name+desc) + `trophies.progress` | 6 |
| `src/app/screens/TrophiesScreen.tsx` | linha de progresso `n/23` | 7 |

---

## Task 1: Contador de power-ups fora do core

**Files:**
- Create: `src/render/pickups.ts`
- Modify: `src/render/audioEvents.ts` (remove `KIND_BITS`/`effectMask`, importa de `./pickups`)
- Modify: `src/render/match.ts` (campo privado + `advance` + `startMatch` + getter)
- Test: `tests/render/pickups.test.ts` (novo), `tests/render/match.test.ts` (existente — acrescentar)

**Interfaces:**
- Consumes: `WorldState` de `@core/sim` (somente leitura), `PowerupKind` de `@core/powerup`.
- Produces:
  - `export function effectMask(world: WorldState): number`
  - `export class PowerupPickupCounter { reset(world: WorldState): void; observe(world: WorldState): void; get count(): number }`
  - `MatchController.powerupsCollected: number` (getter)

- [ ] **Step 1: Escrever o teste que falha**

Criar `tests/render/pickups.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { PowerupPickupCounter, effectMask } from '@render/pickups';
import type { WorldState } from '@core/sim';

/** Mundo mínimo para o contador: ele só lê `effects` e `extraLives`. */
const world = (
  kinds: readonly string[],
  extraLives = 0,
): WorldState =>
  ({
    effects: kinds.map((kind) => ({ kind, remaining: 60 })),
    extraLives,
  }) as unknown as WorldState;

describe('effectMask', () => {
  it('mundo sem efeitos ⇒ 0; kinds diferentes acendem bits diferentes', () => {
    expect(effectMask(world([]))).toBe(0);
    expect(effectMask(world(['shield']))).not.toBe(effectMask(world(['magnet'])));
    expect(effectMask(world(['shield', 'magnet']))).toBe(
      effectMask(world(['shield'])) | effectMask(world(['magnet'])),
    );
  });
});

describe('PowerupPickupCounter', () => {
  it('parte de zero e conta um efeito temporário novo', () => {
    const c = new PowerupPickupCounter();
    c.reset(world([]));
    expect(c.count).toBe(0);
    c.observe(world(['shield']));
    expect(c.count).toBe(1);
  });

  it('não reconta o mesmo efeito enquanto ele continua ativo', () => {
    const c = new PowerupPickupCounter();
    c.reset(world([]));
    c.observe(world(['shield']));
    c.observe(world(['shield']));
    c.observe(world(['shield']));
    expect(c.count).toBe(1);
  });

  it('conta de novo quando o efeito expira e é reapanhado', () => {
    const c = new PowerupPickupCounter();
    c.reset(world([]));
    c.observe(world(['shield']));
    c.observe(world([]));
    c.observe(world(['shield']));
    expect(c.count).toBe(2);
  });

  it('kinds simultâneos distintos contam um cada', () => {
    const c = new PowerupPickupCounter();
    c.reset(world([]));
    c.observe(world(['shield', 'magnet']));
    expect(c.count).toBe(2);
  });

  it('extraLife conta pelo incremento de extraLives', () => {
    const c = new PowerupPickupCounter();
    c.reset(world([], 0));
    c.observe(world([], 1));
    expect(c.count).toBe(1);
  });

  it('bloquear um hit (extraLives cai) NÃO conta e não quebra o baseline', () => {
    const c = new PowerupPickupCounter();
    c.reset(world([], 0));
    c.observe(world([], 1)); // apanhou
    c.observe(world([], 0)); // gastou bloqueando
    expect(c.count).toBe(1);
    c.observe(world([], 1)); // apanhou de novo
    expect(c.count).toBe(2);
  });

  it('reset rearma o baseline com o mundo dado e zera a contagem', () => {
    const c = new PowerupPickupCounter();
    c.reset(world([]));
    c.observe(world(['shield']));
    c.reset(world(['shield'])); // partida nova já com efeito? baseline conta como visto
    expect(c.count).toBe(0);
    c.observe(world(['shield']));
    expect(c.count).toBe(0);
  });
});
```

- [ ] **Step 2: Rodar o teste e confirmar que falha**

Run: `npx vitest run tests/render/pickups.test.ts`
Expected: FAIL — `Failed to resolve import "@render/pickups"`.

- [ ] **Step 3: Criar `src/render/pickups.ts`**

```ts
// Módulo PURO (sem phaser/DOM): deriva "power-ups apanhados" de escalares do WorldState.
// NÃO toca `src/core/` — só LÊ o estado que a simulação já produz (REGRA 1).
// Diff por bitmask em campos primitivos ⇒ alocação-zero por frame (REGRA 3).
import type { WorldState } from '@core/sim';
import type { PowerupKind } from '@core/powerup';

/** Bitmask dos kinds temporários — comparar efeitos sem alocar Set/array por frame. */
export const KIND_BITS: Readonly<Record<PowerupKind, number>> = {
  shield: 1, slowMo: 2, magnet: 4, doubleCoin: 8, extraLife: 16,
};

export function effectMask(world: WorldState): number {
  let mask = 0;
  for (const e of world.effects) mask |= KIND_BITS[e.kind] ?? 0;
  return mask;
}

/**
 * Conta pickups de power-up de UMA partida. Um pickup temporário acende um bit em `effects`;
 * o `extraLife` incrementa `extraLives`. Bloquear um hit derruba `extraLives` — isso NÃO é
 * pickup e não pode contar.
 *
 * Limitação aceita: dois pickups do MESMO kind dentro de um único frame contam 1. O espaçamento
 * de spawn torna isso irrealizável; é a mesma aproximação que o SFX de power-up (9.6) já usa.
 */
export class PowerupPickupCounter {
  private effects = 0;
  private extraLives = 0;
  private _count = 0;

  get count(): number {
    return this._count;
  }

  /** Rearma o baseline a partir do mundo e zera a contagem (chamar na troca de partida). */
  reset(world: WorldState): void {
    this.effects = effectMask(world);
    this.extraLives = world.extraLives;
    this._count = 0;
  }

  /** Observa o mundo do step corrente e acumula os pickups desde a última observação. */
  observe(world: WorldState): void {
    const mask = effectMask(world);
    let gained = 0;
    let bits = mask & ~this.effects;
    while (bits !== 0) {
      bits &= bits - 1; // apaga o bit menos significativo aceso
      gained += 1;
    }
    if (world.extraLives > this.extraLives) gained += world.extraLives - this.extraLives;
    this._count += gained;
    this.effects = mask;
    this.extraLives = world.extraLives;
  }
}
```

- [ ] **Step 4: Rodar o teste e confirmar que passa**

Run: `npx vitest run tests/render/pickups.test.ts`
Expected: PASS (7 testes).

- [ ] **Step 5: Deduplicar o mask em `audioEvents.ts`**

Em `src/render/audioEvents.ts`, apagar o bloco `KIND_BITS` + `function effectMask` (linhas 8-17) e a
importação de `PowerupKind`, e importar de `./pickups`:

```ts
import type { WorldState } from '@core/sim';
import type { SfxId } from '@services/audio';
import { effectMask } from './pickups';
```

Nada mais muda no arquivo — `AudioEventDetector` continua chamando `effectMask(world)`.

- [ ] **Step 6: Rodar os testes de áudio para provar que não houve regressão**

Run: `npx vitest run tests/render/audioEvents.test.ts tests/render/pickups.test.ts`
Expected: PASS nos dois arquivos.

- [ ] **Step 7: Escrever o teste do `MatchController` (falha)**

Acrescentar em `tests/render/match.test.ts` (siga o helper de mundo/fábrica que o arquivo já usa;
se o `MatchController` do arquivo é montado com um `factory` de mundo real do core, use-o):

```ts
it('conta power-ups apanhados na partida e zera no restart', () => {
  // Monte o controller como os outros testes deste arquivo já fazem.
  const match = makeController(); // helper existente do arquivo
  expect(match.powerupsCollected).toBe(0);

  match.notifyFlap(); // ready → playing
  // injeta um efeito no mundo corrente (o contador só lê `effects`/`extraLives`)
  match.world.effects.push({ kind: 'shield', remaining: 60 } as never);
  match.advance(1 / 60);
  expect(match.powerupsCollected).toBe(1);

  match.advance(1 / 60);
  expect(match.powerupsCollected).toBe(1); // efeito ainda ativo ⇒ não reconta
});
```

- [ ] **Step 8: Rodar e confirmar que falha**

Run: `npx vitest run tests/render/match.test.ts`
Expected: FAIL — `powerupsCollected` não existe em `MatchController`.

- [ ] **Step 9: Ligar o contador no `MatchController`**

Em `src/render/match.ts`:

```ts
import { PowerupPickupCounter } from './pickups';
```

Campo privado, junto dos outros:

```ts
  // 10.7: power-ups apanhados na partida corrente. Derivado do WorldState (o core não conta isso);
  // alimenta o agregado vitalício de troféus no game-over.
  private readonly _pickups = new PowerupPickupCounter();
```

Getter:

```ts
  /** Power-ups apanhados na partida corrente (temporários + extraLife). */
  get powerupsCollected(): number {
    return this._pickups.count;
  }
```

Em `advance`, logo depois de `this._loop.advance(dtSeconds);` e **antes** do teste de `alive`:

```ts
    this._loop.advance(dtSeconds);
    this._pickups.observe(this._world);
    if (!this._world.alive) {
```

Em `startMatch`, depois de `this._loop = new FixedStepLoop(...)`:

```ts
    this._pickups.reset(this._world);
```

- [ ] **Step 10: Rodar os testes de render**

Run: `npx vitest run tests/render/`
Expected: PASS.

- [ ] **Step 11: Typecheck e commit**

Run: `npm run check`
Expected: sem erros.

```bash
git add src/render/pickups.ts src/render/audioEvents.ts src/render/match.ts \
        tests/render/pickups.test.ts tests/render/match.test.ts
git commit -m "feat(10.7): conta power-ups apanhados fora do core (PowerupPickupCounter)"
```

---

## Task 2: Agregado vitalício com os 10 campos novos

**Files:**
- Modify: `src/services/trophy/store.ts`
- Test: `tests/services/trophy/store.test.ts`

**Interfaces:**
- Consumes: `PODIUM_RANK`, `TROPHY_CATALOG` de `./catalog` (já importado).
- Produces:
  - `export type TrophyMatchMode = 'endless' | 'daily' | 'weekly'`
  - `TrophyStats` com 16 campos (6 antigos + 10 novos)
  - `MatchSummary` com 9 campos (4 antigos + `level`, `coins`, `powerups`, `mode`, `playedAt`)
  - `TrophyEvalContext` com `dailyRank?` (existente) + `weeklyRank?`
  - `export function epochDay(ms: number): number`
  - `export function foldPodium(stats: TrophyStats, mode: 'daily' | 'weekly'): TrophyStats`
  - `recordMatch(state, m, extra?: { dailyRank?: number; weeklyRank?: number })`

- [ ] **Step 1: Escrever os testes que falham**

Em `tests/services/trophy/store.test.ts`, substituir o helper `match` e acrescentar o bloco novo.
O helper passa a preencher os campos obrigatórios novos:

```ts
const match = (m: Partial<MatchSummary>): MatchSummary => ({
  distance: 0, food: 0, nearMisses: 0, score: 0,
  level: 1, coins: 0, powerups: 0, mode: 'endless', playedAt: 0,
  ...m,
});
```

Os dois testes existentes de `foldMatch` comparam com `toEqual` de objeto inteiro; atualize-os para
incluir os campos novos zerados, por exemplo o primeiro:

```ts
    expect(s1).toEqual({
      gamesPlayed: 1, totalFood: 3, totalDistance: 120,
      bestDistance: 120, bestNearMisses: 2, bestScore: 45,
      bestLevel: 1, totalNearMisses: 2, totalPowerups: 0, totalCoins: 0,
      challengesPlayed: 0, dailyPodiums: 0, weeklyPodiums: 0, bestChallengeScore: 0,
      daysPlayed: 1, lastPlayDay: 0,
    });
```

> Atenção: `playedAt: 0` ⇒ `epochDay(0) === 0`, que **não** é `> lastPlayDay: 0`. Para o estado
> inicial contar o primeiro dia, `emptyStats().lastPlayDay` é `0` e `foldMatch` trata `gamesPlayed
> === 0` como "nunca jogou" (ver implementação). Por isso `daysPlayed: 1` acima.

Testes novos:

```ts
describe('foldMatch — campos do 10.7', () => {
  it('bestLevel é o máximo; totalNearMisses/totalPowerups/totalCoins somam', () => {
    let s = emptyStats();
    s = foldMatch(s, match({ level: 4, nearMisses: 3, powerups: 2, coins: 7 }));
    s = foldMatch(s, match({ level: 2, nearMisses: 5, powerups: 1, coins: 3 }));
    expect(s.bestLevel).toBe(4);
    expect(s.totalNearMisses).toBe(8);
    expect(s.totalPowerups).toBe(3);
    expect(s.totalCoins).toBe(10);
  });

  it('modo endless não mexe em challengesPlayed nem bestChallengeScore', () => {
    const s = foldMatch(emptyStats(), match({ mode: 'endless', score: 9999 }));
    expect(s.challengesPlayed).toBe(0);
    expect(s.bestChallengeScore).toBe(0);
    expect(s.bestScore).toBe(9999); // o best geral continua contando
  });

  it('daily e weekly contam como desafio e alimentam bestChallengeScore', () => {
    let s = foldMatch(emptyStats(), match({ mode: 'daily', score: 300 }));
    s = foldMatch(s, match({ mode: 'weekly', score: 1200 }));
    s = foldMatch(s, match({ mode: 'weekly', score: 500 }));
    expect(s.challengesPlayed).toBe(3);
    expect(s.bestChallengeScore).toBe(1200);
  });

  it('daysPlayed conta dias UTC distintos, não partidas', () => {
    const DAY = 86_400_000;
    let s = emptyStats();
    s = foldMatch(s, match({ playedAt: 10 * DAY + 1 }));
    s = foldMatch(s, match({ playedAt: 10 * DAY + 3600_000 })); // mesmo dia
    expect(s.daysPlayed).toBe(1);
    s = foldMatch(s, match({ playedAt: 11 * DAY }));
    expect(s.daysPlayed).toBe(2);
    expect(s.lastPlayDay).toBe(11);
  });

  it('relógio andando para trás não decrementa nem duplica daysPlayed', () => {
    const DAY = 86_400_000;
    let s = foldMatch(emptyStats(), match({ playedAt: 20 * DAY }));
    s = foldMatch(s, match({ playedAt: 5 * DAY }));
    expect(s.daysPlayed).toBe(1);
    expect(s.lastPlayDay).toBe(20);
  });

  it('a primeira partida sempre conta um dia (mesmo com playedAt 0)', () => {
    const s = foldMatch(emptyStats(), match({ playedAt: 0 }));
    expect(s.daysPlayed).toBe(1);
  });

  it('saneia os campos novos (NaN/negativo ⇒ 0)', () => {
    const s = foldMatch(emptyStats(), match({ level: NaN, coins: -5, powerups: -1 }));
    expect(s.bestLevel).toBe(0);
    expect(s.totalCoins).toBe(0);
    expect(s.totalPowerups).toBe(0);
  });
});

describe('foldPodium', () => {
  it('incrementa só o contador do modo pedido', () => {
    const s = foldPodium(emptyStats(), 'daily');
    expect(s.dailyPodiums).toBe(1);
    expect(s.weeklyPodiums).toBe(0);
    expect(foldPodium(s, 'weekly').weeklyPodiums).toBe(1);
  });

  it('não muta a entrada', () => {
    const s0 = emptyStats();
    foldPodium(s0, 'daily');
    expect(s0.dailyPodiums).toBe(0);
  });
});

describe('recordMatch — pódios', () => {
  it('rank dentro do pódio dobra o contador do modo', () => {
    const r = recordMatch(initialTrophyState(), match({ mode: 'weekly' }), { weeklyRank: 2 });
    expect(r.state.stats.weeklyPodiums).toBe(1);
    expect(r.state.stats.dailyPodiums).toBe(0);
  });

  it('rank fora do pódio não dobra nada', () => {
    const r = recordMatch(initialTrophyState(), match({ mode: 'daily' }), { dailyRank: 9 });
    expect(r.state.stats.dailyPodiums).toBe(0);
  });

  it('sem rank informado não dobra pódio', () => {
    const r = recordMatch(initialTrophyState(), match({ mode: 'daily' }));
    expect(r.state.stats.dailyPodiums).toBe(0);
  });
});

describe('epochDay', () => {
  it('converte ms para dia UTC e satura negativo em 0', () => {
    expect(epochDay(0)).toBe(0);
    expect(epochDay(86_400_000)).toBe(1);
    expect(epochDay(86_400_000 * 3 + 999)).toBe(3);
    expect(epochDay(-5)).toBe(0);
    expect(epochDay(NaN)).toBe(0);
  });
});
```

Ajuste o import do topo do arquivo de teste para incluir `foldPodium` e `epochDay`.

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npx vitest run tests/services/trophy/store.test.ts`
Expected: FAIL — `foldPodium`/`epochDay` não exportados; campos ausentes.

- [ ] **Step 3: Implementar em `src/services/trophy/store.ts`**

Substituir as declarações de tipo e as funções de fold:

```ts
import { PODIUM_RANK, TROPHY_CATALOG } from './catalog';

/** Modo da partida. União declarada aqui de propósito: o serviço de troféus não depende de
 *  `@services/leaderboard` nem de `@render/matchFactory` — as uniões são estruturalmente iguais. */
export type TrophyMatchMode = 'endless' | 'daily' | 'weekly';

/** Agregado vitalício. Todos inteiros ≥ 0. */
export interface TrophyStats {
  readonly gamesPlayed: number;
  readonly totalFood: number;
  readonly totalDistance: number;
  readonly bestDistance: number;
  readonly bestNearMisses: number;
  readonly bestScore: number;
  // ── 10.7 ──────────────────────────────────────────────────────────────────
  /** Maior nível alcançado numa partida. */
  readonly bestLevel: number;
  /** Near-misses somados na vida (o `bestNearMisses` é o pico de UMA partida). */
  readonly totalNearMisses: number;
  /** Power-ups apanhados na vida (contados pelo render — o core não guarda isso). */
  readonly totalPowerups: number;
  /** Moedas CREDITADAS na vida. Lê o valor creditado, não a comida (ver 10.4). */
  readonly totalCoins: number;
  /** Partidas em modo Diário ou Semanal. */
  readonly challengesPlayed: number;
  readonly dailyPodiums: number;
  readonly weeklyPodiums: number;
  /** Melhor score numa partida de desafio (Diário ou Semanal). */
  readonly bestChallengeScore: number;
  /** Dias UTC distintos com ao menos uma partida. */
  readonly daysPlayed: number;
  /** Dia-época UTC da última partida. Estado de suporte do `daysPlayed`, não é exibível. */
  readonly lastPlayDay: number;
}

/** Resultado de UMA partida (desacoplado de WorldState). */
export interface MatchSummary {
  readonly distance: number;
  readonly food: number;
  readonly nearMisses: number;
  readonly score: number;
  /** Nível atingido ao morrer. */
  readonly level: number;
  /** Moedas creditadas por esta partida. */
  readonly coins: number;
  /** Power-ups apanhados nesta partida. */
  readonly powerups: number;
  readonly mode: TrophyMatchMode;
  /** Epoch ms do fim da partida (vem da casca; o agregado não lê relógio). */
  readonly playedAt: number;
}

export interface TrophyState {
  readonly stats: TrophyStats;
  readonly unlocked: readonly string[];
}

/** Contexto de avaliação: agregado vitalício + fatos transientes da partida recém-terminada. */
export interface TrophyEvalContext {
  readonly stats: TrophyStats;
  /** rank 1-based no leaderboard diário; ausente fora do Diário. */
  readonly dailyRank?: number;
  /** rank 1-based no leaderboard semanal; ausente fora do Semanal. */
  readonly weeklyRank?: number;
}

export function emptyStats(): TrophyStats {
  return {
    gamesPlayed: 0, totalFood: 0, totalDistance: 0,
    bestDistance: 0, bestNearMisses: 0, bestScore: 0,
    bestLevel: 0, totalNearMisses: 0, totalPowerups: 0, totalCoins: 0,
    challengesPlayed: 0, dailyPodiums: 0, weeklyPodiums: 0, bestChallengeScore: 0,
    daysPlayed: 0, lastPlayDay: 0,
  };
}
```

`initialTrophyState` e `sanitizeStat` ficam como estão. Acrescentar `epochDay` e reescrever
`foldMatch`, mais `foldPodium`:

```ts
const MS_PER_DAY = 86_400_000;

/** Dia UTC (inteiro ≥ 0) de um instante em epoch ms. Puro: só floor e divisão. */
export function epochDay(ms: number): number {
  return sanitizeStat(sanitizeStat(ms) / MS_PER_DAY);
}

/** Dobra uma partida no agregado. Imutável; não muta a entrada. */
export function foldMatch(stats: TrophyStats, m: MatchSummary): TrophyStats {
  const distance = sanitizeStat(m.distance);
  const food = sanitizeStat(m.food);
  const nearMisses = sanitizeStat(m.nearMisses);
  const score = sanitizeStat(m.score);
  const level = sanitizeStat(m.level);
  const coins = sanitizeStat(m.coins);
  const powerups = sanitizeStat(m.powerups);
  const isChallenge = m.mode === 'daily' || m.mode === 'weekly';

  // Dia novo: `lastPlayDay` só avança para frente (relógio para trás não duplica nem decrementa).
  // `gamesPlayed === 0` é o caso "nunca jogou", em que qualquer dia — inclusive o 0 — é novo.
  const day = epochDay(m.playedAt);
  const isNewDay = stats.gamesPlayed === 0 || day > stats.lastPlayDay;

  return {
    gamesPlayed: stats.gamesPlayed + 1,
    totalFood: stats.totalFood + food,
    totalDistance: stats.totalDistance + distance,
    bestDistance: Math.max(stats.bestDistance, distance),
    bestNearMisses: Math.max(stats.bestNearMisses, nearMisses),
    bestScore: Math.max(stats.bestScore, score),
    bestLevel: Math.max(stats.bestLevel, level),
    totalNearMisses: stats.totalNearMisses + nearMisses,
    totalPowerups: stats.totalPowerups + powerups,
    totalCoins: stats.totalCoins + coins,
    challengesPlayed: stats.challengesPlayed + (isChallenge ? 1 : 0),
    dailyPodiums: stats.dailyPodiums,
    weeklyPodiums: stats.weeklyPodiums,
    bestChallengeScore: isChallenge
      ? Math.max(stats.bestChallengeScore, score)
      : stats.bestChallengeScore,
    daysPlayed: stats.daysPlayed + (isNewDay ? 1 : 0),
    lastPlayDay: isNewDay ? Math.max(day, stats.lastPlayDay) : stats.lastPlayDay,
  };
}

/** Dobra UM pódio (top-3) do modo dado. Imutável. */
export function foldPodium(stats: TrophyStats, mode: 'daily' | 'weekly'): TrophyStats {
  return mode === 'daily'
    ? { ...stats, dailyPodiums: stats.dailyPodiums + 1 }
    : { ...stats, weeklyPodiums: stats.weeklyPodiums + 1 };
}
```

`recordMatch` passa a repassar os dois ranks e a dobrar pódio:

```ts
/**
 * Dobra a partida, dobra o pódio (se houver rank de pódio) e reavalia. Imutável.
 *
 * INVARIANTE: `dailyRank` e o caminho assíncrono `TrophyService.recordDailyPodium` contam o MESMO
 * pódio. `startGame` computa o rank local só quando o board central está indisponível, e usa o
 * central só quando está — nunca os dois para a mesma partida.
 */
export function recordMatch(
  state: TrophyState,
  m: MatchSummary,
  extra?: { readonly dailyRank?: number; readonly weeklyRank?: number },
): { state: TrophyState; newlyUnlocked: readonly string[] } {
  let stats = foldMatch(state.stats, m);
  if (extra?.dailyRank !== undefined && extra.dailyRank <= PODIUM_RANK) {
    stats = foldPodium(stats, 'daily');
  }
  if (extra?.weeklyRank !== undefined && extra.weeklyRank <= PODIUM_RANK) {
    stats = foldPodium(stats, 'weekly');
  }
  const ctx: TrophyEvalContext = {
    stats,
    ...(extra?.dailyRank !== undefined ? { dailyRank: extra.dailyRank } : {}),
    ...(extra?.weeklyRank !== undefined ? { weeklyRank: extra.weeklyRank } : {}),
  };
  return evaluate({ stats, unlocked: state.unlocked }, ctx);
}
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `npx vitest run tests/services/trophy/store.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/services/trophy/store.ts tests/services/trophy/store.test.ts
git commit -m "feat(10.7): agregado vitalício de troféus ganha os 10 fatos que faltavam"
```

> Nota: `npm run check` ainda vai acusar erros nos call sites de `MatchSummary`
> (`startGame.ts`, testes de serviço/tela). Eles são corrigidos nas Tasks 5 e 7. Não tente
> consertá-los aqui.

---

## Task 3: Storage — saneamento dos 16 campos e migração v1 → v2

**Files:**
- Modify: `src/services/trophy/storage.ts`
- Test: `tests/services/trophy/storage.test.ts`

**Interfaces:**
- Consumes: `TrophyStats`, `emptyStats`, `sanitizeStat` de `./store`.
- Produces: `STORAGE_KEY` inalterado (`'jurassicrun.trophies.v1'`); payload `{ version: 2, stats, unlocked }`.

- [ ] **Step 1: Escrever os testes que falham**

Em `tests/services/trophy/storage.test.ts`, atualizar o `sample` para os 16 campos e acrescentar:

```ts
const sample: TrophyState = {
  stats: {
    gamesPlayed: 3, totalFood: 12, totalDistance: 900, bestDistance: 400,
    bestNearMisses: 4, bestScore: 88,
    bestLevel: 2, totalNearMisses: 9, totalPowerups: 5, totalCoins: 12,
    challengesPlayed: 1, dailyPodiums: 1, weeklyPodiums: 0, bestChallengeScore: 70,
    daysPlayed: 2, lastPlayDay: 20_500,
  },
  unlocked: ['firstFlight'],
};

describe('migração v1 → v2', () => {
  beforeEach(() => localStorage.clear());

  it('payload v1 carrega preservando os desbloqueios e zerando os campos novos', () => {
    localStorage.setItem(
      STORAGE_KEY,
      JSON.stringify({
        version: 1,
        stats: {
          gamesPlayed: 40, totalFood: 300, totalDistance: 22000,
          bestDistance: 1500, bestNearMisses: 12, bestScore: 6000,
        },
        unlocked: ['firstFlight', 'centurion', 'persistent'],
      }),
    );
    const loaded = localStorageTrophyStorage().load();
    expect(loaded.unlocked).toEqual(['firstFlight', 'centurion', 'persistent']);
    expect(loaded.stats.gamesPlayed).toBe(40);
    expect(loaded.stats.bestScore).toBe(6000);
    expect(loaded.stats.bestLevel).toBe(0);
    expect(loaded.stats.totalPowerups).toBe(0);
    expect(loaded.stats.daysPlayed).toBe(0);
    expect(loaded.stats.lastPlayDay).toBe(0);
  });

  it('a CHAVE não muda — bumpar a chave apagaria os desbloqueios', () => {
    expect(STORAGE_KEY).toBe('jurassicrun.trophies.v1');
  });

  it('grava version 2', () => {
    localStorageTrophyStorage().save(sample);
    const raw = JSON.parse(localStorage.getItem(STORAGE_KEY) ?? '{}') as { version: number };
    expect(raw.version).toBe(2);
  });

  it('a chave de troféus NÃO está na lista de chaves legadas a purgar', () => {
    expect([...LEGACY_STORAGE_KEYS]).not.toContain(STORAGE_KEY);
  });
});

describe('invariante de saneamento', () => {
  it('todo campo de TrophyStats é saneado na leitura (nenhum vira undefined)', () => {
    localStorage.setItem(STORAGE_KEY, JSON.stringify({ version: 1, stats: {}, unlocked: [] }));
    const loaded = localStorageTrophyStorage().load();
    for (const [k, v] of Object.entries(loaded.stats)) {
      expect(typeof v, `campo ${k}`).toBe('number');
      expect(Number.isInteger(v), `campo ${k}`).toBe(true);
    }
    // e o conjunto de campos bate exatamente com o do estado vazio
    expect(Object.keys(loaded.stats).sort()).toEqual(Object.keys(emptyStats()).sort());
  });
});
```

Importar no topo do arquivo de teste: `LEGACY_STORAGE_KEYS` de `@services/storage/legacy` e
`emptyStats` de `@services/trophy/store`.

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npx vitest run tests/services/trophy/storage.test.ts`
Expected: FAIL — campos novos vêm `undefined` de `sanitizeStats` e `version` é 1.

- [ ] **Step 3: Implementar**

Em `src/services/trophy/storage.ts`, trocar o corpo de `sanitizeStats` por uma varredura sobre as
chaves do estado vazio — assim um campo novo em `TrophyStats` nunca fica de fora por esquecimento:

```ts
function sanitizeStats(raw: unknown): TrophyStats {
  const base = emptyStats();
  if (!isRecord(raw)) return base;
  const out: Record<string, number> = {};
  for (const key of Object.keys(base)) {
    const v = raw[key];
    out[key] = typeof v === 'number' ? sanitizeStat(v) : 0;
  }
  return out as unknown as TrophyStats;
}
```

E o `save`:

```ts
        localStorage.setItem(
          STORAGE_KEY,
          // A CHAVE continua `.v1` de propósito: bumpar apagaria desbloqueios conquistados.
          // Só o schema do payload avança (10.7). Campos ausentes num payload v1 lêem 0.
          JSON.stringify({ version: 2, stats: state.stats, unlocked: state.unlocked }),
        );
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `npx vitest run tests/services/trophy/storage.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/services/trophy/storage.ts tests/services/trophy/storage.test.ts
git commit -m "feat(10.7): storage de troféus migra v1→v2 sem perder desbloqueios"
```

---

## Task 4: Catálogo com os 15 troféus novos

**Files:**
- Modify: `src/services/trophy/catalog.ts`
- Test: `tests/services/trophy/catalog.test.ts` (novo)

**Interfaces:**
- Consumes: `TrophyEvalContext` de `./store`.
- Produces: `TROPHY_CATALOG` com **23** entradas; ids novos:
  `explorer`, `skyLord`, `globetrotter`, `veteran`, `stuntPilot`, `closeShave`, `treasurer`,
  `tycoon`, `empowered`, `powerHungry`, `challenger`, `challengeAce`, `weeklyPodium`, `dedicated`,
  `legend`.

- [ ] **Step 1: Escrever o teste que falha**

Criar `tests/services/trophy/catalog.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { TROPHY_CATALOG, trophyById, isKnownTrophyId } from '@services/trophy/catalog';
import { emptyStats, type TrophyStats, type TrophyEvalContext } from '@services/trophy/store';

/** `Record<string, number>` (e não `Partial<TrophyStats>`) porque os testes montam o override
 *  com chave computada — o TS tipa `{ [field]: n }` como índice string. */
const ctx = (stats: Record<string, number>, extra: Omit<TrophyEvalContext, 'stats'> = {}):
  TrophyEvalContext => ({ stats: { ...emptyStats(), ...stats } as TrophyStats, ...extra });

/** Cada troféu novo: o campo do agregado que o alimenta e o limiar. */
const THRESHOLDS: ReadonlyArray<readonly [string, keyof TrophyStats & string, number]> = [
  ['explorer', 'bestLevel', 5],
  ['skyLord', 'bestLevel', 10],
  ['globetrotter', 'totalDistance', 50_000],
  ['veteran', 'gamesPlayed', 100],
  ['stuntPilot', 'bestNearMisses', 25],
  ['closeShave', 'totalNearMisses', 250],
  ['treasurer', 'totalCoins', 500],
  ['tycoon', 'totalCoins', 5_000],
  ['empowered', 'totalPowerups', 25],
  ['powerHungry', 'totalPowerups', 200],
  ['challenger', 'challengesPlayed', 10],
  ['challengeAce', 'bestChallengeScore', 5_000],
  ['weeklyPodium', 'weeklyPodiums', 1],
  ['dedicated', 'daysPlayed', 7],
  ['legend', 'bestScore', 20_000],
];

describe('catálogo 10.7', () => {
  it('tem 23 troféus, todos com id único', () => {
    expect(TROPHY_CATALOG.length).toBe(23);
    expect(new Set(TROPHY_CATALOG.map((t) => t.id)).size).toBe(23);
  });

  it('todo troféu tem nameKey/descKey no padrão trophy.<id>.*', () => {
    for (const t of TROPHY_CATALOG) {
      expect(t.nameKey).toBe(`trophy.${t.id}.name`);
      expect(t.descKey).toBe(`trophy.${t.id}.desc`);
    }
  });

  it('os 8 troféus originais continuam no catálogo (nenhum desbloqueio antigo vira órfão)', () => {
    for (const id of ['firstFlight', 'centurion', 'forager', 'daredevil', 'marathoner',
                      'highRoller', 'persistent', 'dailyPodium']) {
      expect(isKnownTrophyId(id)).toBe(true);
    }
  });

  it('cada troféu novo: abaixo do limiar não desbloqueia, no limiar e acima sim', () => {
    for (const [id, field, threshold] of THRESHOLDS) {
      const def = trophyById(id);
      expect(def, id).toBeDefined();
      expect(def!.condition(ctx({ [field]: threshold - 1 })), `${id} abaixo`).toBe(false);
      expect(def!.condition(ctx({ [field]: threshold })), `${id} no limiar`).toBe(true);
      expect(def!.condition(ctx({ [field]: threshold * 2 })), `${id} acima`).toBe(true);
    }
  });

  it('nenhum troféu novo desbloqueia com o agregado zerado', () => {
    const zero = ctx({});
    for (const [id] of THRESHOLDS) {
      expect(trophyById(id)!.condition(zero), id).toBe(false);
    }
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npx vitest run tests/services/trophy/catalog.test.ts`
Expected: FAIL — catálogo tem 8 entradas; `trophyById('explorer')` é `undefined`.

- [ ] **Step 3: Implementar**

Em `src/services/trophy/catalog.ts`, trocar o comentário do catálogo e acrescentar as 15 entradas
depois de `dailyPodium`:

```ts
/**
 * Catálogo de conquistas (23 — item 10.7).
 * Limiares ancorados nos números reais do jogo: nível = 1 + floor(distância/500)
 * (`DISTANCE_PER_LEVEL`), score = distância + 10·comida + 5·near-miss
 * (`src/core/economy/constants.ts`), e os 8 troféus originais como degrau de baixo da escada.
 */
export const TROPHY_CATALOG: readonly TrophyDef[] = Object.freeze([
  // ... as 8 entradas existentes, inalteradas ...

  // ── 10.7: progressão de corrida ──────────────────────────────────────────
  { id: 'explorer', nameKey: 'trophy.explorer.name', descKey: 'trophy.explorer.desc',
    condition: (c) => c.stats.bestLevel >= 5 },            // 2000 de distância
  { id: 'skyLord', nameKey: 'trophy.skyLord.name', descKey: 'trophy.skyLord.desc',
    condition: (c) => c.stats.bestLevel >= 10 },           // 4500 de distância
  { id: 'globetrotter', nameKey: 'trophy.globetrotter.name', descKey: 'trophy.globetrotter.desc',
    condition: (c) => c.stats.totalDistance >= 50_000 },   // 5× marathoner
  { id: 'legend', nameKey: 'trophy.legend.name', descKey: 'trophy.legend.desc',
    condition: (c) => c.stats.bestScore >= 20_000 },       // 4× highRoller

  // ── 10.7: volume e perícia ───────────────────────────────────────────────
  { id: 'veteran', nameKey: 'trophy.veteran.name', descKey: 'trophy.veteran.desc',
    condition: (c) => c.stats.gamesPlayed >= 100 },        // 4× persistent
  { id: 'stuntPilot', nameKey: 'trophy.stuntPilot.name', descKey: 'trophy.stuntPilot.desc',
    condition: (c) => c.stats.bestNearMisses >= 25 },      // 2,5× daredevil
  { id: 'closeShave', nameKey: 'trophy.closeShave.name', descKey: 'trophy.closeShave.desc',
    condition: (c) => c.stats.totalNearMisses >= 250 },

  // ── 10.7: economia ───────────────────────────────────────────────────────
  { id: 'treasurer', nameKey: 'trophy.treasurer.name', descKey: 'trophy.treasurer.desc',
    condition: (c) => c.stats.totalCoins >= 500 },         // 10× forager
  { id: 'tycoon', nameKey: 'trophy.tycoon.name', descKey: 'trophy.tycoon.desc',
    condition: (c) => c.stats.totalCoins >= 5_000 },

  // ── 10.7: power-ups ──────────────────────────────────────────────────────
  { id: 'empowered', nameKey: 'trophy.empowered.name', descKey: 'trophy.empowered.desc',
    condition: (c) => c.stats.totalPowerups >= 25 },
  { id: 'powerHungry', nameKey: 'trophy.powerHungry.name', descKey: 'trophy.powerHungry.desc',
    condition: (c) => c.stats.totalPowerups >= 200 },

  // ── 10.7: desafios ───────────────────────────────────────────────────────
  { id: 'challenger', nameKey: 'trophy.challenger.name', descKey: 'trophy.challenger.desc',
    condition: (c) => c.stats.challengesPlayed >= 10 },
  { id: 'challengeAce', nameKey: 'trophy.challengeAce.name', descKey: 'trophy.challengeAce.desc',
    condition: (c) => c.stats.bestChallengeScore >= 5_000 },
  // Par do `dailyPodium`, mas sobre o AGREGADO: assim continua desbloqueável numa avaliação
  // posterior, sem depender de o rank estar no contexto daquele instante.
  { id: 'weeklyPodium', nameKey: 'trophy.weeklyPodium.name', descKey: 'trophy.weeklyPodium.desc',
    condition: (c) => c.stats.weeklyPodiums >= 1 },

  // ── 10.7: constância ─────────────────────────────────────────────────────
  { id: 'dedicated', nameKey: 'trophy.dedicated.name', descKey: 'trophy.dedicated.desc',
    condition: (c) => c.stats.daysPlayed >= 7 },
]);
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `npx vitest run tests/services/trophy/catalog.test.ts`
Expected: PASS (as `it.each` dão 45 casos + 4).

- [ ] **Step 5: Commit**

```bash
git add src/services/trophy/catalog.ts tests/services/trophy/catalog.test.ts
git commit -m "feat(10.7): catálogo de troféus vai de 8 para 23"
```

---

## Task 5: Ligação — serviço, rank semanal e `startGame`

**Files:**
- Modify: `src/services/trophy/index.ts`
- Modify: `src/services/leaderboard/index.ts`
- Modify: `src/app/game/startGame.ts:118-121`
- Test: `tests/services/trophy/index.test.ts`, `tests/services/leaderboard/index.test.ts`

**Interfaces:**
- Consumes: `foldPodium`, `TrophyMatchMode`, `MatchSummary` (9 campos) da Task 2;
  `MatchController.powerupsCollected` da Task 1.
- Produces:
  - `LeaderboardService.weeklyRankForSeed(seed: string): number | undefined`
  - `TrophyService.recordMatch(m: MatchSummary, extra?: { dailyRank?: number; weeklyRank?: number })`
  - `TrophyService.recordDailyPodium(dailyRank: number)` passa a dobrar `dailyPodiums`.

- [ ] **Step 1: Escrever os testes que falham**

Em `tests/services/leaderboard/index.test.ts`, acrescentar:

```ts
it('weeklyRankForSeed devolve o rank 1-based da seed no board semanal', () => {
  const svc = new LeaderboardService();
  svc.init(memoryLeaderboardStorage());
  svc.recordMatch({ mode: 'weekly', seed: '2026-W31', score: 100, distance: 10,
                    food: 0, nearMisses: 0, level: 1, achievedAt: 1 });
  svc.recordMatch({ mode: 'weekly', seed: '2026-W30', score: 500, distance: 50,
                    food: 0, nearMisses: 0, level: 1, achievedAt: 2 });
  expect(svc.weeklyRankForSeed('2026-W30')).toBe(1);
  expect(svc.weeklyRankForSeed('2026-W31')).toBe(2);
  expect(svc.weeklyRankForSeed('2026-W01')).toBeUndefined();
});
```

> Ajuste os imports/nomes de helper aos que o arquivo já usa. Se o arquivo constrói o
> `LeaderboardResult` por um helper, reaproveite-o em vez de repetir o literal.

Em `tests/services/trophy/index.test.ts`, atualizar as chamadas de `recordMatch` para o
`MatchSummary` completo (helper local no topo do arquivo):

```ts
const match = (m: Partial<MatchSummary> = {}): MatchSummary => ({
  distance: 0, food: 0, nearMisses: 0, score: 0,
  level: 1, coins: 0, powerups: 0, mode: 'endless', playedAt: 0,
  ...m,
});
```

e acrescentar:

```ts
it('recordDailyPodium dobra dailyPodiums e desbloqueia o pódio diário', () => {
  const svc = new TrophyService();
  svc.init(memoryTrophyStorage());
  svc.recordMatch(match({ mode: 'daily' }));
  const newly = svc.recordDailyPodium(1);
  expect(newly).toContain('dailyPodium');
  expect(svc.stats.value.dailyPodiums).toBe(1);
});

it('recordDailyPodium fora do pódio não dobra nem desbloqueia', () => {
  const svc = new TrophyService();
  svc.init(memoryTrophyStorage());
  svc.recordMatch(match({ mode: 'daily' }));
  expect(svc.recordDailyPodium(7)).toEqual([]);
  expect(svc.stats.value.dailyPodiums).toBe(0);
});

it('rank semanal de pódio desbloqueia weeklyPodium pelo agregado', () => {
  const svc = new TrophyService();
  svc.init(memoryTrophyStorage());
  const newly = svc.recordMatch(match({ mode: 'weekly' }), { weeklyRank: 3 });
  expect(newly).toContain('weeklyPodium');
  expect(svc.stats.value.weeklyPodiums).toBe(1);
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npx vitest run tests/services/trophy/index.test.ts tests/services/leaderboard/index.test.ts`
Expected: FAIL — `weeklyRankForSeed` e `svc.stats` não existem.

- [ ] **Step 3: `weeklyRankForSeed` no leaderboard**

Em `src/services/leaderboard/index.ts`, logo abaixo de `dailyRankForSeed`:

```ts
  /** Rank 1-based do recorde semanal dessa seed; undefined se não houver. */
  weeklyRankForSeed(seed: string): number | undefined {
    return rankOf(this._state.value.weekly, seed);
  }
```

- [ ] **Step 4: Serviço de troféus**

Em `src/services/trophy/index.ts`:

```ts
import {
  initialTrophyState,
  recordMatch as recordMatchState,
  evaluate,
  foldPodium,
  type MatchSummary,
  type TrophyState,
  type TrophyStats,
} from './store';
import { isKnownTrophyId, PODIUM_RANK } from './catalog';
```

Sinal novo (as telas e os testes precisam ler o agregado):

```ts
  readonly stats: ReadonlySignal<TrophyStats> = computed(() => this._state.value.stats);
```

`recordMatch` repassa os dois ranks:

```ts
  /** Registra o resultado de uma partida; persiste se algo mudou. Retorna os ids recém-desbloqueados. */
  recordMatch(
    m: MatchSummary,
    extra?: { readonly dailyRank?: number; readonly weeklyRank?: number },
  ): readonly string[] {
    const { state, newlyUnlocked } = recordMatchState(this._state.value, m, extra);
    this.commit(state); // stats sempre mudam (gamesPlayed++) ⇒ sempre persiste
    this.pushToServer(newlyUnlocked);
    return newlyUnlocked;
  }
```

`recordDailyPodium` passa a dobrar o pódio antes de reavaliar:

```ts
  /**
   * Reavalia o pódio diário com o rank CENTRAL (chega assíncrono). Push best-effort.
   *
   * INVARIANTE: este caminho e o `extra.dailyRank` de `recordMatch` contam o MESMO pódio.
   * `startGame` usa o rank local só quando o board central está indisponível e o central só
   * quando está — nunca os dois para a mesma partida.
   */
  recordDailyPodium(dailyRank: number): readonly string[] {
    if (dailyRank > PODIUM_RANK) return [];
    const stats = foldPodium(this._state.value.stats, 'daily');
    const { state, newlyUnlocked } = evaluate(
      { stats, unlocked: this._state.value.unlocked },
      { stats, dailyRank },
    );
    this.commit(state); // o agregado mudou (dailyPodiums++) ⇒ persiste sempre
    this.pushToServer(newlyUnlocked);
    return newlyUnlocked;
  }
```

- [ ] **Step 5: `startGame` preenche os campos novos**

Em `src/app/game/startGame.ts`, no bloco `onGameOver` (linhas 113-121), substituir por:

```ts
      const online = leaderboardService.centralAvailable.value;
      // O rank LOCAL só é usado quando o board central está indisponível; quando está, o pódio
      // vem do `centralDailyRank` abaixo. Os dois caminhos dobram o MESMO contador de pódio —
      // por isso são mutuamente exclusivos (ver TrophyService.recordDailyPodium).
      const localDailyRank =
        mode === 'daily' && !online
          ? leaderboardService.dailyRankForSeed(match.seedLabel)
          : undefined;
      const localWeeklyRank =
        mode === 'weekly' ? leaderboardService.weeklyRankForSeed(match.seedLabel) : undefined;
      trophyService.recordMatch(
        {
          distance: w.distance,
          food: w.food,
          nearMisses: w.nearMisses,
          score: w.score,
          level: w.level,
          coins: coinsForFood(w.food),
          powerups: match.powerupsCollected,
          mode,
          playedAt: result.achievedAt,
        },
        localDailyRank !== undefined || localWeeklyRank !== undefined
          ? {
              ...(localDailyRank !== undefined ? { dailyRank: localDailyRank } : {}),
              ...(localWeeklyRank !== undefined ? { weeklyRank: localWeeklyRank } : {}),
            }
          : undefined,
      );
```

> `result.achievedAt` já é `Date.now()` (linha 109) — reutilizar mantém um único instante para a
> partida. `mode` é `MatchMode` de `@render/matchFactory`, estruturalmente igual a
> `TrophyMatchMode`.

- [ ] **Step 6: Prova de compatibilidade das uniões de modo**

Acrescentar em `tests/services/trophy/index.test.ts`:

```ts
it('MatchMode do render é atribuível a TrophyMatchMode (uniões não divergiram)', () => {
  const modes: TrophyMatchMode[] = ['endless', 'daily', 'weekly'] satisfies MatchMode[];
  expect(modes).toHaveLength(3);
});
```

com `import type { MatchMode } from '@render/matchFactory';` e
`import type { TrophyMatchMode } from '@services/trophy/store';`.

- [ ] **Step 7: Rodar os testes de serviço e o typecheck**

Run: `npx vitest run tests/services/ tests/app/game/`
Expected: PASS. Se algum teste de `tests/app/` constrói `MatchSummary`, atualize-o com os campos
novos (não afrouxe o tipo).

Run: `npm run check`
Expected: sem erros — este é o passo em que os call sites quebrados pela Task 2 fecham.

- [ ] **Step 8: Commit**

```bash
git add src/services/trophy/index.ts src/services/leaderboard/index.ts src/app/game/startGame.ts \
        tests/services/trophy/index.test.ts tests/services/leaderboard/index.test.ts
git commit -m "feat(10.7): liga nível/moedas/power-ups/modo/pódios ao agregado de troféus"
```

---

## Task 6: i18n — 15 pares × 10 idiomas + chave de progresso

**Files:**
- Modify: `src/i18n/locales/{en,pt-BR,es,fr,de,it,ja,ko,zh,hi}.json`
- Test: `tests/i18n/locales.test.ts` (guarda de paridade existente — deve continuar verde)

**Interfaces:**
- Consumes: os 15 ids da Task 4.
- Produces: `trophy.<id>.{name,desc}` para os 15 ids + `trophies.progress` com as interpolações
  `{{unlocked}}` e `{{total}}`, nos 10 arquivos.

**REQUIRED SUB-SKILL:** use a skill de projeto `add-locale` — ela é a fonte da verdade do formato e
da lista de idiomas.

- [ ] **Step 1: Escrever o teste que falha**

Criar `tests/i18n/trophy-keys.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { TROPHY_CATALOG } from '@services/trophy/catalog';
import en from '@i18n/locales/en.json';

type Bag = Record<string, unknown>;
const get = (bag: Bag, path: string): unknown =>
  path.split('.').reduce<unknown>((o, k) => (o as Bag | undefined)?.[k], bag);

describe('chaves i18n dos troféus', () => {
  it('todo troféu do catálogo tem nome e descrição não-vazios em en', () => {
    for (const t of TROPHY_CATALOG) {
      expect(typeof get(en as Bag, t.nameKey), t.nameKey).toBe('string');
      expect(String(get(en as Bag, t.nameKey)).length, t.nameKey).toBeGreaterThan(0);
      expect(typeof get(en as Bag, t.descKey), t.descKey).toBe('string');
      expect(String(get(en as Bag, t.descKey)).length, t.descKey).toBeGreaterThan(0);
    }
  });

  it('trophies.progress existe e interpola desbloqueados/total', () => {
    const s = String(get(en as Bag, 'trophies.progress'));
    expect(s).toContain('{{unlocked}}');
    expect(s).toContain('{{total}}');
  });
});
```

> Se o projeto não tiver o alias `@i18n`, importe pelo caminho relativo que
> `tests/i18n/locales.test.ts` já usa para ler os locales.

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npx vitest run tests/i18n/`
Expected: FAIL — chaves ausentes em `en.json`; a guarda de paridade também acusa assim que o
primeiro idioma for editado sozinho.

- [ ] **Step 3: Escrever as chaves em `en.json`**

No bloco `trophy` de `src/i18n/locales/en.json`, acrescentar (mantendo o estilo curto e imperativo
das 8 existentes):

```json
"explorer":     { "name": "Explorer",      "desc": "Reach level 5 in one run." },
"skyLord":      { "name": "Sky Lord",      "desc": "Reach level 10 in one run." },
"globetrotter": { "name": "Globetrotter",  "desc": "Travel 50,000 total distance." },
"legend":       { "name": "Legend",        "desc": "Score 20,000 in one run." },
"veteran":      { "name": "Veteran",       "desc": "Play 100 games." },
"stuntPilot":   { "name": "Stunt Pilot",   "desc": "Get 25 near-misses in one run." },
"closeShave":   { "name": "Close Shave",   "desc": "Get 250 near-misses in total." },
"treasurer":    { "name": "Treasurer",     "desc": "Earn 500 coins in total." },
"tycoon":       { "name": "Tycoon",        "desc": "Earn 5,000 coins in total." },
"empowered":    { "name": "Empowered",     "desc": "Collect 25 power-ups." },
"powerHungry":  { "name": "Power Hungry",  "desc": "Collect 200 power-ups." },
"challenger":   { "name": "Challenger",    "desc": "Play 10 challenge runs." },
"challengeAce": { "name": "Challenge Ace", "desc": "Score 5000 in a challenge run." },
"weeklyPodium": { "name": "Weekly Podium", "desc": "Finish top 3 on the Weekly Challenge." },
"dedicated":    { "name": "Dedicated",     "desc": "Play on 7 different days." }
```

E no bloco `trophies`:

```json
"progress": "{{unlocked}} / {{total}} unlocked"
```

- [ ] **Step 4: Traduzir para os 9 idiomas restantes**

`pt-BR`, `es`, `fr`, `de`, `it`, `ja`, `ko`, `zh`, `hi`. Regras:
- traduza o **sentido**, não a letra; nomes de troféu podem ser adaptados culturalmente;
- os números dos limiares são os mesmos em todos os idiomas (use a separação de milhar da língua);
- em `desc`, "coins" segue o vocabulário de moedas da 10.4 — nunca "comida"/"food";
- `trophies.progress` mantém as duas interpolações exatamente como `{{unlocked}}` e `{{total}}`;
- a ordem das chaves dentro de `trophy` deve ser a mesma em todos os arquivos (facilita o diff).

- [ ] **Step 5: Rodar as guardas de i18n**

Run: `npx vitest run tests/i18n/`
Expected: PASS — paridade, `no-food-keys`, `no-hardcoded-strings` e o teste novo.

- [ ] **Step 6: Commit**

```bash
git add src/i18n/locales/ tests/i18n/trophy-keys.test.ts
git commit -m "feat(10.7): 15 pares nome/descrição de troféu nos 10 idiomas"
```

---

## Task 7: Tela de Troféus com 23 cards

**Files:**
- Modify: `src/app/screens/TrophiesScreen.tsx`
- Test: `tests/app/trophies-screen.test.tsx`

**Interfaces:**
- Consumes: `trophyService.unlockedIds`, `TROPHY_CATALOG` (23), chave `trophies.progress`.
- Produces: elemento `[data-testid="trophies-progress"]`.

- [ ] **Step 1: Escrever o teste que falha**

Em `tests/app/trophies-screen.test.tsx`, primeiro atualizar as chamadas de `recordMatch` para o
`MatchSummary` completo (mesmo helper `match(...)` da Task 5), e acrescentar:

```ts
it('mostra o progresso desbloqueados/total', async () => {
  trophyService.recordMatch(match({}));
  await Promise.resolve();
  render(<TrophiesScreen />, container);
  const el = container.querySelector('[data-testid="trophies-progress"]');
  expect(el).not.toBeNull();
  expect(el?.textContent).toContain(String(TROPHY_CATALOG.length));
  expect(el?.textContent).toContain('1');
});

it('renderiza os 23 cards do catálogo ampliado', () => {
  render(<TrophiesScreen />, container);
  expect(container.querySelectorAll('[data-testid^="trophy-card-"]').length).toBe(23);
});
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `npx vitest run tests/app/trophies-screen.test.tsx`
Expected: FAIL — `trophies-progress` não existe.

- [ ] **Step 3: Implementar**

Em `src/app/screens/TrophiesScreen.tsx`, dentro de `TrophiesScreen`, entre o `<h1>` e a mensagem
vazia:

```tsx
      <p class="trophies__progress" data-testid="trophies-progress">
        {i18n.t('trophies.progress', { unlocked: unlocked.length, total: TROPHY_CATALOG.length })}
      </p>
```

Em `src/app/styles/global.css`, junto de `.trophies__empty`:

```css
.trophies__progress {
  color: var(--color-text-muted);
  font-size: var(--font-size-sm);
  margin: 0;
}
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `npx vitest run tests/app/trophies-screen.test.tsx`
Expected: PASS.

- [ ] **Step 5: Validar o layout POR NÚMERO no `dist`**

`happy-dom` não faz layout — a asserção de que 23 cards cabem/rolam tem de vir do browser real.

```bash
npm run build && npx vite preview --port 4173 &
```

Com o Playwright MCP, navegar para `http://localhost:4173/?nocache=<timestamp>`, **desregistrar o
Service Worker e limpar os caches** antes de medir (o SW serve `dist` velho — gotcha recorrente):

```js
await navigator.serviceWorker.getRegistrations().then(rs => Promise.all(rs.map(r => r.unregister())));
await caches.keys().then(ks => Promise.all(ks.map(k => caches.delete(k))));
```

Navegar até a tela de Troféus e medir, em **740×360** e **390×844**:

```js
({
  cards: document.querySelectorAll('[data-testid^="trophy-card-"]').length,
  zeroHeight: [...document.querySelectorAll('[data-testid^="trophy-card-"]')]
    .filter(e => e.getBoundingClientRect().height === 0).length,
  overflowX: document.documentElement.scrollWidth - document.documentElement.clientWidth,
  scrollable: document.documentElement.scrollHeight > document.documentElement.clientHeight,
})
```

Aceite: `cards === 23`, `zeroHeight === 0`, `overflowX <= 0`, e a tela alcança o último card
(rolagem vertical disponível quando `scrollable`). Registrar os números medidos no arquivo da fase.

- [ ] **Step 6: Commit**

```bash
git add src/app/screens/TrophiesScreen.tsx src/app/styles/global.css \
        tests/app/trophies-screen.test.tsx
git commit -m "feat(10.7): tela de Troféus mostra progresso e aguenta 23 cards"
```

---

## Verificação final (antes do PR)

- [ ] `npm test` — **suíte inteira**, não só os arquivos-alvo (precedente 9.8: uma task que roda só
      seus arquivos esconde quebras; `tests/render/atlas.test.ts` pode estourar timeout na suíte
      cheia e passar isolado — isso **não** é regressão).
- [ ] `npm run check` limpo.
- [ ] `npm run test:determinism` — **73** testes, inalterado. `git diff --stat main -- src/core/`
      tem de vir **vazio**.
- [ ] `docs/roadmap/PHASE-10-polish-and-portrait.md`: marcar os 7 sub-itens do 10.7 e registrar as
      decisões (limiares, migração sem bump de chave, contador fora do core, schema central já
      compatível).
- [ ] `CLAUDE.md` "Estado atual": contagem de testes nova, 10.7 ✅, próximo item.
- [ ] PR para `main` com merge automático (pré-autorizado pelo CLAUDE.md).
