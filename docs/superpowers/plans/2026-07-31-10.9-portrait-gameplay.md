# 10.9 — Gameplay em retrato — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tornar o JurassicRun jogável e apresentável em retrato — sem scrim de "gire o aparelho", sem barra preta, com HUD/chips ancorados na faixa de jogo — e re-resolver a resolução de render quando o aparelho girar, sem reiniciar a partida.

**Architecture:** O campo lógico 320×180 e o `Scale.FIT` do Phaser ficam intocados: em retrato o canvas já encaixa como faixa 16:9 centrada. O trabalho é (1) apresentação, resolvida em CSS ancorado numa expressão fechada da altura da faixa (`calc(100vw * 9/16)`), e (2) uma função pura `shouldRescale` + um método `applyRenderScale` na `GameScene`, ligados ao evento `resize` do `ScaleManager`.

**Tech Stack:** TypeScript estrito, Preact + signals, Phaser 3, Vitest (ambiente `node` por default, `happy-dom` por arquivo), i18next (10 locales), CSS puro em `src/app/styles/global.css`.

## Global Constraints

- `src/core/` é **INTOCADO** nesta fase inteira. Determinismo permanece em **73** testes, sem re-pin de golden. Se algum passo derivar para o core, PARE e reporte.
- Campo lógico **320×180 unidades de mundo** é imutável. Retrato muda a apresentação, nunca o mundo.
- Nenhuma string visível hardcoded: tudo via chave i18next, presente nos **10** locales (`en, pt-BR, es, fr, de, it, zh, ja, ko, hi`).
- Sem alocação por frame no hot path (REGRA 3). Nada de trabalho síncrono pesado no loop.
- TypeScript estrito, sem `any` sem justificativa. `src/core/` não importa de phaser/preact/DOM (irrelevante aqui — nada deste plano toca core).
- Um **commit por task**, na branch `feat/10.9-portrait-gameplay`. Commite **só os arquivos da task** (nunca `git commit -am`).
- Comandos: `npm test` (suíte toda), `npm run check` (lint+typecheck), `npx vitest run <arquivo>` (um arquivo).
- Spec de referência: `docs/superpowers/specs/2026-07-31-10.9-portrait-gameplay-design.md`.

---

### Task 1: Remover a dica de girar o aparelho

Hoje, todo celular em retrato joga a partida inteira debaixo de um scrim escuro pedindo para girar
(`rotate-hint`, `pointer-events: none` — nunca bloqueou o toque, só escureceu). Como o jogo passa a
ser suportado em retrato, o aviso inteiro sai do produto: componente, hook, módulo puro, CSS e chave
i18n nos 10 idiomas.

**Files:**
- Delete: `src/render/orientation.ts`
- Delete: `src/render/orientation.test.ts`
- Delete: `src/app/hooks/useRotateHint.ts`
- Modify: `src/app/screens/PlayScreen.tsx` (remover import da linha 4, `const suggestRotate` da linha 27 e o bloco JSX das linhas 113-120)
- Modify: `src/app/styles/global.css` (remover o bloco de comentário + regras `.rotate-hint`, `.rotate-hint__icon`, `.rotate-hint__text`, linhas 159-186)
- Modify: `src/i18n/locales/{en,pt-BR,es,fr,de,it,zh,ja,ko,hi}.json` (remover o objeto `"rotateHint"`)
- Test: `tests/app/game/play-screen.test.tsx` (criar)

**Interfaces:**
- Consumes: nada de tasks anteriores.
- Produces: `tests/app/game/play-screen.test.tsx` com o helper de mock de `startGame` reutilizado pela Task 4.

- [ ] **Step 1: Escrever o teste que falha**

Criar `tests/app/game/play-screen.test.tsx`. O `PlayScreen` faz `import('../game/startGame')` dinâmico
dentro de um `useLayoutEffect`, o que carregaria o Phaser inteiro num teste de DOM; por isso o módulo
é mockado pelo caminho **resolvido** (é assim que o Vitest casa mocks de import dinâmico).

```tsx
// @vitest-environment happy-dom
import { describe, it, expect, beforeEach, vi } from 'vitest';
import { render } from 'preact';
import { PlayScreen } from '../../../src/app/screens/PlayScreen';
import { i18n } from '@services/i18n';
import type { GameHandle } from '../../../src/app/game/startGame';

// Sem isto o teste carrega o Phaser (canvas/WebGL) no happy-dom.
const handle: GameHandle = {
  stop: () => {},
  snapshot: () => ({ phase: 'ready', paused: false, gameOver: null, dying: false }),
  hud: () => null,
  restart: () => {},
};
vi.mock('../../../src/app/game/startGame', () => ({ startGame: () => handle }));

describe('PlayScreen', () => {
  let host: HTMLElement;

  beforeEach(async () => {
    await i18n.init();
    host = document.createElement('div');
    document.body.appendChild(host);
  });

  it('nunca renderiza a dica de girar o aparelho (o jogo roda em retrato)', () => {
    render(<PlayScreen />, host);
    expect(host.querySelector('.rotate-hint')).toBeNull();
  });

  it('não deixa resíduo da chave i18n do aviso de girar', () => {
    // i18next devolve a própria chave quando ela não existe.
    expect(i18n.t('rotateHint.message')).toBe('rotateHint.message');
  });
});
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run: `npx vitest run tests/app/game/play-screen.test.tsx`
Expected: FAIL — o segundo caso falha porque `rotateHint.message` ainda traduz para uma frase real.
(O primeiro caso pode passar já, porque em `happy-dom` `matchMedia` normalmente não reporta
`pointer: coarse`; isso é esperado e não invalida a guarda.)

- [ ] **Step 3: Remover o aviso do produto**

Apagar os três arquivos:

```bash
git rm src/render/orientation.ts src/render/orientation.test.ts src/app/hooks/useRotateHint.ts
```

Em `src/app/screens/PlayScreen.tsx`, remover a linha de import:

```tsx
import { useRotateHint } from '../hooks/useRotateHint';
```

remover a linha do corpo do componente:

```tsx
  const suggestRotate = useRotateHint();
```

e remover o bloco JSX inteiro (era o último filho antes do `</div>` de fecho):

```tsx
      {suggestRotate && (
        <div class="rotate-hint" aria-live="polite">
          <span class="rotate-hint__icon" aria-hidden="true">
            📱↻
          </span>
          <p class="rotate-hint__text">{i18n.t('rotateHint.message')}</p>
        </div>
      )}
```

Em `src/app/styles/global.css`, apagar o comentário `/* Dica de girar (7.2)… */` e os três blocos
`.rotate-hint`, `.rotate-hint__icon`, `.rotate-hint__text`.

Nos 10 arquivos de `src/i18n/locales/`, apagar o objeto `"rotateHint": { "message": "…" }` (fica
entre `"redeem"` e `"pause"` em todos eles). Preservar a vírgula/JSON válido.

- [ ] **Step 4: Rodar os testes e ver passar**

Run: `npx vitest run tests/app/game/play-screen.test.tsx tests/i18n`
Expected: PASS — inclusive a paridade dos 10 locales (a chave sumiu de todos, então continuam
idênticos em forma).

- [ ] **Step 5: `check` limpo**

Run: `npm run check`
Expected: sem erro. Se acusar `i18n` importado e não usado no `PlayScreen.tsx`, verificar: ele
**continua** sendo usado por `i18n.t('nav.back')`, então o import fica.

- [ ] **Step 6: Commit**

```bash
git add -A src/render src/app/hooks src/app/screens/PlayScreen.tsx src/app/styles/global.css src/i18n/locales tests/app/game/play-screen.test.tsx
git commit -m "feat(10.9): remove a dica de girar o aparelho"
```

---

### Task 2: Letterbox temático — o espaço livre deixa de ser barra preta

O `.play-screen` pinta o vão com `var(--color-bg)` chapado (num 390×844 isso é 74% da tela). Ele passa
a usar **a mesma arte de fundo do pack ativo**, que já está publicada como custom property
`--bg-screen` por `src/app/theme.ts:16` e trocada ao vivo quando a expansão ativa muda. Mesma arte em
zoom diferente ⇒ o vão lê como continuação da cena, não como moldura.

**Files:**
- Modify: `src/app/styles/global.css:133-149` (bloco `.play-screen` e `.play-screen__canvas`)
- Test: `tests/app/play-screen-css.test.ts` (criar)

**Interfaces:**
- Consumes: a custom property `--bg-screen` (já existente; não criar nada).
- Produces: `tests/app/play-screen-css.test.ts` com os helpers `stylesheet()` e `cssBlocks()`, estendido pela Task 3.

- [ ] **Step 1: Escrever o teste que falha**

Criar `tests/app/play-screen-css.test.ts`. Ambiente **node** de propósito: `happy-dom` **não faz
layout** (`getBoundingClientRect()` devolve zeros), então um teste de renderização não enxergaria
nada disto — mesma lição da 10.5 (`tests/app/screen-root-width.test.ts`).

```ts
// Ambiente node (default do projeto): lê o CSS-fonte. `happy-dom` não faz layout, então nada
// disto seria observável num teste de renderização (precedente 10.5).
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { fileURLToPath } from 'node:url';

const CSS_PATH = fileURLToPath(new URL('../../src/app/styles/global.css', import.meta.url));

function stylesheet(): string {
  return readFileSync(CSS_PATH, 'utf-8').replace(/\/\*[\s\S]*?\*\//g, '');
}

/** Blocos `seletor { declarações }`. Declarações sem `{` ⇒ cabeçalhos `@media` não viram bloco. */
function cssBlocks(css: string): Array<{ selector: string; body: string }> {
  const blocks: Array<{ selector: string; body: string }> = [];
  for (const match of css.matchAll(/([^{}]+)\{([^{}]*)\}/g)) {
    blocks.push({ selector: (match[1] ?? '').trim(), body: match[2] ?? '' });
  }
  return blocks;
}

function bodyOf(selector: string): string {
  const found = cssBlocks(stylesheet()).filter((b) => b.selector === selector);
  expect(found.length, `bloco não encontrado: ${selector}`).toBeGreaterThan(0);
  return found.map((b) => b.body).join('\n');
}

describe('letterbox do PlayScreen não é barra preta', () => {
  it('sanidade do extrator: acha o bloco .play-screen', () => {
    expect(cssBlocks(stylesheet()).some((b) => b.selector === '.play-screen')).toBe(true);
  });

  it('.play-screen pinta o vão com a arte de fundo do pack ativo', () => {
    expect(bodyOf('.play-screen')).toMatch(/var\(--bg-screen\)/);
  });

  it('a camada de escurecimento fica ATRÁS do canvas (senão apaga o jogo)', () => {
    expect(bodyOf('.play-screen::before')).toMatch(/z-index\s*:\s*0/);
    expect(bodyOf('.play-screen__canvas')).toMatch(/z-index\s*:\s*1/);
  });

  it('não sobrou nenhuma regra da dica de girar', () => {
    expect(stylesheet()).not.toMatch(/\.rotate-hint/);
  });
});
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run: `npx vitest run tests/app/play-screen-css.test.ts`
Expected: FAIL — "bloco não encontrado: .play-screen::before" e `.play-screen` sem `var(--bg-screen)`.

- [ ] **Step 3: Implementar**

Em `src/app/styles/global.css`, substituir o bloco `.play-screen` e `.play-screen__canvas` por:

```css
/* 10.9: o vão do letterbox (em retrato, a maior parte da tela) recebe a MESMA arte de fundo do
   pack ativo que o jogo usa como backdrop, em zoom diferente — lê como continuação da cena, não
   como moldura. `--bg-screen` já é publicada e trocada ao vivo por src/app/theme.ts. */
.play-screen {
  position: relative;
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 0;
  background-color: var(--color-bg); /* fallback antes de a arte carregar */
  background-image: var(--bg-screen);
  background-position: center;
  background-size: cover;
  background-repeat: no-repeat;
}

/* Escurecimento do vão: mantém a faixa jogável como foco sem apagar a arte. Camada ESTÁTICA
   (nunca repintada por frame) e atrás do canvas. */
.play-screen::before {
  content: '';
  position: absolute;
  inset: 0;
  z-index: 0;
  background: linear-gradient(
    to bottom,
    rgba(0, 0, 0, 0.72),
    rgba(0, 0, 0, 0.55) 35%,
    rgba(0, 0, 0, 0.55) 65%,
    rgba(0, 0, 0, 0.72)
  );
  pointer-events: none;
}

/* Container do canvas Phaser: caixa de dimensões DEFINIDAS (absolute inset:0) para o
   Scale.FIT medir o espaço real e escalar/letterbox corretamente. Sem flex-center aqui —
   o autoCenter:CENTER_BOTH do Phaser é quem centraliza o canvas (evita dupla centralização). */
.play-screen__canvas {
  position: absolute;
  inset: 0;
  z-index: 1; /* acima do escurecimento do vão */
}
```

- [ ] **Step 4: Rodar o teste e ver passar**

Run: `npx vitest run tests/app/play-screen-css.test.ts`
Expected: PASS (4 casos).

- [ ] **Step 5: Commit**

```bash
git add src/app/styles/global.css tests/app/play-screen-css.test.ts
git commit -m "feat(10.9): letterbox usa a arte do pack ativo, não barra chapada"
```

---

### Task 3: Chrome de retrato — HUD e chips ancorados na faixa

Em retrato, `.hud` (canto superior direito) e `.effect-badges` (canto inferior esquerdo) estão presos
aos cantos do **container**, ou seja ~300 px de distância da ação num 390×844. Eles passam a encostar
nas bordas da faixa jogável.

A altura da faixa é uma **expressão fechada**: em qualquer viewport retrato a largura é o eixo
limitante do `Scale.FIT` (todo retrato é mais alto que 16:9), logo `--field-h: calc(100vw * 9 / 16)`.
Como o `autoCenter: CENTER_BOTH` centraliza o canvas dentro do container `inset: 0`, as bordas da
faixa ficam exatamente em `50% ∓ var(--field-h) / 2`. Nada de JS, nada de Phaser (precedente 10.5:
o fix é 100% CSS).

**Files:**
- Modify: `src/app/styles/global.css` (acrescentar um bloco `@media (orientation: portrait)` logo após as regras do `.play-screen`)
- Test: `tests/app/play-screen-css.test.ts` (estender)

**Interfaces:**
- Consumes: helpers `stylesheet()`, `cssBlocks()`, `bodyOf()` da Task 2.
- Produces: a custom property `--field-h` no escopo `.play-screen` em retrato, usada pela Task 4.

- [ ] **Step 1: Escrever o teste que falha**

Acrescentar em `tests/app/play-screen-css.test.ts`, ANTES do `describe` existente, o helper que isola
o conteúdo do `@media`:

```ts
/** Corpo do bloco `@media (orientation: portrait) { … }` (um nível de aninhamento). */
function portraitMedia(): string {
  const css = stylesheet();
  const start = css.indexOf('@media (orientation: portrait)');
  expect(start, 'não há @media (orientation: portrait)').toBeGreaterThanOrEqual(0);
  const open = css.indexOf('{', start);
  let depth = 0;
  for (let i = open; i < css.length; i += 1) {
    if (css[i] === '{') depth += 1;
    else if (css[i] === '}') {
      depth -= 1;
      if (depth === 0) return css.slice(open + 1, i);
    }
  }
  throw new Error('@media (orientation: portrait) sem fechamento');
}
```

e um `describe` novo:

```ts
describe('chrome de retrato ancora na faixa de jogo', () => {
  it('a altura da faixa é derivada da largura (o FIT é limitado pela largura em retrato)', () => {
    expect(portraitMedia()).toMatch(/--field-h\s*:\s*calc\(\s*100vw\s*\*\s*9\s*\/\s*16\s*\)/);
  });

  it('o HUD desancora do topo da tela e encosta acima da faixa', () => {
    const blocks = cssBlocks(portraitMedia());
    const hud = blocks.filter((b) => b.selector === '.hud').map((b) => b.body).join('\n');
    expect(hud, 'sem regra .hud em retrato').not.toBe('');
    expect(hud).toMatch(/top\s*:\s*auto/);
    expect(hud).toMatch(/bottom\s*:\s*calc\(/);
    expect(hud).toMatch(/var\(--field-h\)/);
  });

  it('os chips de power-up desancoram do rodapé e encostam abaixo da faixa', () => {
    const blocks = cssBlocks(portraitMedia());
    const badges = blocks
      .filter((b) => b.selector === '.effect-badges')
      .map((b) => b.body)
      .join('\n');
    expect(badges, 'sem regra .effect-badges em retrato').not.toBe('');
    expect(badges).toMatch(/bottom\s*:\s*auto/);
    expect(badges).toMatch(/top\s*:\s*calc\(/);
    expect(badges).toMatch(/var\(--field-h\)/);
  });
});
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run: `npx vitest run tests/app/play-screen-css.test.ts`
Expected: FAIL — "não há @media (orientation: portrait)".

- [ ] **Step 3: Implementar**

Acrescentar em `src/app/styles/global.css`, logo depois do bloco `.play-screen__canvas`:

```css
/* 10.9 — chrome de retrato. Em qualquer viewport retrato a LARGURA é o eixo limitante do
   Scale.FIT (todo retrato é mais alto que 16:9), então a altura da faixa jogável é fechada:
   largura × 9/16. O autoCenter:CENTER_BOTH do Phaser centra o canvas no container `inset: 0`,
   logo as bordas da faixa estão em 50% ∓ --field-h/2. Daí HUD e chips ancoram NA FAIXA, em vez
   de nos cantos da tela (onde ficariam a ~300 px da ação num 390×844). */
@media (orientation: portrait) {
  .play-screen {
    --field-h: calc(100vw * 9 / 16);
    --field-gap: var(--space-3);
  }

  /* HUD logo ACIMA da faixa (continua alinhado à direita). */
  .hud {
    top: auto;
    bottom: calc(50% + var(--field-h) / 2 + var(--field-gap));
  }

  /* Chips de power-up (9.5) logo ABAIXO da faixa. */
  .effect-badges {
    bottom: auto;
    top: calc(50% + var(--field-h) / 2 + var(--field-gap));
  }
}
```

- [ ] **Step 4: Rodar o teste e ver passar**

Run: `npx vitest run tests/app/play-screen-css.test.ts`
Expected: PASS (7 casos no arquivo).

- [ ] **Step 5: Garantir que paisagem não regrediu**

Run: `npx vitest run tests/app`
Expected: PASS. As regras novas vivem dentro do `@media`, então nada muda em paisagem.

- [ ] **Step 6: Commit**

```bash
git add src/app/styles/global.css tests/app/play-screen-css.test.ts
git commit -m "feat(10.9): HUD e chips ancoram na faixa de jogo em retrato"
```

---

### Task 4: Zona de toque na banda inferior

O flap já funciona em qualquer ponto da tela — `bindGameControls` está ligado ao **`window`**
(`src/app/game/startGame.ts:179`), não ao canvas. Falta o usuário **saber** disso em retrato, onde
existe uma banda inferior grande e vazia. Entra um painel decorativo (`pointer-events: none` — quem
trata o toque continua sendo o listener de `window`), visível só em retrato e só quando a partida
está em curso ou pronta para começar.

**Files:**
- Modify: `src/app/screens/PlayScreen.tsx`
- Modify: `src/app/styles/global.css` (regra base + regra dentro do `@media (orientation: portrait)` da Task 3)
- Modify: `src/i18n/locales/{en,pt-BR,es,fr,de,it,zh,ja,ko,hi}.json` (chave nova `match.tapArea`)
- Test: `tests/app/game/play-screen.test.tsx` (estender)

**Interfaces:**
- Consumes: `--field-h` / `--field-gap` (Task 3); o mock de `startGame` da Task 1.
- Produces: elemento `.play-screen__tap` e chave i18n `match.tapArea`.

- [ ] **Step 1: Escrever o teste que falha**

Acrescentar em `tests/app/game/play-screen.test.tsx`, dentro do `describe('PlayScreen')`:

```tsx
  it('mostra a zona de toque enquanto a partida está em curso', () => {
    render(<PlayScreen />, host);
    const zone = host.querySelector('.play-screen__tap');
    expect(zone).not.toBeNull();
    // Decorativa: quem trata o toque é o listener de `window` de bindGameControls.
    expect(zone?.getAttribute('aria-hidden')).toBe('true');
    expect(zone?.textContent).toBe(i18n.t('match.tapArea'));
    expect(i18n.t('match.tapArea')).not.toBe('match.tapArea'); // chave existe de fato
  });
```

E um `describe` novo no mesmo arquivo, com um mock diferente (fase `dead`):

```tsx
describe('PlayScreen em Game Over', () => {
  it('esconde a zona de toque quando a partida acabou', async () => {
    await i18n.init();
    const host = document.createElement('div');
    document.body.appendChild(host);
    // A zona só some quando o snapshot vira `dead`; o componente lê o snapshot no rAF, então
    // aqui a asserção é sobre a REGRA de renderização, exercitada pelo helper puro.
    expect(showTapZone({ phase: 'dead', paused: false, gameOver: null, dying: false })).toBe(false);
    expect(showTapZone({ phase: 'playing', paused: true, gameOver: null, dying: false })).toBe(false);
    expect(showTapZone({ phase: 'playing', paused: false, gameOver: null, dying: false })).toBe(true);
    expect(showTapZone({ phase: 'ready', paused: false, gameOver: null, dying: false })).toBe(true);
    render(null, host);
  });
});
```

e o import correspondente no topo do arquivo:

```tsx
import { PlayScreen, showTapZone } from '../../../src/app/screens/PlayScreen';
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run: `npx vitest run tests/app/game/play-screen.test.tsx`
Expected: FAIL — `showTapZone` não existe e `.play-screen__tap` não é renderizado.

- [ ] **Step 3: Implementar o predicado puro e o elemento**

Em `src/app/screens/PlayScreen.tsx`, exportar o predicado (regra de renderização testável sem rAF):

```tsx
/**
 * A zona de toque é uma AFORDÂNCIA: o flap já funciona em qualquer ponto porque o input está
 * ligado ao `window` (bindGameControls). Ela aparece enquanto a partida está viva e some quando
 * algum overlay assume a tela (pausa / Game Over).
 */
export function showTapZone(snap: MatchSnapshot): boolean {
  return !snap.paused && snap.phase !== 'dead';
}
```

e renderizar o elemento logo depois do `<div class="play-screen__canvas" …/>`:

```tsx
      {showTapZone(snap) && (
        <div class="play-screen__tap" aria-hidden="true">
          {i18n.t('match.tapArea')}
        </div>
      )}
```

- [ ] **Step 4: Estilo**

Em `src/app/styles/global.css`, acrescentar a regra base (fora do `@media`), logo após
`.play-screen__canvas`:

```css
/* 10.9: afordância da zona de toque — decorativa. O flap é tratado pelo listener de `window`
   (bindGameControls), então ela NUNCA pode capturar ponteiro. Só existe em retrato. */
.play-screen__tap {
  display: none;
}
```

e, DENTRO do bloco `@media (orientation: portrait)` criado na Task 3:

```css
  .play-screen__tap {
    display: flex;
    position: absolute;
    left: max(var(--space-3), env(safe-area-inset-left));
    right: max(var(--space-3), env(safe-area-inset-right));
    top: calc(50% + var(--field-h) / 2 + var(--space-8));
    bottom: max(var(--space-3), env(safe-area-inset-bottom));
    z-index: 11;
    align-items: center;
    justify-content: center;
    padding: var(--space-3);
    border: 1px dashed color-mix(in srgb, var(--color-text) 25%, transparent);
    border-radius: var(--radius-md);
    background: color-mix(in srgb, var(--color-bg) 25%, transparent);
    color: var(--color-text);
    font-size: var(--font-size-sm);
    text-align: center;
    opacity: 0.75;
    pointer-events: none; /* CRÍTICO: o toque tem de chegar ao listener de `window` */
  }
```

Verificar antes de usar que os tokens `--space-8` e `--radius-md` existem em
`src/app/styles/tokens.css`; se algum não existir, usar o maior/mais próximo que exista (ex.:
`--space-6`, `--radius-sm`) em vez de inventar token novo.

- [ ] **Step 5: i18n nos 10 idiomas**

Acrescentar a chave `tapArea` dentro do objeto `match` (que hoje só tem `tapToStart`) nos 10
arquivos de `src/i18n/locales/`:

| Arquivo | Valor |
|---|---|
| `en.json` | `"tapArea": "Tap anywhere to flap"` |
| `pt-BR.json` | `"tapArea": "Toque em qualquer lugar para bater as asas"` |
| `es.json` | `"tapArea": "Toca en cualquier lugar para aletear"` |
| `fr.json` | `"tapArea": "Touchez n'importe où pour battre des ailes"` |
| `de.json` | `"tapArea": "Tippe irgendwo, um zu flattern"` |
| `it.json` | `"tapArea": "Tocca ovunque per battere le ali"` |
| `zh.json` | `"tapArea": "点击任意位置扇动翅膀"` |
| `ja.json` | `"tapArea": "画面のどこでもタップで羽ばたく"` |
| `ko.json` | `"tapArea": "아무 곳이나 탭하여 날갯짓"` |
| `hi.json` | `"tapArea": "पंख फड़फड़ाने के लिए कहीं भी टैप करें"` |

- [ ] **Step 6: Rodar os testes e ver passar**

Run: `npx vitest run tests/app/game/play-screen.test.tsx tests/i18n`
Expected: PASS — paridade dos 10 locales verde (a chave entrou em todos) e o scanner AST não acusa
string hardcoded.

- [ ] **Step 7: Commit**

```bash
git add src/app/screens/PlayScreen.tsx src/app/styles/global.css src/i18n/locales tests/app/game/play-screen.test.tsx
git commit -m "feat(10.9): zona de toque na banda inferior em retrato"
```

---

### Task 5: Re-resolver a resolução de render ao girar, sem reiniciar a partida

`resolveRenderScale` é chamado **uma vez**, na criação do jogo (`src/render/game.ts:34`). Girar o
aparelho mantém o framebuffer antigo e o `Scale.FIT` passa a esticá-lo por CSS: num 390×844 com
dpr 3, girar para paisagem estica um canvas de 1171 px para ~2532 px de dispositivo — **upscale de
2,16×**, que é exatamente o bug W5 que a Fase 8 corrigiu, voltando pela porta da rotação.

A partida em si já sobrevive à rotação por construção: o `useLayoutEffect` do `PlayScreen` depende só
de `[mode]`, então nada remonta. O que falta é re-resolver a escala.

**Files:**
- Modify: `src/render/resolution.ts` (nova função pura `shouldRescale`)
- Modify: `src/render/resolution.test.ts` (casos da função nova)
- Modify: `src/render/GameScene.ts` (campo `renderScale` deixa de ser `readonly`; novo `applyRenderScale`; assinatura do evento `resize` no fim de `create()`)
- Modify: `src/render/constants.ts` (constante da margem)

**Interfaces:**
- Consumes: `resolveRenderScale(cssWidth, cssHeight, dpr): number` e `renderCanvasSize(scale): {width, height}` (já existentes).
- Produces: `shouldRescale(current: number, next: number): boolean` e `GameScene.applyRenderScale(scale: number): void`.

- [ ] **Step 1: Escrever o teste que falha**

Acrescentar em `src/render/resolution.test.ts`:

```ts
import { shouldRescale } from './resolution';
import { RESCALE_EPSILON } from './constants';

describe('shouldRescale', () => {
  it('escala idêntica não redimensiona (evita recursão com o próprio resize)', () => {
    expect(shouldRescale(3.66, 3.66)).toBe(false);
  });

  it('variação abaixo da margem não redimensiona (a rotação dispara uma rajada de resizes)', () => {
    const next = 4 * (1 + RESCALE_EPSILON / 2);
    expect(shouldRescale(4, next)).toBe(false);
  });

  it('variação acima da margem redimensiona', () => {
    expect(shouldRescale(3.66, 6)).toBe(true);
    expect(shouldRescale(6, 3.66)).toBe(true);
  });

  it('entradas não finitas ou não positivas nunca redimensionam', () => {
    expect(shouldRescale(4, Number.NaN)).toBe(false);
    expect(shouldRescale(Number.NaN, 4)).toBe(false);
    expect(shouldRescale(4, 0)).toBe(false);
    expect(shouldRescale(0, 4)).toBe(false);
  });
});
```

- [ ] **Step 2: Rodar o teste e ver falhar**

Run: `npx vitest run src/render/resolution.test.ts`
Expected: FAIL — `shouldRescale`/`RESCALE_EPSILON` não existem.

- [ ] **Step 3: Implementar a decisão pura**

Em `src/render/constants.ts`, acrescentar junto de `MIN_RENDER_SCALE`/`MAX_RENDER_SCALE`:

```ts
/** Margem relativa para re-resolver a escala de render (10.9). Girar o aparelho dispara uma
 *  RAJADA de eventos de resize (e o nosso próprio `scale.resize` dispara mais um): sem margem,
 *  isso vira thrash de framebuffer — ou recursão. 2% é abaixo do que o olho percebe em nitidez. */
export const RESCALE_EPSILON = 0.02;
```

Em `src/render/resolution.ts`, acrescentar `RESCALE_EPSILON` ao import de `./constants` e a função:

```ts
/**
 * Vale a pena re-resolver o framebuffer para `next`?
 *
 * Só quando a diferença relativa passa de `RESCALE_EPSILON`. Redimensionar o canvas do Phaser
 * recria texturas e é caro; e como o próprio `scale.resize` emite um evento de resize, sem esta
 * porta a rotação entraria em laço. Entradas degeneradas (container 0×0 durante o relayout da
 * rotação) nunca disparam resize.
 */
export function shouldRescale(current: number, next: number): boolean {
  if (!Number.isFinite(current) || !Number.isFinite(next)) return false;
  if (current <= 0 || next <= 0) return false;
  return Math.abs(next - current) / current > RESCALE_EPSILON;
}
```

- [ ] **Step 4: Rodar o teste e ver passar**

Run: `npx vitest run src/render/resolution.test.ts`
Expected: PASS.

- [ ] **Step 5: Casca — `applyRenderScale` na `GameScene`**

Em `src/render/GameScene.ts`:

1. trocar a declaração do campo (linha ~125) de `private readonly renderScale: number;` para:

```ts
  /** Px de render por unidade de mundo (W5). Re-resolvido na rotação (10.9); ver resolution.ts. */
  private renderScale: number;
```

2. guardar as referências que `applyRenderScale` precisa re-tocar. As camadas de parallax já vivem
   em `this.parallaxTiles`; o backdrop em `this.bgImage`. Nenhum campo novo é necessário.

3. acrescentar o método (depois de `create()`, antes de `update()`):

```ts
  /**
   * Re-aplica a escala de render aos objetos ESTÁTICOS criados em `create()`. Tudo o que é
   * desenhado por frame (`drawFrame`) lê `this.renderScale` e se corrige sozinho no frame
   * seguinte — por isso aqui só entram os objetos posicionados/dimensionados uma vez.
   *
   * Chamado quando o aparelho gira: a partida, o `WorldState` e o passo fixo NÃO são tocados.
   */
  applyRenderScale(scale: number): void {
    this.renderScale = scale;

    this.bgImage.setDisplaySize(this.px(VIEW_WIDTH), this.px(VIEW_HEIGHT));

    this.parallaxTiles.forEach((tile, index) => {
      const v = PARALLAX_LAYERS[index]!.visual;
      const y = v.kind === 'sprite' ? VIEW_HEIGHT - v.baseFromBottom - v.dispHeight : 0;
      const h = v.kind === 'sprite' ? v.dispHeight : VIEW_HEIGHT;
      tile.setPosition(0, this.px(y));
      tile.setSize(this.px(VIEW_WIDTH), this.px(h));
      const texWidth = this.textures.get(tile.texture.key).getSourceImage().width;
      const tileScale = parallaxTileScale(texWidth, scale);
      tile.setTileScale(tileScale, tileScale);
    });

    // Graphics desenham em unidades de mundo; a ESCALA do objeto converte para px de render.
    this.bandsGfx.setScale(scale);
    this.gfx.setScale(scale);
    this.pauseOverlay.setScale(scale);
    this.gameOverBg.setScale(scale);

    // Textos legados in-canvas (invisíveis com domOverlays, mas mantidos corretos).
    this.hudText.setScale(scale).setPosition(this.px(HUD_TEXT_X), this.px(HUD_TEXT_Y));
    this.readyPrompt.setScale(scale).setPosition(this.px(VIEW_WIDTH / 2), this.px(VIEW_HEIGHT / 2));
    this.gameOverTitle.setScale(scale).setPosition(this.px(VIEW_WIDTH / 2), this.px(36));
    this.gameOverStats.setScale(scale).setPosition(this.px(VIEW_WIDTH / 2), this.px(78));
    this.gameOverRestart.setScale(scale).setPosition(this.px(VIEW_WIDTH / 2 - 44), this.px(130));
    this.gameOverQuit.setScale(scale).setPosition(this.px(VIEW_WIDTH / 2 + 44), this.px(130));
  }
```

4. no FIM de `create()`, ligar ao `ScaleManager` (o Phaser remove estes listeners ao destruir a cena
   — nenhum ciclo de vida novo em `startGame`):

```ts
    // 10.9: girar o aparelho re-resolve o framebuffer. Sem isto o Scale.FIT estica por CSS o
    // canvas da orientação anterior (upscale de 2,16× medido num 390×844 dpr 3) — a volta do
    // bug W5 pela porta da rotação. A PARTIDA não é tocada: só o tamanho do canvas.
    this.scale.on('resize', () => {
      const parent = this.scale.parentSize;
      const next = resolveRenderScale(parent.width, parent.height, window.devicePixelRatio);
      if (!shouldRescale(this.renderScale, next)) return;
      const { width, height } = renderCanvasSize(next);
      this.applyRenderScale(next);
      this.scale.resize(width, height);
    });
```

5. acrescentar aos imports de `./resolution` os nomes `resolveRenderScale`, `renderCanvasSize` e
   `shouldRescale` (o arquivo já importa `toRenderPx` e `parallaxTileScale` de lá).

- [ ] **Step 6: Verificar tipos e suíte**

Run: `npm run check && npx vitest run src/render tests/render`
Expected: sem erro de tipo; testes de render verdes. Se o TS reclamar que `PARALLAX_LAYERS` ou
`HUD_TEXT_X` não estão importados no `GameScene.ts`, verificar a lista de imports do topo do arquivo
— ambos já são usados em `create()`, então devem estar lá.

- [ ] **Step 7: Determinismo (sanidade — nada aqui toca o core)**

Run: `npm run test:determinism`
Expected: **73** testes verdes, sem re-pin de golden.

- [ ] **Step 8: Commit**

```bash
git add src/render/constants.ts src/render/resolution.ts src/render/resolution.test.ts src/render/GameScene.ts
git commit -m "feat(10.9): re-resolve a resolução de render ao girar, sem reiniciar a partida"
```

---

### Task 6: Validação por número no `dist`, docs e fecho da fase

O projeto tem um gotcha recorrente: CSS/print em cache mente. Medir por `getComputedStyle` /
`getBoundingClientRect` no `dist` **servido**, com Service Worker desregistrado, caches limpos e
`?nocache`. Esta task é a única que fecha a fase 10 inteira.

**Files:**
- Modify: `docs/roadmap/PHASE-10-polish-and-portrait.md` (marcar 10.9 e registrar o executado)
- Modify: `CLAUDE.md` (campo "Estado atual" + tabela de fases)
- Modify: `package.json` (bump de `version` — regra de `docs/WORKFLOW.md`: sobe ao FECHAR uma fase)

- [ ] **Step 1: Suíte inteira e typecheck**

Run: `npm test && npm run check`
Expected: verde. Rodar a suíte **inteira**, não só os arquivos-alvo — precedente 9.8: tasks que
rodam arquivos isolados escondem quebras. Nota conhecida: `tests/render/atlas.test.ts` pode estourar
timeout na suíte cheia e passar isolado (custo, não asserção) — isso **não** é regressão.

- [ ] **Step 2: Build e servir**

```bash
npm run build && npx vite preview --port 4173
```

- [ ] **Step 3: Medir em retrato (Playwright MCP)**

Em 390×844 e depois 360×740, com o SW desregistrado + caches limpos + `?nocache`, entrar numa partida
Endless e avaliar no browser:

```js
() => {
  const play = document.querySelector('.play-screen');
  const canvas = play.querySelector('canvas');
  const hud = document.querySelector('.hud');
  const badges = document.querySelector('.effect-badges');
  const tap = document.querySelector('.play-screen__tap');
  const r = (el) => (el ? el.getBoundingClientRect() : null);
  return {
    play: r(play), canvas: r(canvas), hud: r(hud), badges: r(badges), tap: r(tap),
    bg: getComputedStyle(play).backgroundImage,
    framebuffer: canvas ? { w: canvas.width, h: canvas.height } : null,
    rotateHint: document.querySelectorAll('.rotate-hint').length,
    bodyOverflowX: document.body.scrollWidth - document.body.clientWidth,
  };
}
```

Registrar os números. Aceite:
- `canvas.width_css ≈ play.width` (±1 px) e `canvas.height_css ≈ canvas.width_css × 9/16` (±1 px);
- `hud.bottom ≤ canvas.top` e `badges.top ≥ canvas.bottom`;
- `tap.top ≥ canvas.bottom` e `tap.bottom ≤ play.bottom`;
- `bg` contém `bg.screen`;
- `rotateHint === 0`;
- `bodyOverflowX === 0`.

- [ ] **Step 4: Medir a rotação ao vivo**

Com a partida **em curso** em 390×844, ler a distância do HUD, redimensionar para 844×390, esperar
~1 s e ler de novo. Aceite:
- `canvas.width` (framebuffer) **mudou** ⇒ a escala foi re-resolvida;
- a distância **continuou crescendo** ⇒ a partida não reiniciou;
- zero erro no console.

- [ ] **Step 5: Medir fps em retrato**

Em 390×844, durante `playing`, ler o valor de fps do HUD por ~5 s. Aceite: **≥ 55**.

- [ ] **Step 6: Conferir os overlays em 360×740**

Pausa, Game Over e o prompt de início legíveis, sem estouro horizontal (`bodyOverflowX === 0`).

- [ ] **Step 7: Atualizar a documentação**

Em `docs/roadmap/PHASE-10-polish-and-portrait.md`: marcar todos os `- [ ]` do 10.9 como `- [x]`,
acrescentar o bloco `> **Executado em 2026-07-31** …` com decisões, o desvio consciente do
"parallax sangrado" e os números medidos.

Em `CLAUDE.md`: atualizar "Estado atual" (Fase 10 **CONCLUÍDA**, 9/9; métricas de testes reais medidas
no Step 1), a linha da tabela de fases e a marca de Frente D.

Em `package.json`: subir o campo `version` (fecho de fase — `docs/WORKFLOW.md` §"Versão exibida na
Home"). A Home lê esse campo em build time, então o número muda sozinho na UI.

- [ ] **Step 8: Commit**

```bash
git add docs/roadmap/PHASE-10-polish-and-portrait.md CLAUDE.md package.json
git commit -m "docs(10.9): fecha a Fase 10 — retrato jogável validado por número"
```

---

## Self-review

- **Cobertura da spec:** D1 (letterbox travado) não exige código — é o comportamento atual, preservado
  por não tocar em `Scale.FIT`; D2 → Task 2; D3 → Task 1; D4 → Task 3; D5 → Task 4; D6 → Task 5;
  §5 (testes e validação por número) → Tasks 1-5 (unitários) e Task 6 (dist).
- **Tipos:** `shouldRescale(current: number, next: number): boolean` e
  `applyRenderScale(scale: number): void` são usados na Task 5 exatamente com os nomes definidos ali;
  `showTapZone(snap: MatchSnapshot): boolean` idem na Task 4. `--field-h` é definida na Task 3 e
  consumida na Task 4.
- **Risco conhecido:** o Step 5 da Task 5 é a única mudança em `GameScene.ts` (754 linhas, o arquivo
  de maior risco). Ele não altera nenhum caminho por frame — só reposiciona objetos estáticos — e o
  gate real é a medição do Step 4 da Task 6.
