# Home (hub) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Substituir a Home provisória por um hub real: barra de topo com identidade do jogador + stats e um menu com todas as opções.

**Architecture:** Só camada de apresentação (Preact), padrão puro × casca como 4.1/4.2. Lógica pura testável em `src/app/home/` (seam de stats + ação de compartilhar injetável); componentes Preact compõem topo + menu e disparam `navigate`/`shareGame`. `src/core/` intocado ⇒ determinismo (64) inalterado.

**Tech Stack:** Preact + `@preact/signals`, i18next (`i18n.t`), Vitest + happy-dom, CSS tokens existentes.

## Global Constraints

- **REGRA 1 (determinismo):** nada em `src/app/home/` importa de `src/core/`; sem `Math.random`/`Date`/`performance.now` nos módulos puros novos. Core não é tocado.
- **REGRA 4 (i18n):** nenhuma string visível hardcoded; toda chave nova entra nos **10 locales** (`en, es, pt-BR, fr, it, de, ja, zh, ko, hi`) com paridade exata (validado por `tests/i18n/locales.test.ts`).
- **Estilo:** sem cores hardcoded — usar os design tokens de `src/app/styles/tokens.css`. Mobile-first, retrato+paisagem, **sem scroll horizontal**, alvos de toque ≥ 44px.
- **Padrão puro × casca:** id/tempo/DOM/APIs de browser só na casca; lógica pura sem IO.
- **Verificação:** `npm run check` limpo e `npm test` verde ao fim de cada task.

## File Structure

- `src/app/router/routes.ts` — MOD: `Screen` ganha `'daily' | 'weekly'`.
- `src/app/App.tsx` — MOD: casos `daily`/`weekly` → `PlaceholderScreen`.
- `src/i18n/locales/*.json` (10 arquivos) — MOD: chaves novas.
- `src/app/home/stats.ts` — CREATE: seam puro `getHomeStats()`.
- `src/app/home/share.ts` — CREATE: `shareGame(deps)` injetável + `defaultShareDeps()`.
- `src/app/screens/HomeScreen.tsx` — MOD (reescrita): compõe topo + menu.
- `src/app/styles/global.css` — MOD: classes `.home*`, `.stat-chip`.
- Testes: `tests/app/home/stats.test.ts`, `tests/app/home/share.test.ts`, `tests/app/home.test.tsx`; MOD `tests/app/App.test.tsx`.

---

### Task 1: Rotas daily/weekly + i18n completo (10 locales)

Adiciona as rotas placeholder e TODAS as strings novas, deixando o terreno pronto para os componentes. Fecha sozinha (typecheck + locales parity + App placeholder).

**Files:**
- Modify: `src/app/router/routes.ts`
- Modify: `src/app/App.tsx`
- Modify: `src/i18n/locales/en.json` (+ es, pt-BR, fr, it, de, ja, zh, ko, hi)
- Modify: `tests/app/App.test.tsx`

**Interfaces:**
- Produces: `Screen` union inclui `'daily' | 'weekly'`. Chaves i18n: `home.newGame`, `home.coins`, `home.trophies`, `home.level`, `nav.daily`, `nav.weekly`, `nav.share`, `nav.donate`, `screen.daily`, `screen.weekly`, `share.title`, `share.text`.

- [ ] **Step 1: Estender o tipo `Screen`**

Em `src/app/router/routes.ts`, adicionar as duas rotas:

```typescript
export type Screen =
  | 'home'
  | 'play'
  | 'profile'
  | 'daily'
  | 'weekly'
  | 'nest'
  | 'shop'
  | 'settings'
  | 'leaderboard'
  | 'expansions';
```

- [ ] **Step 2: Mapear daily/weekly no switch do App**

Em `src/app/App.tsx`, dentro de `screenFor`, adicionar antes do `case 'nest'`:

```tsx
    case 'daily':
      return <PlaceholderScreen titleKey="screen.daily" />;
    case 'weekly':
      return <PlaceholderScreen titleKey="screen.weekly" />;
```

- [ ] **Step 3: Adicionar as chaves em `en.json`**

Em `src/i18n/locales/en.json`: adicionar `home` bloco, estender `nav`, `screen`, e adicionar `share`:

```json
  "home": {
    "newGame": "New Game",
    "coins": "Coins",
    "trophies": "Trophies",
    "level": "Best Lv"
  },
```
`nav` recebe: `"daily": "Daily Challenge"`, `"weekly": "Weekly Challenge"`, `"share": "Share"`, `"donate": "Donate"`.
`screen` recebe: `"daily": "Daily Challenge"`, `"weekly": "Weekly Challenge"`.
Novo bloco:
```json
  "share": {
    "title": "JurassicRun",
    "text": "Play JurassicRun — a pterodactyl side-scroller!"
  },
```

- [ ] **Step 4: Replicar as MESMAS chaves nos outros 9 locales**

Traduzir os valores (paridade de chaves exata) em `es, pt-BR, fr, it, de, ja, zh, ko, hi`. Exemplo `pt-BR.json`:
```json
  "home": { "newGame": "Novo Jogo", "coins": "Moedas", "trophies": "Troféus", "level": "Nível máx" },
```
`nav`: `"daily": "Desafio Diário", "weekly": "Desafio Semanal", "share": "Compartilhar", "donate": "Doação"`.
`screen`: `"daily": "Desafio Diário", "weekly": "Desafio Semanal"`.
`share`: `{ "title": "JurassicRun", "text": "Jogue JurassicRun — um side-scroller de pterodáctilo!" }`.
Traduções sugeridas por idioma para os rótulos (`newGame` / `daily` / `weekly` / `share` / `donate`):
- es: "Nuevo Juego" / "Reto Diario" / "Reto Semanal" / "Compartir" / "Donar"
- fr: "Nouvelle Partie" / "Défi Quotidien" / "Défi Hebdomadaire" / "Partager" / "Faire un don"
- it: "Nuova Partita" / "Sfida Giornaliera" / "Sfida Settimanale" / "Condividi" / "Dona"
- de: "Neues Spiel" / "Tägliche Herausforderung" / "Wöchentliche Herausforderung" / "Teilen" / "Spenden"
- ja: "新しいゲーム" / "デイリーチャレンジ" / "ウィークリーチャレンジ" / "共有" / "寄付"
- zh: "新游戏" / "每日挑战" / "每周挑战" / "分享" / "捐赠"
- ko: "새 게임" / "일일 도전" / "주간 도전" / "공유" / "기부"
- hi: "नया खेल" / "दैनिक चुनौती" / "साप्ताहिक चुनौती" / "साझा करें" / "दान करें"
(coins/trophies/level e screen.* seguem o mesmo sentido; `share.title` = "JurassicRun" em todos; `share.text` traduzido no espírito do en.)

- [ ] **Step 5: Atualizar o teste do App (Home usa `home.newGame`, não `nav.play`) e cobrir daily/weekly**

Em `tests/app/App.test.tsx`, trocar a asserção da Home e adicionar cobertura de uma rota nova. No teste "renderiza a Home…", substituir `i18n.t('nav.play')` por `i18n.t('home.newGame')`. Adicionar:

```tsx
  it('renderiza daily como placeholder "em breve"', () => {
    render(<App />, container);
    navigate('daily');
    render(<App />, container);
    expect(container.textContent).toContain(i18n.t('screen.daily'));
    expect(container.textContent).toContain(i18n.t('screen.comingSoon'));
  });
```
(`navigate` já é importado no arquivo.)

- [ ] **Step 6: Rodar typecheck e testes de i18n/app**

Run: `npm run check && npx vitest run tests/i18n tests/app`
Expected: PASS (paridade de chaves ok; App renderiza daily/weekly; Home usa `home.newGame`).

- [ ] **Step 7: Commit**

```bash
git add src/app/router/routes.ts src/app/App.tsx src/i18n/locales tests/app/App.test.tsx
git commit -m "feat(4.3): rotas daily/weekly + i18n da Home (10 locales)"
```

---

### Task 2: Módulos puros da Home (stats + share)

Seam de stats (placeholders) e ação de compartilhar injetável — ambos puros/testáveis, sem tocar DOM real.

**Files:**
- Create: `src/app/home/stats.ts`
- Create: `src/app/home/share.ts`
- Test: `tests/app/home/stats.test.ts`, `tests/app/home/share.test.ts`

**Interfaces:**
- Consumes: nada de tasks anteriores (usa `i18n` só no `defaultShareDeps`).
- Produces:
  - `interface HomeStats { coins: number; trophies: number; maxLevel: number }` e `getHomeStats(): HomeStats`.
  - `type ShareResult = 'shared' | 'copied' | 'unsupported'`.
  - `interface ShareDeps { payload: { title: string; text: string; url: string }; share?: (data: { title: string; text: string; url: string }) => Promise<void>; clipboard?: (text: string) => Promise<void> }`.
  - `shareGame(deps: ShareDeps): Promise<ShareResult>` e `defaultShareDeps(): ShareDeps`.

- [ ] **Step 1: Teste do seam de stats**

`tests/app/home/stats.test.ts`:
```typescript
import { describe, it, expect } from 'vitest';
import { getHomeStats } from '@app/home/stats';

describe('getHomeStats', () => {
  it('retorna placeholders enquanto as fontes (4.5/4.7) não existem', () => {
    expect(getHomeStats()).toEqual({ coins: 0, trophies: 0, maxLevel: 1 });
  });
});
```

- [ ] **Step 2: Rodar — falha (módulo inexistente)**

Run: `npx vitest run tests/app/home/stats.test.ts`
Expected: FAIL (cannot find module `@app/home/stats`).

- [ ] **Step 3: Implementar `stats.ts`**

`src/app/home/stats.ts`:
```typescript
/**
 * Stats agregados exibidos na barra de topo da Home.
 *
 * SEAM: as fontes reais ainda não existem — carteira de moedas e nível máx
 * Endless vêm do item 4.5 (economia persistente) e troféus do 4.7. Este é o
 * ÚNICO ponto a religar quando esses serviços chegarem. Puro e determinístico.
 */
export interface HomeStats {
  readonly coins: number;
  readonly trophies: number;
  readonly maxLevel: number;
}

export function getHomeStats(): HomeStats {
  return { coins: 0, trophies: 0, maxLevel: 1 };
}
```

- [ ] **Step 4: Rodar — passa**

Run: `npx vitest run tests/app/home/stats.test.ts`
Expected: PASS.

- [ ] **Step 5: Teste do `shareGame` (ordem de fallback + robustez)**

`tests/app/home/share.test.ts`:
```typescript
import { describe, it, expect, vi } from 'vitest';
import { shareGame } from '@app/home/share';

const payload = { title: 'T', text: 'x', url: 'http://localhost/' };

describe('shareGame', () => {
  it('usa navigator.share quando disponível', async () => {
    const share = vi.fn().mockResolvedValue(undefined);
    const clipboard = vi.fn().mockResolvedValue(undefined);
    const r = await shareGame({ payload, share, clipboard });
    expect(r).toBe('shared');
    expect(share).toHaveBeenCalledOnce();
    expect(clipboard).not.toHaveBeenCalled();
  });

  it('cai no clipboard quando não há share', async () => {
    const clipboard = vi.fn().mockResolvedValue(undefined);
    const r = await shareGame({ payload, clipboard });
    expect(r).toBe('copied');
    expect(clipboard).toHaveBeenCalledWith('http://localhost/');
  });

  it('retorna unsupported sem share nem clipboard', async () => {
    expect(await shareGame({ payload })).toBe('unsupported');
  });

  it('engole cancelamento/erro do share (não propaga)', async () => {
    const share = vi.fn().mockRejectedValue(new DOMException('cancel', 'AbortError'));
    const r = await shareGame({ payload, share });
    expect(r).toBe('unsupported');
  });
});
```

- [ ] **Step 6: Rodar — falha**

Run: `npx vitest run tests/app/home/share.test.ts`
Expected: FAIL (cannot find module `@app/home/share`).

- [ ] **Step 7: Implementar `share.ts`**

`src/app/home/share.ts`:
```typescript
import { i18n } from '@services/i18n';

export type ShareResult = 'shared' | 'copied' | 'unsupported';

export interface SharePayload {
  readonly title: string;
  readonly text: string;
  readonly url: string;
}

export interface ShareDeps {
  readonly payload: SharePayload;
  readonly share?: (data: SharePayload) => Promise<void>;
  readonly clipboard?: (text: string) => Promise<void>;
}

/**
 * Compartilha o jogo. Best-effort: tenta a Web Share API, cai no clipboard,
 * e nunca propaga erro/cancelamento (a UI segue viva). Deps injetáveis p/ teste.
 */
export async function shareGame(deps: ShareDeps): Promise<ShareResult> {
  const { payload, share, clipboard } = deps;
  if (share) {
    try {
      await share(payload);
      return 'shared';
    } catch {
      return 'unsupported'; // usuário cancelou ou API falhou
    }
  }
  if (clipboard) {
    try {
      await clipboard(payload.url);
      return 'copied';
    } catch {
      return 'unsupported';
    }
  }
  return 'unsupported';
}

/** Casca: lê navigator/i18n/location reais. Não usar em teste. */
export function defaultShareDeps(): ShareDeps {
  const nav = typeof navigator !== 'undefined' ? navigator : undefined;
  const url = typeof location !== 'undefined' ? location.href : 'https://jurassicrun.app';
  return {
    payload: { title: i18n.t('share.title'), text: i18n.t('share.text'), url },
    share: nav?.share ? (data) => nav.share!(data) : undefined,
    clipboard: nav?.clipboard?.writeText ? (t) => nav.clipboard!.writeText(t) : undefined,
  };
}
```

- [ ] **Step 8: Rodar — passa + typecheck**

Run: `npx vitest run tests/app/home && npm run check`
Expected: PASS / typecheck limpo.

- [ ] **Step 9: Commit**

```bash
git add src/app/home tests/app/home
git commit -m "feat(4.3): módulos puros da Home (seam de stats + shareGame injetável)"
```

---

### Task 3: HomeScreen (barra de topo + menu) + CSS

Reescreve a Home compondo topo (identidade → Perfil, stat chips) e menu (CTA + navegação + ações). Doação = stub "em breve".

**Files:**
- Modify: `src/app/screens/HomeScreen.tsx` (reescrita)
- Modify: `src/app/styles/global.css`
- Test: `tests/app/home.test.tsx`

**Interfaces:**
- Consumes: `getHomeStats`/`HomeStats` (Task 2), `shareGame`/`defaultShareDeps` (Task 2), `Screen` com daily/weekly (Task 1), chaves i18n (Task 1); `profileService.activeProfile`, `avatarFor` (existentes); `navigate` (existente).

- [ ] **Step 1: Teste de smoke da Home**

`tests/app/home.test.tsx`:
```tsx
// @vitest-environment happy-dom
import { describe, it, expect, beforeEach, afterEach } from 'vitest';
import { render } from 'preact';
import { HomeScreen } from '@app/screens/HomeScreen';
import { route, resetToHome } from '@app/router';
import { i18n } from '@services/i18n';
import { profileService } from '@services/profile';
import { memoryProfileStorage } from '@services/profile/storage';
import { emptyState } from '@services/profile/store';

function click(el: Element | null): void {
  el?.dispatchEvent(new MouseEvent('click', { bubbles: true }));
}

describe('HomeScreen', () => {
  let container: HTMLDivElement;
  beforeEach(async () => {
    await i18n.init();
    resetToHome();
    profileService.init(memoryProfileStorage(emptyState()));
    profileService.create('Rex');
    container = document.createElement('div');
    document.body.appendChild(container);
    render(<HomeScreen />, container);
  });
  afterEach(() => {
    render(null, container);
    container.remove();
  });

  it('mostra nome do perfil ativo e os 3 chips de stats', () => {
    expect(container.textContent).toContain('Rex');
    expect(container.textContent).toContain(i18n.t('home.coins'));
    expect(container.textContent).toContain(i18n.t('home.trophies'));
    expect(container.textContent).toContain(i18n.t('home.level'));
  });

  it('clicar em Novo Jogo navega para play', () => {
    click([...container.querySelectorAll('button')].find(
      (b) => b.textContent === i18n.t('home.newGame'),
    )!);
    expect(route.value).toBe('play');
  });

  it('clicar na identidade navega para o perfil', () => {
    click(container.querySelector('[data-testid="home-identity"]'));
    expect(route.value).toBe('profile');
  });

  it('renderiza os itens de menu de navegação', () => {
    const txt = container.textContent ?? '';
    for (const key of ['nav.daily', 'nav.weekly', 'nav.nest', 'nav.shop',
      'nav.expansions', 'nav.leaderboard', 'nav.settings', 'nav.share', 'nav.donate']) {
      expect(txt).toContain(i18n.t(key));
    }
  });
});
```

- [ ] **Step 2: Rodar — falha (Home ainda é a antiga)**

Run: `npx vitest run tests/app/home.test.tsx`
Expected: FAIL (sem `data-testid="home-identity"` / sem chips).

- [ ] **Step 3: Reescrever `HomeScreen.tsx`**

`src/app/screens/HomeScreen.tsx`:
```tsx
import type { VNode } from 'preact';
import { navigate, type Screen } from '../router';
import { i18n } from '@services/i18n';
import { profileService, avatarFor, type Profile } from '@services/profile';
import { getHomeStats } from '../home/stats';
import { shareGame, defaultShareDeps } from '../home/share';

// Destinos de navegação do menu, na ordem do roadmap 4.3.
const MENU: readonly Screen[] = [
  'daily',
  'weekly',
  'nest',
  'shop',
  'expansions',
  'leaderboard',
  'settings',
];

function Avatar({ profile }: { profile: Profile }): VNode {
  const { initial, hue } = avatarFor(profile);
  return (
    <span class="avatar" style={{ backgroundColor: `hsl(${hue}, 55%, 45%)` }}>
      {initial}
    </span>
  );
}

function StatChip({ glyph, label, value }: { glyph: string; label: string; value: number }): VNode {
  return (
    <div class="stat-chip">
      <span class="stat-chip__glyph" aria-hidden="true">{glyph}</span>
      <span class="stat-chip__value">{value}</span>
      <span class="stat-chip__label">{label}</span>
    </div>
  );
}

export function HomeScreen(): VNode {
  const active = profileService.activeProfile.value;
  const stats = getHomeStats();

  return (
    <div class="home">
      <header class="home__topbar">
        {active !== null && (
          <button
            type="button"
            class="home__identity"
            data-testid="home-identity"
            onClick={() => navigate('profile')}
          >
            <Avatar profile={active} />
            <span class="home__name">{active.name}</span>
          </button>
        )}
        <div class="home__stats">
          <StatChip glyph="🪙" label={i18n.t('home.coins')} value={stats.coins} />
          <StatChip glyph="🏆" label={i18n.t('home.trophies')} value={stats.trophies} />
          <StatChip glyph="📈" label={i18n.t('home.level')} value={stats.maxLevel} />
        </div>
      </header>

      <main class="home__menu">
        <button class="btn home__primary" onClick={() => navigate('play')}>
          {i18n.t('home.newGame')}
        </button>

        <div class="home__grid">
          {MENU.map((screen) => (
            <button key={screen} class="btn btn--ghost" onClick={() => navigate(screen)}>
              {i18n.t(`nav.${screen}`)}
            </button>
          ))}
        </div>

        <div class="home__actions">
          <button class="btn btn--ghost" onClick={() => void shareGame(defaultShareDeps())}>
            {i18n.t('nav.share')}
          </button>
          <button
            class="btn btn--ghost"
            data-testid="home-donate"
            title={i18n.t('screen.comingSoon')}
            onClick={() => {
              /* stub: URL Ko-Fi/BMC + EntitlementsService chegam no 4.6 (ADR-0004) */
            }}
          >
            {i18n.t('nav.donate')}
          </button>
        </div>
      </main>
    </div>
  );
}
```

- [ ] **Step 4: Adicionar o CSS da Home**

Anexar em `src/app/styles/global.css`:
```css
.home {
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: var(--space-5);
  padding: var(--space-4);
  min-height: 0;
}

.home__topbar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: var(--space-3);
}

.home__identity {
  display: inline-flex;
  align-items: center;
  gap: var(--space-3);
  font: inherit;
  color: var(--color-text);
  background: var(--color-surface);
  border: none;
  border-radius: var(--radius-lg);
  padding: var(--space-2) var(--space-4) var(--space-2) var(--space-2);
  min-height: 44px;
  cursor: pointer;
}

.home__name {
  font-size: var(--font-size-md);
  font-weight: 600;
}

.home__stats {
  display: flex;
  gap: var(--space-2);
  flex-wrap: wrap;
}

.stat-chip {
  display: flex;
  align-items: baseline;
  gap: var(--space-1);
  background: var(--color-surface);
  border-radius: var(--radius-md);
  padding: var(--space-2) var(--space-3);
}

.stat-chip__glyph { font-size: var(--font-size-sm); }
.stat-chip__value { font-weight: 700; }
.stat-chip__label { color: var(--color-text-muted); font-size: var(--font-size-sm); }

.home__menu {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: var(--space-4);
  min-height: 0;
}

.home__primary {
  font-size: var(--font-size-lg);
  min-width: min(100%, 20rem);
}

.home__grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(10rem, 1fr));
  gap: var(--space-3);
  width: min(100%, 34rem);
}

.home__grid .btn { min-width: 0; }

.home__actions {
  display: flex;
  gap: var(--space-3);
  flex-wrap: wrap;
  justify-content: center;
}

.home__actions .btn { min-width: 0; }
```

- [ ] **Step 5: Rodar — passa**

Run: `npx vitest run tests/app/home.test.tsx`
Expected: PASS (nome, chips, navegação play/profile, itens de menu).

- [ ] **Step 6: Suíte cheia + typecheck**

Run: `npm run check && npm test`
Expected: PASS — todos os testes verdes, incluindo determinismo (64) inalterado.

- [ ] **Step 7: Commit**

```bash
git add src/app/screens/HomeScreen.tsx src/app/styles/global.css tests/app/home.test.tsx
git commit -m "feat(4.3): Home hub (barra de topo + menu); avatar abre Perfil"
```

---

## Notas de fechamento (após as 3 tasks)

- Marcar 4.3 como `[x]` em `docs/roadmap/PHASE-04-meta-offline.md`.
- Atualizar "Estado atual" do `CLAUDE.md` com o resumo do 4.3.
- Verificação visual (Playwright) opcional: Home em retrato/paisagem, clique na identidade → Perfil, botões de menu navegam, Compartilhar não quebra.
- Merge para `main` (`--no-ff`) e aposentar a branch (sem remote `gh` autenticado, como nos itens anteriores).
