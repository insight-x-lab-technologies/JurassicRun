# HUD (2.4) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sobrepor à cena um HUD (distância, comida, fps, nível, velocidade, seed) com refresh throttled que não custa fps.

**Architecture:** Segue o padrão 2.1–2.3: um módulo PURO testável (`src/render/hud.ts`, sem phaser — throttle+fps e formatação) + uma casca Phaser (`GameScene`) que cria os textos e os atualiza throttled com rótulos i18n. Core NÃO é tocado.

**Tech Stack:** TypeScript estrito, Vitest (env node p/ o módulo puro), Phaser (casca), i18next (`@services/i18n`).

## Global Constraints

- `src/core/` NÃO é alterado ⇒ determinismo intacto (REGRA 1).
- Nenhuma string visível hardcoded — tudo via chaves i18n `hud.*` nos 10 locales (REGRA 4).
- Zero alocação no hot path por frame: `HudTicker.tick(dt)` só mexe em escalares; `setText` só no refresh (~5 Hz) (REGRA 3).
- Módulo puro (`hud.ts`) NÃO importa `phaser`; testado em env node. Casca não tem teste de unidade (verificação visual Playwright).
- `src/render/index.ts` reexporta só módulos puros.
- `npm run check` limpo e `npm test` verde ao fim de cada task. `tests/i18n/locales.test.ts` exige paridade de chaves entre os 10 locales.
- TypeScript estrito, sem `any`.

---

### Task 1: Módulo puro `hud.ts` (HudTicker + formatHudValues)

**Files:**
- Create: `src/render/hud.ts`
- Create: `tests/render/hud.test.ts`
- Modify: `src/render/index.ts` (adicionar reexport)

**Interfaces:**
- Produces:
  - `HUD_REFRESH_INTERVAL: number` (= `0.2`)
  - `class HudTicker { constructor(intervalSeconds?: number); tick(dtSeconds: number): number | null }`
  - `interface HudRaw { distance: number; food: number; fps: number; level: number; speed: number; seed: string }`
  - `interface HudView { distance: string; food: string; fps: string; level: string; speed: string; seed: string }`
  - `function formatHudValues(raw: HudRaw): HudView`

- [ ] **Step 1: Escrever os testes que falham**

`tests/render/hud.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { HudTicker, formatHudValues, HUD_REFRESH_INTERVAL } from '@render/hud';

describe('HudTicker', () => {
  it('retorna null antes de a janela fechar', () => {
    const t = new HudTicker(0.2);
    expect(t.tick(0.05)).toBeNull();
    expect(t.tick(0.05)).toBeNull(); // 0.10 < 0.20
  });

  it('ao cruzar a janela retorna fps = frames / tempo decorrido', () => {
    const t = new HudTicker(0.2);
    expect(t.tick(0.1)).toBeNull(); // 0.10
    const fps = t.tick(0.1); // 0.20 ⇒ fecha: 2 frames / 0.2s = 10
    expect(fps).not.toBeNull();
    expect(fps as number).toBeCloseTo(10, 5);
  });

  it('reseta a janela após fechar (frames e tempo)', () => {
    const t = new HudTicker(0.2);
    t.tick(0.1);
    t.tick(0.1); // fecha
    expect(t.tick(0.1)).toBeNull(); // nova janela recomeça do zero
  });

  it('um frame gigante (aba em background) fecha a janela sem estourar', () => {
    const t = new HudTicker(0.2);
    const fps = t.tick(5); // 1 frame / 5s = 0.2
    expect(fps as number).toBeCloseTo(0.2, 5);
  });

  it('usa HUD_REFRESH_INTERVAL como default', () => {
    const t = new HudTicker();
    expect(t.tick(HUD_REFRESH_INTERVAL)).not.toBeNull();
  });
});

describe('formatHudValues', () => {
  it('faz floor de distância/food/level e round de fps/speed; seed passthrough', () => {
    const view = formatHudValues({
      distance: 123.9,
      food: 4,
      fps: 59.6,
      level: 3,
      speed: 62.4,
      seed: 'endless:DEMO',
    });
    expect(view).toEqual({
      distance: '123',
      food: '4',
      fps: '60',
      level: '3',
      speed: '62',
      seed: 'endless:DEMO',
    });
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npm test -- tests/render/hud.test.ts`
Expected: FAIL (`Cannot find module '@render/hud'`).

- [ ] **Step 3: Implementar `src/render/hud.ts`**

```ts
// Módulo PURO (sem phaser): throttle de refresh do HUD + medidor de fps e formatação dos valores.
// O gate de throttle produz o fps (frames ÷ tempo da janela) ⇒ uma peça, fps suavizado na janela.

/** Intervalo de refresh do HUD (s). ~5 Hz: barato o bastante p/ não custar fps. */
export const HUD_REFRESH_INTERVAL = 0.2;

/** Throttle do HUD que, ao fechar a janela, devolve o fps medido. Alocação-zero no tick. */
export class HudTicker {
  private readonly interval: number;
  private elapsed = 0;
  private frames = 0;

  constructor(intervalSeconds: number = HUD_REFRESH_INTERVAL) {
    this.interval = intervalSeconds;
  }

  /** Chamar 1×/frame. Só escalares. Retorna o fps ao fechar a janela, senão null. */
  tick(dtSeconds: number): number | null {
    this.elapsed += dtSeconds;
    this.frames += 1;
    if (this.elapsed < this.interval) return null;
    const fps = this.frames / this.elapsed;
    this.elapsed = 0;
    this.frames = 0;
    return fps;
  }
}

/** Valores crus do HUD (números do mundo + fps do render + seed injetada). */
export interface HudRaw {
  distance: number;
  food: number;
  fps: number;
  level: number;
  speed: number;
  seed: string;
}

/** Valores formatados (strings). Rótulos/unidades vivem nas chaves i18n `hud.*`. */
export interface HudView {
  distance: string;
  food: string;
  fps: string;
  level: string;
  speed: string;
  seed: string;
}

/** Formatação canônica: floor de contagens, round de fps/velocidade, seed literal. */
export function formatHudValues(raw: HudRaw): HudView {
  return {
    distance: String(Math.floor(raw.distance)),
    food: String(Math.floor(raw.food)),
    fps: String(Math.round(raw.fps)),
    level: String(Math.floor(raw.level)),
    speed: String(Math.round(raw.speed)),
    seed: raw.seed,
  };
}
```

- [ ] **Step 4: Reexportar em `src/render/index.ts`**

Adicionar a linha (após os outros reexports puros):

```ts
export * from './hud';
```

- [ ] **Step 5: Rodar e ver passar**

Run: `npm test -- tests/render/hud.test.ts`
Expected: PASS (todos os testes verdes).

- [ ] **Step 6: Typecheck e commit**

Run: `npm run check`
Expected: sem erros.

```bash
git add src/render/hud.ts tests/render/hud.test.ts src/render/index.ts
git commit -m "feat(2.4): módulo puro do HUD (HudTicker throttle+fps, formatHudValues)"
```

---

### Task 2: Chaves i18n `hud.*` nos 10 locales

**Files:**
- Modify: `src/i18n/locales/{en,es,pt-BR,fr,it,de,ja,zh,ko,hi}.json`

**Interfaces:**
- Produces: namespace `hud` com chaves `distance|food|fps|level|speed|seed`, cada uma com `{{value}}` interpolado. Consumidas pela Task 3 via `i18n.t('hud.<campo>', { value })`.

- [ ] **Step 1: Adicionar o bloco `hud` a cada locale**

Em CADA arquivo, adicionar a chave de topo `"hud"` ao lado de `"app"` (paridade obrigatória — `tests/i18n/locales.test.ts` exige as MESMAS chaves em todos). Conteúdo por idioma:

`en.json`:
```json
"hud": {
  "distance": "Dist: {{value}}m",
  "food": "Food: {{value}}",
  "fps": "FPS: {{value}}",
  "level": "Lv {{value}}",
  "speed": "Speed: {{value}}",
  "seed": "Seed: {{value}}"
}
```
`es.json`:
```json
"hud": {
  "distance": "Dist: {{value}}m",
  "food": "Comida: {{value}}",
  "fps": "FPS: {{value}}",
  "level": "Niv {{value}}",
  "speed": "Vel: {{value}}",
  "seed": "Semilla: {{value}}"
}
```
`pt-BR.json`:
```json
"hud": {
  "distance": "Dist: {{value}}m",
  "food": "Comida: {{value}}",
  "fps": "FPS: {{value}}",
  "level": "Nív {{value}}",
  "speed": "Vel: {{value}}",
  "seed": "Semente: {{value}}"
}
```
`fr.json`:
```json
"hud": {
  "distance": "Dist: {{value}}m",
  "food": "Nourriture: {{value}}",
  "fps": "FPS: {{value}}",
  "level": "Niv {{value}}",
  "speed": "Vit: {{value}}",
  "seed": "Graine: {{value}}"
}
```
`it.json`:
```json
"hud": {
  "distance": "Dist: {{value}}m",
  "food": "Cibo: {{value}}",
  "fps": "FPS: {{value}}",
  "level": "Liv {{value}}",
  "speed": "Vel: {{value}}",
  "seed": "Seme: {{value}}"
}
```
`de.json`:
```json
"hud": {
  "distance": "Dist: {{value}}m",
  "food": "Futter: {{value}}",
  "fps": "FPS: {{value}}",
  "level": "Stufe {{value}}",
  "speed": "Tempo: {{value}}",
  "seed": "Seed: {{value}}"
}
```
`ja.json`:
```json
"hud": {
  "distance": "距離: {{value}}m",
  "food": "エサ: {{value}}",
  "fps": "FPS: {{value}}",
  "level": "Lv {{value}}",
  "speed": "速度: {{value}}",
  "seed": "シード: {{value}}"
}
```
`zh.json`:
```json
"hud": {
  "distance": "距离: {{value}}m",
  "food": "食物: {{value}}",
  "fps": "FPS: {{value}}",
  "level": "等级 {{value}}",
  "speed": "速度: {{value}}",
  "seed": "种子: {{value}}"
}
```
`ko.json`:
```json
"hud": {
  "distance": "거리: {{value}}m",
  "food": "먹이: {{value}}",
  "fps": "FPS: {{value}}",
  "level": "Lv {{value}}",
  "speed": "속도: {{value}}",
  "seed": "시드: {{value}}"
}
```
`hi.json`:
```json
"hud": {
  "distance": "दूरी: {{value}}m",
  "food": "भोजन: {{value}}",
  "fps": "FPS: {{value}}",
  "level": "स्तर {{value}}",
  "speed": "गति: {{value}}",
  "seed": "सीड: {{value}}"
}
```

Cada arquivo é JSON válido — a `hud` vira uma chave irmã de `app` (adicionar a vírgula após o objeto `app`).

- [ ] **Step 2: Rodar o teste de paridade e ver passar**

Run: `npm test -- tests/i18n/locales.test.ts`
Expected: PASS (as chaves de todos os 10 locales batem com `en`).

- [ ] **Step 3: Commit**

```bash
git add src/i18n/locales
git commit -m "feat(2.4): chaves i18n hud.* (distance/food/fps/level/speed/seed) nos 10 locales"
```

---

### Task 3: Casca — HUD na GameScene + plumbing da seed

**Files:**
- Modify: `src/render/constants.ts` (constantes de estilo/depth do HUD)
- Modify: `src/render/GameScene.ts` (criar/atualizar o texto do HUD; receber `seedLabel`)
- Modify: `src/render/game.ts` (`GameDeps.seedLabel` → `GameScene`)
- Modify: `src/app/main.ts` (extrair a seed e passá-la a `createWorld` e `createGame`)

**Interfaces:**
- Consumes: `HudTicker`, `formatHudValues` (Task 1); chaves `hud.*` (Task 2); `i18n.t` (`@services/i18n`); `world.{distance,food,level,scrollSpeed}`.
- Produces: `GameDeps.seedLabel?: string`; `GameScene` renderiza o HUD.

- [ ] **Step 1: Constantes do HUD em `src/render/constants.ts`**

Adicionar ao final:

```ts
/** HUD (2.4): texto de leitura da partida. Depth abaixo do overlay de pausa (1000). */
export const HUD_DEPTH = 900;
export const HUD_TEXT_X = 4;
export const HUD_TEXT_Y = 4;
export const HUD_FONT_SIZE = '8px';
export const HUD_TEXT_COLOR = '#ffffff';
```

- [ ] **Step 2: `GameDeps.seedLabel` em `src/render/game.ts`**

Adicionar o campo à interface e repassar ao `GameScene`:

```ts
export interface GameDeps {
  input?: InputSource;
  pause?: PauseController;
  seedLabel?: string;
}
```

E na criação da cena (dentro de `createGame`), trocar a linha `scene: [new GameScene(world, input, pause)],` por:

```ts
    scene: [new GameScene(world, input, pause, deps.seedLabel ?? '')],
```

- [ ] **Step 3: HUD na `GameScene`**

No topo, adicionar imports:

```ts
import { i18n } from '@services/i18n';
import { HudTicker, formatHudValues } from './hud';
```

Acrescentar às constantes já importadas de `./constants` (dentro do bloco de import existente): `HUD_DEPTH, HUD_TEXT_X, HUD_TEXT_Y, HUD_FONT_SIZE, HUD_TEXT_COLOR`.

Adicionar campos e ajustar o construtor:

```ts
  private hudText!: Phaser.GameObjects.Text;
  private hudTicker!: HudTicker;
  private readonly seedLabel: string;

  constructor(world: WorldState, input: InputSource, pause: PauseController, seedLabel = '') {
    super('GameScene');
    this.world = world;
    this.inputSource = input;
    this.pause = pause;
    this.seedLabel = seedLabel;
  }
```

No fim de `create()` (após o overlay de pausa), criar o texto e o ticker e fazer um refresh inicial:

```ts
    // HUD (2.4): texto de leitura throttled. Depth abaixo do overlay de pausa.
    this.hudTicker = new HudTicker();
    this.hudText = this.add
      .text(HUD_TEXT_X, HUD_TEXT_Y, '', { fontSize: HUD_FONT_SIZE, color: HUD_TEXT_COLOR })
      .setScrollFactor(0)
      .setDepth(HUD_DEPTH);
    this.refreshHud(0);
```

No `update()`, dentro do ramo NÃO-pausado (após o desenho do mundo/dino), acumular e refrescar:

```ts
    const fps = this.hudTicker.tick(deltaMs / 1000);
    if (fps !== null) this.refreshHud(fps);
```

Adicionar o método privado:

```ts
  /** Reconstrói o texto do HUD (só no refresh throttled ⇒ fora do hot path por frame). */
  private refreshHud(fps: number): void {
    const v = formatHudValues({
      distance: this.world.distance,
      food: this.world.food,
      fps,
      level: this.world.level,
      speed: this.world.scrollSpeed,
      seed: this.seedLabel,
    });
    this.hudText.setText([
      i18n.t('hud.distance', { value: v.distance }),
      i18n.t('hud.food', { value: v.food }),
      i18n.t('hud.fps', { value: v.fps }),
      i18n.t('hud.level', { value: v.level }),
      i18n.t('hud.speed', { value: v.speed }),
      i18n.t('hud.seed', { value: v.seed }),
    ]);
  }
```

- [ ] **Step 4: Plumbing da seed em `src/app/main.ts`**

Trocar o corpo relevante do `bootstrap` para extrair a seed e passá-la aos dois consumidores:

```ts
  // Mundo de demonstração para o vertical slice (seed aleatória/fluxo real é 2.5).
  const seed = 'endless:DEMO';
  const world = createWorld({ seed });
  const flap = new FlapInputSource();
  const pause = new PauseController();
  pause.onPause = () => flap.reset(); // anti-flap-fantasma ao pausar
  createGame('app', world, { input: flap, pause, seedLabel: seed });
  bindGameControls(window, { flap, pause });
```

- [ ] **Step 5: Typecheck e suíte completa**

Run: `npm run check`
Expected: sem erros.

Run: `npm test`
Expected: PASS (incluindo `tests/render/hud.test.ts`, `tests/i18n/locales.test.ts` e a bateria de determinismo — core intacto).

- [ ] **Step 6: Commit**

```bash
git add src/render/constants.ts src/render/GameScene.ts src/render/game.ts src/app/main.ts
git commit -m "feat(2.4): casca do HUD na GameScene (texto throttled i18n) + plumbing da seed"
```

---

## Verificação final (após as 3 tasks)

- `npm run check` limpo, `npm test` verde, `npm run test:determinism` verde (core não tocado).
- Verificação visual (Playwright, padrão 2.1–2.3): subir `npm run dev`, carregar, confirmar que
  o HUD mostra os 6 campos, que os valores mudam ao jogar (distância/velocidade/nível sobem;
  comida ao coletar; fps ~60) e que a seed exibida é `endless:DEMO`. Se precisar ler o texto do
  canvas, expor `hudText.text` num hook TEMP de `window` e reverter após confirmar (como 2.2/2.3).
- Marcar 2.4 `[x]` em `docs/roadmap/PHASE-02-endless-vertical-slice.md` e atualizar
  "Estado atual" do `CLAUDE.md`.
</content>
