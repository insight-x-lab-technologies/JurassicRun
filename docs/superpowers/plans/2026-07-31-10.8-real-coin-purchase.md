# 10.8 — Loja com compra real de moedas — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task.
> Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** tirar da Loja o único caminho que dá moeda de graça (`walletService.earn` no clique do
pacote) e pôr no lugar uma Loja honesta: saldo, pacotes com **preço em dinheiro real** que levam ao
Ko-fi, e resgate de código — as três seções sempre visíveis.

**Architecture:** módulo puro novo `src/services/purchase/storefront.ts` (preços + URL de checkout,
molde de `src/services/online/config.ts`: parser puro sobre env-like + casca que lê
`import.meta.env`); abridor de URL externa extraído para `src/app/openUrl.ts`; `ShopScreen`
reescrita sem nenhuma referência a `walletService`; `RedeemCodeForm` passa a se auto-desabilitar
quando não há gateway. Spec: `docs/superpowers/specs/2026-07-31-10.8-real-coin-purchase-design.md`.

**Tech Stack:** TypeScript estrito, Preact + `@preact/signals`, Vitest (`happy-dom` para render),
i18next (10 locales).

## Global Constraints

- **`src/core/` é INTOCÁVEL nesta fase.** Nenhuma task abaixo o toca ⇒ determinismo continua **73**.
- **REGRA 4 (i18n):** nenhuma string visível hardcoded. Toda chave nova entra nos **10** locales
  (`de, en, es, fr, hi, it, ja, ko, pt-BR, zh`). Use a skill `add-locale`.
- **Preço não é string traduzida:** é formatado por `Intl.NumberFormat` — não criar chave i18n para
  o valor monetário.
- **Nunca lançar na borda:** parser de env e abridor de URL são best-effort (engolem erro). Sem
  `.env` configurado o jogo funciona idêntico (offline-first).
- **Teto de largura vai no FILHO, nunca na raiz `.screen`** (invariante do 10.5, com guarda
  automática em `tests/app/screen-root-width.test.ts`).
- **TypeScript estrito, sem `any`.** `npm run check` tem de ficar limpo.
- Rodar `npm test` **inteiro** antes de fechar (precedente 9.8: rodar só o arquivo-alvo esconde
  quebra em outro).
- Commits: um por task, mensagem `feat(10.8): …` / `test(10.8): …`, com trailer
  `Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`.

---

## Estrutura de arquivos

| Arquivo | Responsabilidade |
|---------|------------------|
| `src/services/purchase/storefront.ts` **(novo)** | dado puro: preços por SKU de moeda, URL do Ko-fi, formatação de preço, URL de checkout |
| `src/app/openUrl.ts` **(novo)** | casca injetável de "abrir URL externa em nova aba" |
| `src/app/home/donate.ts` (modificar) | passa a delegar o abridor para `openUrl.ts` |
| `src/app/shop/packs.ts` (modificar) | `CoinPack` ganha o campo `sku` |
| `src/app/purchase/RedeemCodeForm.tsx` (modificar) | auto-desabilita sem gateway |
| `src/app/screens/ShopScreen.tsx` (reescrever) | 3 seções sempre visíveis; zero `walletService` |
| `src/i18n/locales/*.json` (10) | chaves novas da Loja; remoção de `shop.grant`/`shop.honorNote` |
| `src/app/styles/global.css` (modificar) | blocos `.shop*` e `.redeem*` (hoje inexistentes) |
| `tests/services/purchase/storefront.test.ts` **(novo)** | módulo puro |
| `tests/app/shop/no-free-coins.test.ts` **(novo)** | guarda de fonte: Loja não pode citar `earn(` |
| `tests/app/shop/shop-screen.test.tsx` **(novo)** | render: clique não credita, 3 seções, gateway ON/OFF |

---

### Task 1: módulo puro `storefront` (preços, URL de checkout)

**Files:**
- Create: `src/services/purchase/storefront.ts`
- Create: `tests/services/purchase/storefront.test.ts`

**Interfaces:**
- Consumes: `Sku` de `@services/purchase/sku`; `DONATE_OPTIONS` de `@app/home/donate` (só no teste
  de sincronia).
- Produces:
  ```ts
  export type CoinSkuId = 'small' | 'medium' | 'large';
  export interface PriceTag { readonly amountMinor: number; readonly currency: string }
  export interface Storefront {
    readonly kofiUrl: string;
    readonly prices: Readonly<Record<CoinSkuId, PriceTag>>;
  }
  export const KOFI_PAGE_URL: string;
  export const DEFAULT_STOREFRONT: Storefront;
  export function parseStorefront(env: Record<string, unknown>): Storefront;
  export function formatPrice(price: PriceTag, locale: string): string;
  export function checkoutUrlFor(sf: Storefront, sku: Sku): string;
  export function storefront(): Storefront;
  ```

- [ ] **Step 1: Write the failing test**

Create `tests/services/purchase/storefront.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import {
  DEFAULT_STOREFRONT,
  KOFI_PAGE_URL,
  parseStorefront,
  formatPrice,
  checkoutUrlFor,
} from '@services/purchase/storefront';
import { DONATE_OPTIONS } from '@app/home/donate';

describe('storefront — defaults', () => {
  it('tem preço positivo em unidades menores para os 3 SKUs de moeda', () => {
    for (const id of ['small', 'medium', 'large'] as const) {
      const p = DEFAULT_STOREFRONT.prices[id];
      expect(Number.isInteger(p.amountMinor)).toBe(true);
      expect(p.amountMinor).toBeGreaterThan(0);
      expect(p.currency).toMatch(/^[A-Z]{3}$/);
    }
  });

  it('preço cresce com o tamanho do pacote', () => {
    const { small, medium, large } = DEFAULT_STOREFRONT.prices;
    expect(small.amountMinor).toBeLessThan(medium.amountMinor);
    expect(medium.amountMinor).toBeLessThan(large.amountMinor);
  });

  // Guarda de sincronia: Loja e Doação têm de apontar para a MESMA página do estúdio.
  it('a URL do Ko-fi é a mesma da tela de doação', () => {
    const kofi = DONATE_OPTIONS.find((o) => o.id === 'kofi');
    expect(kofi?.url).toBe(KOFI_PAGE_URL);
  });
});

describe('storefront — parseStorefront', () => {
  it('sem env devolve os defaults', () => {
    expect(parseStorefront({})).toEqual(DEFAULT_STOREFRONT);
  });

  it('sobrescreve URL, moeda e preços válidos', () => {
    const sf = parseStorefront({
      VITE_SHOP_KOFI_URL: 'https://ko-fi.com/outro',
      VITE_SHOP_CURRENCY: 'brl',
      VITE_SHOP_PRICE_SMALL: '990',
      VITE_SHOP_PRICE_MEDIUM: 2490,
      VITE_SHOP_PRICE_LARGE: '4990',
    });
    expect(sf.kofiUrl).toBe('https://ko-fi.com/outro');
    expect(sf.prices.small).toEqual({ amountMinor: 990, currency: 'BRL' });
    expect(sf.prices.medium.amountMinor).toBe(2490);
    expect(sf.prices.large.amountMinor).toBe(4990);
  });

  it('ignora valores inválidos campo a campo, sem lançar', () => {
    const sf = parseStorefront({
      VITE_SHOP_KOFI_URL: '   ',
      VITE_SHOP_CURRENCY: 'reais',
      VITE_SHOP_PRICE_SMALL: 'grátis',
      VITE_SHOP_PRICE_MEDIUM: -5,
      VITE_SHOP_PRICE_LARGE: 1.5,
    });
    expect(sf).toEqual(DEFAULT_STOREFRONT);
  });
});

describe('storefront — formatPrice', () => {
  it('formata unidades menores como dinheiro da língua ativa', () => {
    const s = formatPrice({ amountMinor: 199, currency: 'USD' }, 'en');
    expect(s).toContain('1.99');
  });

  it('moeda inválida não quebra: cai no formato textual', () => {
    const s = formatPrice({ amountMinor: 199, currency: 'XX' }, 'en');
    expect(s).toContain('1.99');
    expect(s).toContain('XX');
  });
});

describe('storefront — checkoutUrlFor', () => {
  it('anexa o SKU como query na URL base', () => {
    const url = checkoutUrlFor(DEFAULT_STOREFRONT, 'coins:small');
    expect(url.startsWith(`${KOFI_PAGE_URL}?`)).toBe(true);
    expect(url).toContain('jr_sku=coins%3Asmall');
  });

  it('preserva query já existente na base', () => {
    const sf = { ...DEFAULT_STOREFRONT, kofiUrl: 'https://ko-fi.com/x?ref=game' };
    expect(checkoutUrlFor(sf, 'coins:large')).toBe(
      'https://ko-fi.com/x?ref=game&jr_sku=coins%3Alarge',
    );
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/services/purchase/storefront.test.ts`
Expected: FAIL — `Failed to resolve import "@services/purchase/storefront"`.

- [ ] **Step 3: Write minimal implementation**

Create `src/services/purchase/storefront.ts`:

```ts
import type { Sku } from './sku';

/** Pacote de moedas vendável. Mesmos ids de `COIN_SKU_AMOUNTS`. */
export type CoinSkuId = 'small' | 'medium' | 'large';

/** Preço em UNIDADES MENORES (centavos) — inteiro. Dinheiro nunca em float. */
export interface PriceTag {
  readonly amountMinor: number;
  readonly currency: string;
}

export interface Storefront {
  readonly kofiUrl: string;
  readonly prices: Readonly<Record<CoinSkuId, PriceTag>>;
}

/**
 * Página do estúdio no Ko-fi. É a MESMA de `DONATE_OPTIONS` (`src/app/home/donate.ts`);
 * um teste de sincronia guarda as duas contra divergência silenciosa.
 */
export const KOFI_PAGE_URL = 'https://ko-fi.com/insightxlabgamestudio';

const DEFAULT_CURRENCY = 'USD';

/**
 * Preços default (tuning de hobby, como `COIN_SKU_AMOUNTS`). Existem para a Loja funcionar
 * SEM `.env` — `parseStorefront` só sobrescreve o que vier válido.
 */
export const DEFAULT_STOREFRONT: Storefront = Object.freeze({
  kofiUrl: KOFI_PAGE_URL,
  prices: Object.freeze({
    small: { amountMinor: 199, currency: DEFAULT_CURRENCY },
    medium: { amountMinor: 499, currency: DEFAULT_CURRENCY },
    large: { amountMinor: 999, currency: DEFAULT_CURRENCY },
  }),
});

function nonEmptyString(v: unknown): v is string {
  return typeof v === 'string' && v.trim().length > 0;
}

/** Aceita number ou string numérica; exige inteiro positivo (centavos). */
function positiveInt(v: unknown): number | null {
  const n = typeof v === 'number' ? v : typeof v === 'string' ? Number(v.trim()) : NaN;
  return Number.isInteger(n) && n > 0 ? n : null;
}

/** ISO-4217 tem 3 letras. Qualquer outra coisa é lixo de configuração. */
function currencyCode(v: unknown): string | null {
  if (!nonEmptyString(v)) return null;
  const up = v.trim().toUpperCase();
  return /^[A-Z]{3}$/.test(up) ? up : null;
}

const ENV_PRICE_KEY: Readonly<Record<CoinSkuId, string>> = {
  small: 'VITE_SHOP_PRICE_SMALL',
  medium: 'VITE_SHOP_PRICE_MEDIUM',
  large: 'VITE_SHOP_PRICE_LARGE',
};

/**
 * Puro: monta a vitrine a partir de um objeto env-like, campo a campo. Nunca lança e nunca
 * devolve estado parcial — o que não vier válido fica no default.
 */
export function parseStorefront(env: Record<string, unknown>): Storefront {
  const kofiUrl = nonEmptyString(env['VITE_SHOP_KOFI_URL'])
    ? (env['VITE_SHOP_KOFI_URL'] as string).trim()
    : DEFAULT_STOREFRONT.kofiUrl;
  const currency = currencyCode(env['VITE_SHOP_CURRENCY']);

  const ids: readonly CoinSkuId[] = ['small', 'medium', 'large'];
  const prices = {} as Record<CoinSkuId, PriceTag>;
  for (const id of ids) {
    const fallback = DEFAULT_STOREFRONT.prices[id];
    prices[id] = {
      amountMinor: positiveInt(env[ENV_PRICE_KEY[id]]) ?? fallback.amountMinor,
      currency: currency ?? fallback.currency,
    };
  }
  return { kofiUrl, prices: Object.freeze(prices) };
}

/**
 * Formata para exibição na língua ativa. `Intl` recusa moeda fora do ISO-4217 lançando
 * `RangeError` — nesse caso cai num formato textual em vez de derrubar a tela.
 */
export function formatPrice(price: PriceTag, locale: string): string {
  const units = price.amountMinor / 100;
  try {
    return new Intl.NumberFormat(locale, {
      style: 'currency',
      currency: price.currency,
    }).format(units);
  } catch {
    return `${price.currency} ${units.toFixed(2)}`;
  }
}

/**
 * URL de checkout. O SKU viaja na query para o pedido chegar rastreável ao fulfillment manual
 * (`supabase/README.md`). A UI também mostra o SKU, porque o Ko-fi não garante prefill de mensagem.
 */
export function checkoutUrlFor(sf: Storefront, sku: Sku): string {
  const sep = sf.kofiUrl.includes('?') ? '&' : '?';
  return `${sf.kofiUrl}${sep}jr_sku=${encodeURIComponent(sku)}`;
}

/** Casca: lê o ambiente Vite. */
export function storefront(): Storefront {
  return parseStorefront(import.meta.env as unknown as Record<string, unknown>);
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/services/purchase/storefront.test.ts`
Expected: PASS (todos os blocos).

Se `formatPrice` com `'XX'` **não** lançar no runtime do CI (algumas implementações aceitam
qualquer código de 3 letras), troque o teste da moeda inválida para `'XXX'` → se ainda passar pelo
`Intl`, ajuste a asserção para exigir só que a saída contenha `1.99`; **não** relaxe o `try/catch`
da implementação, que existe para o caso real de `RangeError`.

- [ ] **Step 5: Run typecheck**

Run: `npm run check`
Expected: sem erros.

- [ ] **Step 6: Commit**

```bash
git add src/services/purchase/storefront.ts tests/services/purchase/storefront.test.ts
git commit -m "feat(10.8): vitrine pura com preço real por SKU e URL de checkout

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 2: abridor de URL externa compartilhado + SKU no catálogo de pacotes

**Files:**
- Create: `src/app/openUrl.ts`
- Modify: `src/app/home/donate.ts` (delegar o abridor; assinatura pública inalterada)
- Modify: `src/app/shop/packs.ts` (campo `sku`)
- Modify: `tests/app/shop/packs.test.ts` (cobre o campo novo)

**Interfaces:**
- Consumes: `Sku` de `@services/purchase/sku`; `COIN_SKU_AMOUNTS` de `@services/purchase/sku`.
- Produces:
  ```ts
  // src/app/openUrl.ts
  export interface OpenUrlDeps { readonly openUrl?: (url: string) => void }
  export function openExternal(url: string, deps?: OpenUrlDeps): void;
  export function defaultOpenDeps(): OpenUrlDeps;
  // src/app/shop/packs.ts
  export interface CoinPack {
    readonly id: 'small' | 'medium' | 'large';
    readonly sku: Sku;
    readonly coins: number;
  }
  ```

- [ ] **Step 1: Write the failing test**

Create `tests/app/openUrl.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import { openExternal } from '@app/openUrl';

describe('openExternal', () => {
  it('chama o abridor injetado com a URL', () => {
    const seen: string[] = [];
    openExternal('https://exemplo.test/x', { openUrl: (u) => seen.push(u) });
    expect(seen).toEqual(['https://exemplo.test/x']);
  });

  it('sem abridor disponível não faz nada e não lança', () => {
    expect(() => openExternal('https://exemplo.test/x', {})).not.toThrow();
  });

  it('abridor que lança (popup bloqueado) não derruba o chamador', () => {
    expect(() =>
      openExternal('https://exemplo.test/x', {
        openUrl: () => {
          throw new Error('popup blocked');
        },
      }),
    ).not.toThrow();
  });
});
```

E acrescente ao final de `tests/app/shop/packs.test.ts`, dentro do `describe` existente:

```ts
  it('cada pacote carrega o SKU correspondente e a quantidade do catálogo', () => {
    expect(COIN_PACKS.map((p) => p.sku)).toEqual(['coins:small', 'coins:medium', 'coins:large']);
    for (const p of COIN_PACKS) expect(p.sku).toBe(`coins:${p.id}`);
  });
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run tests/app/openUrl.test.ts tests/app/shop/packs.test.ts`
Expected: FAIL — módulo `@app/openUrl` inexistente; `p.sku` é `undefined`.

- [ ] **Step 3: Write minimal implementation**

Create `src/app/openUrl.ts`:

```ts
/**
 * Abrir URL externa (doação, checkout do Ko-fi). Casca injetável: o teste passa um `openUrl`
 * espião; a produção usa `window.open`. Best-effort — popup bloqueado não derruba a UI.
 */
export interface OpenUrlDeps {
  readonly openUrl?: (url: string) => void;
}

export function openExternal(url: string, deps: OpenUrlDeps = defaultOpenDeps()): void {
  const { openUrl } = deps;
  if (!openUrl) return;
  try {
    openUrl(url);
  } catch {
    // popup bloqueado / ambiente sem window.
  }
}

/** Casca: nova aba com `noopener`. Não usar em teste. */
export function defaultOpenDeps(): OpenUrlDeps {
  if (typeof window === 'undefined') return {};
  return { openUrl: (url) => window.open(url, '_blank', 'noopener') };
}
```

Em `src/app/home/donate.ts`, substitua o corpo de `openDonation` e `defaultDonateDeps` por
delegação (mantendo os nomes exportados, de que os testes de doação dependem):

```ts
import { openExternal, defaultOpenDeps, type OpenUrlDeps } from '../openUrl';

export type DonateDeps = OpenUrlDeps;

/** Abre uma página de doação. Best-effort: engole erro (a UI segue viva). Deps injetáveis. */
export function openDonation(
  deps: DonateDeps = defaultDonateDeps(),
  url: string = DONATE_URL,
): void {
  openExternal(url, deps);
}

/** Casca: abre em nova aba com noopener. Não usar em teste. */
export function defaultDonateDeps(): DonateDeps {
  return defaultOpenDeps();
}
```

Em `src/app/shop/packs.ts`:

```ts
import { COIN_SKU_AMOUNTS, type Sku } from '@services/purchase/sku';

/** Pacote de moedas da Loja. Valores = fonte única em `@services/purchase/sku`. */
export interface CoinPack {
  readonly id: 'small' | 'medium' | 'large';
  /** SKU vendável — viaja na URL do Ko-fi e no código que o jogador resgata. */
  readonly sku: Sku;
  readonly coins: number;
}

export const COIN_PACKS: readonly CoinPack[] = Object.freeze([
  { id: 'small', sku: 'coins:small', coins: COIN_SKU_AMOUNTS.small },
  { id: 'medium', sku: 'coins:medium', coins: COIN_SKU_AMOUNTS.medium },
  { id: 'large', sku: 'coins:large', coins: COIN_SKU_AMOUNTS.large },
]);
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `npx vitest run tests/app/openUrl.test.ts tests/app/shop/packs.test.ts tests/app/donate.test.tsx`
Expected: PASS nos três (o de doação prova que a delegação não mudou o contrato).

- [ ] **Step 5: Run typecheck**

Run: `npm run check`
Expected: sem erros.

- [ ] **Step 6: Commit**

```bash
git add src/app/openUrl.ts src/app/home/donate.ts src/app/shop/packs.ts \
        tests/app/openUrl.test.ts tests/app/shop/packs.test.ts
git commit -m "feat(10.8): abridor de URL externa compartilhado e SKU no catálogo de pacotes

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 3: chaves i18n da Loja nos 10 idiomas

**Files:**
- Modify: `src/i18n/locales/{de,en,es,fr,hi,it,ja,ko,pt-BR,zh}.json`

**Interfaces:**
- Produces: as chaves consumidas pelas Tasks 4 e 5.

**Removidas** (a Loja deixa de dar moeda de graça, os textos mentem):
`shop.grant`, `shop.honorNote`.

**Mantidas:** `shop.title`, `shop.balance`, `shop.coinPacks`, `shop.pack`, `shop.expansionsSoon`,
`shop.back`.

**Novas** (mesma estrutura nos 10 arquivos):

| Chave | Papel |
|-------|-------|
| `shop.buy` | rótulo do botão que abre o Ko-fi |
| `shop.howItWorks` | explica o fluxo pagar-no-Ko-fi → receber código → resgatar aqui |
| `shop.skuHint` | instrução de incluir o código do SKU na mensagem — placeholder `{{sku}}` |
| `shop.gatewayOffline` | aviso quando não há gateway de resgate |
| `shop.freeTitle` | título da seção de moedas gratuitas |
| `shop.freePlay` | fonte grátis nº 1: jogar |
| `shop.freeChallenges` | fonte grátis nº 2: desafios |

- [ ] **Step 1: Write the failing test**

Nenhum teste novo — as guardas `tests/i18n/locales.test.ts` (paridade de chaves + de placeholders
entre os 10 locales) e `tests/i18n/no-hardcoded-strings.test.ts` já cobrem. O sinal de falha vem na
Task 4/5, quando o JSX referenciar chave inexistente.

- [ ] **Step 2: Editar `en.json` e `pt-BR.json` (referência) — valores exatos**

`src/i18n/locales/en.json`, objeto `shop`:

```json
  "shop": {
    "title": "Shop",
    "balance": "Balance: {{value}} coins",
    "coinPacks": "Coin Packs",
    "pack": "{{value}} coins",
    "buy": "Buy on Ko-fi",
    "howItWorks": "Payment happens on Ko-fi. You get a code, then redeem it below.",
    "skuHint": "Include the code {{sku}} in your Ko-fi message.",
    "gatewayOffline": "Redeeming is offline right now — codes are validated on our server.",
    "freeTitle": "Free coins",
    "freePlay": "Every coin you grab in a run lands in your wallet.",
    "freeChallenges": "Daily and Weekly challenges pay coins too.",
    "expansionsSoon": "Expansions arrive soon.",
    "back": "Back"
  },
```

`src/i18n/locales/pt-BR.json`, objeto `shop`:

```json
  "shop": {
    "title": "Loja",
    "balance": "Saldo: {{value}} moedas",
    "coinPacks": "Pacotes de Moedas",
    "pack": "{{value}} moedas",
    "buy": "Comprar no Ko-fi",
    "howItWorks": "O pagamento acontece no Ko-fi. Você recebe um código e resgata aqui embaixo.",
    "skuHint": "Inclua o código {{sku}} na mensagem do Ko-fi.",
    "gatewayOffline": "O resgate está fora do ar — os códigos são validados no nosso servidor.",
    "freeTitle": "Moedas de graça",
    "freePlay": "Toda moeda apanhada numa partida cai na sua carteira.",
    "freeChallenges": "Os desafios Diário e Semanal também pagam moedas.",
    "expansionsSoon": "Expansões chegam em breve.",
    "back": "Voltar"
  },
```

- [ ] **Step 3: Traduzir para os 8 idiomas restantes**

Use a skill `add-locale`. Aplique as mesmas chaves em `de, es, fr, hi, it, ja, ko, zh`, **na mesma
ordem**, removendo `grant`/`honorNote` de todos. Regras:
- Traduza de fato — valor byte-idêntico ao inglês é rejeitado por
  `tests/i18n/locales.test.ts` a menos que esteja na `IDENTICAL_TO_EN_ALLOWLIST`.
- **"Ko-fi" é marca:** mantenha a grafia em todos, dentro de frases traduzidas (a frase inteira
  difere do inglês, então a allowlist não é necessária).
- Os placeholders têm de bater exatamente: `{{value}}` em `balance`/`pack`, `{{sku}}` em `skuHint`.

- [ ] **Step 4: Run i18n guards**

Run: `npx vitest run tests/i18n`
Expected: PASS. Falha típica: `missing key` (esqueceu um locale) ou `identical to en`.

- [ ] **Step 5: Commit**

```bash
git add src/i18n/locales
git commit -m "feat(10.8): chaves i18n da Loja com compra real nos 10 idiomas

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 4: `RedeemCodeForm` se auto-desabilita sem gateway

**Files:**
- Modify: `src/app/purchase/RedeemCodeForm.tsx`
- Modify: `tests/app/redeem-form.test.tsx` (novo caso)

**Interfaces:**
- Consumes: `purchaseService.available` (`ReadonlySignal<boolean>`), chave `shop.gatewayOffline`.
- Produces: `data-testid="redeem-offline"` (parágrafo de aviso, presente só quando indisponível);
  `redeem-input` e `redeem-submit` ganham `disabled` nesse caso.

- [ ] **Step 1: Write the failing test**

Acrescente a `tests/app/redeem-form.test.tsx` um `describe` novo (fora do `describe` existente,
porque o `beforeEach` de lá instala um gateway disponível):

```ts
describe('RedeemCodeForm sem gateway', () => {
  let container: HTMLDivElement;
  beforeEach(async () => {
    await i18n.init();
    walletService.init(memoryWalletStorage());
    entitlementsService.init(memoryEntitlementsStorage());
    purchaseService.init({}); // sem gateway ⇒ `unavailableGateway`
    container = document.createElement('div');
    document.body.appendChild(container);
    render(<RedeemCodeForm />, container);
  });
  afterEach(() => {
    render(null, container);
    container.remove();
  });

  it('mostra aviso e desabilita entrada e botão', () => {
    expect(container.querySelector('[data-testid="redeem-offline"]')?.textContent).toBe(
      i18n.t('shop.gatewayOffline'),
    );
    expect((container.querySelector('[data-testid="redeem-input"]') as HTMLInputElement).disabled).toBe(true);
    expect((container.querySelector('[data-testid="redeem-submit"]') as HTMLButtonElement).disabled).toBe(true);
  });
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run tests/app/redeem-form.test.tsx`
Expected: FAIL — `redeem-offline` é `null`; `disabled` é `false`.

- [ ] **Step 3: Write minimal implementation**

Em `src/app/purchase/RedeemCodeForm.tsx`: leia a disponibilidade e propague.

```tsx
export function RedeemCodeForm(): VNode {
  const [code, setCode] = useState('');
  const [status, setStatus] = useState<PurchaseStatus | null>(null);
  const [busy, setBusy] = useState(false);
  // Sem gateway o resgate não some da tela (item 10.8): fica visível e desabilitado, para o
  // jogador entender que o caminho existe e está temporariamente fora do ar.
  const offline = !purchaseService.available.value;
  const inputRef = useRef<HTMLInputElement>(null);

  async function submit(e: Event): Promise<void> {
    e.preventDefault();
    if (busy || offline) return;
    // …resto inalterado…
  }
```

No JSX: `disabled={offline}` no `<input>`, `disabled={busy || offline}` no `<button>`, e antes de
`{status !== null && …}`:

```tsx
      {offline && (
        <p class="redeem__offline" data-testid="redeem-offline">
          {i18n.t('shop.gatewayOffline')}
        </p>
      )}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `npx vitest run tests/app/redeem-form.test.tsx`
Expected: PASS nos 3 casos (os 2 antigos + o novo).

- [ ] **Step 5: Commit**

```bash
git add src/app/purchase/RedeemCodeForm.tsx tests/app/redeem-form.test.tsx
git commit -m "feat(10.8): resgate fica visível e desabilitado quando não há gateway

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 5: `ShopScreen` reescrita — 3 seções, zero moeda grátis

**Files:**
- Modify: `src/app/screens/ShopScreen.tsx` (reescrita completa)
- Create: `tests/app/shop/shop-screen.test.tsx`
- Create: `tests/app/shop/no-free-coins.test.ts`

**Interfaces:**
- Consumes: `COIN_PACKS` (com `sku`, Task 2); `storefront`/`formatPrice`/`checkoutUrlFor`
  (Task 1); `openExternal`/`defaultOpenDeps` (Task 2); chaves da Task 3; `RedeemCodeForm` (Task 4).
- Produces: `ShopScreen(props?: { readonly openDeps?: OpenUrlDeps })` — a única dependência
  injetável, para o teste espionar a abertura sem `window.open` real.

- [ ] **Step 1: Write the failing tests**

Create `tests/app/shop/no-free-coins.test.ts` (guarda de fonte — dura mais que um teste de clique):

```ts
import { describe, it, expect } from 'vitest';
import { readFileSync, readdirSync } from 'node:fs';
import { join } from 'node:path';

// Arquivos que compõem a Loja e o resgate. Nenhum pode creditar moeda por conta própria:
// o ÚNICO caminho de moeda paga é purchaseService.redeem → gateway → wallet.earn, dentro do
// serviço, depois de o servidor validar o código (item 10.8). Ler o saldo é permitido; creditar
// não. A guarda é sobre a CHAMADA (`.earn(`), não sobre o import — é a chamada que credita.
const SHOP_FILES = [
  'src/app/screens/ShopScreen.tsx',
  ...readdirSync('src/app/shop').map((f) => join('src/app/shop', f)),
  ...readdirSync('src/app/purchase').map((f) => join('src/app/purchase', f)),
];

describe('a Loja não credita moedas', () => {
  it('nenhum arquivo da Loja chama earn(', () => {
    const offenders = SHOP_FILES.filter((f) => /\.earn\s*\(/.test(readFileSync(f, 'utf8')));
    expect(offenders).toEqual([]);
  });

  it('a varredura enxerga mesmo os arquivos da Loja (a guarda não é vácua)', () => {
    expect(SHOP_FILES).toContain('src/app/screens/ShopScreen.tsx');
    expect(SHOP_FILES.length).toBeGreaterThan(2);
  });
});
```

Create `tests/app/shop/shop-screen.test.tsx`:

```tsx
// @vitest-environment happy-dom
import { describe, expect, it, beforeEach, afterEach } from 'vitest';
import { render } from 'preact';
import { ShopScreen } from '@app/screens/ShopScreen';
import { walletService } from '@services/wallet';
import { memoryWalletStorage } from '@services/wallet/storage';
import { entitlementsService } from '@services/entitlements';
import { memoryEntitlementsStorage } from '@services/entitlements/storage';
import { purchaseService } from '@services/purchase';
import { memoryRedemptionGateway } from '@services/purchase/gateway';
import { i18n } from '@services/i18n';
import { COIN_PACKS } from '@app/shop/packs';
import { checkoutUrlFor, storefront } from '@services/purchase/storefront';

let opened: string[] = [];

function mount(): HTMLDivElement {
  const container = document.createElement('div');
  document.body.appendChild(container);
  render(<ShopScreen openDeps={{ openUrl: (u) => opened.push(u) }} />, container);
  return container;
}

describe('ShopScreen', () => {
  let container: HTMLDivElement;
  beforeEach(async () => {
    opened = [];
    await i18n.init();
    walletService.init(memoryWalletStorage());
    entitlementsService.init(memoryEntitlementsStorage());
    purchaseService.init({ gateway: memoryRedemptionGateway({ GOLD: 'coins:medium' }) });
    container = mount();
  });
  afterEach(() => {
    render(null, container);
    container.remove();
  });

  it('clicar em qualquer pacote NÃO credita moeda e abre o checkout do SKU certo', () => {
    const before = walletService.balance.value;
    for (const pack of COIN_PACKS) {
      const btn = container.querySelector(`[data-testid="shop-buy-${pack.id}"]`) as HTMLButtonElement;
      btn.click();
      expect(walletService.balance.value).toBe(before);
    }
    expect(opened).toEqual(COIN_PACKS.map((p) => checkoutUrlFor(storefront(), p.sku)));
  });

  it('cada pacote mostra um preço em dinheiro e o código do SKU', () => {
    for (const pack of COIN_PACKS) {
      const price = container.querySelector(`[data-testid="shop-price-${pack.id}"]`);
      expect(price?.textContent?.trim()).not.toBe('');
      expect(price?.textContent).toMatch(/\d/);
      expect(container.querySelector(`[data-testid="shop-sku-${pack.id}"]`)?.textContent).toContain(
        pack.sku,
      );
    }
  });

  it('as três seções coexistem com gateway disponível', () => {
    expect(container.querySelector('[data-testid="shop-balance"]')).not.toBeNull();
    expect(container.querySelector('[data-testid="shop-packs"]')).not.toBeNull();
    expect(container.querySelector('[data-testid="redeem-input"]')).not.toBeNull();
    expect(container.querySelector('[data-testid="redeem-offline"]')).toBeNull();
  });

  it('cita as fontes gratuitas de moeda', () => {
    const free = container.querySelector('[data-testid="shop-free"]')?.textContent ?? '';
    expect(free).toContain(i18n.t('shop.freePlay'));
    expect(free).toContain(i18n.t('shop.freeChallenges'));
  });

  it('não mostra mais nenhum botão de "adicionar" moeda', () => {
    for (const pack of COIN_PACKS) {
      expect(container.querySelector(`[data-testid="shop-grant-${pack.id}"]`)).toBeNull();
    }
  });
});

describe('ShopScreen sem gateway', () => {
  let container: HTMLDivElement;
  beforeEach(async () => {
    opened = [];
    await i18n.init();
    walletService.init(memoryWalletStorage());
    entitlementsService.init(memoryEntitlementsStorage());
    purchaseService.init({}); // gateway indisponível
    container = mount();
  });
  afterEach(() => {
    render(null, container);
    container.remove();
  });

  it('mantém as três seções e desabilita só o resgate', () => {
    expect(container.querySelector('[data-testid="shop-balance"]')).not.toBeNull();
    expect(container.querySelector('[data-testid="shop-packs"]')).not.toBeNull();
    expect(container.querySelector('[data-testid="redeem-offline"]')).not.toBeNull();
    const buy = container.querySelector(
      `[data-testid="shop-buy-${COIN_PACKS[0]!.id}"]`,
    ) as HTMLButtonElement;
    expect(buy.disabled).toBe(false); // comprar no Ko-fi não depende do nosso servidor
  });
});
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npx vitest run tests/app/shop`
Expected: FAIL — `ShopScreen` ainda importa `walletService` (guarda de fonte) e não tem
`shop-buy-*` chamando o abridor nem `shop-price-*`.

- [ ] **Step 3: Write minimal implementation**

Reescreva `src/app/screens/ShopScreen.tsx`:

```tsx
import type { VNode } from 'preact';
import { back } from '../router';
import { i18n } from '@services/i18n';
import { walletService } from '@services/wallet';
import { RedeemCodeForm } from '../purchase/RedeemCodeForm';
import { COIN_PACKS } from '../shop/packs';
import { checkoutUrlFor, formatPrice, storefront } from '@services/purchase/storefront';
import { openExternal, defaultOpenDeps, type OpenUrlDeps } from '../openUrl';

export interface ShopScreenProps {
  /** Injetável só para teste: espiona a abertura da aba externa. */
  readonly openDeps?: OpenUrlDeps;
}

export function ShopScreen({ openDeps }: ShopScreenProps = {}): VNode {
  // LER o saldo é permitido; creditar não. `tests/app/shop/no-free-coins.test.ts` proíbe
  // qualquer `.earn(` nos arquivos da Loja — o único caminho de moeda paga é o `redeem`.
  const balance = walletService.balance.value;
  const sf = storefront();
  const deps = openDeps ?? defaultOpenDeps();

  return (
    <div class="screen shop">
      <h1 class="screen__title">{i18n.t('shop.title')}</h1>
      <p class="shop__balance" data-testid="shop-balance">
        {i18n.t('shop.balance', { value: balance })}
      </p>

      <h2 class="shop__section">{i18n.t('shop.coinPacks')}</h2>
      <p class="shop__note">{i18n.t('shop.howItWorks')}</p>
      <ul class="shop__packs" data-testid="shop-packs">
        {COIN_PACKS.map((pack) => (
          <li key={pack.id} class="shop-pack" data-testid={`shop-pack-${pack.id}`}>
            <span class="shop-pack__amount">{i18n.t('shop.pack', { value: pack.coins })}</span>
            <span class="shop-pack__price" data-testid={`shop-price-${pack.id}`}>
              {formatPrice(sf.prices[pack.id], i18n.language)}
            </span>
            <button
              type="button"
              class="btn"
              data-testid={`shop-buy-${pack.id}`}
              onClick={() => openExternal(checkoutUrlFor(sf, pack.sku), deps)}
            >
              {i18n.t('shop.buy')}
            </button>
            <span class="shop-pack__sku" data-testid={`shop-sku-${pack.id}`}>
              {i18n.t('shop.skuHint', { sku: pack.sku })}
            </span>
          </li>
        ))}
      </ul>

      <RedeemCodeForm />

      <section class="shop__free" data-testid="shop-free">
        <h2 class="shop__section">{i18n.t('shop.freeTitle')}</h2>
        <ul class="shop__free-list">
          <li>{i18n.t('shop.freePlay')}</li>
          <li>{i18n.t('shop.freeChallenges')}</li>
        </ul>
      </section>

      <p class="shop__note shop__note--muted">{i18n.t('shop.expansionsSoon')}</p>

      <button class="btn btn--ghost" onClick={() => back()}>
        {i18n.t('shop.back')}
      </button>
    </div>
  );
}
```

Repare no que **saiu**: o `import { walletService }` continua (para LER o saldo), mas não sobrou
nenhum `onClick` que chame `earn`, nenhum `shop.grant` e nenhum `if` que esconda o resgate.

- [ ] **Step 4: Run tests to verify they pass**

Run: `npx vitest run tests/app/shop tests/app/redeem-form.test.tsx`
Expected: PASS.

Se `App.test.tsx` ou o roteador construírem `<ShopScreen />` sem props, confirme que o parâmetro
default `= {}` cobre o caso (é por isso que ele existe).

- [ ] **Step 5: Run typecheck**

Run: `npm run check`
Expected: sem erros.

- [ ] **Step 6: Commit**

```bash
git add src/app/screens/ShopScreen.tsx tests/app/shop
git commit -m "feat(10.8): Loja sem moeda grátis — pacotes levam ao Ko-fi e resgate fica visível

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 6: CSS da Loja e do resgate

**Files:**
- Modify: `src/app/styles/global.css`

**Interfaces:**
- Consumes: as classes emitidas na Task 5 (`.shop__balance`, `.shop__section`, `.shop__note`,
  `.shop__packs`, `.shop-pack`, `.shop-pack__amount|__price|__sku`, `.shop__free`,
  `.shop__free-list`) e da Task 4 (`.redeem`, `.redeem__row`, `.redeem__input`, `.redeem__status`,
  `.redeem__offline`).

**Contexto:** hoje **não existe uma única regra `.shop*` ou `.redeem*`** no `global.css` — a tela
herda só o genérico de `.screen`. Siga os tokens de `src/app/styles/tokens.css` e o estilo dos
blocos vizinhos (`.leaderboard__list`, `.trophies__grid`).

- [ ] **Step 1: Escrever o CSS**

Acrescente ao `global.css`, junto dos outros blocos de tela:

```css
/* ---- Loja (10.8) ---------------------------------------------------------- */
/* Teto de largura vive nos FILHOS + margin-inline:auto — nunca na raiz `.screen shop`
   (invariante do 10.5, com guarda em tests/app/screen-root-width.test.ts). */
.shop__balance,
.shop__section,
.shop__note,
.shop__packs,
.shop__free,
.shop .redeem {
  max-width: 44rem;
  margin-inline: auto;
  width: 100%;
}
.shop__section { text-align: left; }
.shop__note { font-size: 0.85rem; opacity: 0.8; text-align: left; }
.shop__note--muted { opacity: 0.55; }
.shop__packs { list-style: none; padding: 0; display: grid; gap: 0.75rem; }
.shop-pack {
  display: grid;
  grid-template-columns: 1fr auto;
  grid-template-areas: 'amount price' 'sku buy';
  gap: 0.35rem 0.75rem;
  align-items: center;
  padding: 0.75rem;
  border-radius: 0.75rem;
  background: rgb(255 255 255 / 6%);
  text-align: left;
}
.shop-pack__amount { grid-area: amount; font-weight: 700; }
.shop-pack__price { grid-area: price; font-variant-numeric: tabular-nums; }
.shop-pack__sku { grid-area: sku; font-size: 0.75rem; opacity: 0.7; }
.shop-pack .btn { grid-area: buy; }
.shop__free-list { list-style: disc; padding-inline-start: 1.25rem; text-align: left; }
.shop__free-list li { font-size: 0.85rem; opacity: 0.85; margin-block: 0.25rem; }

/* ---- Resgate de código ---------------------------------------------------- */
.redeem { display: grid; gap: 0.5rem; text-align: left; }
.redeem__row { display: flex; gap: 0.5rem; }
.redeem__input { flex: 1 1 auto; min-width: 0; }
.redeem__help { font-size: 0.85rem; opacity: 0.8; }
.redeem__status { font-size: 0.9rem; }
.redeem__offline { font-size: 0.85rem; opacity: 0.75; }
```

- [ ] **Step 2: Rodar a guarda de largura de raiz**

Run: `npx vitest run tests/app/screen-root-width.test.ts`
Expected: PASS — nenhuma classe-raiz de tela recebeu `max-width` (os tetos foram todos para filhos).

- [ ] **Step 3: Verificar no navegador POR NÚMERO**

Não confie em screenshot (precedente W5/10.5). Build e meça:

```bash
npm run build && npx vite preview --port 4173
```

Com Playwright, em **390×844** e **740×360**, na rota da Loja: desregistre o service worker, limpe
caches e navegue com `?nocache`. Meça e registre:
- `document.querySelector('.shop__packs').getBoundingClientRect().width` vs. a largura útil do
  `.screen` — os pacotes têm de ocupar a largura útil (até o teto de 44rem);
- `.screen` rola (`scrollHeight > clientHeight`) e o botão "Voltar" é alcançável — **medir
  `.screen`, não `document.documentElement`** (o shell é de viewport fixo; gotcha do 10.7);
- nenhum card com `height === 0`, `scrollWidth` do body sem overflow horizontal.

- [ ] **Step 4: Commit**

```bash
git add src/app/styles/global.css
git commit -m "feat(10.8): estilo da Loja e do formulário de resgate

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Task 7: fechamento — documentação e verificação total

**Files:**
- Modify: `docs/roadmap/PHASE-10-polish-and-portrait.md` (marcar 10.8)
- Modify: `CLAUDE.md` ("Estado atual")
- Modify: `.env.example` se existir; caso não exista, documentar as chaves novas em
  `docs/architecture/ARCHITECTURE.md` na seção de configuração

- [ ] **Step 1: Rodar a suíte inteira**

Run: `npm test`
Expected: tudo verde. **Exceção conhecida, não é regressão:** `tests/render/atlas.test.ts` pode
estourar timeout na suíte cheia e passar isolado (custo do encoder, não asserção) — confirme
isoladamente com `npx vitest run tests/render/atlas.test.ts` antes de chamar de falha.

- [ ] **Step 2: Rodar o typecheck e o lint**

Run: `npm run check`
Expected: sem saída de erro.

- [ ] **Step 3: Confirmar que o determinismo não se mexeu**

Run: `npm run test:determinism`
Expected: **73** testes verdes. Nenhuma task tocou `src/core/`; se este número mudar, algo saiu do
escopo e precisa voltar.

- [ ] **Step 4: Marcar o item e atualizar a memória do projeto**

Em `docs/roadmap/PHASE-10-polish-and-portrait.md`, marque `### 10.8` como `✅`, troque os `- [ ]` por
`- [x]` e acrescente o bloco `> **Executado em 2026-07-31**` com as decisões (env de preço,
guarda de fonte, desvio das "fontes gratuitas" — troféu não paga moeda).
Em `CLAUDE.md`, atualize "Estado atual": contagem de testes, `10.8 ✅` com o resumo curto e
**Próximo item: 10.9**.

- [ ] **Step 5: Commit**

```bash
git add docs/roadmap/PHASE-10-polish-and-portrait.md CLAUDE.md
git commit -m "docs(10.8): marca o item concluído e atualiza o estado do projeto

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Notas para quem revisar

- **O ponto do item** é que nenhum clique na UI vire moeda. Se um review propuser "só um
  fallback quando o gateway está fora", isso é o bug que o item veio matar.
- **Deviação consciente do roadmap:** ele pede citar "jogar, desafios, **troféus**" como fontes
  grátis. Troféu **não** credita moeda hoje (`walletService.earn` só tem dois call sites:
  `startGame.ts:99` e `purchase/index.ts:64`) — a UI lista as duas fontes verdadeiras. Fazer troféu
  pagar é mudança de economia, item da Fase 11.
- **Preço em unidades menores** e `Intl` para exibir: qualquer PR que reintroduza float de dinheiro
  ou string de preço hardcoded no JSX está errado.
