# 10.3 — Purga de dados pré-9.9 · Plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development
> (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use
> checkbox (`- [ ]`) syntax for tracking.

**Goal:** aposentar o leaderboard local pré-9.9 (chave `v1`→`v2`) e apagar no boot as chaves
órfãs de `localStorage` deixadas pelos bumps de replay, sem migrar nada.

**Architecture:** um módulo novo `src/services/storage/legacy.ts` com uma lista fixa de chaves
mortas, uma função PURA `purgeLegacyKeys(store, keys?)` sobre uma interface mínima de storage e
uma CASCA `purgeLegacyStorage()` que aplica no `localStorage` real engolindo qualquer erro. O
boot (`src/app/main.tsx`) chama a casca antes de qualquer serviço inicializar.

**Tech Stack:** TypeScript estrito, Vitest (`// @vitest-environment happy-dom` quando o teste toca
`localStorage`), Preact no boot. Aliases `@services/*` já configurados.

**Spec:** `docs/superpowers/specs/2026-07-30-10.3-legacy-data-purge.md`

## Global Constraints

- **`src/core/` INTOCADO.** Nada neste item entra no núcleo; determinismo segue em 73 testes.
- **Nenhuma string visível ao usuário** é criada ⇒ zero chave i18n nova (REGRA 4 satisfeita por
  omissão).
- **TypeScript estrito, sem `any`.** Sem import de DOM dentro de código puro: a função pura recebe
  a interface `LegacyStore`, nunca lê o global.
- **Falha de storage nunca derruba o boot.** Toda leitura/escrita real vai em `try/catch`, incluindo
  o acesso à propriedade global `localStorage` (Safari privado lança no acesso, não só na chamada).
- **Lista de chaves legadas é explícita**, jamais derivada por prefixo/heurística de versão.
- Commits: um por task, mensagem em português, prefixo `feat:`/`test:`/`docs:` conforme o conteúdo.

---

### Task 1: helper de purga de chaves legadas

**Files:**
- Create: `src/services/storage/legacy.ts`
- Create: `tests/services/storage/legacy.test.ts`

**Interfaces:**
- Consumes: nada (módulo folha).
- Produces:
  - `export interface LegacyStore { getItem(key: string): string | null; removeItem(key: string): void }`
  - `export const LEGACY_STORAGE_KEYS: readonly string[]` — 3 entradas
  - `export function purgeLegacyKeys(store: LegacyStore, keys?: readonly string[]): string[]`
  - `export function purgeLegacyStorage(): string[]`

- [ ] **Step 1: Escrever os testes que falham**

Criar `tests/services/storage/legacy.test.ts`:

```ts
// @vitest-environment happy-dom
import { describe, it, expect, beforeEach, vi } from 'vitest';
import {
  LEGACY_STORAGE_KEYS,
  purgeLegacyKeys,
  purgeLegacyStorage,
  type LegacyStore,
} from '@services/storage/legacy';
import { STORAGE_KEY as PROFILE_KEY } from '@services/profile/storage';
import { STORAGE_KEY as ENTITLEMENTS_KEY } from '@services/entitlements/storage';
import { STORAGE_KEY as LEADERBOARD_KEY } from '@services/leaderboard/storage';
import { STORAGE_KEY as REPLAY_KEY } from '@services/replay/storage';
import { STORAGE_KEY as WALLET_KEY } from '@services/wallet/storage';
import { STORAGE_KEY as TROPHY_KEY } from '@services/trophy/storage';
import { STORAGE_KEY as NEST_KEY } from '@services/nest/storage';
import { STORAGE_KEY as SETTINGS_KEY } from '@services/settings/storage';

function fakeStore(initial: Record<string, string>): LegacyStore & { data: Record<string, string> } {
  const data = { ...initial };
  return {
    data,
    getItem: (k) => (k in data ? data[k] : null),
    removeItem: (k) => {
      delete data[k];
    },
  };
}

describe('purgeLegacyKeys', () => {
  it('remove as chaves legadas presentes e devolve o que removeu', () => {
    const store = fakeStore({
      'jurassicrun.leaderboard.v1': '{}',
      'jurassicrun.replays.v1': '{}',
      'jurassicrun.replays.v2': '{}',
    });
    const removed = purgeLegacyKeys(store);
    expect(removed.sort()).toEqual([...LEGACY_STORAGE_KEYS].sort());
    expect(store.data).toEqual({});
  });

  it('não toca em chave não listada', () => {
    const store = fakeStore({ 'jurassicrun.leaderboard.v1': '{}', 'jurassicrun.wallet.v1': '{"coins":10}' });
    expect(purgeLegacyKeys(store)).toEqual(['jurassicrun.leaderboard.v1']);
    expect(store.data).toEqual({ 'jurassicrun.wallet.v1': '{"coins":10}' });
  });

  it('devolve [] e não chama removeItem quando nenhuma legada existe', () => {
    const store = fakeStore({ 'jurassicrun.wallet.v1': '{}' });
    const spy = vi.spyOn(store, 'removeItem');
    expect(purgeLegacyKeys(store)).toEqual([]);
    expect(spy).not.toHaveBeenCalled();
  });

  it('aceita uma lista de chaves customizada', () => {
    const store = fakeStore({ a: '1', b: '2' });
    expect(purgeLegacyKeys(store, ['a'])).toEqual(['a']);
    expect(store.data).toEqual({ b: '2' });
  });
});

describe('purgeLegacyStorage', () => {
  beforeEach(() => localStorage.clear());

  it('apaga as chaves legadas do localStorage real', () => {
    localStorage.setItem('jurassicrun.replays.v2', '{}');
    localStorage.setItem('jurassicrun.wallet.v1', '{}');
    expect(purgeLegacyStorage()).toEqual(['jurassicrun.replays.v2']);
    expect(localStorage.getItem('jurassicrun.replays.v2')).toBeNull();
    expect(localStorage.getItem('jurassicrun.wallet.v1')).toBe('{}');
  });

  it('devolve [] sem lançar quando o storage recusa a remoção', () => {
    localStorage.setItem('jurassicrun.replays.v1', '{}');
    const spy = vi.spyOn(Storage.prototype, 'removeItem').mockImplementation(() => {
      throw new Error('SecurityError');
    });
    expect(() => purgeLegacyStorage()).not.toThrow();
    expect(purgeLegacyStorage()).toEqual([]);
    spy.mockRestore();
  });
});

describe('invariante de chaves', () => {
  it('nenhuma chave legada coincide com uma chave viva', () => {
    const live = [
      PROFILE_KEY, ENTITLEMENTS_KEY, LEADERBOARD_KEY, REPLAY_KEY,
      WALLET_KEY, TROPHY_KEY, NEST_KEY, SETTINGS_KEY,
    ];
    for (const legacy of LEGACY_STORAGE_KEYS) {
      expect(live).not.toContain(legacy);
    }
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run tests/services/storage/legacy.test.ts`
Expected: FAIL — `Failed to resolve import "@services/storage/legacy"`.

- [ ] **Step 3: Implementar o mínimo**

Criar `src/services/storage/legacy.ts`:

```ts
/**
 * Purga de chaves de `localStorage` aposentadas (item 10.3).
 *
 * Pré-lançamento, a decisão do usuário é DESCARTAR dado legado, nunca migrar. Duas fontes de
 * lixo: os bumps de versão da chave de replays (v1→v2→v3, feitos em 9.8 e 9.9) e o leaderboard
 * local anterior aos modificadores de desafio da 9.9.
 *
 * A lista é EXPLÍCITA de propósito: um varredor por prefixo/versão apagaria dado vivo no primeiro
 * formato de chave inesperado, e o custo do erro é assimétrico.
 */
export const LEGACY_STORAGE_KEYS: readonly string[] = [
  'jurassicrun.leaderboard.v1', // pré-9.9: recordes obtidos sob as regras antigas de desafio
  'jurassicrun.replays.v1',     // aposentada em 9.8 (catálogo de obstáculos novo)
  'jurassicrun.replays.v2',     // aposentada em 9.9 (modificadores por seed)
];

/** Fatia mínima de `Storage` que a purga precisa — mantém a função pura e testável. */
export interface LegacyStore {
  getItem(key: string): string | null;
  removeItem(key: string): void;
}

/** PURO: remove só as chaves presentes e devolve as que removeu. */
export function purgeLegacyKeys(
  store: LegacyStore,
  keys: readonly string[] = LEGACY_STORAGE_KEYS,
): string[] {
  const removed: string[] = [];
  for (const key of keys) {
    if (store.getItem(key) === null) continue;
    store.removeItem(key);
    removed.push(key);
  }
  return removed;
}

/**
 * CASCA: aplica a purga no `localStorage` real. Best-effort — o acesso ao global pode lançar
 * (Safari em modo privado lança na PROPRIEDADE, não só na chamada) e a remoção também. Lixo
 * órfão nunca pode derrubar o boot.
 */
export function purgeLegacyStorage(): string[] {
  try {
    return purgeLegacyKeys(localStorage);
  } catch {
    return [];
  }
}
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run tests/services/storage/legacy.test.ts`
Expected: PASS (7 testes).

Se o `happy-dom` não expuser `Storage.prototype`, trocar o spy do teste de erro por
`vi.spyOn(localStorage, 'removeItem')` — a asserção (não lançar, devolver `[]`) é a mesma.

- [ ] **Step 5: Commit**

```bash
git add src/services/storage/legacy.ts tests/services/storage/legacy.test.ts
git commit -m "feat(10.3): helper de purga de chaves de storage legadas"
```

---

### Task 2: chave nova do leaderboard + chamada no boot

**Files:**
- Modify: `src/services/leaderboard/storage.ts:13` (constante `STORAGE_KEY`)
- Modify: `src/app/main.tsx:23-27` (primeira linha de `bootstrap()`)
- Modify: `tests/services/leaderboard/storage.test.ts` (asserções da chave nova)

**Interfaces:**
- Consumes: `purgeLegacyStorage()` de `@services/storage/legacy` (Task 1).
- Produces: `STORAGE_KEY === 'jurassicrun.leaderboard.v2'` em `@services/leaderboard/storage`.

- [ ] **Step 1: Escrever os testes que falham**

Acrescentar ao final de `tests/services/leaderboard/storage.test.ts`, DENTRO do
`describe('localStorageLeaderboardStorage', …)` já existente:

```ts
  it('usa a chave v2 (dados pré-9.9 ficam na v1 e são ignorados)', () => {
    expect(STORAGE_KEY).toBe('jurassicrun.leaderboard.v2');
    localStorage.setItem('jurassicrun.leaderboard.v1', JSON.stringify({
      version: 1,
      endless: [{ seed: 'velho', score: 999, distance: 1, food: 1, nearMisses: 0, achievedAt: 1 }],
      daily: [], weekly: [], bestEndlessLevel: 9,
    }));
    expect(localStorageLeaderboardStorage().load()).toEqual(initialLeaderboardState());
  });
```

- [ ] **Step 2: Rodar e ver falhar**

Run: `npx vitest run tests/services/leaderboard/storage.test.ts`
Expected: FAIL — `expected 'jurassicrun.leaderboard.v1' to be 'jurassicrun.leaderboard.v2'`.

- [ ] **Step 3: Bumpar a chave**

Em `src/services/leaderboard/storage.ts`, trocar a linha da constante por:

```ts
// v2 (10.3): a 9.9 passou a aplicar modificadores derivados da seed nos desafios. Um recorde da
// MESMA seed gravado antes disso foi obtido sob outras regras e apareceria como `yourBest` no
// briefing, indistinguível de um legítimo. Chave nova ⇒ o histórico pré-9.9 é descartado.
export const STORAGE_KEY = 'jurassicrun.leaderboard.v2';
```

- [ ] **Step 4: Rodar e ver passar**

Run: `npx vitest run tests/services/leaderboard/storage.test.ts`
Expected: PASS.

- [ ] **Step 5: Chamar a purga no boot**

Em `src/app/main.tsx`, adicionar o import junto dos demais `@services/*`:

```ts
import { purgeLegacyStorage } from '@services/storage/legacy';
```

e como PRIMEIRA linha do corpo de `bootstrap()`, antes de `await i18n.init();`:

```ts
  purgeLegacyStorage(); // 10.3: apaga chaves órfãs antes de qualquer serviço ler o storage
```

- [ ] **Step 6: Rodar a suíte inteira e o check**

Run: `npm test` — Expected: tudo verde (o único teste conhecido por falhar só na suíte cheia é
`tests/render/atlas.test.ts` por custo do encoder; se falhar, reconfirmar isolado com
`npx vitest run tests/render/atlas.test.ts` e reportar como não-regressão).
Run: `npm run check` — Expected: sem erros de tipo nem de lint.

- [ ] **Step 7: Commit**

```bash
git add src/services/leaderboard/storage.ts src/app/main.tsx tests/services/leaderboard/storage.test.ts
git commit -m "feat(10.3): leaderboard local v1->v2 e purga no boot"
```

---

### Task 3: fechar a documentação

**Files:**
- Modify: `docs/roadmap/PHASE-10-polish-and-portrait.md` (marcar os 3 checkboxes do 10.3)
- Modify: `docs/roadmap/PHASE-09-structural-improvements.md:476-479` (débito conhecido resolvido)
- Modify: `CLAUDE.md` (bloco "Estado atual")

**Interfaces:**
- Consumes: resultado das Tasks 1 e 2.
- Produces: nada de código.

- [ ] **Step 1: Marcar o item na fase**

Em `docs/roadmap/PHASE-10-polish-and-portrait.md`, trocar `### 10.3 Purga de dados pré-9.9 (#8)`
por `### 10.3 Purga de dados pré-9.9 (#8) ✅`, marcar os três `- [ ]` como `- [x]` e acrescentar
logo abaixo do bloco **Aceite** uma nota curta com o que foi entregue (arquivos e chave nova).

- [ ] **Step 2: Baixar o débito da Fase 9**

Em `docs/roadmap/PHASE-09-structural-improvements.md`, no parágrafo "Débito conhecido (aceito,
pré-lançamento)" (linha ~476), anotar que o débito foi **resolvido no 10.3** pelo bump
`leaderboard.v1→v2`, sem reescrever o histórico do parágrafo.

- [ ] **Step 3: Atualizar o "Estado atual" do CLAUDE.md**

Registrar `10.3 ✅` com o resumo de uma linha (chave `leaderboard.v2`, `purgeLegacyStorage()` no
boot, lista explícita) e mudar "Próximo item" para **10.4**. Manter métricas: determinismo 73,
suíte no número novo, `check` limpo.

- [ ] **Step 4: Commit**

```bash
git add docs/roadmap/PHASE-10-polish-and-portrait.md docs/roadmap/PHASE-09-structural-improvements.md CLAUDE.md
git commit -m "docs(10.3): fecha o item e baixa o débito de leaderboard pré-9.9"
```
