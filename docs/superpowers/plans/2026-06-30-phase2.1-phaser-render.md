# Fase 2.1 — Render Phaser sobre o core — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Phaser desenha a simulação determinística da Fase 1 num loop de passo fixo com interpolação entre steps, lendo o `WorldState` e desenhando os entities via manifesto de assets (primitivas).

**Architecture:** `src/render/` depende de `core` (nunca o contrário). Módulos puros e testáveis (constantes, interpolação, fonte de input, manifesto, loop de passo fixo) ficam livres de `phaser` e são testados em Vitest/node; a casca Phaser (`GameScene`, `game`) é fina e validada por `npm run check` + execução manual. A interpolação usa só a posição do dino (obstáculos são estáticos em coords de mundo), evitando clone de mundo por frame.

**Tech Stack:** TypeScript estrito, Phaser 4.2, Vite, Vitest.

## Global Constraints

- **Determinismo (REGRA 1):** 2.1 NÃO toca `src/core/`. O render só **lê** `WorldState` e só o muta chamando `step(world, input)` do core. Nenhuma fonte proibida (`Math.random`/`Date.now`/`performance.now`) é introduzida em `src/core/`.
- **Arte desacoplada (REGRA 2):** o visual vem do manifesto; posição/forma vêm da hitbox lógica. Nada de pixels na lógica.
- **Performance (REGRA 3):** sem alocação por frame no caminho quente; a interpolação não clona o mundo.
- **i18n (REGRA 4):** nenhuma string visível hardcoded (2.1 não tem UI textual).
- **TS estrito:** `noUncheckedIndexedAccess`, `noImplicitOverride`, `exactOptionalPropertyTypes`, `verbatimModuleSyntax` ligados. Sem `any`.
- **Import do Phaser:** use `import * as Phaser from 'phaser';` (o pacote tem `export = Phaser` nos tipos e named exports no bundle ESM — namespace import casa com ambos). Só `GameScene.ts`/`game.ts` importam `phaser`.
- **Módulos puros não importam `phaser`**; `src/render/index.ts` NÃO reexporta `GameScene`/`game` (para não puxar `phaser` em testes/consumidores puros).
- **Aliases:** `@core/*`, `@render/*`, `@app/*`, `@services/*`.
- **Commits:** um por task, na branch de feature.

---

### Task 1: Constantes, interpolação e fonte de input (módulos puros)

**Files:**
- Create: `src/render/constants.ts`
- Create: `src/render/interpolate.ts`
- Create: `src/render/input.ts`
- Test: `tests/render/interpolate.test.ts`
- Test: `tests/render/input.test.ts`

**Interfaces:**
- Consumes: `InputFrame` de `@core/sim`.
- Produces:
  - `constants.ts`: `VIEW_WIDTH=320`, `VIEW_HEIGHT=180`, `DINO_SCREEN_X=80`, `MAX_FRAME_TIME=0.25`, `SKY_COLOR`, `GROUND_COLOR`, `CEILING_COLOR`, `GROUND_THICKNESS` (todos `number`).
  - `interpolate.ts`: `lerp(a: number, b: number, t: number): number`.
  - `input.ts`: `interface InputSource { sample(): InputFrame }`; `class NullInputSource implements InputSource`.

- [ ] **Step 1: Escrever os testes que falham**

`tests/render/interpolate.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { lerp } from '@render/interpolate';

describe('lerp', () => {
  it('retorna a nos extremos t=0 e b em t=1', () => {
    expect(lerp(10, 20, 0)).toBe(10);
    expect(lerp(10, 20, 1)).toBe(20);
  });
  it('interpola no meio', () => {
    expect(lerp(10, 20, 0.5)).toBe(15);
    expect(lerp(-4, 4, 0.25)).toBe(-2);
  });
  it('extrapola fora de [0,1] (linear)', () => {
    expect(lerp(0, 10, 1.5)).toBe(15);
    expect(lerp(0, 10, -0.5)).toBe(-5);
  });
});
```

`tests/render/input.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { NullInputSource } from '@render/input';

describe('NullInputSource', () => {
  it('nunca pede flap', () => {
    const src = new NullInputSource();
    expect(src.sample()).toEqual({ flap: false });
    expect(src.sample()).toEqual({ flap: false });
  });
});
```

- [ ] **Step 2: Rodar para ver falhar**

Run: `npx vitest run tests/render/interpolate.test.ts tests/render/input.test.ts`
Expected: FAIL (módulos `@render/interpolate` e `@render/input` não existem).

- [ ] **Step 3: Implementar os módulos**

`src/render/constants.ts`:
```ts
/** Tamanho lógico do canvas (px). VIEW_HEIGHT = WORLD_HEIGHT default ⇒ 1 unidade de mundo = 1px. */
export const VIEW_WIDTH = 320;
export const VIEW_HEIGHT = 180;

/** x de tela fixo do pterodáctilo (≈¼ da largura); a câmera scrolla em x para segui-lo. */
export const DINO_SCREEN_X = 80;

/** Clamp do dt de frame (s): evita spiral-of-death quando a aba volta do background. */
export const MAX_FRAME_TIME = 0.25;

// Cores de cenário (placeholder; parallax real é 2.3).
export const SKY_COLOR = 0x9ad4e6;
export const GROUND_COLOR = 0x4a7a3a;
export const CEILING_COLOR = 0x3a2f4a;
export const GROUND_THICKNESS = 6;
```

`src/render/interpolate.ts`:
```ts
/** Interpolação linear. t fora de [0,1] extrapola. */
export function lerp(a: number, b: number, t: number): number {
  return a + (b - a) * t;
}
```

`src/render/input.ts`:
```ts
import type { InputFrame } from '@core/sim';

/** Fonte de input amostrada uma vez por step da simulação. 2.2 adiciona toque/tecla. */
export interface InputSource {
  sample(): InputFrame;
}

/** Fonte nula: nunca pede flap. Usada em 2.1 (input real é 2.2). */
export class NullInputSource implements InputSource {
  sample(): InputFrame {
    return { flap: false };
  }
}
```

- [ ] **Step 4: Rodar para ver passar**

Run: `npx vitest run tests/render/interpolate.test.ts tests/render/input.test.ts`
Expected: PASS (todos verdes).

- [ ] **Step 5: Commit**

```bash
git add src/render/constants.ts src/render/interpolate.ts src/render/input.ts tests/render/interpolate.test.ts tests/render/input.test.ts
git commit -m "feat(2.1): constantes de view, lerp e InputSource/NullInputSource (render puro)"
```

---

### Task 2: Manifesto de assets + guarda de completude

**Files:**
- Create: `src/render/manifest.ts`
- Test: `tests/render/manifest.test.ts`

**Interfaces:**
- Consumes: `OBSTACLE_CATALOG`, `COLLECTIBLE_CATALOG` de `@core/spawn` (apenas no teste).
- Produces:
  - `type Renderable = { kind:'primitive'; color:number; shape?:'hitbox'|'triangle' } | { kind:'sprite'; atlas:string; frame?:string; animation?:string }`
  - `const DINO_TYPE_ID = 'dino.default'`
  - `const ASSET_MANIFEST: Readonly<Record<string, Renderable>>`
  - `function renderableFor(typeId: string): Renderable`

- [ ] **Step 1: Escrever o teste que falha**

`tests/render/manifest.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { ASSET_MANIFEST, DINO_TYPE_ID, renderableFor } from '@render/manifest';
import { OBSTACLE_CATALOG, COLLECTIBLE_CATALOG } from '@core/spawn';

describe('manifesto de assets', () => {
  it('mapeia um id conhecido para um Renderable', () => {
    const r = renderableFor(DINO_TYPE_ID);
    expect(r.kind).toBe('primitive');
    if (r.kind === 'primitive') {
      expect(r.color).toBe(0xcc5544);
      expect(r.shape).toBe('triangle');
    }
  });

  it('cai num fallback primitivo para id desconhecido (não quebra)', () => {
    const r = renderableFor('nao.existe');
    expect(r.kind).toBe('primitive');
  });

  it('COMPLETUDE: todo tipo do catálogo + o dino têm entrada no manifesto', () => {
    const ids = [
      DINO_TYPE_ID,
      ...OBSTACLE_CATALOG.map((t) => t.id),
      ...COLLECTIBLE_CATALOG.map((t) => t.id),
    ];
    for (const id of ids) {
      expect(ASSET_MANIFEST[id], `id sem entrada no manifesto: ${id}`).toBeDefined();
    }
  });
});
```

- [ ] **Step 2: Rodar para ver falhar**

Run: `npx vitest run tests/render/manifest.test.ts`
Expected: FAIL (`@render/manifest` não existe).

- [ ] **Step 3: Implementar o manifesto**

`src/render/manifest.ts`:
```ts
/**
 * Representação visual de um tipo lógico (REGRA 2). Na fase geométrica, `primitive` desenha a
 * geometria da HITBOX do entity na cor dada; `shape:'triangle'` é exceção cosmética (dino),
 * inscrita nos bounds da hitbox. Trocar para arte = mudar a entrada para `kind:'sprite'`.
 */
export type Renderable =
  | { readonly kind: 'primitive'; readonly color: number; readonly shape?: 'hitbox' | 'triangle' }
  | { readonly kind: 'sprite'; readonly atlas: string; readonly frame?: string; readonly animation?: string };

/** Chave do pterodáctilo do jogador (não é um Entity com tags). */
export const DINO_TYPE_ID = 'dino.default';

/** Visível quando um id não tem entrada (não deveria acontecer — há guarda de completude). */
const FALLBACK: Renderable = { kind: 'primitive', color: 0xff00ff };

/** Mapa id lógico → visual. Cores dos asset-specs / RENDERING-AND-ASSETS.md. */
export const ASSET_MANIFEST: Readonly<Record<string, Renderable>> = {
  [DINO_TYPE_ID]: { kind: 'primitive', color: 0xcc5544, shape: 'triangle' },
  'obstacle.tree': { kind: 'primitive', color: 0x6b4a2f },
  'obstacle.vine': { kind: 'primitive', color: 0x2f6b2f },
  'obstacle.boulder': { kind: 'primitive', color: 0x808896 },
  'obstacle.stalactite': { kind: 'primitive', color: 0x9aa3b2 },
  'bird.coin': { kind: 'primitive', color: 0xffd54a },
};

/** Visual de um tipo lógico; fallback primitivo para ids desconhecidos. */
export function renderableFor(typeId: string): Renderable {
  return ASSET_MANIFEST[typeId] ?? FALLBACK;
}
```

- [ ] **Step 4: Rodar para ver passar**

Run: `npx vitest run tests/render/manifest.test.ts`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/render/manifest.ts tests/render/manifest.test.ts
git commit -m "feat(2.1): manifesto de assets (primitivas) + guarda de completude"
```

---

### Task 3: Loop de passo fixo com interpolação (`FixedStepLoop`)

**Files:**
- Create: `src/render/loop.ts`
- Create: `src/render/index.ts`
- Test: `tests/render/loop.test.ts`

**Interfaces:**
- Consumes: `step`, `FIXED_DT`, `WorldState`, `createWorld` de `@core/sim`; `InputSource` de `./input`; `lerp` de `./interpolate`; `MAX_FRAME_TIME` de `./constants`.
- Produces:
  - `class FixedStepLoop` com construtor `(world: WorldState, input: InputSource)`, campo público `readonly world`, método `advance(dtSeconds: number): number` (retorna nº de steps rodados), getters `alpha`, `renderX`, `renderY` (todos `number`).
  - `src/render/index.ts` reexporta os módulos puros (`constants`, `interpolate`, `input`, `manifest`, `loop`) — NÃO `GameScene`/`game`.

- [ ] **Step 1: Escrever os testes que falham**

`tests/render/loop.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { createWorld, FIXED_DT } from '@core/sim';
import type { InputFrame } from '@core/sim';
import { FixedStepLoop } from '@render/loop';
import { NullInputSource } from '@render/input';
import { MAX_FRAME_TIME } from '@render/constants';

/** Mundo alto: o dino não toca o chão em poucos steps ⇒ tick avança sem morte. */
function tallWorld() {
  return createWorld({ worldHeight: 100000 });
}

describe('FixedStepLoop', () => {
  it('advance(FIXED_DT) roda exatamente 1 step e zera o acumulador', () => {
    const loop = new FixedStepLoop(tallWorld(), new NullInputSource());
    expect(loop.advance(FIXED_DT)).toBe(1);
    expect(loop.world.tick).toBe(1);
    expect(loop.alpha).toBeCloseTo(0, 10);
  });

  it('acumula frações até completar um step', () => {
    const loop = new FixedStepLoop(tallWorld(), new NullInputSource());
    expect(loop.advance(FIXED_DT / 2)).toBe(0);
    expect(loop.alpha).toBeCloseTo(0.5, 10);
    expect(loop.advance(FIXED_DT / 2)).toBe(1);
    expect(loop.world.tick).toBe(1);
  });

  it('60 chamadas de advance(FIXED_DT) ⇒ 60 steps (tick=60)', () => {
    const loop = new FixedStepLoop(tallWorld(), new NullInputSource());
    let total = 0;
    for (let i = 0; i < 60; i++) total += loop.advance(FIXED_DT);
    expect(total).toBe(60);
    expect(loop.world.tick).toBe(60);
  });

  it('clampa dt grande (anti spiral-of-death): mesmo nº de steps que MAX_FRAME_TIME', () => {
    const big = new FixedStepLoop(tallWorld(), new NullInputSource()).advance(100);
    const max = new FixedStepLoop(tallWorld(), new NullInputSource()).advance(MAX_FRAME_TIME);
    expect(big).toBe(max);
    expect(big).toBeLessThanOrEqual(Math.ceil(MAX_FRAME_TIME / FIXED_DT));
    expect(big).toBeGreaterThan(0);
  });

  it('alpha fica em [0,1)', () => {
    const loop = new FixedStepLoop(tallWorld(), new NullInputSource());
    loop.advance(FIXED_DT * 2.5);
    expect(loop.alpha).toBeGreaterThanOrEqual(0);
    expect(loop.alpha).toBeLessThan(1);
  });

  it('amostra o InputSource uma vez por step', () => {
    let calls = 0;
    const counting = { sample(): InputFrame { calls++; return { flap: false }; } };
    const loop = new FixedStepLoop(tallWorld(), counting);
    const steps = loop.advance(FIXED_DT * 5);
    expect(calls).toBe(steps);
    expect(calls).toBe(5);
  });

  it('renderX/renderY ficam entre prev e curr; sem salto no início (prev=curr)', () => {
    const world = tallWorld();
    const loop = new FixedStepLoop(world, new NullInputSource());
    // Antes de qualquer step: render == posição inicial.
    expect(loop.renderX).toBe(world.pterodactyl.transform.position.x);
    expect(loop.renderY).toBe(world.pterodactyl.transform.position.y);
    // Avança 1 step + meia fração: renderX entre prev (após o step) e curr.
    loop.advance(FIXED_DT * 1.5);
    const currX = world.pterodactyl.transform.position.x;
    expect(loop.renderX).toBeLessThanOrEqual(currX);
  });
});
```

- [ ] **Step 2: Rodar para ver falhar**

Run: `npx vitest run tests/render/loop.test.ts`
Expected: FAIL (`@render/loop` não existe).

- [ ] **Step 3: Implementar o loop**

`src/render/loop.ts`:
```ts
import { FIXED_DT, step } from '@core/sim';
import type { WorldState } from '@core/sim';
import type { InputSource } from './input';
import { lerp } from './interpolate';
import { MAX_FRAME_TIME } from './constants';

/**
 * Loop canônico de passo fixo (ARCHITECTURE.md): acumulador + steps fixos, render desacoplado.
 * O estado de interpolação é só a posição do dino (obstáculos são estáticos em coords de mundo,
 * então a câmera seguindo o dino interpolado suaviza todo o cenário) ⇒ sem clone de mundo (REGRA 3).
 */
export class FixedStepLoop {
  readonly world: WorldState;
  private readonly input: InputSource;
  private accumulator = 0;
  private prevX: number;
  private prevY: number;

  constructor(world: WorldState, input: InputSource) {
    this.world = world;
    this.input = input;
    const p = world.pterodactyl.transform.position;
    this.prevX = p.x;
    this.prevY = p.y;
  }

  /** Acumula dt (clampado) e roda os steps fixos devidos. Retorna quantos steps rodaram. */
  advance(dtSeconds: number): number {
    const dt = dtSeconds > MAX_FRAME_TIME ? MAX_FRAME_TIME : dtSeconds;
    this.accumulator += dt;
    const pos = this.world.pterodactyl.transform.position;
    let steps = 0;
    while (this.accumulator >= FIXED_DT) {
      this.prevX = pos.x; // snapshot 1 step atrás de `curr` (= pos após o step)
      this.prevY = pos.y;
      step(this.world, this.input.sample());
      this.accumulator -= FIXED_DT;
      steps += 1;
    }
    return steps;
  }

  /** Fração [0,1) do passo corrente (para interpolar o render). */
  get alpha(): number {
    return this.accumulator / FIXED_DT;
  }

  get renderX(): number {
    return lerp(this.prevX, this.world.pterodactyl.transform.position.x, this.alpha);
  }

  get renderY(): number {
    return lerp(this.prevY, this.world.pterodactyl.transform.position.y, this.alpha);
  }
}
```

`src/render/index.ts`:
```ts
// Reexporta apenas os módulos PUROS (sem phaser). GameScene/game são importados direto.
export * from './constants';
export * from './interpolate';
export * from './input';
export * from './manifest';
export * from './loop';
```

- [ ] **Step 4: Rodar para ver passar**

Run: `npx vitest run tests/render/loop.test.ts`
Expected: PASS.

- [ ] **Step 5: Verificar suíte + typecheck e commitar**

Run: `npm test && npm run check`
Expected: tudo verde (incluindo determinismo, intacto — core não foi tocado).

```bash
git add src/render/loop.ts src/render/index.ts tests/render/loop.test.ts
git commit -m "feat(2.1): FixedStepLoop (passo fixo + interpolação do dino) + index puro"
```

---

### Task 4: Casca Phaser — GameScene, bootstrap do jogo e integração no app

**Files:**
- Create: `src/render/GameScene.ts`
- Create: `src/render/game.ts`
- Modify: `src/app/main.ts` (substitui o shell Preact vazio pelo canvas do jogo)

**Interfaces:**
- Consumes: `import * as Phaser from 'phaser'`; `WorldState`, `Entity`, `Hitbox`, `boundsOf`, `createWorld` de `@core/sim`; `FixedStepLoop`, `renderableFor`, `DINO_TYPE_ID`, `InputSource`, `NullInputSource`, e constantes de `@render/*`.
- Produces:
  - `class GameScene extends Phaser.Scene` com `constructor(world: WorldState, input: InputSource)`.
  - `function createGame(parent: string | HTMLElement, world: WorldState, input?: InputSource): Phaser.Game`.

> Sem teste de unidade (exigiria WebGL/browser; Vitest roda em node). Validação por `npm run check` (tsc+eslint) e execução manual `npm run dev`.

- [ ] **Step 1: Implementar a cena**

`src/render/GameScene.ts`:
```ts
import * as Phaser from 'phaser';
import { boundsOf } from '@core/sim';
import type { Entity, Hitbox, WorldState } from '@core/sim';
import { FixedStepLoop } from './loop';
import { renderableFor, DINO_TYPE_ID } from './manifest';
import type { InputSource } from './input';
import {
  VIEW_WIDTH,
  VIEW_HEIGHT,
  DINO_SCREEN_X,
  GROUND_COLOR,
  CEILING_COLOR,
  GROUND_THICKNESS,
} from './constants';

/** Renderiza o WorldState lido do core. Não altera a simulação (REGRA 1). */
export class GameScene extends Phaser.Scene {
  private readonly world: WorldState;
  private readonly input: InputSource;
  private loop!: FixedStepLoop;
  private gfx!: Phaser.GameObjects.Graphics;

  constructor(world: WorldState, input: InputSource) {
    super('GameScene');
    this.world = world;
    this.input = input;
  }

  create(): void {
    // Cenário fixo (scrollFactor 0): faixas de teto e chão. Parallax real é 2.3.
    const bg = this.add.graphics().setScrollFactor(0);
    bg.fillStyle(CEILING_COLOR, 1);
    bg.fillRect(0, 0, VIEW_WIDTH, GROUND_THICKNESS);
    bg.fillStyle(GROUND_COLOR, 1);
    bg.fillRect(0, VIEW_HEIGHT - GROUND_THICKNESS, VIEW_WIDTH, GROUND_THICKNESS);

    // Graphics do mundo (scrollFactor 1 ⇒ acompanha a câmera).
    this.gfx = this.add.graphics();
    this.loop = new FixedStepLoop(this.world, this.input);
  }

  override update(_time: number, deltaMs: number): void {
    this.loop.advance(deltaMs / 1000);

    // Câmera segue o dino interpolado; vertical não scrolla (o mundo cabe na altura).
    this.cameras.main.scrollX = this.loop.renderX - DINO_SCREEN_X;

    const g = this.gfx;
    g.clear();
    for (const o of this.world.obstacles) this.drawEntity(g, o);
    for (const c of this.world.collectibles) this.drawEntity(g, c);
    this.drawPrimitive(
      g,
      DINO_TYPE_ID,
      this.world.pterodactyl.hitbox,
      this.loop.renderX,
      this.loop.renderY,
    );
  }

  private drawEntity(g: Phaser.GameObjects.Graphics, e: Entity): void {
    const typeId = e.tags[0] ?? '';
    this.drawPrimitive(g, typeId, e.hitbox, e.transform.position.x, e.transform.position.y);
  }

  /** Desenha a geometria da hitbox (ou triângulo cosmético) na cor do manifesto. */
  private drawPrimitive(
    g: Phaser.GameObjects.Graphics,
    typeId: string,
    hitbox: Hitbox,
    cx: number,
    cy: number,
  ): void {
    const r = renderableFor(typeId);
    if (r.kind !== 'primitive') return; // sprites entram com a arte (fase posterior)
    g.fillStyle(r.color, 1);

    if (r.shape === 'triangle') {
      const b = boundsOf(hitbox); // ápice em +x (pássaro voltado para a direita)
      g.fillTriangle(cx + b.minX, cy + b.minY, cx + b.minX, cy + b.maxY, cx + b.maxX, cy);
      return;
    }

    switch (hitbox.kind) {
      case 'aabb':
        g.fillRect(cx - hitbox.halfW, cy - hitbox.halfH, hitbox.halfW * 2, hitbox.halfH * 2);
        break;
      case 'circle':
        g.fillCircle(cx, cy, hitbox.radius);
        break;
      case 'polygon':
        g.fillPoints(
          hitbox.points.map((p) => ({ x: cx + p.x, y: cy + p.y })),
          true,
        );
        break;
    }
  }
}
```

- [ ] **Step 2: Implementar o bootstrap do jogo**

`src/render/game.ts`:
```ts
import * as Phaser from 'phaser';
import type { WorldState } from '@core/sim';
import { GameScene } from './GameScene';
import { NullInputSource } from './input';
import type { InputSource } from './input';
import { VIEW_WIDTH, VIEW_HEIGHT, SKY_COLOR } from './constants';

/** Cria o Phaser.Game montando a GameScene que renderiza `world`. Input default = nulo (2.2 pluga real). */
export function createGame(
  parent: string | HTMLElement,
  world: WorldState,
  input: InputSource = new NullInputSource(),
): Phaser.Game {
  return new Phaser.Game({
    type: Phaser.AUTO,
    parent,
    width: VIEW_WIDTH,
    height: VIEW_HEIGHT,
    backgroundColor: SKY_COLOR,
    scale: { mode: Phaser.Scale.FIT, autoCenter: Phaser.Scale.CENTER_BOTH },
    scene: [new GameScene(world, input)],
  });
}
```

- [ ] **Step 3: Integrar no app shell**

Substituir todo o conteúdo de `src/app/main.ts` por:
```ts
import { i18n } from '@services/i18n';
import { createWorld } from '@core/sim';
import { createGame } from '@render/game';

async function bootstrap(): Promise<void> {
  await i18n.init();
  document.documentElement.lang = i18n.getLanguage();
  document.title = i18n.t('app.title');

  // Mundo de demonstração para o vertical slice (seed aleatória/fluxo real é 2.5).
  // O shell Preact de telas entra na Fase 4; em 2.1 o canvas ocupa a tela.
  const world = createWorld({ seed: 'endless:DEMO' });
  createGame('app', world);
}

void bootstrap();
```

- [ ] **Step 4: Typecheck, lint e build**

Run: `npm run check`
Expected: PASS (tsc sem erros; eslint limpo). Se o `import * as Phaser` reclamar de tipos, confirme que está usando exatamente `import * as Phaser from 'phaser';`.

Run: `npm run build`
Expected: build do Vite conclui (tsc --noEmit + bundle) sem erro.

- [ ] **Step 5: Verificação manual (registrar evidência)**

Run: `npm run dev` (ou `bash scripts/run.sh` em background) e abrir no navegador.
Expected: aparece o pterodáctilo (triângulo vermelho) em ~¼ da largura; o cenário (obstáculos geométricos variados e pássaros-moeda amarelos) scrolla da direita para a esquerda enquanto a câmera segue o dino; com input nulo o dino cai e ao tocar o chão o estado congela (Game Over é 2.6). Parar com `bash scripts/stop.sh`.

- [ ] **Step 6: Commit**

```bash
git add src/render/GameScene.ts src/render/game.ts src/app/main.ts
git commit -m "feat(2.1): GameScene + createGame (Phaser) e integração no app shell"
```

---

## Fechamento (após as 4 tasks)

Feito pelo orquestrador, não como task de subagente:
- Marcar `2.1` como `[x]` em `docs/roadmap/PHASE-02-endless-vertical-slice.md`.
- Atualizar "Estado atual" no `CLAUDE.md` (Fase 2 iniciada; 2.1 concluído; resumo do que entrou).
- Atualizar `docs/assets/asset-registry.md` se algum status mudou (segue `placeholder` — primitivas; sem arte PNG ainda).
- Review final da branch + `npm test` + `npm run check` (evidência) e merge para `main` (`--no-ff`, ou PR+merge se houver remote).

## Self-Review (cobertura da spec)

- Loop passo fixo + interpolação → Task 3 (testado). Render desacoplado → Task 3/4.
- GameScene lê WorldState; interpolação entre steps → Task 4 (câmera/renderX) + Task 3.
- Entities via manifesto (primitivas) → Task 2 (manifesto) + Task 4 (desenho por hitbox/cor).
- InputSource/NullInputSource (input real adiado p/ 2.2) → Task 1.
- Mapeamento de coords/câmera/scale → Task 1 (constantes) + Task 4.
- Testes dos módulos puros → Tasks 1–3. Casca Phaser via check/build/manual → Task 4.
- Determinismo intacto (core não tocado) → verificado em Task 3/fechamento.
- Sem placeholders; tipos consistentes entre tasks (`FixedStepLoop`, `renderableFor`, `Renderable`, `InputSource`). Nenhuma string de UI hardcoded.
