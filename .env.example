# JurassicRun — configuração do cliente Supabase (Fase 6). Copie para `.env` (git-ignored)
# e preencha. NÃO commite `.env`. A anon/publishable key é segura no cliente (protegida por RLS).
VITE_SUPABASE_URL=https://wpbryudhqerpnzbpmcio.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_YvN2XGdZp8IidXB9zOtC-g_wy9RkeXN

# --- Loja (item 10.8) -------------------------------------------------------
# TODAS opcionais: sem nenhuma delas a Loja usa os defaults de
# `src/services/purchase/storefront.ts` (US$ 1,99 / 4,99 / 9,99 na página do estúdio).
# Cada chave é validada isoladamente — valor inválido cai no default, sem derrubar a tela.
# Preços em UNIDADES MENORES (centavos), inteiro positivo. Moeda em ISO-4217 (3 letras).
# VITE_SHOP_KOFI_URL=https://ko-fi.com/insightxlabgamestudio
# VITE_SHOP_CURRENCY=USD
# VITE_SHOP_PRICE_SMALL=199
# VITE_SHOP_PRICE_MEDIUM=499
# VITE_SHOP_PRICE_LARGE=999
