export function renderAppIcon(size: number, opts?: { maskable?: boolean }): Buffer;
export const APP_ICONS: ReadonlyArray<{
  readonly name: string;
  readonly size: number;
  readonly maskable: boolean;
}>;
